# Council review — 1778256562

**Problem:** Post-impl review of 5-commit autonomous session: Telegram MVP forcing-function + reflex triggers + POO sentinels + ALO header guard + SO+RC bulk archival scrape.

**Proposed approach:** See session diff. Cron `0 9 * * *` already installed prior to council. User asked specifically for blind-spot scan on 5 named concerns.

**Project context:** jarvis-kotlin life-OS. UAIC AI student, finals June 1-21, PS HW deadline 2026-05-21 (NO restanță). Telegram = only push channel; phone APK is poll-only. Loop discipline says apply CRITICAL/HIGH inline, defer MEDIUM/LOW.

**Timestamp:** 2026-05-08T16:09:22Z

---

## 🔴 Devil's Advocate

```
AGENT: Devil's Advocate
STANCE: REJECT
REASONING: The Telegram push picks weakest-subject-by-ratio, then surfaces THAT
subject's nearest assignment. POO is currently weakest (1.6%) with Lab eval 2 due
2026-05-25. PS HW has the no-restanță cliff on 2026-05-21. Until POO's ratio
overtakes PS's — which it won't if the user follows the push — every daily message
from 2026-05-09 through 2026-05-21 will point at POO and stay silent on PS. The
doc's "Known limits" section actually admits this ("only POO surfaces until POO
catches up") without recognising it lethally inverts the user's stated priority.
A forcing-function that confidently points away from the no-restanță deadline is
strictly worse than no forcing-function: it manufactures false reassurance for
two weeks while the cliff approaches. The fix is trivial (sort assignments by
daysToDue across ALL subjects, not within the weakest; or two-stage:
nearest-due-within-N-days first, weakest only as fallback) — but until that
ships, the cron entry should not be installed.
KEY CONCERN: Selector inverts the user's actual deadline priority — PS HW
(2026-05-21, no restanță) gets zero pushes for the full lead-up while POO Lab
eval 2 (2026-05-25, has restanță) is pushed daily.
```

## ⚙️ Pragmatist

```
AGENT: Pragmatist
STANCE: CONDITIONAL
REASONING: Bulk of session is fine. Two pieces have hand-off cost the user will
eat in 6 weeks. (1) Reflex-trigger list is 30+ verbatim substrings shipped inline
in CHAT_SYSTEM_PROMPT — "tomorrow" fires inside "not tomorrow", "behind" fires
inside "left it behind on the bus". List will grow unbounded; in 3 months it's a
60-item soup nobody can audit. (2) Mask-row-with-max=0 convention for POO is a
landmine. When extractor evolves and someone writes a legitimate (earned=X, max=0)
row for an unscored component, the conventional meaning ("deleted by mask") and
natural meaning ("not yet graded") collide silently.
KEY CONCERN: The mask=0 convention silently overloads the (earned=0, max=0)
tuple with two incompatible meanings. Replace with an explicit "deleted": true
field, or move the seven ids to a skiplist constant in Grades.kt.
```

## ⚠️ Risk Analyst

```
AGENT: Risk Analyst
STANCE: CONDITIONAL
REASONING: CRITICAL — Telegram silent-fail under T-12 finals pressure. The push
pipeline has zero observability from the user's side. Token-revoke (401),
chat-not-found (400), and .env sourcing failures all exit-log to /var/log/
jarvis-push.log which the user never reads. 15s timeout + no-retry: a single
VPS network blip = silent miss. HIGH — reflex-trigger context bloat doubles
token cost on innocuous chit-chat. Mask-row, ALO header, archival risks are
bounded.
KEY CONCERN: Telegram push has no liveness check. Mitigations: (1) push.py
wraps urlopen in 3-attempt retry with backoff; (2) write last_push_status.json
the bot's [[status]] surfaces and APK can poll — silent-fail becomes
poll-visible; (3) include date-stamp + sequence number in message so missing
days are visible.
```

---

## Sanity Check

- SANITY Devil's Advocate: PASS. Reasoning is mathematically verifiable: send-daily-push.py
  pick_weakest then pick_next_action(weakest_subject) means PS Temas can't surface
  while POO is below PS by ratio. Confirmed in pick_next_action signature
  (filters to subject parameter).
- SANITY Pragmatist: PASS. Convention-encoded-as-data-shape is legit anti-pattern.
  Concrete example given. Future-self collision is real if extractor evolves.
- SANITY Risk Analyst: PASS. Cron silent-fail is documented well-known anti-pattern.
  Mitigations are concrete and proportionate.

---

## Judge

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: FLAWED

CORE FINDING:
Telegram push selector inverts the user's actual deadline priority. With POO 1.6%
(weakest) and PS 35% (no-restanță deadline 2026-05-21, T-12 days), the daily push
will point at POO Lab eval 2 (2026-05-25, has restanță) every morning through
2026-05-21 and never surface PS HW. A forcing function that confidently points
away from the no-restanță cliff is strictly worse than no forcing function. The
cron line MUST NOT remain installed until the selector is fixed.

AGENT CONSENSUS: 1 REJECT, 2 CONDITIONAL — 0 agents flagged

KEY ISSUES:
1. CRITICAL — selector picks weakest-subject-by-ratio + that-subject's-nearest-
   assignment. Should pick: (a) any overdue/due-soon assignment first regardless
   of subject ratio, with no-restanța-flag weighting; (b) only fall back to
   weakest-subject if no due-soon work exists.
2. HIGH — push pipeline has no observability. Token revoke, network blip, .env
   syntax error all silent-fail. Need retry + status JSON + date-stamp/sequence
   in message body.
3. MEDIUM — reflex-trigger list will accumulate false positives ("not today"
   fires the trigger). Scope to study-context co-occurrence or add negation skip.
4. MEDIUM — mask-row (earned=0, max=0) convention overloads the tuple. Replace
   with explicit "deleted": true field or skiplist constant.

RECOMMENDED PATH:
Apply CRITICAL + HIGH inline before re-enabling cron:
- Rewrite send-daily-push.py selector: pick nearest-due-active-assignment across
  all subjects, prefer no-restanță deadlines (PS), weakest-by-ratio as tiebreaker
  among similar-due-date items. Update tests.
- Add 3-attempt retry with exponential backoff + write last_push_status.json
  + include "Day N" date-stamp in message body.
- Re-smoke: PS Tema must surface in dry-run today.

Defer MEDIUM:
- Reflex-trigger negation handling: schedule for next session unless user notices
  false positives.
- Mask-row convention rework: schedule next session; doc the convention in
  Grades.kt header for now.

CONFIDENCE: 9
Devil's Advocate finding is mathematically certain given current grades.jsonl state
(POO 1.6/100 ratio < PS 42/120 ratio = 0.016 < 0.35). The other concerns are
maintainability/observability, where the assessment depends on whether the user
catches a silent-fail before it costs them — finals stakes argue for paranoia.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
