# Council review — 1778787266

**Problem:** Post-CLI-swap, what should Surface Y's persona loop optimize for? Surface Y is a Playwright + LLM-persona harness that drives a simulated naive first-year student through the tutor's drill workspace. A decision was made this session to build a Claude-CLI provider so Y's persona runs on capable Claude instead of weak `:free` OpenRouter models. The question: does that swap reopen what Y is *for*?

**Proposed approach:** A 3-option menu — (a) optimize for DRILL COMPLETION / real tutor_events (lean on Claude's capability); (b) optimize for NAIVE-STUDENT FRICTION DISCOVERY (the spec's stated job for Y; add naivety-hardening to replace a lost mitigation leg); (c) BOTH equally. Council asked to rule (a)/(b)/(c) or reject the framing.

**Project context:**
- Domain: Personal AI tutor + life-OS web app ("jarvis-kotlin"); single user (Alex). Surface X/Y/Z is an automated quality/UX-eval suite. Surface Y = a Playwright + LLM-persona harness simulating a naive first-year student.
- Stack: Kotlin backend, React/TS frontend, Node ESM tooling (`tools/*.mjs`), Playwright, LLMs.
- Constraints: single-maintainer; build-everything mode; work lands on `main`. The user's durable preference permits `:free` OpenRouter models OR claude-max-via-subprocess (the local `claude` CLI — hits the Claude Code subscription rate-limit pool, NOT a metered API budget; that pool is also used for the user's actual coding/finals-study sessions).
- Prior decisions: Surface X/Y/Z shipped; the semantic `submit` action shipped (63/63 tests). This session's prior council (council-1778785261) ruled a `:free` model-roulette menu WRONG APPROACH — core finding: Surface Y's acceptance criterion wrongly WELDED two goals (surface UX friction = needs a stochastic persona; produce a real `drill_grade` event for the X-fixture = deterministic fixture-seeding that should NOT depend on a stochastic agent) — recommended decouple + deterministic non-LLM seeder + re-scope Y to friction discovery. Then: decision to build a Claude-CLI provider for the Surface tools, Y's persona defaulting to it.

**Timestamp:** 2026-05-14T19:34:26Z

---

## 🔴 Devil's Advocate

AGENT: Devil's Advocate
STANCE: REJECT
REASONING: The question smuggles in a false premise: it presents "(a)/(b)/(c)" as the decision space while quietly asserting the CLI swap "lifts" the prior constraint — but I verified the codebase and the Claude-CLI provider does not exist (no provider file, no `spawn`/`claude` reference in `surface-y-persona.mjs`, `tools/lib/` holds only `openrouter.mjs`, no swap commit in 25-deep history). You are being asked to re-optimize Surface Y around a capability you have not built and whose naivety-leakage cost you have not measured — that is optimizing a hypothesis. And the cost is not hypothetical: the spec (`2026-05-13-student-standin-design.md:786`) lists "Persona naivety leakage" as HIGH risk with exactly three mitigations, and the swap deletes one ("Weak `:free` model") while the surviving two — schema-gate and confusion-tuples — only catch *concept* leakage (persona names an off-ledger term), never *behavioral* hyper-competence (Claude writes correct R first try, never gets stuck, never misreads an affordance). Option (a) is therefore not a "less authentic friction signal" — it is a friction detector that returns clean on a broken UI because a superhuman agent glided past the dead-end a real first-year would have face-planted into; a green Surface Y run becomes unfalsifiable. Worst of all, tension #3 is decisive and capability-independent: the most recent council (1778785261, "WRONG APPROACH", confidence 8) already ruled the real-event goal must be served by a deterministic non-LLM seeder — Claude being *able* to complete a drill does not make a stochastic agent the *right* fixture factory, so (a)'s entire premise ("lean on Claude for real events") is relitigating a question the council closed three rounds ago.
KEY CONCERN: The framing must be rejected before (a)/(b)/(c) can be ruled on, because (a) is the only option the swap appears to "enable" and it is built on two unverified loads: that the CLI provider exists (it does not) and that behavioral naivety can survive a hyper-competent substrate with the "weak model" mitigation removed and nothing replacing it (the spec has no behavioral-leakage gate, only a concept-leakage gate). If the council picks anything other than (b)-with-a-new-behavioral-naivety-gate-as-a-hard-precondition, Surface Y silently converts from a fresh-eyes friction detector into a regression bot that proves the happy path works — which is Surface X's job, already shipped — and Alex loses the one signal in the entire suite that catches unknown-unknowns.

## 📚 Domain Expert

AGENT: Domain Expert
STANCE: CONDITIONAL
REASONING: Approve (b) — friction discovery — and reject (a)/(c). The CLI swap does not move the prior verdict, because that verdict was capability-independent: it split "produce a `drill_grade` event for the X-fixture" (deterministic, non-LLM, or the already-shipped `submit` macro-action) from "surface naive-user friction" (stochastic persona). Established practice in synthetic-user UX testing is unambiguous here — Nielsen-style discount usability testing, ISO 9241-11 "fresh-eyes" walkthroughs, and the LLM-persona literature the spec already cites (Milička et al. 2024 on GPT-4 hyper-accuracy leakage; MathVC's symbolic-schema gate; "Generative Students" confusion-tuples) all exist precisely because a high-capability simulator of a low-capability user is the known anti-pattern — it's the "competence floor" / persona-prompting-ceiling problem, and swapping in Claude *worsens* it by removing the weak-model leg the spec listed as one of three HIGH-risk mitigations. Option (a) reinvents the deterministic fixture-seeder as a stochastic agent (the exact wheel the prior council and the shipped `submit` macro-action already built) and (c) is gold-plating — "invest in capability" on a single-user side-tool has no consumer once seeding is decoupled.
KEY CONCERN: Behavioral hyper-competence leakage is now unmitigated — Claude navigating with superhuman efficiency and writing flawless first-try R produces a friction signal that is silently worthless, and the two surviving mitigations (schema-gate, confusion-tuples) only catch CONCEPT leakage, not competence leakage; (b) must add behavioral naivety-hardening (e.g. forced exploration/error-injection budget, latency/backtrack realism, capped action-efficiency) or convene the spec's own "touches naivety contract" council trigger before the swap ships.

## ⚙️ Pragmatist

AGENT: Pragmatist
STANCE: CONDITIONAL
REASONING: Reject (a) and (c), conditionally approve (b) — but with a far cheaper mitigation than "naivety-hardening." The prior verdict is capability-independent: the deterministic non-LLM seeder still beats a stochastic Claude persona for X-fixture events, so the CLI swap doesn't reopen "make Y complete the drill" — it just means Y will now *also* complete drills as a side effect, which is exactly the trap, because a hyper-competent run produces a friction signal that's quietly worthless (tension 1) while looking like a success. Option (c) is gold-plating a single-user side-tool: building a behavioral-competence gate plus naivety-hardening plus capability is three workstreams for an instrument whose entire job is one bullet ("≥1 unknown-unknown per run"). The honest cost-aware move for (b): don't build a behavioral gate — just lower the persona's model to a `:free` model for Y specifically (the lost mitigation leg costs ~one line of model-routing config to restore, since the CLI provider is the *default*, not a hard-wire), keep Claude-CLI as the X-fixture seeder's muscle where competence is *wanted*, and add one lightweight transcript heuristic (flag runs with zero `ask_sidekick`/zero confusion as "leakage-suspect, friction signal untrusted") instead of a full gate.
KEY CONCERN: The framing's hidden assumption is that the CLI provider must be Y's persona engine — but the prior council already split the two jobs; Y's persona doesn't *need* Claude's competence (that competence is the bug for Y), so the cheapest correct answer is "Claude-CLI seeds the X-fixture deterministically; Y's persona stays on a weak `:free` model" — which costs near-zero and dissolves tension 1 without any naivety-hardening project at all.

## 🧱 First Principles

AGENT: First Principles
STANCE: REJECT
REASONING: The framing smuggles in a false premise — that "post-CLI-swap, what should Y optimize for?" is even an open question. Strip it: Surface Y exists for exactly one irreducible reason — it is the only signal in the suite that *acts as a user*, exposing dead-ends/broken-affordances/missing-scaffolding that a trace-grader (judges, doesn't act), a visual critic (looks, doesn't act), a deterministic seeder (generates events, isn't naive), and Alex's own dogfood (not fresh-eyes — Alex wrote the site) all structurally cannot. That answer is capability-independent: Claude being smarter changes *how well Y can drive a browser*, not *why Y is in the suite*. So (a) is self-defeating — optimizing a fresh-eyes instrument for completion-via-superhuman-capability destroys the only thing it measures, and you'd have spent capability to manufacture a worthless signal; (c) is gold-plating because "capability" is not a co-equal goal, it's a means, and "both equally" on a single-user side-tool burns the most effort to half-serve the actual purpose; the deterministic seeder already owns event-generation, and it owns it *better* than a stochastic Claude persona ever could. (b) is the only answer consistent with why the tool exists — but it is not "approve (b)," it is "the question is mis-posed": Y was always (b), the CLI swap was justified as buying *reliable navigation of the friction loop*, not *drill-completion as a goal*, and "real tutor_events" was correctly severed from Y by the prior council and must stay severed.
KEY CONCERN: The naivety-leakage mitigation stack lost its load-bearing leg and the framing treats that as an acceptable trade for capability — it is not. The schema-gate + confusion-tuples catch *concept* leakage (persona names an off-ledger term); nothing remaining catches *behavioral* hyper-competence (Claude writes correct R first try, navigates with zero hesitation, never re-reads, never stalls). A Claude persona that completes the drill smoothly is indistinguishable in transcript from a site with zero friction — the instrument's calibration error becomes invisible, which is the exact disease the prior Devil's Advocate named. Before Claude drives Y at all, the lost leg must be replaced with a *behavioral* naivety control (hesitation/backtrack injection, forced first-attempt-wrong via confusion-tuples, a per-step "could a real beginner know to do this?" external check) — and drill-completion must be explicitly demoted to incidental, never a success metric, or Surface Y becomes a confidence-laundering loop that reports "smooth!" because Claude is smooth, not because the tutor is.

## ⚠️ Risk Analyst

AGENT: Risk Analyst
STANCE: CONDITIONAL
REASONING: Risk-ranked, option (a) is CRITICAL: a Claude persona that completes drills produces clean transcripts and real `drill_grade` events while its friction signal is silently void — and the only leakage detector in the codebase (`surface-y-gate.mjs`, a substring scan for off-ledger concept *names*) is structurally blind to behavioral hyper-competence, so the degradation is undetectable by design; Alex would see green runs forever and never know Surface Y stopped doing its job. Option (c) carries a HIGH, second-order risk: the spec already gates runs as "quota-scarce" and the only thing making them cheap was the `:free` ceiling — moving the persona onto claude-max-via-subprocess and *also* investing to run Y more often draws down the exact subscription rate-limit pool Alex's finals-study coding sessions share, and that contention surfaces as throttled coding work, not as a Surface-Y error, so again it fails silently and off-target. Option (b) is the lowest-blast-radius choice and the only one whose failure mode is loud: if naivety-hardening is imperfect, the friction findings are merely *noisier*, not *fraudulent*, and the drill-completion gap was already a known, visible state (six logged loop-halts) — plus the prior council already established the capability-independent escape hatch: a non-LLM deterministic seeder for the X-fixture, which beats any stochastic persona for pure event-generation regardless of model. The CLI swap genuinely changes nothing about the prior verdict — it removes a *mitigation* (the "weak model" leakage leg) without removing the *reason for the split*; treating "Claude can now finish drills" as a goal is optimizing the instrument toward the metric that is easiest to fake. Verdict: reject (a) outright, reject (c) as disproportionate gold-plating on a single-user side-tool, adopt (b) — CONDITIONAL on two guards: (1) keep X-fixture seeding on the non-LLM deterministic path so Y is never the event source, and (2) before merge, add a behavioral-competence tripwire to the finding doc (e.g. flag first-try-flawless-code / zero-confusion-steps runs) so a hyper-competent persona fails loudly instead of silently.
KEY CONCERN: Option (a)'s failure is the worst class — silent, self-congratulating (green metrics), and undetectable with the leakage tooling that actually exists in the repo; a worthless friction signal that *looks* like success is more dangerous than the honest, visible "Y didn't finish the drill" state it would replace.

---

## Sanity Check

SANITY Devil's Advocate: PASS
NOTE: clean. Codebase claims tool-verified (no provider file, persona has no spawn/claude ref, tools/lib has only openrouter.mjs). The "provider doesn't exist yet" point is slightly belabored — it's a brainstorm, of course it isn't built — but the substantive critique (option (a) optimizes around an unmeasured behavioral-leakage cost; the framing smuggles in (a) as newly-enabled) is sound and not a strawman.

SANITY Domain Expert: PASS
NOTE: clean. Named real patterns specifically (Nielsen discount usability testing, ISO 9241-11, Milička 2024, MathVC, Generative Students, the "competence floor" anti-pattern); concrete comparison; CONDITIONAL carries an explicit condition.

SANITY Pragmatist: PASS
NOTE: clean. CONDITIONAL with a specified, cheaper condition (keep Y on :free, Claude-CLI for the seeder, one transcript heuristic). The recommendation appears to contradict the CLI-swap premise but is internally consistent — its point is precisely that Y's persona doesn't need Claude's competence. Stayed in the cost/maintenance lane.

SANITY First Principles: PASS
NOTE: clean. Stripped the framing to "why does Y exist," rejected the question as mis-posed (in-lane for the role), verified against council-1778693954 + the spec risk table, compared the from-scratch answer to the options. Correctly ignored a stray task-list artifact.

SANITY Risk Analyst: PASS
NOTE: clean. Risks ranked CRITICAL/HIGH/lowest-blast-radius; top risks tool-verified (surface-y-gate.mjs is a substring scan); CONDITIONAL carries two specified guards. Focused on the silent-failure and subscription-pool risks as briefed.

---

## Judge

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: WRONG APPROACH

CORE FINDING:
The (a)/(b)/(c) menu is the wrong question. All five agents land on the same substantive answer — (b), naive-student friction discovery — and three of them explicitly attack the framing itself: it smuggled in a false premise, that the Claude-CLI swap "reopened" what Surface Y is for. It did not. Y was always (b) — it is the only signal in the eval suite that *acts as a user* and catches unknown-unknowns; a trace-grader, a visual critic, a deterministic seeder, and Alex's own dogfooding all structurally cannot. That purpose is capability-independent. The swap changes how *well* Y can navigate the friction loop, not *why* Y exists.

AGENT CONSENSUS: 2 REJECT (framing), 3 CONDITIONAL — 0 flagged. Unanimous that the answer is (b); unanimous that (a) and (c) are wrong; unanimous that (b) carries a hard, non-negotiable condition.

KEY ISSUES:
- Option (a) is the dangerous one — and it was the only option the swap appeared to "enable." A hyper-competent Claude persona that completes drills produces clean transcripts and green metrics while its friction signal is silently void. Risk Analyst ranks this CRITICAL: the only leakage detector in the repo (`surface-y-gate.mjs`) is a substring scan for off-ledger *concept names* — structurally blind to *behavioral* hyper-competence (flawless first-try code, zero hesitation, zero backtracking). Surface Y would become a confidence-laundering loop reporting "smooth!" because Claude is smooth.
- The lost mitigation leg must be replaced before any Claude-on-Y ships. The original spec listed three naivety-leakage mitigations; the swap deletes one ("weak `:free` model" — the only leg addressing *behavioral* competence, per the Milička-grounded rationale that smaller models are more faithful novices). Concept-leakage gate + confusion-tuples survive; behavioral-competence leakage is now unmitigated.
- (c) is gold-plating plus a second-order resource risk. "Both equally" is three workstreams on a single-user side-tool. And running Y on the subscription pool more often draws down the same rate-limit pool Alex's finals-study coding sessions use — contention surfaces as throttled coding work: off-target and silent.

RECOMMENDED PATH (replaces the a/b/c question):
1. Y is (b). Drill-completion is explicitly DEMOTED to incidental — never a success metric. Bake this into the spec in those words.
2. X-fixture seeding stays on the deterministic non-LLM path — Y is never the event source. The prior council's verdict is unchanged; the CLI swap does not touch it. The Claude-CLI provider, if built, powers the *seeder* (where competence is wanted), not Y's persona by default.
3. Replace the lost leg — non-negotiable minimum: add a behavioral-competence tripwire to Y's finding doc — flag any run with zero `ask_sidekick`, zero confusion steps, or first-try-flawless code as "leakage-suspect: friction signal untrusted." This converts a silent failure into a loud one. Cheap; mandatory.
4. Resolve in the spec (the Pragmatist's reframe — the real open question): does Y's persona go on Claude at all? If Y's job is (b) and Claude's competence is the *bug* for Y, the cheapest correct design may be — Y's persona stays on a `:free`/mid-tier model (restores the lost leg for ~one line of config), and Claude-CLI is reserved for the seeder. The counter is a competence *floor*: `:free` deepseek click-looped incoherently, which surfaces noise, not friction. Frame this as a "competence band" decision, not "weak vs. strong."

CONFIDENCE: 9
The verdict on (b) and on rejecting (a)/(c) is unanimous and well-grounded. The one genuine open sub-question — whether Y's persona runs on Claude or stays `:free` — is explicitly handed to the spec as a competence-band decision. What would sharpen it: the existing run logs already partly answer the floor question (gpt-oss-120b *did* surface friction; deepseek did not) — a single deliberate mid-tier probe would settle it.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
