# Council review — 1778164815 (POST-IMPL Phase 1 — full diff)

**Problem:** Loop-discipline post-impl council on the 5-commit Phase 1 diff (commits `193f836` 1.1, `01c4357` 1.2, `ada922f` 1.3 + 2 wrap commits — 927 LOC across 9 src + 3 test files). Did Phase 1 actually deliver "observe with weights" cleanly? What's load-bearing-broken vs cosmetic-defer? Is it safe to start Phase 2?

**Project context:**
- jarvis-kotlin Kotlin 2.0.21 / JDK 21 / Ktor 3.0.1, VPS @ -Xmx512m, single-user
- Phase 1.1+1.2+1.3 shipped + smoked today
- 35 tests green, full suite passes, deploy ok, smoke ok

**Timestamp:** 2026-05-08 (Unix 1778164815)

---

## 🔴 Devil's Advocate

```
AGENT: Devil's Advocate (POST-IMPL)
STANCE: CONDITIONAL
KEY CONCERN: Salient block (Prompts.kt:94-99) renders raw conversation
    content into the system prompt with NO PII scanner — every provider
    API call now ships verbatim user/assistant text picked by importance,
    not by user curation.

FINDINGS:
A) Salient block bypasses privacy boundary — HIGH (CoreMemory.scanForPii
   gates only core_memory.md; conversations.jsonl never scanned)
B) Activity.append O(file) read on every 5-min sample — MEDIUM
C) Score-read race outside LOCK in Activity.append — MEDIUM
D) Salient renders blank [imp=] for pre-1.2 rows — verified safe
E) Pool-cap 500 vs Generative-Agents last_accessed_at — LOW
F) firstOrNull{!isBlank}.take(300) cap — LOW (1.8KB salient block bound)
G) ConversationScorer ASCII-lowercase keyword matching — LOW
H) Corrupt-ts handling — ACCEPTABLE (test covers)

FIX-INLINE-NOW:
- A: Run CoreMemory.scanForPii equivalent over salient block before emit;
     drop tripping rows with WARN. Conservative skip-not-redact.
DEFER-TO-NOTE:
- B, C, D, E, G — track in 2026-05-08-deferred-mediums.md
```

## 📚 Domain Expert

```
AGENT: Domain Expert (POST-IMPL)
STANCE: CONDITIONAL
KEY CONCERN: `ts` (creation) vs `last_accessed_at` (Generative-Agents
    Algorithm 1, line 6 / LangChain TimeWeightedVectorStoreRetriever)
    is a load-bearing semantic gap — recency in Park et al. decays from
    LAST RETRIEVAL, not creation. Important rows can't stay surfaced.

FINDINGS:
A) lastAccessedAt missing on ConversationEntry — MEDIUM
B) ConversationScorer 24-word lexicon vs Park §A.2 LLM scoring — LOW
   (explicit roadmap deviation; Phase 5 retrain documented)
C) Pool cap 500 vs Letta RecallMemoryEvictionPolicy paginated — LOW
D) keywordBonus substring vs \b boundaries — LOW ("fail" hits "failsafe")
E) Null fallback 0.4 — INFO (matches Mem0 neutral, intentional)

REMEDIATION:
- Phase 1.4 / Phase 2 candidate: lastAccessedAt + sidecar index file
- Document v1 heuristic-by-design vs Park et al. LLM scoring (kdoc)
- Defer pool eviction until conversations.jsonl > 5k rows
```

## ⚙️ Pragmatist

```
AGENT: Pragmatist (POST-IMPL)
STANCE: ACCEPT
KEY CONCERN: Phase 1 produces no behavior change yet — only Phase 2
    closes the value loop. Don't camp here.

FINDINGS:
A) Salient block adds tokens, no eval of LLM use yet — LOW
B) Heuristic false positives bounded — LOW
C) ACTIVITY_LINE_CAP=200 truncates ~17h windows on 5-min cadence —
   MEDIUM (worth a flag; not a blocker)
D) ActivityScorer reads loadEntries(hours=1) per append — LOW
E) No score-this-row CLI for hand-grading mis-scores — LOW (Phase 5)

WHAT-TO-FIX-NOW:
- Nothing blocking.
WHAT-TO-DEFER:
- Raise ACTIVITY_LINE_CAP or switch to time-window cap (Phase 2)
- Lexicon expansion (Phase 5)
- Sliding cache for loadEntries

GO/NO-GO PHASE 2:
- GO. Phase 1 is plumbing by design. ProactiveLoop is the value.
```

---

## SYNTHESIS / VERDICT

**STATUS: CONDITIONAL — fix HIGH-A inline, defer rest, then GO Phase 2.**

### FIX-INLINE-NOW (HIGH)
- **HIGH-A** Devil's Advocate: salient block bypasses CoreMemory PII scanner. Add a `scanTextForPii` overload, filter salient rows in `buildChatContext`, WARN-loud when dropping. **Pure additive change; same regex set already in production for core_memory.**

### DEFER-TO-NOTE (MEDIUM)
- **MED-B** Activity.append loadEntries(hours=1) on every 5-min sample is O(file_size); fine today, problematic at >50 MB. Track in `docs/notes/2026-05-08-deferred-mediums.md`.
- **MED-C** Score-read race outside LOCK in Activity.append. Not corrupting — just briefly stale scoring. Move read inside LOCK (or accept eventual-consistency) at convenience.
- **MED-A_Domain** `last_accessed_at` semantic gap. Important rows decay from creation, not last retrieval. Phase 1.4 or Phase 2 candidate; sidecar index file probably right shape.
- **MED-C_Pragmatist** ACTIVITY_LINE_CAP=200 truncates ~17h windows on 5-min cadence. Switch to time-window cap when ProactiveLoop reads activity.

### ACCEPTABLE-AS-IS
- recentByImportance math + chronological re-emit
- ChatTurnWriter dual-write atomicity
- recent(n) regression guard
- Corrupt-ts safe-fallback
- Heuristic-first scoring (deliberate per roadmap)

### GO/NO-GO PHASE 2
- **GO** after HIGH-A fix lands.
- Phase 2.1 (ProactiveLoop) requires its OWN pre-impl council per roadmap.
