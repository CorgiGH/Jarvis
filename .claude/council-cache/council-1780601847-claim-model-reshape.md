# Council — claim-model reshape (post dry-run 0/6) — 1780601847

**Question:** dry-run proved 0/6 KCs can reach faithful + a structural finding (every KC has DEFINITION claims with no non-LLM leg -> conjunction never faithful). How to reshape the claim model? Does the LOCKED non-LLM-leg invariant need revisiting?
**Method:** grounded workflow claim-model-reshape-council (run wf_fe41bdd0-dc9), 6 lenses read the runner/reconcile/KCs, then a judge.

**Note (verified post-council):** VerificationGate.gate has ZERO production callers; cards serve today on status=ACTIVE (badge unverified). The judge's "serves 0 cards today" = the DESIGNED end-state once the gate is wired, NOT a current outage.

---
## devils-advocate  — O5_HYBRID | MUST_REVISIT_LOCKED
**Reshape:** Adopt O1's per-kind routing BUT only after killing the self-entailment that makes the attractive version a fraud: for a DEFINITION, the round-trip leg and the NLI leg currently key on the SAME string (round-trip relocates `claim.source.quote`; NLI entails `hypothesis=claim.content` where `content==ref.quote`), so "two families agree SUPPORTED + round-trip passes" is two tautologies stacked — zero independent signal. Fix: a DEFINITION's machine-side IS the round-trip (it is the only non-LLM check prose can have), so make DEFINITION-faithful == round-trip-pass ALONE, drop the redundant self-NLI, and explicitly RE-CHARTER the locked invariant so "non-LLM-leg-pass" reads "non-LLM-leg-pass where the leg is round-trip for prose / SymPy for equations." For INVARIANT/GRADER_RULE keep SymPy + require the LLM family to entail an NL RESTATEMENT (authored separately), never the bare equation or the "sympy:" directive string. This forces Alex to revisit the LOCKED decision — surface it, don't silently widen it.

**Reasoning:** Verified in code: ContentReconcile.claimsFor emits one DEFINITION per source ref with content=ref.quote (ContentReconcile.kt:71); SymPyLeg returns kind=NONE/ran=false for any non-equational claim (NonLlmLegs.kt:46-48); decideOutcome floors that to NONLLM_LEG_NONE=uncertain and aggregateKc is a strict conjunction (VerificationRunner.kt:118-122, 295-299). So finding (1) holds exactly: every KC has a DEFINITION claim, no DEFINITION has a non-LLM leg, conjunction => no KC can EVER be faithful. The most attractive reshape (O1) is a trap: for a DEFINITION the round-trip leg keys on claim.source.quote (SpanClaimRoundTrip.kt:30) and the NLI leg entails content==that-same-quote — both check the quote against itself, so "agree SUPPORTED + round-trip" is two self-referential tautologies, the exact "relabel uncertain as faithful cheaply" failure the lens must reject; the NLI leg adds nothing a definition round-trip didn't already prove. The locked invariant "no faithful without non-LLM-leg-pass" is literally unsatisfiable for prose (there is no machine checker), so ANY reshape that lets definition-bearing KCs go faithful MUST revisit it; and because VerificationGate ALLOWs SR-entry iff status==faithful (interface-signatures-lock.md:254/267, non-faithful items OMITTED at :76), the current state isn't a cosmetic badge gap — it BRICKS SR for 100% of content.

**Key concern:** The trap is treating round-trip and NLI as two independent legs for DEFINITIONs when they both reduce to "the quote we copied from the PDF is in the PDF." Whatever reshape ships, DEFINITION-faithfulness can only honestly mean round-trip-pass; if the design counts the self-NLI as a second independent confirmation, faithful is meaningless. And "matches your lecture" (the LOCKED badge promise) is precisely round-trip — so it should be driven by SpanClaimRoundTrip, NOT by the faithful conjunction, which over-claims (faithful implies family-agreement the badge never promised) and currently never fires.

**Grounding:**
- ContentReconcile.kt:64-92 claimsFor — DEFINITION content=ref.quote, one per source ref; INVARIANT/GRADER_RULE anchored on first span-bearing ref
- ContentReconcile.kt:86-89 grader_rule emits invariant only if isPlainEquation; the 'sympy:' prose rule => invariant=null
- NonLlmLegs.kt:46-48 SymPyLeg returns kind=NONE ran=false for any non INVARIANT/GRADER_RULE claim (every DEFINITION)
- VerificationRunner.kt:292-301 decideOutcome — case-1 span==null=>uncertain; case-4 NONLLM NONE=>uncertain floor
- VerificationRunner.kt:118-122 aggregateKc strict conjunction: faithful iff EVERY claim faithful
- SpanClaimRoundTrip.kt:29-49 round-trip keys on claim.source.quote — same string a DEFINITION's content equals
- content/PA/kcs/pa-kc-001.yaml definitions have NO span; pa-kc-005/006 DEFINITION source refs DO have span+page
- interface-signatures-lock.md:224 LOCKED 'no faithful without non-LLM-leg pass AND families-agree'; :254/:267 gate ALLOW iff faithful; :76 non-faithful items omitted from queue
- TrustRoutes.kt:143-147 badgeTextFor: faithful=>'matches your lecture', else 'unverified' — badge bound to faithful, not round-trip
- correctness-engine.md:8 LOCKED goal chains all four legs before SR-entry

---
## domain-expert  — O5_HYBRID | MUST_REVISIT_LOCKED
**Reshape:** Adopt per-claim-kind verification routing (O1) as the core, plus decouple the LOCKED "matches your lecture" badge from `faithful` and drive it off SpanClaimRoundTrip (O3's served-tier idea). Concretely: (a) DEFINITION claims become faithful-eligible via two-family-agree-SUPPORTED + round-trip pass, with NO SymPy leg required — for prose there is no machine check and demanding one is a category error; the round-trip IS the deterministic non-LLM leg for definitions. (b) Stop setting DEFINITION content = the verbatim quote; the NLI hypothesis must be an independent paraphrase/atomic-fact restatement so the families do real entailment work, not self-entailment. (c) For INVARIANT, feed NLI a natural-language restatement of the invariant (not the bare equation), keep SymPy on the equation. (d) GRADER_RULE content prefixed "sympy:" must never reach NLI as a hypothesis — strip the directive, route only the equation to SymPy. (e) Fix the aggregate so a SUPPORTED-but-not-machine-checkable definition does not floor the conjunction (O2's anchor-claim idea). Author real non-tautological invariants where they exist (O4) but treat that as content hygiene, not the fix.

**Reasoning:** Verified in code: ContentReconcile.claimsFor emits one DEFINITION per source ref with content=ref.quote (ContentReconcile.kt:70-71); SymPyLeg returns NONE/ran=false for any non-equational claim (NonLlmLegs.kt:46-48); decideOutcome floors that to NONLLM_LEG_NONE=uncertain (VerificationRunner.kt:297-299); aggregateKc requires EVERY claim faithful (VerificationRunner.kt:121). Since every KC has a DEFINITION and no DEFINITION can clear a non-LLM leg, NO KC can EVER be faithful — finding (1) holds, and D6/NLI alone cannot fix it. The LOCKED invariant "non-LLM-leg-pass required for faithful" (correctness-engine.md:8, active-constraints.md:26) is therefore structurally unsatisfiable for the dominant claim type, and R1 acceptance "≥1 KC FAITHFUL with page_anchor_status=LIVE" (correctness-engine.md:11-12) can never be met. This is exactly the forbidden silent break — it must be surfaced and Alex must re-scope the invariant. In mature claim-verification (FEVER/SciFact/RAG-grounding) practice, verification is ROUTED by claim type: a definition is checked by retrieval+entailment (here: round-trip + NLI), a computable assertion by a solver. There is no universal "machine leg" requirement; demanding SymPy on prose is the design error. On the two sub-questions: DEFINITION content==quote is a TAUTOLOGY, not a check — NLI(quote, quote) is trivial self-entailment with zero independent signal (finding 4 confirmed); a real definition check needs an independent restatement as the hypothesis. And "matches your lecture" — the LOCKED product promise — is precisely the span-relocatable property SpanClaimRoundTrip already computes deterministically; it should be driven by round-trip, NOT by the much stronger and currently-unreachable `faithful` (TrustRoutes.kt:145 wires the badge to faithful, the architectural mismatch).

**Key concern:** The locked invariant is not merely "rarely satisfiable" — it is structurally UNSATISFIABLE for every KC, so shipping as-is means the badge "matches your lecture" can NEVER light up despite content that genuinely matches the lecture; this silently guts the product's only promise unless Alex re-scopes the invariant to be claim-kind-routed.

**Grounding:**
- ContentReconcile.kt:70-71 — DEFINITION claim content = ref.quote, one per source ref
- ContentReconcile.kt:86-89 — GRADER_RULE invariant null unless isPlainEquation; prose floors to uncertain
- NonLlmLegs.kt:46-48 — non-equational/DEFINITION claim returns kind=NONE ran=false (no machine check)
- VerificationRunner.kt:295-299 — decideOutcome: faithful requires bothSupported+nonLlm.ran+pass+roundTrip; NONE floors to uncertain
- VerificationRunner.kt:118-123 — aggregateKc CONJUNCTION: faithful iff EVERY per-claim faithful
- SpanClaimRoundTrip.kt:29-49 — deterministic quote relocation + fold-equal = the 'matches your lecture' check
- content/PA/kcs/pa-kc-001.yaml — pure DEFINITION KC, no span, no invariant
- content/PA/kcs/pa-kc-005.yaml:16-19 — invariant '1+1+1=3' (tautology), grader_rule literally 'sympy: simplify(...)==0'
- content/PA/kcs/pa-kc-006.yaml:16-19 — invariant 't+t+t=3*t' (tautology) + 'sympy:' grader_rule
- correctness-engine.md:8 — LOCKED goal requires '≥1 non-LLM leg passes'
- correctness-engine.md:11-12 — R1 acceptance requires '≥1 KC FAITHFUL with page_anchor_status=LIVE'
- active-constraints.md:26 — LOCKED: NO faithful without non-LLM-leg-pass; 'current PA content honestly aggregates to uncertain — faithful waits on D6 NLI'
- TrustRoutes.kt:145 — badge 'matches your lecture' driven by VerificationStatus.faithful, not round-trip

---
## pragmatist  — O5_HYBRID | MUST_REVISIT_LOCKED
**Reshape:** Two-part hybrid, ordered by signal-per-unit-work. PART 1 (content, near-zero code risk, do FIRST): add the verbatim `span` field to pa-kc-001..004's source refs (the council already found 14/15 quotes locate verbatim in the real `Curs 1 PA.pdf`); this alone clears `decideOutcome` case-1, the floor the brief's finding-1 doesn't even name, and lets the round-trip leg actually run on those KCs. PART 2 (the minimal code reshape = O1 scoped to DEFINITION): in `decideOutcome` add ONE branch — a DEFINITION claim is faithful when bothSupported AND roundTrip.pass AND !anyThrew, with NO non-LLM leg required, because prose has no machine check and the round-trip IS its deterministic check. Leave INVARIANT/GRADER_RULE on the existing case-3 (non-LLM pass still required) UNCHANGED. Drive the LOCKED "matches your lecture" badge off SpanClaimRoundTrip's deterministic result, NOT off `faithful` — that badge is what the product actually promises and round-trip already computes it. Defer O4's real non-tautological invariants as a content follow-up (it does not unblock anything and the tautologies plus the bare-equation-to-NLI mismatch make INVARIANT faithful unreachable today anyway).

**Reasoning:** The locked invariant is unsatisfiable as written for the only KCs Alex has: every KC has a DEFINITION claim, DEFINITION has no equation so SymPyLeg returns NONE/ran=false (NonLlmLegs.kt:46-48), and aggregateKc (VerificationRunner.kt:118-122) is a strict conjunction — so NO KC can ever reach faithful (verified, this is structural not calibration). O1's DEFINITION-faithful branch drops the per-claim non-LLM requirement, which literally contradicts the locked clause 'NO faithful without non-LLM-leg-pass' (correctness-engine.md:8, active-constraints.md:26). That is a locked decision; the plan-first rule forbids silently breaking it, so Alex must explicitly bless the reinterpretation 'non-LLM check = the deterministic round-trip leg for prose claims; the SymPy leg only applies to equational claims.' Honestly, the round-trip leg IS a non-LLM machine check (pure text, no model, SpanClaimRoundTrip.kt) — the locked wording conflated 'non-LLM leg' with 'SymPy,' so the cleanest framing for Alex is that the invariant stays true if 'non-LLM-leg-pass' is read as 'the deterministic span round-trip passes,' which every faithful path already requires anyway.

**Key concern:** DEFINITION content == the quote itself (ContentReconcile.kt:71), so the NLI bothSupported check is near-tautological self-entailment (premise=quote, hypothesis=quote) — verified p=0.98-1.00 in the dry-run. Under O1 the only INDEPENDENT signal certifying a DEFINITION-faithful KC is the round-trip leg; the LLM/NLI agreement adds almost nothing. That is acceptable ONLY because round-trip is exactly the locked 'matches your lecture' promise (re-locatable span) and D8 content-hash already fails it closed on edits — but Alex must accept that DEFINITION-faithful is essentially 'round-trip passed,' not 'two families independently judged it true.'

**Grounding:**
- VerificationRunner.kt:292-301 decideOutcome — case-1 span==null short-circuits before legs; case-4 NONLLM_LEG_NONE floors to uncertain
- VerificationRunner.kt:118-122 aggregateKc — strict conjunction, faithful iff all claims faithful
- NonLlmLegs.kt:46-48 — DEFINITION/non-equational claim returns kind=NONE ran=false (SymPy never runs)
- ContentReconcile.kt:64-92 claimsFor — every KC emits ≥1 DEFINITION per source ref, content=ref.quote
- content/PA/kcs/pa-kc-001..004.yaml — source refs have quote but NO span field (case-1 floor)
- content/PA/kcs/pa-kc-005.yaml & 006.yaml — have spans + tautological invariants 1+1+1=3 / t+t+t=3*t + a literal 'sympy: ...' grader_rule
- SpanClaimRoundTrip.kt:29-49 — deterministic, no LLM, needs claim.source.quote; folds-equal check IS the 'matches your lecture' computation
- correctness-engine.md:8 + active-constraints.md:26 — LOCKED: NO faithful without families-agree-on-SUPPORTED AND non-LLM-leg-pass AND round-trip AND nothing-threw

---
## first-principles  — O5_HYBRID | MUST_REVISIT_LOCKED
**Reshape:** Drive the LOCKED "matches your lecture" badge + HonestFloor.FAITHFUL_TO_SOURCE off SpanClaimRoundTrip (the deterministic re-locatability of every cited quote in the live source), NOT off `faithful`. That is exactly and only what the product promises. Then make per-claim `faithful` criteria kind-routed (O1): a DEFINITION is faithful when round-trip passes AND both families agree-SUPPORTED (no non-LLM leg — prose has no machine check), an equational INVARIANT/GRADER_RULE additionally requires the SymPy pass. Stop feeding the literal "sympy: ..." directive string and the bare equation to NLI; the NL family must entail an NL restatement, not a directive. Keep `faithful` as the rare strong tier for genuinely machine-checkable claims; it stops being the gate for the badge Alex actually ships.

**Reasoning:** Stripped to first principles: this product makes ONE promise — "matches your lecture", never "correct". The thing that computes that promise is SpanClaimRoundTrip (relocate the cited quote in the live source, fold-equal) — verified at SpanClaimRoundTrip.kt:29-49 and it needs only a quote, not a pre-authored span. Yet the badge (TrustRoutes.kt:145) and HonestFloor (interface-signatures-lock.md:453) are both gated on VerificationStatus.faithful, and the LOCKED invariant (interface-signatures-lock.md:224) requires a non-LLM-leg pass for faithful. The non-LLM leg only exists for plain equations (NonLlmLegs.kt:46-54); a prose DEFINITION can NEVER have one, so requiring it for prose is incoherent — it is a machine-arithmetic check demanded of a sentence. Combined with decideOutcome case-1 (VerificationRunner.kt:293, span==null floors pa-kc-001..004) and aggregateKc's conjunction (VerificationRunner.kt:118-122, every claim must be faithful), the locked invariant makes the product's own promised badge structurally unreachable on 6/6 real KCs. That is not calibration; the locked invariant is solving the wrong problem (machine-truth of prose) instead of the promised one (lecture-provenance), so Alex must revisit it — I am surfacing it loudly, not silently breaking it.

**Key concern:** The DEFINITION content==quote self-entailment is genuinely zero-signal: NLI(premise=quote, hypothesis=quote) always SUPPORTED proves nothing about lecture-grounding that round-trip doesn't already prove deterministically — so leaning the badge on round-trip is not just convenient, it is the only honest signal here. The risk if you ignore this and pick O3/O4: you ship a tutor whose every real KC reads "unverified" forever, which trains Alex to distrust the badge entirely.

**Grounding:**
- VerificationRunner.kt:118-122 aggregateKc is a strict conjunction (faithful iff ALL claims faithful)
- VerificationRunner.kt:292-301 decideOutcome: case-1 span==null -> uncertain fires BEFORE the NONLLM_LEG_NONE/faithful branches
- NonLlmLegs.kt:46-54 SymPyLeg returns kind=NONE/ran=false for any non-equational or DEFINITION claim
- ContentReconcile.kt:64-91 claimsFor emits >=1 DEFINITION per source ref with content=ref.quote (self-entailment) and feeds the raw 'sympy: ...' grader_rule string as claim content
- content/PA/kcs/pa-kc-001.yaml has 3 DEFINITION sources with NO span field -> all hit decideOutcome case-1
- content/PA/kcs/pa-kc-005.yaml invariant is the tautology '1 + 1 + 1 = 3' and a grader_rule literally prefixed 'sympy:'
- SpanClaimRoundTrip.kt:29-49 relocates claim.source.quote in live source and needs only a quote (not a pre-authored span)
- TrustRoutes.kt:143-147 badgeTextFor: 'matches your lecture / faithful to your source' is emitted ONLY for VerificationStatus.faithful
- interface-signatures-lock.md:224 LOCKED: 'no path reaches faithful without BOTH a non-LLM-leg pass AND families-agree'
- interface-signatures-lock.md:441-453 HonestFloor.FAITHFUL_TO_SOURCE is derived ONLY from VerificationStatus.faithful

---
## risk-analyst  — O5_HYBRID | STAYS_WITHIN_LOCKED
**Reshape:** O1 per-claim-kind faithful + O3's split-the-badge, with two HARD risk guards. (a) Make the LOCKED "non-LLM-leg pass" a per-CLAIM-KIND obligation, NOT a per-claim-universal-SymPy: DEFINITION's required machine leg IS SpanClaimRoundTrip (deterministic, no model, a different family per SpanClaimRoundTrip.kt:6-18) — faithful-DEFINITION = bothSupported AND roundTrip.pass AND span!=null AND nothing-threw; INVARIANT/GRADER_RULE keep SymPy. This honestly satisfies "BOTH a non-LLM-leg pass AND families-agree" because round-trip IS a non-LLM leg. (b) Kill self-entailment: a DEFINITION with content==quote must NOT be NLI'd as premise==hypothesis==quote; feed the LLM/NLI family premise=surrounding source span context, hypothesis=the claim — or drop the LLM vote on identity-definitions and require round-trip + a second independent locate. (c) Drive the LOCKED "matches your lecture" badge from SpanClaimRoundTrip.pass as a served "lecture-grounded" tier, decoupled from faithful. Author real non-tautological invariants (O4) as the genuine-faithful path for tier-3/4 KCs.

**Reasoning:** Finding (1) is real and structural (VerificationRunner.kt:118-122 conjunction + every KC emits a DEFINITION at ContentReconcile.kt:70-71, which SymPyLeg refuses at NonLlmLegs.kt:46-48), so faithful is unreachable today — and because the gate ALLOWs only faithful (lock:267), the tutor currently serves ZERO cards. The reshape must let DEFINITIONs reach faithful WITHOUT inviting LLM-alone certification. The escape is that the LOCKED text (lock:224) says "a non-LLM-leg pass," not "SymPy" — and SpanClaimRoundTrip is by construction a non-LLM, deterministic, different-family leg. Routing DEFINITION's machine obligation to round-trip-against-the-LIVE-source keeps the locked guarantee literally true while making it satisfiable, so Alex need not re-open the lock. O2 is the dangerous one: relaxing aggregateKc so definitions need only be SUPPORTED lets the LLM family certify content with no machine anchor — exactly the false-faithful regression the net exists to stop, compounded by the self-entailment loophole. Risk ranking: O2 = CRITICAL (LLM-alone path); naive-O1 (no round-trip requirement on DEFINITION) = CRITICAL (same); O1+round-trip-anchor = MED (round-trip on a self-quote proves locatability but the LLM vote is near-vacuous — accept it only because round-trip is the real check); O3 = LOW (honest, just rare faithful); O4-alone = HIGH (doesn't fix finding 1, definitions still floor the conjunction).

**Key concern:** O2 and naive-O1 both open a path where the LLM/NLI family ALONE certifies a DEFINITION (no machine check), and finding (4)'s self-entailment makes that vote near-vacuous: premise==hypothesis==quote is always SUPPORTED, so a hallucinated definition that round-trips its OWN injected quote could reach faithful with zero independent signal. The machine anchor for DEFINITION MUST be SpanClaimRoundTrip against the LIVE source, never the LLM vote — and the audited quote must be re-located in the live source, not trusted as authored, or false-faithful returns.

**Grounding:**
- VerificationRunner.kt:118-122 aggregateKc is a strict conjunction — faithful iff EVERY per-claim verdict faithful
- VerificationRunner.kt:292-301 decideOutcome: case-1 source.span==null -> uncertain fires BEFORE round-trip; case-3 demands nonLlm.ran && nonLlm.pass (SymPy only); case-4 NONE -> uncertain floor
- ContentReconcile.kt:64-92 claimsFor: every KC emits >=1 DEFINITION claim with content=ref.quote (self-entailment confirmed, line 71)
- NonLlmLegs.kt:46-48 SymPyLeg returns kind=NONE/ran=false for any non-INVARIANT/GRADER_RULE claim — DEFINITION can never get a non-LLM pass
- SpanClaimRoundTrip.kt:6-18,22-49 is pure-text, no model, explicitly 'a DIFFERENT family from the two LLM legs' — qualifies as a non-LLM leg
- pa-kc-001.yaml:11-17 source refs have NO span field -> all 4 definitions hit decideOutcome case-1 uncertain; pa-kc-005.yaml:16 invariant '1 + 1 + 1 = 3' tautology; pa-kc-006.yaml:16 't + t + t = 3*t' tautology
- interface-signatures-lock.md:224 LOCKED: 'no path reaches faithful without BOTH a non-LLM-leg pass AND families-agree' — does NOT name SymPy specifically
- interface-signatures-lock.md:267 gate ALLOW iff status==faithful -> under current floor NO KC passes the gate, so NO KC enters SR (whole tutor serves nothing)
- interface-signatures-lock.md:453 HonestFloor.FAITHFUL_TO_SOURCE only when status==faithful; 'matches your lecture' badge currently bound to faithful, NOT round-trip
- correctness-engine.md:115,122 plan already states current PA content floors to uncertain + flags tautology invariants as no-signal — reshape is continuous with approved direction

---
## sequencer-pm  — O5_HYBRID | MUST_REVISIT_LOCKED
**Reshape:** O1 (per-claim-kind faithful: a DEFINITION is faithful on families-agree-SUPPORTED + round-trip; an INVARIANT/GRADER_RULE keeps SymPy + agree) layered with O3's framing (SpanClaimRoundTrip already computes the LOCKED "matches your lecture" deterministically — drive the badge from THAT, not from faithful). Fold in O4 as the content cleanup (real non-tautological invariants + an NL restatement for the NLI family + fix the "sympy:"-string GRADER_RULE that gets fed to NLI as a hypothesis). Sequence it as: Phase-2 internal edit to decideOutcome's faithful rule keyed by ClaimKind (frozen enums VerificationStatus/AuditOutcome untouched), THEN a Phase-3-scoped decision because the badge/queue/gate all key on faithful.

**Reasoning:** Grounded: decideOutcome (VerificationRunner.kt:292-301) gates faithful on nonLlm.ran && nonLlm.pass; SymPyLeg.check returns NONE/ran=false for any non-equational claim (NonLlmLegs.kt:46-48); every KC emits >=1 DEFINITION (ContentReconcile.kt:70-71); aggregateKc is a conjunction (VerificationRunner.kt:118-123). So 0/6 reachable is structural, exactly as stated. The signatures-lock LOCKED invariant (line 224) reads as a GLOBAL claim — 'no path reaches faithful without BOTH a non-LLM-leg pass AND families-agree' — and O1 makes a DEFINITION faithful with NO non-LLM leg. That literally contradicts the locked text, so O1 cannot be slipped in as a 'contained' Phase-2 edit; it re-litigates a frozen decision and MUST be surfaced to Alex (plan-first rule). The honest reframe to put to him: the locked invariant was written for machine-checkable claims and is structurally unsatisfiable for prose definitions, which are the dominant claim kind — the lock is not wrong, it is mis-scoped. The code change itself is contained (decideOutcome internals + a content pass; enums at signatures-lock.md:204/212 stay frozen), but the DOWNSTREAM ripple is not Phase-2: VerificationGate (VerificationGate.kt:30) and the queue (signatures-lock.md:76) make faithful the sole SR-admission key, so any change to what reaches faithful directly changes what the Phase-3 product can ever teach.

**Key concern:** O1 silently violates the LOCKED 'non-LLM-leg-pass required for faithful' invariant (signatures-lock.md:224) — shipping it without an explicit Alex sign-off would be exactly the forbidden quiet re-litigation. The DEFINITION content==quote self-entailment is the trap: relaxing faithful to 'families agree SUPPORTED' on a hypothesis that IS the premise means the LLM legs add zero independent signal, so the only real check left for a definition is SpanClaimRoundTrip — which is precisely the LOCKED 'matches your lecture' badge. That argues the badge should be driven by SpanClaimRoundTrip, and 'faithful' should NOT be the SR-admission key for definition-only KCs, or the product stays empty (gate + queue both faithful-keyed).

**Grounding:**
- VerificationRunner.kt:292-301 decideOutcome — faithful case-3 requires nonLlm.ran && nonLlm.pass; case-4 NONLLM_LEG_NONE floors to uncertain
- VerificationRunner.kt:118-123 aggregateKc — conjunction: all-faithful else uncertain/failed
- NonLlmLegs.kt:46-48,67-68 SymPyLeg returns kind=NONE/ran=false for any non-INVARIANT/GRADER_RULE or non-equation claim
- ContentReconcile.kt:70-71 claimsFor emits one DEFINITION per source ref for every KC; :86-88 GRADER_RULE invariant=null unless isPlainEquation
- SpanClaimRoundTrip.kt:29-49 deterministic re-locate+fold-equal — computes the 'matches your lecture' signal with no LLM
- interface-signatures-lock.md:224 LOCKED 'no path reaches faithful without BOTH a non-LLM-leg pass AND families-agree'; :204/:212 frozen VerificationStatus + AuditOutcome enums
- interface-signatures-lock.md:76 queue OMITS non-faithful items server-side; VerificationGate.kt:30 ALLOW iff status==faithful
- TrustRoutes.kt:143-147 badgeTextFor + HonestFloor.kt:27-34 honestFloorOf — both derive solely from VerificationStatus.faithful; CitationGuard.kt:45-50 1-arg attach pins uncertain (F3)
- correctness-engine.md:122 plan ALREADY flags pa-kc-005/006 invariants as SymPy tautologies needing real non-tautological invariants + notes §Q /drill/grade emit is Phase-3-activated

---
## JUDGE
**VERDICT:** O5 hybrid = O1 per-claim-kind routing + O3 badge-split, gated behind an explicit Alex sign-off, with the devil's-advocate/risk-analyst self-entailment guards made mandatory. Concretely: (a) drive the LOCKED "matches your lecture" badge + HonestFloor.FAITHFUL_TO_SOURCE off SpanClaimRoundTrip against the LIVE source, NOT off `faithful`; (b) make DEFINITION-faithful = round-trip-pass + families-agree-SUPPORTED on an INDEPENDENT restatement (NOT the verbatim quote) + nothing-threw, no SymPy; (c) keep INVARIANT/GRADER_RULE on SymPy + NL-restatement; (d) add the pragmatist's PART-1 span fields first.

**LOCKED_INVARIANT_RULING:** MUST REVISIT — surface to Alex, do NOT ship silently. The lock (interface-signatures-lock.md:224, active-constraints.md:26, correctness-engine.md:8) enumerates "families-agree-on-SUPPORTED AND non-LLM-leg-pass AND round-trip AND nothing-threw" — it lists the non-LLM leg and the round-trip as DISTINCT conjuncts. The risk-analyst's minority "STAYS_WITHIN" claim (round-trip IS the non-LLM leg, so DEFINITION-faithful satisfies the lock as written) is REJECTED: it collapses two separately-named requirements into one, which is a reinterpretation of a frozen decision, not satisfaction of it. I side with the 5-lens majority. BUT the correct frame for Alex is not "the lock is wrong" — it is "the lock is structurally UNSATISFIABLE and mis-scoped." It demands a SymPy-style machine-truth check (`non-LLM-leg pass`) of every claim, but the dominant claim kind is prose DEFINITION, which has no machine check by construction (NonLlmLegs.kt:46-48 returns NONE/ran=false). Since aggregateKc is a strict conjunction (VerificationRunner.kt:118-122) and EVERY KC emits >=1 DEFINITION (ContentReconcile.kt:70-71), NO KC can EVER reach faithful — and because the gate ALLOWs only faithful (VerificationGate.kt:30) and the queue OMITS non-faithful items (signatures-lock.md:76), the tutor currently serves ZERO cards. The approved plan (correctness-engine.md:78) explicitly expected the D6 NLI leg to "unblock the live FAITHFUL" — the dry-run evidence proves it will NOT (NLI correctly refuses bare equations and self-quotes). That is a plan-invalidating discovery, exactly the kind that the plan-first rule requires be surfaced as a decision, not patched around. Alex must bless re-scoping the invariant to: "non-LLM-leg pass = SymPy for equational claims, the deterministic round-trip for prose claims."

**CORE_FINDING:** The trust-net does not merely fail to light a badge — it is structurally unsatisfiable and currently bricks the entire product: 0 cards can be served because faithful is unreachable and the gate/queue admit only faithful. This is real, not calibration. The fix is NOT to relax the LLM side (that reopens false-faithful); it is to recognize that the product's ONLY promise — "matches your lecture" — is exactly what SpanClaimRoundTrip computes deterministically, and to drive the badge off THAT, decoupled from the much stronger, currently-empty `faithful`. On the sharp split over the locked invariant, the five-lens majority (MUST_REVISIT) beats the risk-analyst's lone STAYS_WITHIN: reading round-trip as the locked "non-LLM leg" silently merges two separately-locked conjuncts and is forbidden; Alex must explicitly re-scope it.

**CONSENSUS:** All six lenses converge on O5_HYBRID with the same spine: per-claim-kind routing (O1) layered with badge-from-round-trip (O3), reject O2 outright (LLM-alone certification = false-faithful regression), treat O4 as content hygiene that does not fix finding-1. All six independently verified the structural chain (every KC has a DEFINITION; DEFINITION has no non-LLM leg; conjunction => no KC ever faithful). Five of six (devils-advocate, domain-expert, pragmatist, first-principles, sequencer-pm) rule MUST_REVISIT_LOCKED; only risk-analyst rules STAYS_WITHIN. All six flag the DEFINITION content==quote self-entailment as zero/near-zero independent signal, and all six say "matches your lecture" should be driven by SpanClaimRoundTrip, not by faithful.

**CONFIDENCE:** 8

**KEY ISSUES:**
- BRICKED, not cosmetic: faithful unreachable (every KC has a DEFINITION claim with no non-LLM leg, strict conjunction) + gate ALLOWs only faithful (VerificationGate.kt:30) + queue OMITS non-faithful (signatures-lock.md:76) => the tutor serves 0 cards today, not just shows a missing badge.
- SECOND floor below finding-1, named only by the pragmatist and verified: decideOutcome case-1 reads claim.source.span (the AUTHORED span, VerificationRunner.kt:293) BEFORE the round-trip runs. pa-kc-001..004 have NO span field, only a quote, so they floor to DEFINITIONAL_NO_GOLD_SPAN even though their quotes WOULD relocate. The round-trip leg (SpanClaimRoundTrip.kt:30) needs only the quote, not a pre-authored span — so adding span fields is a prerequisite, and decideOutcome must read the RELOCATED span, not the authored one.
- SELF-ENTAILMENT TRAP (devils-advocate + risk-analyst, verified): naive-O1 stacks two tautologies — round-trip relocates claim.source.quote and NLI entails content==that-same-quote. Both check the quote against itself. If O1 ships without fixing this, the LLM vote is vacuous and DEFINITION-faithful reduces to 'the quote we copied is in the PDF' wearing a two-family-agreement costume. The NLI hypothesis MUST be an independent restatement, OR the LLM vote must be dropped for identity-definitions and faithful rests honestly on round-trip alone.
- FALSE-FAITHFUL GUARD: O2 (definitions need only be SUPPORTED to not block) is the one path that lets the LLM family ALONE certify a hallucinated definition with no machine anchor — REJECT. The machine anchor for a DEFINITION MUST be SpanClaimRoundTrip against the LIVE source (re-located, not trusted-as-authored), never the LLM vote.
- PLAN INVALIDATION: correctness-engine.md:78 + active-constraints.md:26 say the approved plan expected the D6 NLI leg to 'unblock the live FAITHFUL.' The brief's dry-run proves NLI correctly REFUSES bare equations ('1+1+1=3' -> UNCLEAR/REFUTED) and adds nothing on self-quotes. D6 alone does NOT fix finding-1. This is a discovery that the locked acceptance (R1: '>=1 KC faithful with page_anchor_status=LIVE') is unreachable on the current claim model — must be surfaced.
- The 'sympy:' GRADER_RULE string fed to NLI as a hypothesis is garbage-in (REFUTED/UNCLEAR). It is a directive to the SymPy leg, not an NL assertion. Strip it from the NLI path entirely (route only the equation to SymPy).

**RECOMMENDED PATH:** 1. SURFACE THE LOCKED-INVARIANT DECISION TO ALEX FIRST (do not write code first). Frame it as: 'the locked non-LLM-leg requirement is structurally unsatisfiable for prose definitions and currently serves 0 cards; the D6 NLI leg the plan expected to unblock faithful will not (dry-run proof); I propose re-scoping the invariant to non-LLM-leg = SymPy-for-equations / round-trip-for-prose, with the badge driven by round-trip. Bless or redirect.' This is the plan-first gate; everything below is contingent on his GO. Top mitigation: present it as ONE decision with the dry-run table attached, not a code diff. 2. (after GO) PART-1 content, near-zero risk: add verbatim `span` fields to pa-kc-001..004 source refs (council found 14/15 quotes relocate in the real Curs 1 PA.pdf) AND fix decideOutcome to read the RELOCATED span from the round-trip result, not the authored claim.source.span — otherwise case-1 still floors them. Top mitigation: re-run the offline relocation over the live extraction to confirm each added span before trusting it (the global memory verify rule — re-locate, never trust-as-authored). 3. DECOUPLE the badge: drive 'matches your lecture' + HonestFloor.FAITHFUL_TO_SOURCE off SpanClaimRoundTrip.pass as a served 'lecture-grounded' tier, and decouple it from `faithful` in TrustRoutes.badgeTextFor + the gate/queue admission. Top mitigation: keep `faithful` as the rarer strong tier so the badge change does not silently widen what enters SR — re-confirm the gate still fails CLOSED on a mutated quote (R1 acceptance b). 4. KILL self-entailment before any O1 DEFINITION-faithful branch: stop setting DEFINITION content = verbatim quote; the NLI hypothesis must be an independent restatement, OR drop the LLM vote for identity-definitions and rest DEFINITION-faithful on round-trip + a second independent locate. Top mitigation: add a test asserting NLI(premise==hypothesis) can NEVER alone produce faithful. 5. INVARIANT/GRADER_RULE keep SymPy + require the NL-restatement (not the bare equation/directive) for the family vote; strip the 'sympy:' string from the NLI path. Author real non-tautological invariants (O4) as the genuine strong-faithful path — but as follow-up content hygiene, not the unblock. 6. RE-RUN the offline acceptance CLI (not /admin/verify) and prove (a) >=1 KC reaches the new lecture-grounded served tier with page_anchor_status=LIVE, (b) a deliberately-mutated quote is REJECTED.

**WHAT WOULD CHANGE IT:** If grep of the live Curs 1 PA.pdf extraction showed the pa-kc-001..004 quotes do NOT relocate (the council's 14/15 claim is from memory and must be re-verified against the current extraction per the trust-but-verify rule), then PART-1 span-adding is moot and the badge-decoupling becomes the only lever — strengthening O3 over O1. If Alex declines to re-scope the locked invariant, the honest fallback is O3-pure: ship the deterministic round-trip as a 'lecture-grounded' served tier under a DIFFERENT badge string, leave `faithful` rare/empty, and accept that the LOCKED 'matches your lecture' wording must then itself move off faithful — which still requires his sign-off because the badge string is locked (active-constraints.md:24, D2). If a real non-tautological machine-checkable invariant exists in the lecture (not the authored 1+1+1=3 tautologies), that KC could reach genuine faithful under the unchanged lock, narrowing the revisit to prose-only — but no such invariant is currently authored.