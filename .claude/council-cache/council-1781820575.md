# Council review — 1781820575

**Problem:** Reorganize the 2.6 GB raw `material/` corpus into a clean per-subject, year-tagged tree with content-based dedup, before the digestion pipeline ingests it.
**Proposed approach:** `build-review/2026-06-19-material-reorg-plan-for-council.md` (4-phase: manifest → classify → dry-run move-map → destructive in-place move; dedup by whitespace-normalize+sha256; rule 4 = keep PDF only, drop md conversions).
**Project context:** jarvis-kotlin one-pass digestion teaching engine; material/ gitignored, no git net, off-box backup stale 2026-06-03; no paid APIs; non-LLM-oracle preferred for dedup-truth; PM-delegation.
**Timestamp:** 2026-06-19 (SESSION-83)

---

## 🔴 Devil's Advocate
STANCE: REJECT
REASONING: Rule 4's dedup depends on content-hash *equality* across PDF↔md, but proven on real files: `raw-corpus-md/PA/courses/lecture11_en.md` text-hash `74a2873f…` ≠ `lecture11_en.pdf` text-hash `8e7807f6…`, because 998 of 1176 md files carry a `source_pdf:`/`sha256:`/`extracted_at:` YAML frontmatter the PDF text lacks. So Phase-1 content-hashing clusters STRUCTURALLY ZERO of the ~998 format-dupes the job exists to remove. Compounding: 529 PDFs have spaces in paths (no null-delim handling shown), `_gdrive` has un-modeled subject variants (`PA_Y1`, `SO_vidrascu`), destructive move on un-backed-up data driven by a dedup oracle that is provably wrong.
KEY CONCERN: Content-hash dedup as specified cannot match a PDF to its .md twin — the md frontmatter already embeds the source PDF's `sha256:`, the correct join key the plan ignores in favor of a re-hash that provably cannot match.

## 📚 Domain Expert
STANCE: CONDITIONAL
REASONING: The deterministic-manifest + byte-hash-fallback-for-scanned-PDFs spine is sound (confirmed `Specimen Examen 2022.pdf` → 0 chars, fallback genuinely needed). But exact whitespace-normalize+sha256 is the wrong primitive for the two cases that matter: (a) md↔pdf format-dupe does NOT exact-match (85% of md prepend a provenance header) so rule 4 never fires and every md is wrongly kept as "sole copy"; (b) same-named cross-year RC lecture has different hashes so exact hashing under-merges true near-dupes. Fix is cheap and the data is already there: the md frontmatter embeds the source PDF's exact byte-sha256 (verified `ce12c9e5…` matches).
KEY CONCERN: Rule 4 is the main lever and is silently broken under exact-hash. Conditions: (1) link md→pdf via the embedded `sha256:` (non-LLM oracle, already present) or strip frontmatter before hashing; (2) add near-dup (SimHash/MinHash) scoped within (subject,year) for cross-year; (3) tar-before-move non-negotiable + prefer copy-into-new-tree-then-verify-then-delete over in-place move. Prior art omitted: jdupes/rdfind (exact), MinHash/SimHash (text near-dup), pHash/dHash (scanned-image near-dup).

## ⚙️ Pragmatist
STANCE: CONDITIONAL
REASONING: Deterministic-first spine is vindicated, not over-engineered: 4355/4690 files (92.9%) carry a subject token in their path, the 335 misses are almost all `SO_vidrascu` (one alias rule), so the LLM fan-out is a handful of files. 4-phase split is appropriate for a destructive move. Byte-hash fallback for scanned PDFs confirmed necessary.
KEY CONCERN: Space-in-path handling (1156 files, 24.6%) is the highest-probability data-corruption vector on Windows/git-bash — `find | head` already shattered `Specimen Examen 2022.pdf` into three phantom rows. Mandate `-print0`/`-0`/`-z` everywhere AND a pre-move `tar` (175 G free on C:, cost trivial). Also: the 4690-row move-map is NOT human-reviewable as a flat list → deliver as aggregated per-subject/per-year counts + dupe-drop log + flagged-anomalies subset. Side flag: `raw-corpus-study-guides/` holds 151 PDFs + 26 js besides jsx/md — spot-check before blanket-quarantining as `_derived`.

## 🧱 First Principles
STANCE: REJECT
REASONING: The plan destructively re-implements a pipeline the downstream consumer already owns. BINDING spec §2.5 (`2026-06-11-one-pass-digestion-teaching-engine-design.md:127-129`) specs content-hash dedup WITH canonical-path resolution at the ingest layer, hard-gated by INV-2.3 — so dedup belongs at the artifact/ingest layer, idempotent and reversible, not a 2.6 GB irreversible in-place move. Worse, Rule 4 contradicts the actual ingest tool: `curate-tutor/SKILL.md:24` says "use the pre-extracted `.md` sibling when one exists — it is cleaner," and spec §2.3 documents the very PDFs the plan would re-extract from as garbled (PS watermark noise, ALO c01/c02 Unicode-garbled — and `raw-corpus-md/ALO/courses/alo_c01.md` exists, plausibly the only legible copy the plan would delete).
KEY CONCERN: Rule 4 (PDF-only) is a net-negative quality move — deletes the cleaner artifact the pipeline explicitly prefers, to save ~57 MB (2% of corpus), on an irreversible move. Minimal correct version: build only `_manifest.json` (Phase 1) as advisory metadata, keep every byte in place, let the spec'd ingest/dedup/extraction-legibility gates do the rest at the content/ layer.

## ⚠️ Risk Analyst
STANCE: CONDITIONAL
REASONING: Core dedup design is sound and byte-hash does NOT wrongly collapse unique content (374/404 png byte-unique). But the blast radius is larger than implied and one failure mode is under-mitigated to guaranteed silent loss.
KEY CONCERN: Move-target filename collision = silent overwrite of distinct exam content. 750 non-unique basenames (1248 files, 26.6%). `problema.pdf` ×8 → 5 distinct contents; P1/P2/P3 2024 are three different POO exam problems distinguished ONLY by parent folder — a flatter target tree overwrites two of three, irreversible. Required: move-map asserts injective source→target (zero many-to-one), HARD-FAIL on collision, disambiguate with a context segment.
Ranked risks: (1) filename-collision overwrite [HIGH/HIGH, irreversible]; (2) pdftotext nonzero-EXIT (not just empty) routed to text-hash → mass false-dedup [MED/HIGH] — treat error == empty → byte-hash; (3) scanned-PDF false-dedup ~50/1216 [MED/HIGH] — §5.1 fallback correct, contingent on #2; (4) no backup before destructive move [MED/TOTAL] — tar mandatory; "reversible via move-map" is FALSE for any overwrite; (5) survey double-migration collision 172/329 basenames [HIGH/MED]; (6) cross-year collapse — adopt within-(subject,year) dedup; (7) md-stub false-canonical — fine contingent on #2-3.

---

## Sanity Check
SANITY Devil's Advocate: PASS — claim verified with actual divergent hashes on real files; frontmatter count (998/1176) measured. The embedded-sha256-as-join-key insight is correct and high-value.
SANITY Domain Expert: PASS — corroborates DA independently; verified embedded sha256 == PDF byte-sha256; prior-art (MinHash/SimHash/pHash, rdfind keep-by-rank) accurate and relevant.
SANITY Pragmatist: PASS — all numbers measured (92.9% subject-token, 1156 space-paths, 175 G free, 151 study-guide PDFs); PA_Y1-not-in-enum catch valid.
SANITY First Principles: FLAG (minor) — core insight is sound (destructive dedup at the wrong layer; Rule 4 deletes the better artifact, citing spec §2.3 + curate-tutor SKILL.md:24). Slight over-reach: INV-2.1 governs the PIPELINE not trusting path-labels for KC classification; it does NOT forbid a human-facing browsable reorg that uses path context as advisory input. So "classification work is non-transferable" is overstated — the reorg's value (clean browsable tree + manifest) survives. The conclusion (prefer non-destructive manifest, keep bytes) still holds.
SANITY Risk Analyst: PASS — filename-collision finding is the sharpest in the panel, measured with distinct hashes (problema.pdf ×8→5, P1/P2/P3 2024); error-exit path and survey-collision both grounded.

---

## Judge
VERDICT: FLAWED — fixable, but NOT ready to execute. Significant revision required first.
CONFIDENCE: 9 (findings are grounded in real corpus measurement with hashes, multiple independent agents converged, two REJECT + three CONDITIONAL).

WHY: The plan's headline mechanism is broken-as-written AND net-negative-if-fixed, and the destructive move is unsafe on un-backed-up data. Three independent, proven defects:
1. **Rule 4 (keep PDF only) must be DROPPED, not fixed.** (a) As specified it dedups nothing — 998/1176 md carry a provenance frontmatter so PDF↔md text-hashes never match. (b) Even fixed, it's net-negative: the md is often the *cleaner or only legible* copy (spec §2.3 documents the PDFs as garbled; curate-tutor explicitly prefers the md sibling), to save 2% of disk. Keep BOTH formats; link them via the md's embedded `sha256:` (a non-LLM oracle already in the files).
2. **The move must be NON-DESTRUCTIVE.** tar the 2.6 GB off-tree first (175 G free); copy-into-new-tree → verify checksums → only then remove originals. "Reversible via move-map" is FALSE for any overwrite.
3. **Filename-collision overwrite is guaranteed loss as written** (750 colliding basenames; P1/P2/P3 2024 distinct exams differ only by parent folder). The move-map MUST assert an injective source→target mapping and HARD-FAIL on any many-to-one; disambiguate with a context segment.

REQUIRED CHANGES before execution:
- Drop Rule 4; keep both formats; dedup PDF↔md via embedded `source_pdf`/`sha256` frontmatter (or strip-frontmatter-then-hash), not naive text-hash.
- Non-destructive: tar-first, copy → verify → delete; never in-place.
- Injective move-map gate (hard-fail on target collision; context-segment disambiguation).
- pdftotext nonzero-EXIT treated identically to empty → byte-sha256.
- Add near-dup pass (MinHash/SimHash over text shingles; pHash for scanned images) scoped within (subject, year) for cross-year revisions.
- Null-delimited iteration everywhere (`-print0`/`-0`/`-z`); 1156 space-paths.
- Extend subject enum/aliases: `PA_Y1`→PA, `SO_vidrascu`→SO, `Curs_Optional_OOB`.
- Spot-check the 151 study-guide PDFs before quarantining `_derived`.
- Deliver the dry-run as an AGGREGATED report (per-subject/year counts + dupe log + anomalies), not a 4690-row flat list.
- Reframe (First Principles): the reorg yields a clean browsable tree + advisory `_manifest.json`; the digestion pipeline still runs its OWN spec'd dedup/classification at the content/ layer — do not make the pipeline trust the new paths as truth.

Output saved to: .claude/council-cache/council-1781820575.md
