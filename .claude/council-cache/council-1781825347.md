# Council review — 1781825347 (FINAL, before destructive move)

**Problem:** Go/No-Go on executing an irreversible move of a ~3.2 GB un-git-tracked material corpus into material/archive/.
**Proposed approach:** `build-review/2026-06-19-material-reorg-FINAL-before-move.md` (tar → copy → verify-sha256 → delete originals).
**Timestamp:** 2026-06-19 (SESSION-83). Supersedes council-1781820575.

---

## 🔴 Devil's Advocate
STANCE: CONDITIONAL
Independently verified the move-map: 5038 rows, 4233 keep + 112 derived + 693 drop = 4345 kept; 0 sources under material/current/ (Q2 PASS); 0 many-to-one collisions; all 321 `__`-disambiguated targets distinct; all 4345 sources exist on disk (incl. 1646 spaces, 76 commas, 26 non-ASCII); longest target 207 chars (<260). The dry-run is sound.
KEY CONCERN: The MOVER DOESN'T EXIST YET (§5 unwritten) — can't approve executing a verify-then-delete whose code was never written/dry-run. Backup size figure wrong: corpus is ~3.2 GB (2.7 GB material + 539 MB _material_survey), not 2.6 GB; current/ (96 MB) has NO other copy. Conditions: (1) build mover + dry-run copy+verify into scratch first; (2) DEFER delete to a later session; (3) fix size figure, verify tar member count + sha256 before touching sources.

## ⚠️ Risk Analyst
STANCE: CONDITIONAL
Tested the planner: 4345 kept = 4345 unique targets, 0 many-to-one, 0 empty; 0/693 dropped dupes with a missing canonical, 0 canonicals self-dropped; 0 targets >260 chars; 0 case-insensitive collisions (Windows overwrite vector clear); 0 missing sources; 0 symlinks; 3 benign zero-byte files; current/ fully excluded. Math holds.
KEY CONCERN: The delete step must be DEFERRED, not just gated on verify-pass. `grep -rE "shutil.(move|copy)|os.remove|tarfile|rmtree"` over tools/material-reorg/ = ZERO hits — the mover is unwritten code being approved sight-unseen against a gitignored corpus with a 16-day-stale off-box backup. Conditions: build mover + review; tar excludes current/ + archive/; split delete into a separate next-session step after Alex eyeballs the tree; 175 G free covers keeping originals.

## ⚙️ Pragmatist
STANCE: CONDITIONAL
Independent CSV parse confirms 4345 unique targets, 0 collisions; current/ excluded (0 rows); longest target 207 chars; no embedded-newline paths; 175 G free vs ~3.2 G tar+copy fine. `__`-disambiguation is content-correct (alo_c01.md differs between dumps), ugly but lossless.
KEY CONCERN: Defer the delete (§7-Q1 failure window) — tar+copy+verify now, keep raw-corpus-* + _material_survey until Alex confirms next session. Also two brief errors to fix: (1) the 11 `_neclasificat` files (README, _failures.log, scrape-index, zip) are MOVED to archive/_neclasificat/, NOT "junk excluded" as the brief says; (2) the disambiguated count is ~409 `__`-rows, not 321. Neither is data-loss, but the brief misdescribes the tree.

## 📚 Domain Expert
STANCE: CONDITIONAL
Verified the load-bearing curation invariant — never drop the last copy of a content — PASSES: 0 orphan canonicals across all 693 drops; counts reconcile (4233+112+693=5038); current/ excluded; longest target 207 chars; 175 G free.
KEY CONCERN: The "tar→copy→verify→delete" pattern is right but the delete is SEQUENCED WRONG — deleting in the same run as the copy is the textbook unsafe-migration anti-pattern when the only fallback is a just-created, never-restore-tested tar over a gitignored corpus (backup stale 2026-06-03). Best practice (rsync --checksum, restic/borg, git copy-then-gc) defers reclamation. Conditions: (1) split mover into copy+verify now + a separate delete gated on Alex confirming the tree AND a successful `tar tf` listing ("a tar you cannot list is not a backup"); (2) make explicit that 35 empty-body md shells collapse by empty-hash COINCIDENCE (underlying scanned PDFs survive byte-hashed) so a future scanned-PDF-with-real-md-notes never silently collapses. current/+archive/ under one gitignored material/ is correct layering.

## 🧱 First Principles
STANCE: CONDITIONAL
Artifacts sound: injective gate holds (0 residual collisions in manifest.json), current/ excluded (0 rows; walk_corpus prunes current+archive), the `__`-disambiguations are real distinct content, material/ gitignored with 0 tracked files, max path ~179+prefix (<260).
KEY CONCERN: Step-4 delete is GRATUITOUS irreversibility. Raw dumps are gitignored+untracked → they live in exactly ONE place; deleting makes a single tar the sole net for a 2.6 GB partly-unrecoverable corpus (old-year CS scrapes behind dead 404 links are NOT re-pullable). With 175 G free, COPY costs nothing and DELETE buys only tidiness — bad risk/reward. Also: the archive reorg is NOT on the tutor's critical path; current/ (96 MB, already verified) is the only layer the pipeline needs short-term. Run tar→copy→verify, then STOP; defer/skip delete.

---

## Sanity Check
SANITY Devil's Advocate: PASS — every claim verified against move-map.csv/manifest.json with a proper CSV parser (caught the awk-comma trap); mover-absence confirmed; size-figure correction grounded in `du`.
SANITY Risk Analyst: PASS — ran the injectivity/orphan-canonical/case-collision/MAX_PATH checks directly; grep for mover code = 0 hits confirmed.
SANITY Pragmatist: PASS — independent CSV parse; caught two real brief misstatements (_neclasificat moved-not-dropped; disambig count 409 not 321).
SANITY Domain Expert: PASS — verified 0 orphan canonicals (the key invariant); the empty-hash-coincidence observation is correct and worth surfacing.
SANITY First Principles: PASS — the critical-path + risk/reward reasoning is sound; verified gitignored+untracked status directly.

---

## Judge
VERDICT: CONDITIONAL GO. Proceed with TAR + COPY + VERIFY this session; DEFER the DELETE to a later session after Alex browses and confirms the new material/archive/ tree. Unanimous (5/5 CONDITIONAL).
CONFIDENCE: 9 — all five independently verified the move-map's safety properties against the real artifacts and converged; the dry-run math is proven sound, the only issue is the destructive delete's irreversibility + the unwritten mover.

WHY: The planner is correct and proven (injective, no orphan canonicals, current/ excluded, paths safe, sources present). The risk is entirely in the unwritten destructive mover + deleting an untracked corpus whose sole net would be one fresh, untested tar. With 175 G free, the delete buys only tidiness at the cost of irreversibility — so decouple it.

REQUIRED before any execution:
1. **Build the mover, then DRY-RUN it** (copy + sha256-verify into the real archive/ tree; NO delete). Bring nothing destructive online unreviewed.
2. **DEFER DELETE entirely** — keep `material/raw-corpus-*` and `~/Downloads/_material_survey` in place this session; delete only in a later session after Alex eyeballs the tree.
3. **Tar safety**: tar the raw dumps + _material_survey ONLY (exclude current/ + archive/); record + re-read its sha256; `tar tf` list-test it before trusting ("a tar you cannot list is not a backup"). Corpus is ~3.2 GB, not 2.6 GB.
4. **Fix the brief's factual errors**: the 11 `_neclasificat` files are MOVED to archive/_neclasificat/ (not dropped); disambiguated count ≈409 not 321; 35 empty-body md shells collapse by empty-hash coincidence (underlying PDFs survive) — make explicit.
5. current/ + archive/ under one gitignored material/ = correct; leave cross-layer redundancy alone; dropped dupes vanish with originals at gc time (don't separately delete).

Output saved to: .claude/council-cache/council-1781825347.md
