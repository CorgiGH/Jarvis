# B5-RESHAPE review + council + audit — 1780613715

Workflow b5-reshape-review-council-audit (run wf_5395b4a4-2ff). Diff 93dd8f1..1a12fdf.

---
## CODE REVIEW (6 dims)

### trustnet-safety — CONCERNS
most_serious: A prose GRADER_RULE can reach per-claim `faithful` and light `lecture_grounded` even though its asserted text is never round-tripped against the source — the round-trip checks an UNRELATED anchor quote (`source = anchorRef`), not the rule content. This is a live, reachable false-faithful/false-grounded path (pa-kc-005 grader rule line 23), and the test suite actively asserts the unsafe behavior as correct.
- [HIGH] Prose GRADER_RULE reaches faithful/grounded via an UNRELATED anchor quote — its text is never checked against the source @ src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:370-372 (case 3p); src/main/kotlin/jarvis/content/ContentReconcile.kt:116 (content=rule, source=anchorRef)
  The reshape's prose path (case 3p) makes the LIVE round-trip the sole positive anchor for faithful: `!isEquationalKind && roundTrip.pass && !anyThrew && !agreedNonSupported` (VerificationRunner.kt:370-372). This is SAFE for DEFINITION because ContentReconcile emits a DEFINITION with `content = ref.quote` and `source = that ref` (ContentReconcile.kt:77) — content IS the round-tripped quote, so a hallucinated body cannot exist independently. But a PROSE GRADER_RULE is emitted with `content = rule` and `source = anchorRef` = the KC's FIRST span-bearing source ref (ContentReconcile.kt:116, 67). The round-trip (SpanClaimRoundTrip.check) relocates `claim.source.quote` (the anchor quote), NOT `claim.content` (the rule). So `roundTrip.pass` is true whenever the unrelated anchor quote is a real lecture quote — REGARDLESS of whether the rule text has any basis in the lecture. The only thing that can block faithful is `agreedNonSupported` (BOTH families REFUTE). The LLM/NLI is prompted with premise=anchor-quote, hypothesis=rule-text (TwoFamilyDeriver.kt:75-91); an unrelated pairing yields neutral/UNCLEAR far more often than a confident bothREFUTED (NLI abstain-floor 0.60, NliEntailmentLlm.kt:220). Net: a grader-rule whose content is decoupled from its cited quote reaches per-claim `faithful`, and (since round-trip passes and nothing failed) drives `groundedKc` = true (VerificationRunner.kt:141-144) ⇒ the 'matches your lecture' badge lights. This is LIVE-reachable: pa-kc-005 grader_rules[1] = 'the answer must distinguish the three size measures...' (content/PA/kcs/pa-kc-005.yaml:23) is prose (invariant=null), anchors on the quote 'For the values of each data type, the size of representation must be mentioned' (lines 26-30) which relocates, and concerns a DIFFERENT claim than that quote. The spec's own step-1 mental model ('if an NLI vote is kept it must run on an INDEPENDENT restatement, never the verbatim quote') only reasons about DEFINITION content==quote; it never addresses the prose-GRADER_RULE content!=anchor-quote case. Violates the stated invariant 'no path may let hallucinated/non-lecture content reach faithful OR grounded'. Fix options: (a) for a prose GRADER_RULE require an INDEPENDENT positive signal on the RULE text (e.g. bothSupported on the rule-vs-quote NLI, i.e. promote the LLM from veto-only to a required vote for grader rules where content!=quote), or (b) round-trip the rule content itself / require the rule's own cited span, or (c) at minimum exclude prose GRADER_RULE from the faithful/grounded path until it has its own anchor.
- [HIGH] Test suite codifies the unsafe prose-GRADER_RULE-faithful behavior as the expected contract @ src/test/kotlin/jarvis/tutor/verify/VerificationRunnerTest.kt:480-532, 593-606
  The multi-claim regression test builds pa-kc-005 with two prose GRADER_RULEs ('award full marks when the size is mentioned for every type', 'deduct one point per type whose representation size is omitted') ALL sharing `source = goldSource()` — i.e. content != the round-tripped quote (VerificationRunnerTest.kt:465-486). It then ASSERTS these reach `faithful` 'via the prose anchor' (lines 520-524) and that the KC aggregate is `faithful`/grounded (lines 593-606). So the false-faithful path is not an unnoticed bug — it is locked in by a green test. There is NO test of the safety-critical case: a prose GRADER_RULE whose `content` is hallucinated/non-lecture text while its anchor quote legitimately relocates, asserting it must NOT reach faithful. Every B5r-1 self-entailment guard test (VerificationRunnerTest.kt:636-712) uses DEFINITION fixtures where content==quote, so they never exercise the decoupled-content case. This means the suite's green status gives false assurance on exactly the dimension the reshape was supposed to protect. Recommend adding a red-then-green test: prose GRADER_RULE, content='completely fabricated rule with no lecture basis', source.quote=a real relocating anchor, both families NOT both-REFUTED ⇒ assert NOT faithful and grounded=false.
- [LOW] groundedKc keys on roundTripPass without excluding DEFINITIONAL_NO_GOLD_SPAN — already flagged as D-R14 but still latent @ src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:141-144; docs/superpowers/findings/2026-06-04-b5-reshape-decisions.md:39 (D-R14)
  groundedKc (VerificationRunner.kt:141-144) = isNotEmpty && all{roundTripPass} && none{failed}. roundTripPass is true for any claim whose quote relocates even when the claim outcome was DEFINITIONAL_NO_GOLD_SPAN (source.span==null but source.quote still relocates). The decision log D-R14 acknowledges this affects only the `*-fixture-*` KCs today (real KCs all have authored spans post-B5r-4) and is not a false-faithful (grounded != faithful and the quote genuinely relocates). It is correctly scoped as fixtures-only and the live corpus is clean, so this is LOW. But it remains a latent grounding-laxity: a future KC that ships a quote without a span would light grounded while its claim floored to uncertain. Recommend the D-R14 tightening (require claim outcome ∉ {DEFINITIONAL_NO_GOLD_SPAN} for grounded, or exclude no-span claims) before more subjects are onboarded.
- [LOW] Serve-side D8 staleness gate and EQUATIONAL_LLM_UNCONFIRMED softening are SOLID — confirmed no regression @ src/main/kotlin/jarvis/web/TrustRoutes.kt:204-273; src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:258-259, 388-391
  Positive confirmations (not defects): (1) servedHonestFloor (TrustRoutes.kt:248-273) wraps lecture_grounded in the exact same fail-closed D8 hash gate as faithful — a NULL content_hash, NULL lecture_grounded (legacy row), or hash mismatch all return UNVERIFIED. Well-tested (TrustRoutesTest.kt:367-409: stale-hash, NULL-hash, NULL-grounded all serve unverified). (2) resolveStatus keeps verification_status honest (faithful only on hash match; TrustRoutes.kt:204-227). (3) EQUATIONAL_LLM_UNCONFIRMED (D-R9, VerificationRunner.kt:388-391) softens to `uncertain` ONLY when SymPy ran+passed AND `!anyRefuted` AND `!bothSupported` — `anyRefuted` (VerificationRunner.kt:258-259, true if EITHER family REFUTED) keeps any contradicted equational claim in the `else`→failed branch, so a SymPy-proven-but-LLM-contradicted claim never certifies; it floors to uncertain only in the genuinely-inconclusive (both-UNCLEAR) case. This correctly does NOT certify a contradicted claim. (4) FAIL-LOUD on the NLI/SymPy bridges is intact: infra failure throws (NliEntailmentLlm.kt:116-127, runNli), `anyThrew` blocks faithful, and the D-R13 temp-file fix removed the silent-empty-output degrade. The agreed-REFUTED veto (agreedNonSupported, VerificationRunner.kt:250) holds for prose. These pieces are genuinely solid.

### decideoutcome-logic — CONCERNS
most_serious: Prose GRADER_RULE (and any future non-DEFINITION prose kind) reaches `faithful`/`grounded` via case 3p purely because an UNRELATED anchor quote round-trips — the 'self-entailment is zero-signal, round-trip is the anchor' rationale (D-R3) is only valid for DEFINITION where content==quote. For a prose grader-rule, content=the rule text but the round-trip anchors on the KC's anchor quote (ContentReconcile.kt:116 + SpanClaimRoundTrip.kt:30), so the only leg that judges the rule text (the LLM/NLI family) is discarded for promotion and a hallucinated rule can be certified faithful unless both families agree-REFUTED. This is a latent false-faithful path — the precise invariant the reshape exists to protect — with live exposure on pa-kc-005/006.
- [HIGH] Prose GRADER_RULE reaches `faithful`/`grounded` on an UNRELATED anchor-quote round-trip — the 'self-entailment is zero-signal' rationale only holds for DEFINITION (content==quote), not for prose kinds where content != the cited quote @ src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:370-372 (case 3p); src/main/kotlin/jarvis/content/ContentReconcile.kt:116; src/main/kotlin/jarvis/tutor/verify/SpanClaimRoundTrip.kt:30
  case 3p certifies a prose claim as faithful when `roundTrip.pass && !anyThrew && !agreedNonSupported`. The justification (D-R3) is that for prose the LLM/NLI vote is self-entailment noise so round-trip is the sole positive anchor. But that is only true for a DEFINITION, where ContentReconcile.claimsFor emits `content = ref.quote` (the round-trip target IS the claim content). For a PROSE GRADER_RULE, claimsFor emits `content = rule` with `source = anchorRef` (ContentReconcile.kt:116) — the round-trip anchors on the KC's first span-bearing quote, which is NOT the rule text. SpanClaimRoundTrip.check keys purely on `claim.source.quote` (SpanClaimRoundTrip.kt:30). So decideOutcome's case 3p certifies the grader-rule `faithful` purely because an unrelated anchor quote relocates verbatim, while the ONLY leg that actually judged the rule text (the LLM/NLI family) is discarded for promotion and only honored as an agreed-REFUTED veto. A hallucinated grader-rule attached to a KC with a genuine anchor quote reaches `faithful` (and lights the 'matches your lecture' badge) unless BOTH families happen to agree REFUTED — a one-family REFUTED disagreement (agree=false ⇒ agreedNonSupported=false) does NOT veto. This is a path to false-faithful for non-DEFINITION prose, the exact invariant the reshape must protect. Live exposure exists: pa-kc-005 and pa-kc-006 each carry a prose grader_rule. (The same applies to any future MISCONCEPTION_REFUTATION/STEM claim where content != the anchor quote; currently claimsFor only emits DEFINITION/INVARIANT/GRADER_RULE so prose GRADER_RULE is the live surface.) Fix options: gate case 3p on `claim.kind == DEFINITION` (content==quote) and route prose GRADER_RULE through a path that REQUIRES bothSupported (the LLM IS the only judge of a rule that isn't its own quote), or require the round-trip to anchor on the claim's own content for prose grader rules.
- [MEDIUM] A SymPy bridge that COULD-NOT-RUN on an equational claim maps to `failed`, not the `uncertain` floor — violating the system's own FAIL-LOUD 'never-ran ≠ disagreed' principle @ src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:377-392 (case 4/4b/else); src/main/kotlin/jarvis/tutor/verify/NonLlmLegs.kt:56-63; src/test/kotlin/jarvis/tutor/verify/VerificationRunnerTest.kt:717-737
  SymPyLeg.check tags a real equation whose bridge failed (python missing / parse error / timeout) as `kind=SYMPY, ran=false` (NonLlmLegs.kt:56-63 via realSymPyEquals which returns ran=false on any infra failure). In decideOutcome, case 4 (NONLLM_LEG_NONE ⇒ uncertain) requires `nonLlm.kind == NONE`, and case 4b requires `nonLlm.ran && nonLlm.pass`. A `kind=SYMPY, ran=false` equational claim matches NEITHER, so it falls to the `else` ⇒ DISAGREE_OR_ROUNDTRIP_FAIL_OR_THREW ⇒ `failed`. Confirmed by test B5r-1(d) (VerificationRunnerTest.kt:717-737), which only asserts assertNotEquals(faithful) — it does NOT pin the outcome, masking that the actual result is `failed`. Consequence: a transient python/sympy outage during an offline audit silently turns a genuinely-faithful equational KC into `failed`, which then aggregates the WHOLE KC to `failed` (aggregateKc) and suppresses grounding — and `failed` has no auto-clear (VerificationStatus_ : failed+ALL_AGREE stays failed), so it requires a re-curate to pending to recover. This is direction-SAFE (a false-reject, never false-faithful, so the badge invariant holds), but it contradicts the explicit FAIL-LOUD H5 contract that never-ran floors to uncertain (the NonLlmLegs.kt:40-43 comment even states a non-running checker should floor to uncertain). The 'could-not-run' vs 'machine-disproved' distinction is collapsed. Fix: route `isEquationalKind && nonLlm.kind==SYMPY && !nonLlm.ran && !anyRefuted && roundTrip.pass && !anyThrew` to NONLLM_LEG_NONE (or EQUATIONAL_LLM_UNCONFIRMED), reserving `failed` for `ran && !pass` (genuine math contradiction).
- [MEDIUM] `groundedKc` keys on the round-trip leg, not the claim outcome — a span-less DEFINITION (DEFINITIONAL_NO_GOLD_SPAN ⇒ uncertain) whose quote merely FUZZY-relocates still lights the 'matches your lecture' badge; this is a code seam, not a 'fixtures-only' data accident @ src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:141-144 (groundedKc); src/main/kotlin/jarvis/content/ContentValidator.kt:294-295; src/main/kotlin/jarvis/tutor/verify/LiveSourceLocator.kt:28
  groundedKc (VerificationRunner.kt:141-144) = `all { roundTripPass } && none { newStatus==failed }`. For a DEFINITION with `source.span == null`, decideOutcome's first case returns DEFINITIONAL_NO_GOLD_SPAN ⇒ uncertain (honest). But SpanClaimRoundTrip keys on quote, not span, and LiveSourceLocator.locate does FUZZY matching (LiveSourceLocator.kt:7,28 — located up to FUZZY_MAX even with non-zero fuzzy distance), so a span-less quote that fuzzy-matches yields roundTripPass=true. groundedKc then sees roundTripPass=true + not-failed ⇒ grounded=true ⇒ servedHonestFloor returns FAITHFUL_TO_SOURCE ⇒ the badge claims 'matches your lecture' for content that was NEVER anchored to a precise offset and only fuzzy-located. The decision log (D-R14) dismisses this as 'fixtures only — real KCs all have authored spans post-B5r-4', but ContentValidator only ERRORS on a missing span for `grounding_tier=strict` KCs (ContentValidator.kt:294); a NON-strict KC with a span-less, fuzzy-found quote passes validation with a WARNING (line 295). So nothing in the code prevents a non-strict KC from authoring a span-less DEFINITION and lighting the grounded badge — it is a latent false-grounded seam gated only on current corpus data + author tier choice, not a structural invariant. Fix (the log's own suggestion): require the claim's outcome ∉ {DEFINITIONAL_NO_GOLD_SPAN} for it to count toward grounded (i.e. exclude span-less claims), making grounded depend on the claim VERDICT, not the raw leg boolean.
- [LOW] Decision-log live table reports pa-kc-005's prose grader-rule as `uncertain`, which is inconsistent with the case 3p code path (a prose claim with a passing anchor round-trip and a non-agreed-REFUTED LLM verdict resolves to faithful) @ docs/superpowers/findings/2026-06-04-b5-reshape-decisions.md:33; src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:370-372
  The B5r-7 acceptance table (findings/2026-06-04-b5-reshape-decisions.md:33) annotates pa-kc-005 as 'INVARIANT+1 grader honestly uncertain'. But a prose GRADER_RULE (invariant=null) routes through case 3p, which fires on `roundTrip.pass && !anyThrew && !agreedNonSupported` and does NOT read the LLM verdict to demote — an UNCLEAR/UNCLEAR NLI result is not agreedNonSupported, so case 3p would resolve that grader-rule to `faithful`, not uncertain. Either the live grader-rule's anchor quote did not round-trip (then it would be `failed`, also not uncertain), or the table is loosely aggregating. Worth reconciling because it is the same code path as the HIGH finding above: if 005's prose grader actually landed uncertain, the reason is incidental, not a guard — confirming that the only thing keeping prose grader-rules out of false-faithful today is the NLI verdict happening not to be SUPPORTED, which is not a correctness guarantee. Recommend an explicit unit test that pins the resolved outcome of a prose GRADER_RULE whose anchor quote round-trips and whose families return mixed/UNCLEAR verdicts.
- [LOW] Positive note: case ordering, the anyRefuted/agreedNonSupported distinction, AuditOutcome→VerificationStatus wiring, and the aggregateKc conjunction are correct and well-guarded @ src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:360-405; src/main/kotlin/jarvis/tutor/VerificationStatus.kt:73-136
  Verified concretely: (1) decideOutcome's when-cascade is correctly ordered — DEFINITIONAL_NO_GOLD_SPAN and FAMILY_COLLAPSE are checked BEFORE any faithful case, so a span-less or collapsed claim can never reach faithful. (2) The equational faithful path (case 3) correctly reads `bothSupported` (not `agree`), so a both-REFUTED never certifies. (3) case 4b (EQUATIONAL_LLM_UNCONFIRMED) is correctly narrowed by `!anyRefuted` so an asymmetric one-REFUTED/one-UNCLEAR equational claim falls to `failed` (else), not the uncertain floor — anyRefuted (either family) vs agreedNonSupported (agree && !bothSupported) are used correctly in their respective places. (4) The new EQUATIONAL_LLM_UNCONFIRMED → uncertain mapping is wired consistently in ALL FIVE `from` branches of VerificationStatus_.transition and the when is exhaustive over the 7 AuditOutcome values; it does not widen the frozen 5-literal VerificationStatus surface. (5) aggregateKc is a correct conjunction (empty⇒uncertain, any failed⇒failed, all faithful⇒faithful, else⇒uncertain) and operates on per-claim verdicts derived from a FRESH `pending` prior (order-independent, the FIX-A multi-claim de-poisoning). (6) The runner always uses prior=pending so only the {faithful,uncertain,failed} outcomes are reachable per claim; the faithful path is the sole pending+ALL_AGREE path. No ordering bug or wrong-status mapping found in the AuditOutcome/transition layer itself; the concerns above live at the boundary between decideOutcome's prose-leg assumption and what claimsFor/round-trip actually feed it.

### python-bridge — SOLID_WITH_NITS
most_serious: No false-faithful path exists in the bridge: every infra/parse/timeout failure throws (NLI) or returns ran=false (SymPy), which the runner converts to anyThrew/uncertain — the badge can never lie via this layer. The findings are robustness/maintainability nits, not correctness holes. The most actionable is the latent waitFor-before-stdout-drain ordering (deadlock-until-timeout if any imported python lib ever floods stdout), but even that fail-loud-degrades rather than producing a false pass.
- [LOW] waitFor() precedes stdout drain — latent deadlock-until-timeout if any python lib writes to stdout @ src/main/kotlin/jarvis/NliEntailmentLlm.kt:165-172; src/main/kotlin/jarvis/tutor/verify/NonLlmLegs.kt:128-134
  In runNli, proc.waitFor (line 165) is called BEFORE the stdout drain readNBytes (line 170); realSymPyEquals has the identical ordering (waitFor line 128, drain line 133). This is SAFE TODAY only because the runner writes a single ~20-byte line to stdout and all of transformers'/torch's noise goes to stderr (now DISCARDed — a good fix). But it relies on the unenforced invariant 'no imported library ever writes to stdout.' Some HuggingFace hub-download code paths and certain torch/warnings configurations DO print to stdout; if such output ever exceeds the OS pipe buffer (~4-64KB on Windows) before the verdict line, the python process blocks on the full stdout pipe while the JVM blocks in waitFor → deadlock until the 120s (NLI) / 10s (SymPy) timeout fires. Fail-loud is preserved (timeout → throw / ran=false, never a false pass), so the worst case is a 120s stall per claim, not a wrong badge. Robustness nit, not a correctness hole. Hardening: spawn a draining thread for inputStream before waitFor (or redirect stdout to a temp file and read it after), and/or set HF_HUB_DISABLE_PROGRESS_BARS=1 + TRANSFORMERS_VERBOSITY=error + PYTHONUNBUFFERED in the ProcessBuilder env to belt-and-suspenders the stdout cleanliness.
- [LOW] Temp-file leak when Files.write fails after createTempFile succeeds (both bridges) @ src/main/kotlin/jarvis/NliEntailmentLlm.kt:146-147; src/main/kotlin/jarvis/tutor/verify/NonLlmLegs.kt:105-111
  runNli creates the temp file (line 146) and writes it (line 147) OUTSIDE the try/finally that deletes it — the cleanup finally is attached to the process try block at line 159. If Files.write throws (disk full, AV lock, EACCES), the file created at 146 is orphaned and the exception propagates uncaught out of runNli. Fail-loud is preserved (the leg throws → anyThrew), but the temp file leaks. realSymPyEquals wraps createTempFile().also{write} in a try (lines 105-111), but its catch at line 109 returns a PyResult WITHOUT deleting a file that createTempFile may already have created before the write threw — same leak, different shape. Both are minor (temp dir, .py files, low frequency on an owner-manual batch). Fix: move createTempFile+write inside the same try whose finally deletes the file, or delete in the write-failure catch.
- [LOW] parseNliInput↔buildPrompt coupling is verified only against a hand-copy, never the real prompt @ src/main/kotlin/jarvis/NliEntailmentLlm.kt:74-102; src/test/kotlin/jarvis/NliEntailmentLlmTest.kt:30-40
  parseNliInput hard-codes three marker literals (CLAIM (, \nSTATED INVARIANT:\n, \nSOURCE QUOTE:\n) that must stay byte-identical to TwoFamilyDeriver.buildPrompt (TwoFamilyDeriver.kt:75-92). buildPrompt is private and the parser test (NliEntailmentLlmTest.kt:33 deriverPrompt) MIRRORS the format by hand rather than calling the real buildPrompt; the test comment itself flags 'if the deriver's format ever drifts, this fixture and the parser must move in lockstep.' Consequence: if someone edits the real buildPrompt (e.g. reorders quote/invariant, renames a marker, drops the \n after CLAIM (...)), zero test fails — the mirror drifts in lockstep with the bug, and parseNliInput silently extracts the wrong premise/hypothesis. There is also no end-to-end test wiring a real TwoFamilyDeriver to an NliEntailmentLlm (TwoFamilyDeriverTest.kt:43 uses fakes). The parsing logic itself is otherwise robust (reads the LAST user message; uses lastIndexOf for the quote so a stray marker mention in content can't hijack the premise; first-of-{invariant,quote} bounds the hypothesis). Fix: make buildPrompt internal and have the test call it directly, OR add one VerificationRunner/deriver integration test that proves a real prompt round-trips through parseNliInput.
- [LOW] Empty-but-present SOURCE QUOTE spawns python on a blank premise instead of abstaining cheaply @ src/main/kotlin/jarvis/NliEntailmentLlm.kt:40-41, 79-82
  complete() short-circuits to UNCLEAR only when parsed.premise == null (no SOURCE QUOTE marker at all). If a claim's source.quote is an empty/whitespace string, buildPrompt still emits the \nSOURCE QUOTE:\n marker with nothing after it, so parseNliInput returns premise="" (non-null), and complete() spawns python + loads the ~0.5GB model to run NLI on an empty premise (which will just abstain to UNCLEAR/neutral). Wasteful (full model load for a guaranteed abstain) but not a correctness hole — an empty premise can never yield SUPPORTED, and faithfulness for prose doesn't depend on the NLI vote anyway. Minor: treat blank-after-trim premise the same as null in complete().
- [LOW] Model loaded from scratch per claim (~6s × 23) — no persistent process; acceptable now, flag for a follow-up @ src/main/kotlin/jarvis/NliEntailmentLlm.kt:139-182; src/main/kotlin/jarvis/tutor/verify/TwoFamilyDeriver.kt:38-40
  Each claim spawns a fresh python that re-imports torch/transformers and re-loads DeBERTa-v3 from the HF cache, then exits — no warm process is reused. The decision log (D-R12/D-R13) and task framing acknowledge ~6s/claim. This is acceptable: verifyContent is the owner/manual OFFLINE batch (VerifyContentCli is explicitly NOT on a request path; D7 keeps the VPS on OpenRouter so the blocking ProcessBuilder never runs on a Ktor request thread). It has ZERO false-faithful impact. Two correctness-adjacent observations for the persistent-process follow-up: (a) legs run SEQUENTIALLY in TwoFamilyDeriver.derive (a then b), so relay+NLI latency is additive per claim; (b) a persistent NLI server would need its own fail-loud contract (a crashed/hung server must still surface as a throw, not a silent UNCLEAR) to preserve the no-false-faithful invariant. Defer, don't fix now.
- [LOW] Bridges duplicated rather than shared — divergence risk between the two near-identical ProcessBuilder runners @ src/main/kotlin/jarvis/NliEntailmentLlm.kt:139-182; src/main/kotlin/jarvis/tutor/verify/NonLlmLegs.kt:96-141
  runNli and realSymPyEquals are now ~95% identical: same JARVIS_PYTHON3 resolution, temp-file staging, redirectError(DISCARD), \n-flattened stdin, bounded waitFor, readNBytes(4096), tab-delimited single-line parse. D-R13 was literally 'the same latent bug in both, fixed in both' — which is exactly the failure mode duplication invites (the -c quote bug, and the SymPy integration test that always skipped on Windows, masked it for both). The fail-loud SEMANTICS deliberately differ (NLI throws; SymPy returns ran=false) and that distinction is correct, but the PROCESS plumbing is identical and should be a single shared helper (e.g. runPythonRunner(script, stdinLines, timeout) -> raw stdout) so the next plumbing fix lands once. Maintainability nit, not a bug.

### badge-serve — CONCERNS
most_serious: report-wrong (TrustRoutes.kt:554-575) flips KC status faithful→pending but never clears lecture_grounded, so servedHonestFloor still returns FAITHFUL_TO_SOURCE and the badge keeps showing "matches your lecture" on content a student has actively disputed. A production faithful KC always carries lecture_grounded=true, and the content_hash stays matching (content unchanged), so the D8 gate does not save it. The existing route test masks this by seeding lecture_grounded as NULL. The decision log confirms the report_wrong lifecycle was deferred and never reconciled with the new grounded signal. This is the badge lying — the exact invariant the dimension is meant to protect.
- [HIGH] report-wrong flips status faithful→pending but never clears lecture_grounded — the badge keeps lying "matches your lecture" on content a student actively disputed @ src/main/kotlin/jarvis/web/TrustRoutes.kt:554-575
  The report-wrong handler only updates `status` and `updatedAt` (TrustRoutes.kt:554-575). It does NOT touch `lectureGrounded` or `contentHash`. In production a faithful KC ALWAYS has lecture_grounded=true (faithful requires every claim faithful, which requires every claim's round-trip to pass ⇒ groundedKc() true — VerificationRunner.kt:141-144, confirmed by the finalizeKc test at VerificationRunnerTest.kt:760-770). After a student files report-wrong on that KC: status=pending, lecture_grounded STILL true, content_hash STILL matching (content unchanged). The next GET /verify/{kcId}/status runs servedHonestFloor (TrustRoutes.kt:248-273): the early guard `status != faithful && !grounded` evaluates to `true && false` = false (grounded is still true), so it does NOT fall to UNVERIFIED; the D8 hash gate then passes (content unchanged) ⇒ returns FAITHFUL_TO_SOURCE ⇒ badge_text = "matches your lecture / faithful to your source". So a KC whose content a student has flagged as wrong still lights the lecture badge. Before B5r-2 the badge was keyed purely on status=faithful, and report-wrong's faithful→pending correctly dropped it; the decoupling re-introduced the lie via a signal report-wrong forgets to clear. D-R6 says "a failed claim SUPPRESSES the grounded badge" — but report-wrong produces `pending`, not a `failed` claim, so D-R6 does not cover it. The VerificationGate correctly DENYs SR-entry on an open report_wrong (VerificationGate.kt:30), but the gate guards SCHEDULING, not the badge serve — the two are decoupled, so the badge still lies. Fix: in the report-wrong transaction set lectureGrounded = false (or null) alongside status. The decision log confirms this gap was unhandled: "report_wrong REVERIFY lifecycle ... deferred" (docs/superpowers/findings/2026-06-04-b5-reshape-decisions.md:50). The existing route test (TrustRoutesTest.kt:797-857) misses it because seedFaithfulCard leaves lectureGrounded NULL — masking the production reality where it is true.
- [MEDIUM] lecture_grounded can be true while every claim is DEFINITIONAL_NO_GOLD_SPAN (uncertain) — badge lights on un-anchored citations, guarded only by authoring discipline @ src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:141-144
  groundedKc() keys on roundTripPass (VerificationRunner.kt:143). SpanClaimRoundTrip.pass is true whenever the QUOTE relocates, independent of whether the authored `source.span` is non-null (SpanClaimRoundTrip.kt:29-48 — it never reads claim.source.span). But decideOutcome routes a claim with `claim.source?.span == null` to DEFINITIONAL_NO_GOLD_SPAN ⇒ status uncertain (VerificationRunner.kt:361). So a DEFINITION with a verbatim quote but a NULL authored span yields outcome=uncertain (no-gold-span) YET roundTripPass=true ⇒ contributes lecture_grounded=true. A KC made entirely of such claims serves "matches your lecture" while every claim's own signal says the citation isn't anchored. This does NOT violate the strict no-false-faithful invariant (the quote genuinely relocates LIVE — it is not hallucinated), but it lets the lecture badge light on the exact condition (no gold span) the audit flagged as not-anchored. The decision log (D-R14, findings:39) acknowledges this and judges it "test fixtures only" because real KCs all have authored spans post-B5r-4 — i.e. it is an authoring-discipline assumption, not an enforced invariant. Recommend tightening groundedKc to exclude claims whose outcome ∈ {DEFINITIONAL_NO_GOLD_SPAN}, as D-R14 itself suggests, so the invariant is enforced in code rather than relied on in content.
- [LOW] Dead pre-decoupling badge/floor functions (badgeTextFor, honestFloorOf) remain — divergence trap for a future maintainer @ src/main/kotlin/jarvis/web/TrustRoutes.kt:142-146
  After B5r-2 the production route is wired entirely through badgeTextForFloor(servedFloor) + servedHonestFloor (TrustRoutes.kt:420-427). The OLD status-keyed badgeTextFor(status) (TrustRoutes.kt:142-146) now has ZERO callers, and honestFloorOf(status) (HonestFloor.kt:27-34) is referenced only by its own test (HonestFloorTest.kt). honestFloorOf encodes the PRE-decoupling logic (faithful⇒FAITHFUL_TO_SOURCE, everything else⇒UNVERIFIED) — if a future maintainer reaches for it instead of servedHonestFloor, the grounded-badge decoupling silently regresses (a grounded-but-uncertain KC would lose its badge). The two badge functions currently emit identical strings so the pin is not violated today, but keeping both invites drift. Recommend deleting badgeTextFor and either deleting honestFloorOf or documenting it as serve-irrelevant / removing its standalone use. The LOCKED pin itself holds: a full grep finds no code path emitting "verified correct" / "grade-calibrated"; both live functions emit only "matches your lecture / faithful to your source" or "unverified", and the frontend has no badge consumer that could reconstruct copy (tutor-web/src has zero references to badge_text/honest_floor).
- [LOW] No migration test asserts the lecture_grounded column is added, unlike content_hash @ src/test/kotlin/jarvis/tutor/NewTablesMigrationTest.kt:57-94
  The additive-nullable migration is itself SAFE: both content_hash and lecture_grounded are nullable, added via SchemaUtils.createMissingTablesAndColumns (Migration.kt:107) which emits a bare ALTER ADD COLUMN with no SQL default; on the live 0-row table NULL is the correct fail-closed default (Phase1Tables.kt:142-147 comment is accurate, and NULL lecture_grounded ⇒ servedHonestFloor early-returns UNVERIFIED). But NewTablesMigrationTest has explicit presence + idempotency tests for content_hash (NewTablesMigrationTest.kt:58-94) and NONE for lecture_grounded. Both columns ride the same code path so the risk is small, but the asymmetry means a regression that drops lecture_grounded from the table object would go uncaught by migration tests (the serve tests seed rows directly via Exposed and would also not catch a missing ALTER on the live DB). Recommend mirroring the content_hash column-presence + idempotency test for lecture_grounded.

### content-reconcile — SOLID_WITH_NITS
most_serious: Two committed [FIXTURE] KCs with no authored spans (pa-kc-fixture-compute/-recursion) are in the LIVE served corpus and will light the "matches your lecture" grounded badge, because groundedKc keys purely on roundTripPass (quote-search, span-agnostic). Confirmed live-reachable; not a hallucination-class false-faithful (the quotes genuinely relocate) but it weakens the badge's meaning and was explicitly deferred (D-R14).
- [MEDIUM] Live [FIXTURE] KCs with no span can light the grounded 'matches your lecture' badge @ content/PA/kcs/pa-kc-fixture-compute.yaml:11; src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:141; src/main/kotlin/jarvis/content/ContentRepo.kt:59
  content/PA/kcs/pa-kc-fixture-compute.yaml and pa-kc-fixture-recursion.yaml are git-tracked and live in the production kcs/ dir. ContentRepo.loadSubject (ContentRepo.kt:59) loads EVERY *.yaml (only skips .gitkeep) — no fixture filter — so these reach reconcile+audit+serve. Their single DEFINITION claim has a real quote but NO span: decideOutcome (VerificationRunner.kt:361) routes it to DEFINITIONAL_NO_GOLD_SPAN → uncertain, yet SpanClaimRoundTrip.check (SpanClaimRoundTrip.kt:35) relocates by quote-search (ignores the authored span) so roundTripPass=true. groundedKc (VerificationRunner.kt:141-144) = all{roundTripPass} && none{failed} ⇒ grounded=true, and servedHonestFloor (TrustRoutes.kt:248-273) then serves FAITHFUL_TO_SOURCE for a [FIXTURE] artifact. Core no-hallucination invariant still holds (the quote is real lecture text and provably relocates), so this is not a false-faithful in the hallucination sense — but a test fixture should not light the trust badge. The author surfaced this exact gap (D-R14) and deferred it; fix = exclude *-fixture-* from the served corpus, or require claim outcome ∉ {DEFINITIONAL_NO_GOLD_SPAN} for grounded.
- [LOW] lecture_grounded accepts an anchorless quote-search hit (span-agnostic), wider than the faithful path @ src/main/kotlin/jarvis/tutor/verify/VerificationRunner.kt:141-144; src/main/kotlin/jarvis/tutor/verify/SpanClaimRoundTrip.kt:35-43
  groundedKc keys on roundTripPass, and SpanClaimRoundTrip relocates by searching the quote text rather than slicing the authored span. A KC with ZERO authored spans (every claim flooring to DEFINITIONAL_NO_GOLD_SPAN/uncertain) can still be lecture_grounded=true. The faithful path requires a gold span (decideOutcome case-1 gates on claim.source?.span != null), so 'grounded' is a strictly looser door than 'faithful' on anchoring. For the real authored KCs this is moot (001-006 all carry verified spans, verified byte-exact below), but the predicate's intent ('every cited quote relocates LIVE') is met by quote-presence, not by the anchored round-trip the spec language implies. Consider requiring the located span to equal/contain the authored span for grounded, or excluding no-gold-span outcomes.
- [LOW] pa-kc-005 under-cites the lecture rule its invariant_statement asserts (honest uncertain, but a grounding gap) @ content/PA/kcs/pa-kc-005.yaml:20-23
  pa-kc-005's invariant_statement asserts 'the size of an array is the sum of the sizes of its elements' (the rule |a|_d = |a0|_d + ... + |a_{n-1}|_d). That rule IS in the source-of-record (verified at offset ~17730, the page-36 'Arrays: size: |a|d = |a0|d + ... + |an-1|d' block) and the YAML comment even references 'page 36', but NEITHER of 005's two cited source refs (page 34, offsets 16611 / 16691) covers it — they only cite 'size of representation must be mentioned' and 'three ways to define size of values'. Consequence: NLI cannot confirm the statement against the cited quotes ⇒ 005 honestly lands uncertain (the SAFE direction, exactly as D-R11 intended). Not a soundness bug, but adding a third source ref anchoring the summation rule would let the INVARIANT claim reach SUPPORTED→faithful instead of merely grounded. (006 does NOT have this gap: its time_d(τ)=Σ_i ... rule is cited at span 21865-21927.)
- [LOW] splitTopLevelMinus splits on the LAST top-level minus; cosmetically wrong NLI hypothesis for un-parenthesized multi-minus directives (no soundness impact) @ src/main/kotlin/jarvis/content/ContentReconcile.kt:193-206, 147-148
  ContentReconcile.splitTopLevelMinus (ContentReconcile.kt:194-206) overwrites cut on every depth-0 '-', so 'simplify(a - b - c) == 0' extracts 'a - b = c' rather than 'a - b - c = 0'. This is SOUND for SymPy: realSymPyEquals checks simplify(lhs - rhs), and (a-b) - c is algebraically identical to the intended inner a-b-c, so the math verdict is unchanged regardless of split point. The only effect is when there is NO invariant_statement: the extracted equation becomes the NLI hypothesis 'content', so a mis-split yields an oddly-phrased (but still true) hypothesis. The two REAL directives both wrap each side ('(1+1+1) - 3', '(t+t+t) - 3*t') so they extract cleanly to the exact KC invariant (verified). Doc/intent says the directives are expected parenthesized; recommend documenting that contract or splitting on the FIRST top-level minus to match the canonical 'simplify((A) - (B)) == 0' shape. Also: the body comment-strip cuts at the first '#', so a '#' inside an equation body would truncate the check — not hit by the real corpus (all '#' are trailing comments).
- [LOW] ContentSchema provenance doc lists only {pdftotext, vision-confirmed}; YAMLs use 'located' (accepted, but undocumented + unvalidated literal) @ src/main/kotlin/jarvis/content/ContentSchema.kt:9-20; content/PA/kcs/pa-kc-001.yaml:18
  SourceRef.provenance KDoc (ContentSchema.kt:11-12, 19) documents exactly two values, but pa-kc-001..004 all use provenance: located (a third literal introduced in B5r-4). It is accepted because those KCs are grounding_tier=standard and the validator only enforces provenance=='vision-confirmed' for strict KCs (ContentValidator.kt:290). So 'located' passes for 001-004 and the round-trip leg ignores provenance entirely. No bug — but the schema doc is now stale (doesn't mention 'located') and there is no allowlist of valid provenance literals, so a typo like 'locatedd' would pass silently. Recommend documenting 'located' and/or validating provenance against a known set.
- [LOW] VERIFIED CLEAN: span provenance, the 3 quote corrections, content_hash identity, and D-R7/D-R8 routing all hold @ content/PA/_sources/pa-lecture-01.md; src/main/kotlin/jarvis/content/ContentReconcile.kt:274-300
  Positive confirmations (so the verdict is concrete, not a rubber-stamp): (1) All 15 authored spans across pa-kc-001..006 relocate BYTE-FOR-BYTE in content/PA/_sources/pa-lecture-01.md (LF-only, no astral chars) — text[start:end] == quote exactly for every ref. (2) The 3 quote corrections (002 q1/q2, 004 q1) correctly stripped a phantom '\n  ' two-space list-indent that was NOT in the raw source, restoring verbatim text (diffed against 93dd8f1) — the opposite of fabrication. (3) The audit-side == serve-side content-hash identity holds: VerifyContentCli builds claims via ContentReconcile.claimsFor (VerifyContentCli.kt:101), audit computes kcContentHashOf(claimsFor(kc)) (VerificationRunner.kt:98), serve computes kcContentHash(kc)=kcContentHashOf(claimsFor(kc)) (TrustRoutes.kt:267) — same function, same input; invariant_statement folds in transitively as the equational claim's content, and the dedicated test ('kcContentHash over a KC equals the hash over its emitted claims') passes. (4) D-R8 routing verified on the real corpus: both sympy directives extract to exactly the KC invariant ('1 + 1 + 1 = 3', 't + t + t = 3*t'), no emitted claim carries a 'sympy:' hypothesis, unparseable directives emit no claim. ContentReconcileTest + ContentValidatorTest + VerificationRunnerTest + TrustRoutesTest + VerifyContentCliTest all GREEN (run via alternate buildDir to dodge a stale file lock from a running java process).

### tests-coverage — SOLID_WITH_NITS
most_serious: The python-bridge integration tests (NLI + SymPy) are self-masking: they use the bridge's OWN failure signal (a throw / ran=false) as the "env absent → skip" trigger, so any bridge-mechanics bug — exactly like the D-R13 `-c` quote-mangle — is converted into a silent skip instead of a failure. CI confirms it (ubuntu, no JARVIS_PYTHON3, no transformers/sympy installed, runs only `gradle :check`), so the bug shipped through green CI twice and there is still zero hermetic coverage of the temp-file ProcessBuilder path that the fix introduced.
- [HIGH] Bridge integration tests are structurally self-masking — they cannot catch a bridge-mechanics bug @ src/test/kotlin/jarvis/NliEntailmentLlmIntegrationTest.kt:19-27; src/test/kotlin/jarvis/tutor/verify/NonLlmLegsTest.kt:150-157
  NliEntailmentLlmIntegrationTest.runOrSkip catches RuntimeException and calls assumeTrue(false) → SKIP; NonLlmLegsTest's INTEGRATION test calls assumeTrue(r.ran). The `-c` quote-mangle bug's failure mode is precisely `runNli throws "empty output"` and `realSymPyEquals returns ran=false` — the SAME signal both tests interpret as "env absent, skip." So even on a box WITH python+model present (I confirmed the NLI integration tests actually RAN here: 36s + 79s, 0 skipped), the `-c` bug would have turned a would-be PASS into a silent abort, never a failure. The guard predicate and the regression signal are the same predicate, so by construction these tests can prove the bridge WORKS but can never prove it's BROKEN. That is exactly why the bug shipped twice (NLI freshly, SymPy latently) through green CI. There is no hermetic test exercising the new temp-file ProcessBuilder path: `runNli` is referenced only by the self-masking integration test, and `realSymPyEquals` is referenced by NO test at all (grep src/test). A hermetic regression test (stage a script containing double-quotes, run it via the real ProcessBuilder against a stub interpreter or `cmd /c`/`python3` guarded by assumeTrue on python presence ONLY — NOT on success — and assert non-empty parseable stdout) would have caught the `-c` mangling and is still absent.
- [MEDIUM] CI does not provision or forward JARVIS_PYTHON3 / the model, so the two-family audit path has zero CI coverage @ .github/workflows/test.yml:28-29; build.gradle.kts:176-197
  .github/workflows/test.yml runs `gradle :check` on ubuntu-latest with no python/transformers/sympy install step and no JARVIS_PYTHON3 env. build.gradle.kts only changed the verifyContent TASK DESCRIPTION (commit 1a12fdf) — it does not add `environment`/`systemProperty` forwarding to the `test` task (jvmArgs at lines 176/197 are encoding-only). Net effect: the entire real-python leg of the trust-net (NliEntailmentLlm.runNli, realSymPyEquals, and the VerifyContentCli family-B wiring) is never exercised by any non-skipping test in CI. The reshape's central safety claim (FAIL-LOUD: a leg that can't run is never a silent pass; the badge never lies) rests on code paths CI never runs. The honest fix is one of: (a) install python+sympy+transformers in CI and forward JARVIS_PYTHON3 so the integration tests run for real (heavy — model download), or (b) at minimum add the hermetic bridge-mechanics regression test from the finding above (cheap, deterministic, would have caught `-c`). The acceptance evidence in the decision log (23 claims → 17/5/1) is a manual one-off on the dev box, not a repeatable gate.
- [LOW] NLI prompt-parse tests mirror a private buildPrompt by hand — drift is silent @ src/test/kotlin/jarvis/NliEntailmentLlmTest.kt:27-40
  NliEntailmentLlmTest.deriverPrompt (lines 33-40) re-implements TwoFamilyDeriver.buildPrompt's format verbatim by hand because buildPrompt is private. parseNliInput (production) keys off the literal markers `CLAIM (`, `\nSTATED INVARIANT:\n`, `\nSOURCE QUOTE:\n`. I verified the mirror currently matches the real buildPrompt (TwoFamilyDeriver.kt:76-89). But nothing pins them in lockstep: if buildPrompt's marker text or ordering ever changes, BOTH the test fixture and the prod parser would need hand-edits, and the test would stay green against the stale fixture while production NLI silently extracts the wrong premise/hypothesis (e.g. premise = empty, or hypothesis includes the equation) — which on the prose path could degrade verdicts. The test even admits the hazard in its own comment but does not enforce it. Fix: add one test that feeds the REAL deriver's buildPrompt output (expose it test-internal, or capture it through a fake Llm that records the messages it receives) into parseNliInput and asserts the round-trip — so a buildPrompt change that the parser doesn't track fails a test.
- [LOW] Bridge-fix error-handling asymmetry between SymPy (degrade) and NLI (throw) is real but untested @ src/main/kotlin/jarvis/tutor/verify/NonLlmLegs.kt:105-121; src/main/kotlin/jarvis/NliEntailmentLlm.kt:146-158
  The D-R13 fix introduces a temp-file staging step in BOTH bridges, but they handle a staging failure (createTempFile/write throwing) DIFFERENTLY: realSymPyEquals returns PyResult(ran=false) → uncertain floor (NonLlmLegs.kt:109-111), while NliEntailmentLlm.runNli rethrows as RuntimeException → leg's anyThrew → fail-loud (NliEntailmentLlm.kt:152-158). Both are defensible (SymPy=degrade-to-uncertain matches its never-ran contract; NLI=fail-loud matches its leg contract), and neither produces a false-faithful, so the core invariant holds. But neither staging-failure branch has any test, and the difference is exactly the kind of subtlety the next refactor could flip. Low severity because it cannot cause false-faithful (a thrown NLI leg blocks faithful; a ran=false SymPy on an equational claim floors to uncertain via case-4), but it's an untested seam in safety-critical fail-loud code.
- [LOW] decideOutcome tests assert the negative (not-faithful) but several skip pinning the EXACT outcome @ src/test/kotlin/jarvis/tutor/verify/VerificationRunnerTest.kt:695,711,200
  Several guard tests assert only assertNotEquals(faithful) without pinning the specific resolved outcome, leaving room for a wrong-but-not-faithful resolution to pass unnoticed. E.g. VerificationRunnerTest B5r-1(c) self-entailment guard (line 695) asserts only NOT faithful; the agreed-REFUTED DEFINITION (line 711) asserts only NOT faithful; the thrown-non-LLM test (line 200) asserts only NOT faithful. By contrast the stronger guard tests DO pin the outcome (e.g. round-trip-fail ⇒ failed at line 305/672, NONLLM_NONE ⇒ uncertain at 214). This is the GOOD-not-gamed direction overall — the per-claim-kind routing, the agreed-REFUTED veto, the D-R9 UNCLEAR-vs-REFUTED split, the seed-independence/order-independence tests, and the multi-claim poisoning tests are all real and exercise the production decideOutcome (no stubbed-out logic). But tightening the assertNotEquals-only guards to assertEquals on the intended floor/fail outcome would close the gap where a routing change silently relabels a guard's outcome. Not gamed-to-pass; just under-pinned.

---
## COUNCIL ON DECISIONS
### devils-advocate — A_DECISION_IS_WRONG
  D-R3 (and its case-3p instantiation in D-R2) is wrong as generalized, and I verified it in the live code — not as a style nit but as a reachable breach of the single invariant the reshape exists to protect.

D-R3's premise: "for prose the LLM/NLI vote can only VETO, never PROMOTE — round-trip is the sole positive anchor (self-entailment on content==quote is zero-signal)." That reasoning is SOUND for DEFINITION and only DEFINITION, because ContentReconcile.claimsFor emits a DEFINITION with content = ref.quote, source = that ref (ContentReconcile.kt:77). There, content IS the round-tripped string, so a hallucinated body cannot exist independently of the quote that round-trips. The round-trip genuinely anchors the content.

But case 3p (VerificationRunner.kt:370-372) fires for EVERY non-equational claim: `!isEquationalKind && roundTrip.pass && !anyThrew && !agreedNonSupported`. And a prose GRADER_RULE is emitted with content = the rule, source = anchorRef (the KC's FIRST span-bearing ref) (ContentReconcile.kt:116, 67). SpanClaimRoundTrip.check keys purely on claim.source?.quote (SpanClaimRoundTrip.kt:30) — it relocates the UNRELATED anchor quote, never the rule text. So roundTrip.pass is true whenever the KC has any legitimately-relocating anchor quote, REGARDLESS of whether the rule text has any lecture basis. The only thing that can block faithful is agreedNonSupported (BOTH families REFUTE) — a one-family REFUTED does not veto (agree=false ⇒ agreedNonSupported=false), and the NLI abstains below 0.60, so an unrelated premise/hypothesis pairing yields UNCLEAR far more often than a confident both-REFUTED. Net: a hallucinated grader-rule attached to a KC with one real anchor quote reaches per-claim faithful, drives groundedKc=true (VerificationRunner.kt:141-144), and lights "matches your lecture". That is the exact false-faithful/false-grounded path the invariant forbids. D-R3's self-entailment rationale silently extended from "content==quote" (true for DEFINITION) to "all prose" (false for GRADER_RULE) without ever addressing the content!=quote case — the spec's own step-1 mental model only reasons about DEFINITION.

This is not theoretical. pa-kc-005 (grounding_tier:strict, real corpus) carries exactly this shape: grader_rules[1] is prose, anchors on a page-34 quote that relocates, and concerns a different claim than that quote. The decision-log acceptance table claims 005's grader landed `uncertain` (findings:33) — but case 3p does not read the LLM verdict to demote, so an UNCLEAR result resolves to faithful, not uncertain. Either the table is loosely aggregating or the only thing keeping 005 out of false-faithful was the NLI happening to not be SUPPORTED — incidental, not a guard. Worse: the suite CODIFIES the unsafe behavior. VerificationRunnerTest.kt:501-532 builds two prose GRADER_RULEs whose content is decoupled from goldSource()'s quote and ASSERTS they reach faithful "via the prose anchor" (lines 520-524). So the false-faithful path is locked in by a green test, and there is zero test of the safety-critical case (hallucinated rule content + legitimately-relocating anchor ⇒ must NOT be faithful). The green suite gives FALSE assurance on precisely the dimension the reshape was supposed to protect. Two independent reviewers (trustnet-safety, decideoutcome-logic) reached this same HIGH from different doors — that convergence is the tell.

Compounding it, D-R6's badge model has a second live false-grounded leak that the decision log itself flags as "deferred": report-wrong (TrustRoutes.kt:554-575) flips status faithful→pending but never clears lectureGrounded or contentHash. I confirmed a production faithful KC ALWAYS carries lecture_grounded=true (faithful ⇒ every claim faithful ⇒ every roundTripPass ⇒ groundedKc true). After report-wrong: status=pending, lecture_grounded STILL true, content_hash STILL matching. servedHonestFloor's early guard `status != faithful && !grounded` = true && false = false, so it does NOT fall to UNVERIFIED; the D8 hash gate passes (content unchanged) ⇒ FAITHFUL_TO_SOURCE. The badge keeps lying "matches your lecture" on content a student has actively disputed. Before D-R5/D-R6 decoupled the badge from status, report-wrong's faithful→pending correctly dropped the badge; the decoupling re-introduced the lie via a signal report-wrong forgets to clear. The route test masks it (seedFaithfulCard leaves lectureGrounded NULL — not the production reality). D-R6 reasoned only about a `failed` claim suppressing grounded; report-wrong produces `pending`, which D-R6 never covers.

The python-bridge dimension is genuinely SOLID (every infra failure throws or returns ran=false → anyThrew/uncertain; the D-R13 temp-file fix is correct and FAIL-LOUD is intact). The D8 staleness gate, EQUATIONAL_LLM_UNCONFIRMED softening (D-R9 correctly narrowed by !anyRefuted so a contradicted equation never certifies), the aggregateKc conjunction, and the AuditOutcome→transition wiring are all correct and well-guarded. So the architecture is mostly right — but D-R3-as-generalized and the D-R6 report-wrong gap are two distinct, live, reachable false-faithful/false-grounded paths, and the suite actively blesses the first one. That is "a decision is wrong," not "sound with fixes," because the wrong decision is enforced as the intended contract by a passing test.
  revisit: D-R3 / D-R2 (case 3p): WRONG as generalized to all prose. The 'self-entailment is zero-signal, round-trip is the anchor' rationale holds ONLY for DEFINITION (content==quote). For a prose GRADER_RULE content=rule but the round-trip anchors on an UNRELATED quote (ContentReconcile.kt:116 + SpanClaimRoundTrip.kt:30), so a hallucinated rule reaches faithful/grounded unless both families agree-REFUTED. FIX: gate case 3p on claim.kind==DEFINITION; route prose GRADER_RULE through a path that REQUIRES an independent positive signal on the RULE text (promote the LLM/NLI from veto-only to a required bothSupported vote where content!=quote), OR round-trip the rule's own cited span, OR at minimum exclude prose GRADER_RULE from faithful/grounded until it has its own anchor. ADD the missing red-then-green test: prose GRADER_RULE, content='fabricated rule with no lecture basis', source.quote=a real relocating anchor, families not both-REFUTED ⇒ assert NOT faithful AND grounded=false. DELETE/REWRITE VerificationRunnerTest.kt:501-532 which currently asserts the unsafe path as correct. || D-R6 (report-wrong lifecycle): the badge decoupling re-introduced a lie D-R6 never closed. report-wrong (TrustRoutes.kt:569) flips status→pending but leaves lecture_grounded=true and content_hash matching, so servedHonestFloor keeps serving FAITHFUL_TO_SOURCE on disputed content. FIX: in the report-wrong transaction set lectureGrounded=false (or null) alongside status. Fix the masking test (TrustRoutesTest.kt seedFaithfulCard) to seed lectureGrounded=true so it reflects production. || D-R14 (grounded keys on roundTripPass, not the claim verdict): still latent and NOT fixtures-only as the log claims — ContentValidator only ERRORS on a missing span for grounding_tier=strict, so a non-strict KC can ship a span-less DEFINITION (DEFINITIONAL_NO_GOLD_SPAN⇒uncertain) whose quote fuzzy-relocates and lights grounded. Two committed [FIXTURE] KCs (pa-kc-fixture-compute/-recursion) already do this in the LIVE served corpus. FIX: require claim outcome ∉ {DEFINITIONAL_NO_GOLD_SPAN} for grounded, and exclude *-fixture-* from the served corpus. || D-R2 case-4 / FAIL-LOUD seam (MEDIUM, surfaced by review not a logged decision): an equational claim whose SymPy bridge COULD-NOT-RUN (kind=SYMPY, ran=false from a transient python outage) matches neither case 4 (requires kind==NONE) nor 4b (requires ran&&pass), so it falls to else⇒failed — contradicting the system's own 'never-ran floors to uncertain' contract and, since failed has no auto-clear, requiring a re-curate to recover. Direction-safe (false-reject not false-faithful) but inconsistent. FIX: route isEquationalKind && kind==SYMPY && !ran && !anyRefuted && roundTrip.pass && !anyThrew to NONLLM_LEG_NONE/EQUATIONAL_LLM_UNCONFIRMED; reserve failed for ran && !pass.
  key_concern: D-R3's 'round-trip is the sole positive anchor for prose' is valid only where content==quote (DEFINITION). Case 3p applies it to prose GRADER_RULE, where the round-trip anchors on an UNRELATED quote — so a hallucinated rule reaches faithful and lights the lecture-grounded badge unless both families agree-REFUTED. It is LIVE-reachable (pa-kc-005), and the test suite asserts it as the intended contract — a real false-faithful/false-grounded path the reshape was built to prevent, hidden behind green tests.

### domain-expert — A_DECISION_IS_WRONG
  I read decideOutcome (VerificationRunner.kt:360-405), claimsFor (ContentReconcile.kt:64-121), SpanClaimRoundTrip.check (kt:29-49), TwoFamilyDeriver.buildPrompt (kt:75-92), NliEntailmentLlm, pa-kc-005.yaml, the multi-claim test (VerificationRunnerTest.kt:465-608), and the report-wrong handler (TrustRoutes.kt:554-575). The reshape is mostly aligned with how robust faithfulness pipelines are built — but D-R3 is wrong as generalized, and that error is load-bearing for the central invariant.

WHAT'S RIGHT (and matches prior art):
- D-R5/D-R6 (grounded vs faithful as two tiers): This is exactly the standard "attributability/citation-grounding" vs "factual-correctness" split in attributed-QA. Grounding = "every cited span relocates in the source" (RARR / Bohnet et al. AIS — Attributable to Identified Sources; ALCE citation-recall). Faithful = the stronger entailment-confirmed tier. Decoupling the badge from the strong tier and gating BOTH behind the D8 content-hash freshness gate is correct and well-implemented (servedHonestFloor wraps grounded in the same fail-closed hash gate, TrustRoutes.kt:248-273). Sound.
- D-R9 (SymPy-proven + LLM-UNCLEAR ⇒ uncertain, not failed): Correct and is textbook "abstention beats wrong rejection." A symbolic/deterministic checker (SymPy here, akin to a calculator/program-of-thought verifier) is the authority on equation truth; an NLI model's failure to confirm an NL restatement is epistemic uncertainty, not contradiction. The anyRefuted-vs-bothSupported distinction (kt:258-259, 388-391) is the right asymmetry — keeps a genuinely-contradicted equation in failed. Sound.
- D-R1 (DeBERTa-v3-MNLI-FEVER-ANLI, 3-class, native neutral, 0.60 abstain floor): Defensible and well-motivated. The native neutral class IS the value here — abstaining ("UNCLEAR") rather than forcing a binary is exactly what fact-checking pipelines want for a veto-only/triage role. MiniCheck (Tang et al. 2024) is the stronger document-grounded factuality choice and would reduce the over-abstention you saw on pa-kc-005/006, but deferring it behind the same adapter is fine. The MNLI caveat (it judges sentence-pair entailment, not document grounding, and is brittle to premise/hypothesis length mismatch) is real but acceptable for a veto-only role — NOT for a promotion role (see below).
- D-R13 (temp-file ProcessBuilder, not python -c): Correct and the standard fix. JVM ProcessBuilder on Windows routes through a single command-line string and CreateProcess; embedding a multiline script with double-quotes via -c reliably mangles. Temp-file + redirectError(DISCARD) is the right pattern. The robustness nits (waitFor-before-drain deadlock risk, temp-file leak on write failure, per-claim cold model load) are real but fail-loud-safe; none can produce a false pass. Fine to defer.

WHAT'S WRONG — D-R3 (the prose veto-only rationale) is the one wrong decision, and it breaches the core invariant:
D-R3 says "for prose the LLM/NLI vote is self-entailment noise, so round-trip is the sole positive anchor; the LLM can only veto." That rationale is VALID ONLY for DEFINITION, where ContentReconcile emits content = ref.quote (ContentReconcile.kt:77) — content IS the round-tripped string, so self-entailment is genuinely zero-signal and the round-trip already proves the content is verbatim lecture text. But the rationale was over-generalized to ALL prose kinds. For a prose GRADER_RULE, claimsFor emits content = the rule text with source = anchorRef = the KC's FIRST span-bearing ref (ContentReconcile.kt:116, 67). SpanClaimRoundTrip.check keys purely on claim.source.quote (kt:30), NOT claim.content. So case 3p (VerificationRunner.kt:370-372: !isEquationalKind && roundTrip.pass && !anyThrew && !agreedNonSupported) certifies the rule as faithful purely because an UNRELATED anchor quote relocates — while the ONLY leg that ever read the rule text (the NLI/LLM family, premise=anchor-quote/hypothesis=rule-text per buildPrompt kt:75-92) is discarded for promotion and honored only as an agreed-REFUTED veto. A hallucinated grader-rule on a KC with a genuine anchor quote reaches faithful/grounded unless BOTH families agree REFUTED. Given the 0.60 NLI abstain floor and that an unrelated premise/hypothesis pair yields neutral/UNCLEAR far more often than confident bothREFUTED, the veto rarely fires — so the guard is statistical, not structural. This is a live, reachable false-faithful/false-grounded path (pa-kc-005 grader_rules[1], content/PA/kcs/pa-kc-005.yaml:23), and VerificationRunnerTest.kt:483-484,520-524 ASSERTS it as the contract (two prose rules with content decoupled from goldSource().quote, asserted faithful). In faithfulness-pipeline terms: case 3p conflates "this claim's cited quote exists in the source" (attribution of the anchor) with "this claim's text is supported by the source" (faithfulness of the content). For DEFINITION those collapse; for a prose rule they are different propositions, and the pipeline certifies the wrong one. Prior art is unambiguous here: in attributed generation (RARR, AIS, ALCE) attribution is ALWAYS evaluated as entailment of the CLAIM by its cited passage, never as mere existence of the passage. The fix is standard: for a prose claim whose content != its cited quote, the entailment leg must be a REQUIRED positive vote (promote NLI from veto-only to required bothSupported), OR round-trip the rule's own cited span, OR exclude prose GRADER_RULE from faithful/grounded until it has its own anchor. Note this also means the stated invariant "no path may let non-lecture content reach faithful OR grounded" does not currently hold.

SECONDARY (D-R6 incompleteness, also a badge-lie): D-R6 only reasoned about a failed claim suppressing grounding. report-wrong produces pending, not failed, and the handler (TrustRoutes.kt:554-575) never clears lectureGrounded or contentHash. A production faithful KC always has lectureGrounded=true with matching hash, so post-report servedHonestFloor still returns FAITHFUL_TO_SOURCE — the badge keeps saying "matches your lecture" on content a student actively disputed. This is the badge lying. The decoupling (D-R5) silently re-introduced a lie that status-keyed badging used to handle. The lifecycle being deferred is fine as a roadmap item but NOT while the badge serve reads the un-cleared signal.

D-R14 (grounded keys on roundTripPass, not claim outcome): correctly scoped as direction-safe (the quote genuinely relocates, not hallucinated) and currently fixtures-only, but it is a code seam not a data accident — ContentValidator only errors on a missing span for strict KCs, so a non-strict KC could ship a span-less fuzzy-located DEFINITION and light grounded. Worth the tightening before more subjects onboard, but it does not by itself breach no-false-faithful.
  revisit: D-R3: WRONG as generalized. 'Round-trip is the sole positive anchor, LLM veto-only' is valid ONLY for DEFINITION (content==quote). For a prose GRADER_RULE content != source.quote, so case 3p certifies on an unrelated anchor quote — a live false-faithful path. Fix: gate case 3p on claim.kind==DEFINITION (content==quote); route prose GRADER_RULE through a path REQUIRING bothSupported on the rule text (the NLI/LLM IS the only judge of a rule that is not its own quote), or round-trip the rule's own cited span, or exclude prose GRADER_RULE from faithful/grounded until it has its own anchor. Aligns the pipeline with RARR/AIS/ALCE: attribution = entailment of the CLAIM by its cited passage, never mere passage existence. Add the missing red-then-green test (hallucinated rule content + relocating anchor + non-both-REFUTED ⇒ NOT faithful, grounded=false), and fix VerificationRunnerTest.kt:483-484,520-524 which currently codify the unsafe behavior. || D-R6: INCOMPLETE. report-wrong (pending, not failed) leaves lectureGrounded=true + matching content_hash, so servedHonestFloor still serves FAITHFUL_TO_SOURCE — badge lies on student-disputed content. Fix: in the report-wrong transaction (TrustRoutes.kt:554-575) set lectureGrounded=false (or null) alongside status. Add a route test seeding a production-shaped faithful row (lectureGrounded=true) and asserting the badge drops to unverified after report-wrong. || D-R9 follow-up (D-R15 open item): a SymPy bridge that COULD-NOT-RUN (ran=false from infra/parse/timeout) on an equational claim currently falls to else ⇒ failed, contradicting the system's own FAIL-LOUD 'never-ran floors to uncertain' contract (NonLlmLegs.kt:40-43 comment). Direction-safe (false-reject, never false-faithful) but violates the stated principle and has no auto-clear. Route isEquationalKind && nonLlm.kind==SYMPY && !nonLlm.ran && !anyRefuted && roundTrip.pass && !anyThrew to NONLLM_LEG_NONE/EQUATIONAL_LLM_UNCONFIRMED; reserve failed for ran && !pass (genuine contradiction). || D-R14: tighten groundedKc to exclude claims whose outcome ∈ {DEFINITIONAL_NO_GOLD_SPAN} so grounded keys on the claim VERDICT not the raw round-trip boolean; make it a structural invariant rather than an authoring-discipline assumption. Also exclude *-fixture-* KCs from the served corpus. || Test/CI gap (supports D-R3/D-R9): bridge integration tests are self-masking (the guard predicate == the regression signal: a throw/ran=false is read as 'env absent, skip'), and CI never provisions JARVIS_PYTHON3, so the entire real-python family-B path has zero non-skipping CI coverage. Add a hermetic bridge-mechanics regression test (stage a double-quote-containing script, run via real ProcessBuilder, assert non-empty parseable stdout) — would have caught the D-R13 -c bug. Pin the assertNotEquals-only guard tests to the exact resolved outcome.
  key_concern: D-R3 over-generalizes the 'self-entailment is zero-signal, round-trip is the anchor' rationale from DEFINITION (where content==quote) to all prose. For a prose GRADER_RULE content != its cited quote, so case 3p certifies faithful/grounded on an UNRELATED anchor quote while the only leg that judged the rule text is veto-only — a live, test-codified false-faithful path (pa-kc-005:23) that breaches the reshape's central no-false-faithful invariant. The correct faithfulness-pipeline shape (RARR/AIS/ALCE) requires the cited passage to ENTAIL the claim, not merely exist; the NLI must be a required positive vote whenever content != quote.

### risk-analyst — SOUND_WITH_FIXES
  I verified the code, the decision log, the live YAML, and the tests. The reshape's SPINE is sound — per-claim-kind routing, the agreed-REFUTED veto, the D8 staleness gate wrapping grounded exactly as faithful, EQUATIONAL_LLM_UNCONFIRMED→uncertain, the order-independent pending-prior aggregation, and the fail-loud python bridges are all correctly built and the badge-copy pin holds. But the reshape SHIPPED THREE LIVE FALSE-GROUNDED/FALSE-FAITHFUL PATHS that violate its own stated invariant. Ranked by risk:

CRITICAL #1 — D-R3 is correct ONLY for DEFINITION (content==quote); it was applied to ALL prose, including prose GRADER_RULE where content != the anchor quote. Confirmed concretely: ContentReconcile.claimsFor:116 emits a prose grader_rule with content=rule, source=anchorRef (the KC's first span-bearing ref). SpanClaimRoundTrip.check:30 round-trips claim.source.quote (the anchor), NOT claim.content (the rule). decideOutcome case 3p (VerificationRunner.kt:370-372) certifies faithful on roundTrip.pass && !anyThrew && !agreedNonSupported — the ONLY leg that judges the rule text (LLM/NLI) is discarded for promotion and only honored as an agreed-REFUTED veto. A two-family agree-REFUTED is rare for an unrelated premise/hypothesis pairing (NLI abstains to UNCLEAR at the 0.60 floor; a single-family REFUTED is agree=false ⇒ NOT a veto). So a HALLUCINATED grader rule bolted onto a KC with one genuine anchor quote reaches per-claim faithful ⇒ lights lecture_grounded ⇒ the 'matches your lecture' badge. LIVE exposure: pa-kc-005 grader_rules[1] (line 23, prose, invariant=null) anchors on the line-26 quote about a DIFFERENT claim. This is the EXACT invariant the reshape was built to protect, and it is reachable in the served corpus.

CRITICAL #2 — the test suite codifies the unsafe behavior as the contract. VerificationRunnerTest.kt:483-484 builds two prose GRADER_RULEs ('award full marks…', 'deduct one point…') with source=goldSource() (content != the goldQuote, verified: goldQuote is the size-of-representation lecture line, line 77), then ASSERTS at :520-524 they MUST reach faithful 'via the prose anchor', and :604-606 that the KC aggregates faithful. There is ZERO test of the safety-critical case (prose grader rule with FABRICATED content + a genuine relocating anchor ⇒ must NOT be faithful). So green CI gives false assurance on precisely the dimension at issue. This is worse than an unnoticed bug — it is a locked-in wrong contract that will fight any fix.

CRITICAL #3 — report-wrong re-introduces a badge lie via the decoupled signal. TrustRoutes.kt:554-575 flips status faithful→pending but never touches lectureGrounded or contentHash. In production a faithful KC ALWAYS carries lecture_grounded=true (faithful⇒every claim faithful⇒every roundTripPass⇒groundedKc true — airtight from VerificationRunner.kt:141-144 + case 3/3p both requiring roundTrip.pass). After report-wrong: status=pending, grounded STILL true, hash STILL matching (content unchanged) ⇒ servedHonestFloor:262 guard (status!=faithful && !grounded) = (true && false) = false ⇒ does NOT fall to UNVERIFIED ⇒ D8 passes ⇒ returns FAITHFUL_TO_SOURCE. The badge keeps saying 'matches your lecture' on content a STUDENT ACTIVELY DISPUTED. Before B5r-2 the badge was status-keyed and faithful→pending dropped it correctly; the decoupling re-opened the lie. The route test (seedFaithfulCard:810-814) leaves lectureGrounded NULL, masking the production reality where it is true. D-R6 ('a FAILED claim suppresses grounded') does not cover this because report-wrong yields PENDING, not a failed claim. The decision log even admits 'report_wrong REVERIFY lifecycle deferred' (findings:50) — but the deferral was taken BEFORE grounded was decoupled, so it was never re-evaluated against the new signal. That is the D-R-level miss: a deferral that became unsafe when a later decision changed the badge's keying.

HIGH #4 — D-R14/grounded-on-no-span (LOW-to-MED): groundedKc keys on roundTripPass, which is true for a DEFINITIONAL_NO_GOLD_SPAN claim whose quote fuzzy-relocates (SpanClaimRoundTrip ignores authored span; LiveSourceLocator does fuzzy match). The decision log scopes this as 'fixtures only', but pa-kc-fixture-compute.yaml / -recursion.yaml are GIT-TRACKED in the live kcs/ dir and ContentRepo.loadSubject loads every *.yaml with no fixture filter — so they DO serve a grounded badge for a [FIXTURE] artifact. Not a hallucination-class false-faithful (the quote is real and relocates), but the lecture badge lights on the exact condition (no gold span) the audit flagged as not-anchored, and ContentValidator only ERRORS on missing-span for grounding_tier=strict — a non-strict KC can author a span-less DEFINITION and light grounded. This is a structural seam, not just a data accident; D-R14's own suggested tightening (grounded requires claim outcome ∉ {DEFINITIONAL_NO_GOLD_SPAN}) should land before more subjects onboard.

MEDIUM #5 — decideOutcome maps a SymPy-COULD-NOT-RUN equational claim to FAILED, not the uncertain floor, contradicting the system's own never-ran≠disagreed (FAIL-LOUD H5) principle. A kind=SYMPY, ran=false claim matches neither case 4 (requires kind==NONE) nor case 4b (requires ran && pass) ⇒ falls to else ⇒ failed ⇒ aggregates the whole KC to failed, which has no auto-clear. Direction-SAFE (false-reject, never false-faithful), so the badge invariant holds — but a transient python outage silently bricks a genuinely-faithful equational KC until a re-curate. D-R2's framing ('an equational claim whose checker could not run floors to uncertain') is contradicted by the actual case-4 guard (kind==NONE only). Worth fixing for robustness, lower priority than the false-grounded paths.

LOW — python-bridge nits (waitFor-before-drain deadlock-until-timeout, temp-file leak on write-failure, parseNliInput↔buildPrompt hand-mirror drift, per-claim model reload) are all fail-loud-degrading, never false-faithful. The self-masking integration tests + CI not provisioning JARVIS_PYTHON3 (HIGH in tests-coverage) is real: the bridge-mechanics class of bug (the D-R13 -c quote-mangle) is invisible to CI by construction because the skip-predicate IS the failure-signal — that is why it shipped twice. A cheap hermetic temp-file-ProcessBuilder regression test (stub interpreter, assumeTrue on python PRESENCE only, not success) should be added; it would have caught -c. NOTE a stale doc: NliEntailmentLlm.kt:132 KDoc still says ProcessBuilder(python, '-c', runner) while line 149 correctly uses the temp file — cosmetic, fix the comment.

NET: the reshape's mechanism is right and the equational/serve/fail-loud halves are solid, but D-R3 over-generalized from DEFINITION to all prose, and the report-wrong deferral went stale under D-R5/D-R6's decoupling. Both are LIVE-reachable badge lies. SOUND_WITH_FIXES, not WRONG, because the spine holds and the fixes are surgical — but these are must-fix-before-ship, not nice-to-haves.
  revisit: D-R3 — WRONG as generalized: the 'self-entailment is zero-signal, round-trip is the sole anchor, LLM can only veto' rule is valid ONLY for DEFINITION (content==quote). For a prose GRADER_RULE (content != anchor quote) it discards the ONLY leg that judges the rule text. FIX: gate case 3p on claim.kind==DEFINITION (where content==quote); route prose GRADER_RULE through a path that REQUIRES bothSupported on the rule-vs-quote NLI (the LLM IS the only judge of a rule that isn't its own quote), OR round-trip the rule's own content/require its own cited span. At minimum, exclude prose GRADER_RULE from the faithful/grounded path until it has an independent positive signal. || VerificationRunnerTest.kt:483-484/520-524/604-606 — the test that locks in the unsafe prose-GRADER_RULE-faithful contract must be inverted. Add a red-then-green test: prose GRADER_RULE, content='fabricated rule with no lecture basis', source.quote=a real relocating anchor, families NOT both-REFUTED ⇒ assert NOT faithful AND grounded=false. The existing assertion that decoupled-content prose rules reach faithful 'via the prose anchor' is the false-faithful path, not the contract. || report-wrong lifecycle (TrustRoutes.kt:554-575) + D-R6 — the deferral became unsafe under D-R5/D-R6's badge decoupling. FIX: in the report-wrong transaction set lectureGrounded=false (or null) alongside status (and consider clearing/invalidating contentHash). Re-key servedHonestFloor's reachability against report-wrong, not only against a 'failed' claim. Update the route test to seed lectureGrounded=true in seedFaithfulCard so it reflects production and would catch the regression. || D-R14 — promote from 'fixtures-only minor' to a code invariant: groundedKc must require each contributing claim's outcome ∉ {DEFINITIONAL_NO_GOLD_SPAN} (depend on the claim VERDICT, not the raw roundTripPass leg boolean), AND exclude *-fixture-* KCs from the served corpus (ContentRepo.loadSubject filter). Enforce span-presence for grounded regardless of grounding_tier, not only strict. || D-R2 — the case-4 guard does not actually implement the stated 'equational checker could-not-run floors to uncertain': a kind=SYMPY ran=false claim falls to failed (else), not NONLLM_LEG_NONE. FIX: route isEquationalKind && nonLlm.kind==SYMPY && !nonLlm.ran && !anyRefuted && roundTrip.pass && !anyThrew to the uncertain floor, reserving failed for ran && !pass (genuine math contradiction). || tests-coverage / D-R12-D-R13 — add a hermetic bridge-mechanics regression test (temp-file ProcessBuilder over a stub interpreter, assumeTrue on python PRESENCE only, assert non-empty parseable stdout) so the -c-class bug can fail CI; the current integration tests use the bridge's own failure signal as the skip trigger and are structurally unable to catch it. Also fix the stale KDoc at NliEntailmentLlm.kt:132 (still references the removed '-c' invocation).
  key_concern: D-R3 was generalized from DEFINITION to all prose, but it is only sound where content==quote. For a prose GRADER_RULE the round-trip anchors on an UNRELATED quote and the only leg that judges the rule text is discarded — so a hallucinated grader rule reaches faithful/grounded and lights 'matches your lecture' (live on pa-kc-005), and a green test codifies this as the contract. Compounded by report-wrong never clearing the newly-decoupled lecture_grounded, so a student-disputed KC keeps the badge. Both are live badge lies — the exact invariant the reshape exists to protect — and both must be fixed before ship.

### pragmatist — SOUND_WITH_FIXES
  The reshape's spine is sound: per-kind routing, the agreed-REFUTED veto, EQUATIONAL_LLM_UNCONFIRMED→uncertain (D-R9), the D8 staleness gate wrapping lecture_grounded exactly as faithful, the FAIL-LOUD bridges (D-R12/D-R13 temp-file fix), and the order-independent per-claim conjunction (aggregateKc) are all correct and well-guarded — I verified each against the code. The python-bridge has zero false-faithful path (infra failure throws/returns ran=false → anyThrew/uncertain).

But two findings are NOT in the safe-to-defer class — they are live badge LIES, the exact invariant the reshape exists to protect, and both have one-line/one-clause fixes. They override the PRAGMATIST instinct to ship: a tutor whose entire value proposition is a trustworthy "matches your lecture" badge cannot ship while two reachable paths light that badge on non-lecture / disputed content.

MUST-FIX-NOW #1 (the prose-GRADER_RULE false-faithful, the HIGH in trustnet-safety + decideoutcome-logic). I confirmed it in code, not just the report: case 3p (VerificationRunner.kt:370-372) certifies any non-equational claim on `roundTrip.pass && !anyThrew && !agreedNonSupported`. D-R3's rationale ("self-entailment is zero-signal, round-trip is the anchor") is ONLY valid for a DEFINITION, where ContentReconcile.claimsFor emits content==quote (kt:77). A prose GRADER_RULE is emitted with content=rule, source=anchorRef (kt:116) — the round-trip relocates the UNRELATED anchor quote, never the rule text (SpanClaimRoundTrip.kt:30 keys on source.quote). So a rule whose content has no lecture basis reaches faithful (and lights grounded) whenever the KC's anchor quote is real, unless BOTH families agree-REFUTED — and the NLI abstain-floor (0.60) makes an unrelated pairing land UNCLEAR far more than confidently-bothREFUTED. This is LIVE: pa-kc-005:23 and pa-kc-006:23 each carry a prose grader_rule, and VerificationRunnerTest.kt:483-484,520-524 LOCKS IN the unsafe behavior as the expected contract (two prose rules with source=goldSource() asserted faithful "via the prose anchor"). The only thing keeping 005/006 honest today is the NLI verdict happening not to be agreed-REFUTED — incidental, not a guard. Cheapest correct fix (~30 min): in decideOutcome, gate case 3p on `claim.kind == ClaimKind.DEFINITION` (the sole content==quote kind), and route prose GRADER_RULE through a path requiring `bothSupported` (the LLM IS the only judge of a rule that is not its own quote). Then flip the test to red-then-green: prose GRADER_RULE, content=fabricated, real relocating anchor, families not-both-REFUTED ⇒ assert NOT faithful, grounded=false. This is the audit's fix-option (a)/(c) combined.

MUST-FIX-NOW #2 (report-wrong leaves lecture_grounded stale — the HIGH in badge-serve). Confirmed: the handler (TrustRoutes.kt:554-574) updates only status+updatedAt. In production a faithful KC ALWAYS has lecture_grounded=true (faithful⇒every claim round-trips⇒groundedKc true). After a student files report-wrong: status=pending, lecture_grounded STILL true, content_hash STILL matching ⇒ servedHonestFloor's early guard `status!=faithful && !grounded` = `true && false` = false ⇒ D8 gate passes ⇒ returns FAITHFUL_TO_SOURCE. The badge keeps saying "matches your lecture" on content a student actively disputed. D-R6 ("a failed claim suppresses grounded") does NOT cover this because report-wrong produces `pending`, not failed. The route test masks it (seedFaithfulCard, kt:810-814, leaves lecture_grounded NULL — the opposite of production). Cheapest fix (~10 min): add `it[lectureGrounded] = false` to the report-wrong UPDATE/INSERT, plus a regression test that seeds lecture_grounded=true (the production reality) and asserts the badge goes UNVERIFIED post-report.

CORRECTLY DEFERRED (the PRAGMATIST call holds for these): D-R14 (span-less DEFINITION lights grounded) and the two live *-fixture-* KCs — these are direction-safe: the quote GENUINELY relocates, so it is not a hallucination-class lie; it is grounding-laxity gated on authoring discipline + a non-strict tier choice. Real KCs 001-006 all carry verified byte-exact spans (content-reconcile confirmed). Acceptable for finals; tighten groundedKc to exclude DEFINITIONAL_NO_GOLD_SPAN when onboarding more subjects. D-R15 (006 equational over-reject) — SAFE direction (false-reject, never false-faithful); pick content-tuning (soften the invariant_statement) over the code change, since giving SymPy-proved equations an LLM-can't-REFUTE rule is a broader policy choice better made deliberately later. CI gap (no JARVIS_PYTHON3, self-masking integration tests) — the integration path is offline/owner-CLI, never on a request thread, so a CI miss here cannot ship a false badge to a user; BUT add the cheap hermetic bridge-mechanics regression test (stage a script with double-quotes, run the real ProcessBuilder, assert non-empty parseable stdout) since that is exactly the class of bug (-c quote-mangle) that shipped twice through green CI — ~20 min, deterministic, no model download. Coverage monitor / F2 gate wiring — genuinely post-acceptance, defer.

Cheapest trustworthy steady state: the two HIGH code fixes (~40 min combined) + their inverse tests + the one hermetic bridge test. That closes both live badge-lies and the only structural test-coverage hole on the bridge mechanics. The deferred set is then all direction-safe and finals-appropriate.
  revisit: D-R3: the 'LLM can only veto for prose, round-trip is the sole anchor' rule is sound ONLY for DEFINITION (content==quote). Restrict case 3p to claim.kind==DEFINITION; route prose GRADER_RULE to a path that REQUIRES bothSupported, because the round-trip anchors on an unrelated quote and cannot judge the rule text. Live exposure: pa-kc-005/006 grader_rules. || Report-wrong lifecycle (the 'deferred' note at decisions-log:50): NOT safely deferrable — it is a live badge-lie. Set lecture_grounded=false (or null) in the report-wrong transaction alongside status=pending, in the same commit as the D-R3 fix. || Test contract: VerificationRunnerTest.kt:483-484,520-524 codifies the unsafe prose-GRADER_RULE-faithful behavior as expected. Replace with a red-then-green test asserting a content-decoupled prose GRADER_RULE with a real relocating anchor must NOT reach faithful/grounded. || D-R13/D-R16 follow-up: add ONE hermetic bridge-mechanics test (stage a double-quote script, run the real ProcessBuilder, assert non-empty parseable stdout) — the self-masking integration tests cannot catch the -c-class bug that shipped twice. Cheap, deterministic, no model. || D-R14 (defer is fine for finals): tighten groundedKc to exclude DEFINITIONAL_NO_GOLD_SPAN, and exclude *-fixture-* from the served corpus, BEFORE onboarding more subjects.
  key_concern: Two LIVE badge-lies violate the reshape's one job (the badge must never lie) and both have cheap fixes, so they are must-fix-now, not deferrable: (1) a prose GRADER_RULE reaches faithful/grounded because case 3p round-trips an UNRELATED anchor quote, never the rule text — D-R3's self-entailment rationale only holds for DEFINITION (content==quote), and the test suite locks the unsafe behavior in as expected (live on pa-kc-005/006); (2) report-wrong flips status to pending but never clears lecture_grounded, so servedHonestFloor keeps lighting "matches your lecture" on content a student actively disputed (the route test masks it by seeding lecture_grounded NULL, not the production true). Fix both in one commit (~40 min) plus inverse tests; the remaining deferrals (D-R14 fixtures, D-R15 over-reject, CI/coverage/gate) are direction-safe false-rejects or grounding-on-real-quotes and are correctly parked for finals.

### first-principles — SOUND_WITH_FIXES
  The reshape's CORE move is right. The old conjunction was structurally unsatisfiable (every KC emits ≥1 DEFINITION with no machine check, so no KC could ever be faithful), and D-R3's insight — that for a DEFINITION where content==quote the LLM self-entailment vote is zero-signal, so the LIVE deterministic round-trip is the real anchor — is correct first-principles reasoning. D-R5/D-R6 decoupling the badge into an additive-nullable lecture_grounded, wrapped by the SAME D8 fail-closed staleness gate, is the right shape. D-R9's EQUATIONAL_LLM_UNCONFIRMED softening (SymPy proved the math, LLM merely couldn't confirm the NL meaning ⇒ uncertain not failed, but anyRefuted keeps a contradiction at failed) is correctly guarded — I verified anyRefuted vs agreedNonSupported are used in the right places. The FAIL-LOUD bridge fixes (D-R13 temp-file, stderr DISCARD) and the per-claim order-independence (FIX-A pending prior) are solid. So the engine is NOT solving the wrong problem.

But the single stated invariant — "no path may let hallucinated/non-lecture content reach faithful OR grounded" — is LIVE-VIOLATED on two independent paths, both traceable to specific decisions that over-generalized a DEFINITION-only argument:

(1) D-R3 generalized "self-entailment is zero-signal ⇒ round-trip is the anchor" from DEFINITION to ALL prose, but it is ONLY valid where content==quote. ContentReconcile.claimsFor:116 emits a prose GRADER_RULE with content=rule, source=anchorRef (the KC's first span-bearing quote, NOT the rule text). SpanClaimRoundTrip.check:30 round-trips claim.source.quote, not claim.content. So case 3p (VerificationRunner.kt:370-372) certifies the rule faithful purely because an UNRELATED anchor quote relocates, while the only leg that judged the rule text (LLM/NLI) is discarded for promotion and can only veto via agree-REFUTED. A hallucinated grader-rule on a KC with a real anchor quote reaches faithful (and lights the badge) unless BOTH families happen to agree-REFUTED — a one-family REFUTE does not veto, and NLI's 0.60 abstain-floor makes an unrelated pairing land UNCLEAR far more often than confident-bothREFUTED. This is hallucination-class false-faithful, the exact thing the reshape exists to prevent. Live surface: pa-kc-005/006 each carry a prose grader_rule. I confirmed the YAML, the emission, and the round-trip key by reading the files.

(2) D-R5/D-R6 decoupled the badge onto lecture_grounded but the report_wrong lifecycle was DEFERRED (decision log line 50) and never reconciled. report-wrong (TrustRoutes.kt:554-575) flips status faithful→pending but never clears lectureGrounded or contentHash. In production a faithful KC ALWAYS has lectureGrounded=true (faithful ⇒ every claim round-trips ⇒ groundedKc true). So after a student disputes content: status=pending, grounded still true, hash still matching ⇒ servedHonestFloor early guard (status!=faithful && !grounded = true && false = false) does NOT fall to unverified ⇒ D8 gate passes ⇒ FAITHFUL_TO_SOURCE. The badge keeps lying on actively-disputed content. Before the decoupling, report-wrong's faithful→pending correctly dropped the badge; the decoupling re-introduced the lie via a signal report-wrong forgets to clear. D-R6 only covers a `failed` claim suppressing grounded — report_wrong produces `pending`, not failed, so D-R6 does not cover it.

Both are masked by tests that seed the non-production state: VerificationRunnerTest.kt:480-532 ASSERTS prose-GRADER_RULE→faithful as the intended contract (so the unsafe path is a green-locked spec, not an unnoticed bug), and seedFaithfulCard (TrustRoutesTest.kt:810) leaves lectureGrounded NULL so the report-wrong test never exercises the production reality where it is true. Green CI gives false assurance on exactly the dimension the reshape protects.

On the lens question "is grounded = quote-relocates + no-failed the right definition": directionally yes (grounded is honestly weaker than faithful — a relocating quote IS lecture-traceable), but it currently OVER-CLAIMS in two ways the decisions themselves flagged-but-deferred: groundedKc keys on the raw roundTripPass leg boolean, not the claim VERDICT, so a DEFINITIONAL_NO_GOLD_SPAN claim (outcome=uncertain, fuzzy-relocated, no authored span) still counts toward grounded (D-R14, live on the two committed pa-kc-fixture-* KCs which ContentRepo loads into the served corpus). That is not hallucination-class (the quote genuinely relocates) so it is LOWer than (1)/(2), but it means grounded is gated on authoring discipline + corpus data, not a code invariant. The principled definition is "every cited quote relocates AND every claim's own verdict is not failed AND no claim is no-gold-span" — i.e. grounded should key on the claim OUTCOME, not the leg boolean.

Net: the decisions are the right strategy, but D-R3 (prose), the deferred report_wrong lifecycle under D-R5/D-R6, and the deferred D-R14 each leave a live invariant breach. SOUND_WITH_FIXES, not WRONG, because the fixes are local and the engine's spine holds.
  revisit: D-R3: restrict case 3p to claim.kind==DEFINITION (content==quote, where the self-entailment-zero-signal argument actually holds). Route prose GRADER_RULE (content!=anchor-quote) through a path that REQUIRES bothSupported on the rule-text-vs-quote NLI — promote the LLM from veto-only to a required positive vote for any claim whose content is not its own round-tripped quote. Until then, exclude prose GRADER_RULE from the faithful/grounded path. Add the red-then-green test the review specifies: content='fabricated rule, no lecture basis', anchor quote legitimately relocates, families NOT both-REFUTED ⇒ assert NOT faithful and grounded=false. This is the load-bearing fix — it closes the only hallucination-class false-faithful path. || D-R5/D-R6 (report_wrong lifecycle, log line 50): in the report-wrong transaction set lectureGrounded=false (or null) ALONGSIDE status, so a disputed KC drops the lecture badge. Then fix seedFaithfulCard to seed lectureGrounded=true (production reality) and extend the report-wrong route test to assert servedHonestFloor returns UNVERIFIED after the report. Without this the badge lies on content a student actively flagged wrong. || D-R14: make groundedKc key on the claim VERDICT, not the raw roundTripPass leg boolean — require each contributing claim's outcome NOT-IN {DEFINITIONAL_NO_GOLD_SPAN}. And exclude *-fixture-* from the served corpus in ContentRepo.loadSubject (the two committed pa-kc-fixture-* KCs currently light the grounded badge live). Enforces 'grounded' as a code invariant instead of an authoring-discipline assumption. || D-R9/case-routing (the SymPy could-not-run gap, MEDIUM): route isEquationalKind && nonLlm.kind==SYMPY && !nonLlm.ran && !anyRefuted && roundTrip.pass && !anyThrew to the uncertain floor (NONLLM_LEG_NONE / EQUATIONAL_LLM_UNCONFIRMED), reserving `failed` for ran && !pass (genuine math contradiction). Today a transient python outage turns a faithful equational KC into `failed` (which has no auto-clear), collapsing the system's own could-not-run vs machine-disproved distinction. Direction-safe but contradicts the FAIL-LOUD H5 contract the NonLlmLegs.kt:34-35 comment states. || Test hardening (HIGH, tests-coverage): the bridge integration tests use the bridge's OWN failure signal (throw / ran=false) as the env-absent skip trigger, so they CANNOT catch a bridge-mechanics bug — which is why the -c quote-mangle shipped twice through green CI. Add the hermetic ProcessBuilder regression test (stage a script with double-quotes, assert non-empty parseable stdout, guard assumeTrue on python PRESENCE only, never on success). Independent of the decisions but it is why two of these breaches went unverified.
  key_concern: A prose GRADER_RULE reaches per-claim `faithful` and lights the "matches your lecture" badge on content the round-trip NEVER checks — it relocates an unrelated anchor quote (source=anchorRef) while the rule text (claim.content) is judged only by the LLM, which case 3p discards for promotion. A hallucinated rule on any KC with a genuine anchor quote certifies unless both families agree-REFUTE. This is hallucination-class false-faithful — a direct breach of the one invariant the reshape exists to protect — it is live on pa-kc-005/006, and the test suite codifies it as the intended contract. D-R3's "self-entailment is zero-signal ⇒ round-trip is the anchor" is valid ONLY for DEFINITION (content==quote); it was over-generalized to prose where content!=quote. Compounded by report_wrong forgetting to clear lecture_grounded, so the badge also keeps lying on student-disputed content.

---
## AUDIT
**overall_state:** NEEDS_FIXES_BEFORE_TRUSTING

**acceptance_real:** PARTLY GENUINE, PARTLY ILLUSORY — and not yet safe. GENUINE: pa-kc-001..004 reaching faithful is real and safe. Those are DEFINITION claims where ContentReconcile emits content==quote (ContentReconcile.kt:77), so the LIVE round-trip anchors the exact content; two genuinely-independent families (relay Claude + local DeBERTa NLI) agree-SUPPORTED; FAIL-LOUD is intact (NliEntailmentLlm.runNli throws on empty/ERR/timeout/can't-start, verified kt:113-181). The D8 staleness gate wraps grounded exactly as faithful and fails closed (TrustRoutes.kt:262-272). The equational softening (D-R9) correctly never certifies a contradicted equation. ILLUSORY/UNSAFE: the "no false-faithful" invariant the acceptance claims to prove does NOT hold. (1) A prose GRADER_RULE reaches faithful/grounded via case 3p (VerificationRunner.kt:370-372) purely because an UNRELATED anchor quote round-trips — its content (the rule text) is NEVER checked against the source (source=anchorRef, ContentReconcile.kt:116; SpanClaimRoundTrip keys on claim.source.quote, kt:30). The only leg judging the rule text is veto-only, and an unrelated NLI pairing abstains far more than it agree-REFUTEs. Live on pa-kc-005 (line 23, grounding_tier:strict) and pa-kc-006. The decision-log's own claim that 005's prose grader landed "honestly uncertain" (line 33) is inconsistent with case 3p — nothing structural kept that grader out of faithful; it was incidental. (2) report-wrong (TrustRoutes.kt:554-575) flips status faithful→pending but never clears lectureGrounded or contentHash, so servedHonestFloor keeps serving FAITHFUL_TO_SOURCE on student-disputed content (verified: no lectureGrounded write exists in TrustRoutes). The 23→17/5/1 number is a real one-off dev-box run, but it is NOT a repeatable gate (CI never provisions JARVIS_PYTHON3) and it does not exercise the hallucinated-prose-rule adversarial case at all.

**MUST_FIX_NOW:**
- Restrict decideOutcome case 3p (VerificationRunner.kt:370-372) to claim.kind==DEFINITION (the sole content==quote kind). Route prose GRADER_RULE through a path that REQUIRES an independent positive signal on the RULE text (promote NLI from veto-only to a required bothSupported vote where content!=quote), OR round-trip the rule's own cited span, OR at minimum exclude prose GRADER_RULE from the faithful/grounded path until it has its own anchor. This is the load-bearing fix — it closes the only hallucination-class false-faithful path.
- Add the missing red-then-green test: prose GRADER_RULE, content='fabricated rule with no lecture basis', source.quote=a real relocating anchor, families NOT both-REFUTED ⇒ assert NOT faithful AND grounded=false. AND invert VerificationRunnerTest.kt:480-532 which currently asserts the unsafe behavior as correct.
- In the report-wrong transaction (TrustRoutes.kt:569) set lectureGrounded=false (or null) alongside status=pending, so a disputed KC drops the lecture badge. Fix seedFaithfulCard (TrustRoutesTest.kt:810-814) to seed lectureGrounded=true (production reality) and extend the report-wrong test to assert servedHonestFloor returns UNVERIFIED after the report.

**SAFE_TO_DEFER:**
- Tighten groundedKc to require each contributing claim's outcome NOT-IN {DEFINITIONAL_NO_GOLD_SPAN} (key on the claim verdict, not the raw roundTripPass leg) AND exclude *-fixture-* from the served corpus in ContentRepo.loadSubject — direction-safe (quotes genuinely relocate), but do BEFORE onboarding more subjects.
- Route a could-not-run SymPy equational claim (kind==SYMPY && !ran && !anyRefuted && roundTrip.pass && !anyThrew) to NONLLM_LEG_NONE/EQUATIONAL_LLM_UNCONFIRMED ⇒ uncertain, reserving failed for ran&&!pass — robustness, never false-faithful.
- Add a hermetic bridge-mechanics regression test (stage a double-quote-containing script, run the real ProcessBuilder over a stub interpreter, assumeTrue on python PRESENCE only — never on success, assert non-empty parseable stdout). Cheap, deterministic, no model download; would have caught the -c bug.
- D-R15 equational over-reject on pa-kc-006: prefer content-tuning (soften invariant_statement to the general rule, not the 3-instance example) over the broader code policy of letting SymPy-proved equations ignore an LLM REFUTED — SAFE direction (false-reject), park as a deliberate later choice.
- python-bridge nits: waitFor-before-drain deadlock hardening (spawn a drain thread or HF_HUB_DISABLE_PROGRESS_BARS=1 / TRANSFORMERS_VERBOSITY=error), temp-file leak on write failure, per-claim model reload (persistent NLI process with its own fail-loud contract), fix the stale KDoc at NliEntailmentLlm.kt:132, share the duplicated ProcessBuilder plumbing, make buildPrompt internal + test the real round-trip through parseNliInput.
- Delete the dead pre-decoupling badge/floor functions (badgeTextFor, honestFloorOf) or document them serve-irrelevant; add a lecture_grounded migration column-presence + idempotency test mirroring content_hash.

**confirmed_findings:**
- [HIGH] Prose GRADER_RULE reaches faithful + lights lecture_grounded via an UNRELATED anchor quote — VERIFIED in code: case 3p (VerificationRunner.kt:370-372) fires for ANY !isEquationalKind claim on roundTrip.pass && !anyThrew && !agreedNonSupported; ContentReconcile.kt:116 emits the rule with content=rule, source=anchorRef; SpanClaimRoundTrip.kt:30 round-trips claim.source.quote (the anchor), never claim.content. D-R3's 'self-entailment is zero-signal, round-trip is the anchor' rationale is sound ONLY for DEFINITION (content==quote); it was over-generalized to all prose. Live on pa-kc-005:23 (strict) + pa-kc-006. This breaches the one stated invariant.
- [HIGH] The test suite CODIFIES the unsafe path as the intended contract — VERIFIED: VerificationRunnerTest.kt:480-532 builds two prose GRADER_RULEs (content decoupled from goldSource().quote) and asserts they MUST reach faithful 'via the prose anchor' (lines 520-524), plus the KC aggregates faithful (593-606). The all-SUPPORTED fake LLMs mask that the LLM vote is irrelevant to promotion. There is ZERO test of a fabricated-rule-content + relocating-anchor case. Green CI gives false assurance on exactly the protected dimension.
- [HIGH] report-wrong never clears lecture_grounded — VERIFIED: TrustRoutes.kt:554-575 writes only status+updatedAt; grep confirms no lectureGrounded write anywhere in TrustRoutes. A production faithful KC always has lectureGrounded=true (groundedKc, VerificationRunner.kt:141-144). After report-wrong: status=pending, grounded=true, hash matching ⇒ servedHonestFloor guard (status!=faithful && !grounded = true && false = false) does not fall to UNVERIFIED ⇒ D8 passes ⇒ FAITHFUL_TO_SOURCE. The route test masks it (seedFaithfulCard, TrustRoutesTest.kt:810-814, leaves lectureGrounded NULL — not production) and never re-checks the served badge after report-wrong.
- [MEDIUM] groundedKc keys on the raw roundTripPass leg boolean, not the claim verdict — VERIFIED: VerificationRunner.kt:143 (perClaim.all{it.roundTripPass}). A DEFINITIONAL_NO_GOLD_SPAN claim (outcome uncertain, VerificationRunner.kt:361) whose quote fuzzy-relocates still has roundTripPass=true ⇒ contributes grounded=true. ContentValidator only ERRORS on a missing span for grounding_tier=strict, so a non-strict KC can author a span-less DEFINITION and light the grounded badge. This is a code seam, not just a data accident.
- [MEDIUM] Both pa-kc-fixture-compute.yaml and pa-kc-fixture-recursion.yaml are LIVE in content/PA/kcs/ — VERIFIED via Glob: ContentRepo.loadSubject loads every *.yaml with no fixture filter, so these span-less [FIXTURE] KCs reach the served corpus and (via the groundedKc seam above) light 'matches your lecture'. Not hallucination-class (the quote relocates) but a fixture must not light the trust badge.
- [MEDIUM] A SymPy bridge that COULD-NOT-RUN on an equational claim maps to failed, not uncertain — VERIFIED: a kind=SYMPY/ran=false claim matches neither case 4 (requires kind==NONE) nor case 4b (requires ran&&pass), falls to else ⇒ DISAGREE_OR_ROUNDTRIP_FAIL_OR_THREW ⇒ failed (VerificationRunner.kt:377-392), and failed has NO auto-clear (VerificationStatus.kt:127-135) ⇒ requires re-curate to recover. Direction-SAFE (false-reject, never false-faithful) but contradicts the system's own FAIL-LOUD 'never-ran floors to uncertain' contract.
- [HIGH-coverage] Bridge integration tests are structurally self-masking — the env-absent skip predicate (throw / ran=false) IS the bridge-mechanics failure signal, so they cannot catch a -c-class bug; CI never provisions JARVIS_PYTHON3, so the entire real-python family-B path has zero non-skipping CI coverage. This is why the -c quote-mangle shipped twice. No hermetic temp-file-ProcessBuilder regression test exists.
- [LOW] python-bridge is otherwise SOLID — VERIFIED: FAIL-LOUD intact (empty/ERR/unparseable/timeout/can't-start all throw, NliEntailmentLlm.kt:113-181), no false-faithful path through the bridge. Nits real but defer-safe: waitFor-before-stdout-drain deadlock-until-timeout risk; temp-file leak on write failure; per-claim cold model load; stale KDoc at NliEntailmentLlm.kt:132 still references the removed '-c' invocation; parseNliInput hand-mirrors a private buildPrompt (silent drift risk).

**risk_register:**
- The badge can currently lie in TWO live, reachable ways (hallucinated prose grader rule; student-disputed-then-still-grounded KC) — a tutor whose entire value is a trustworthy 'matches your lecture' badge cannot be trusted on this dimension until both are closed.
- The wrong contract is locked in by a GREEN test (VerificationRunnerTest.kt:520-524). Any correct fix will be fought by this test — it must be inverted, not just patched around. This also means future maintainers will 'restore' the unsafe behavior if they treat the test as ground truth.
- Five independent review lenses converged on D-R3 from different doors (trustnet-safety, decideoutcome-logic, devils-advocate, domain-expert, first-principles all HIGH/WRONG). That convergence is a strong signal, not noise — the two SOUND_WITH_FIXES verdicts (risk-analyst, pragmatist) still classify the same two paths as must-fix-now, not deferrable.
- The acceptance evidence (23→17/5/1) is a manual one-off; there is no repeatable CI gate over the real-python legs, so a regression in the bridge or the routing would ship green. The -c bug already demonstrated this failure mode twice.
- Latent grounding-laxity (span-less DEFINITION + live fixture KCs) will widen as more subjects onboard if groundedKc is not tightened to key on the claim verdict before then.
- A transient python/SymPy outage during an offline audit silently bricks a genuinely-faithful equational KC to failed (no auto-clear) — operationally annoying, requires re-curate, though never a false badge.

**BOTTOM_LINE:** The reshape's spine is right (per-kind routing, agreed-REFUTED veto, D8 staleness gate, EQUATIONAL_LLM_UNCONFIRMED softening, fail-loud bridges, order-independent aggregation are all correct and verified), and pa-kc-001..004 faithful is genuine + safe. But the central no-false-faithful invariant does NOT currently hold: D-R3 was over-generalized from DEFINITION (content==quote) to all prose, so a prose GRADER_RULE reaches faithful/grounded on an UNRELATED anchor quote (live on pa-kc-005/006), and report-wrong forgets to clear the newly-decoupled lecture_grounded, so the badge keeps lighting on student-disputed content. Both are confirmed in code, both are LIVE-reachable, and the first is locked in by a green test. Do not trust the badge or build on this until the three must-fix items land; everything else is direction-safe and deferrable.
**CONFIDENCE:** 9