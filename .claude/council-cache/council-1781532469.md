# Council review — 1781532469

**Problem:** Given the prior council's FLAWED verdict on the figure-redesign plan, decide the single concrete NEXT ACTION Claude should take (Alex delegated the decision to the council).
**Proposed approach:** Before any design/pixel work, run a DIAGNOSIS pass (delegated; Claude stays PM): (a) open the actual rejected renders at Alex's real viewport, classify failure mode per family (flat/static vs broken-layout vs wrong-encoding); (b) read the SVG scrubber shell + render contract, determine if motion is expressible today or needs a contract change. Deliverable = one-page per-family diagnosis + can-it-animate finding. THEN build a per-family visual-intuition RUBRIC on the worst family (sort/merge) Alex signs ONCE. Only then does renderer-design begin.
**Project context:** Same as council-1781489738 / 1781532034. Plus: agent self-report is INADMISSIBLE in this project (a prior "4/4 green" was actually 2/3 red); only PM's own re-check or green CI counts. Scars: design_by_proliferation + dev hand-doing instead of delegating.
**Timestamp:** 2026-06-15T (epoch 1781532469) UTC

---

## 🔴 Devil's Advocate
STANCE: CONDITIONAL — diagnosis sound, but the "single-page rubric Alex signs ONCE" smuggles back the no-termination flaw: a taste-signer who can't articulate pedagogy signs prose, then rejects pixels. A rubric tuned to sort/merge can also bless a renderer that lowest-common-denominators the OTHER families, baking the coarse-granularity defect into the acceptance criterion. KEY CONCERN: the rubric must be a concrete checklist of observable per-family visual properties with a falsifiable kill condition before any signature counts — else "signed once" just relocates proliferation one layer up.

## 📚 Domain Expert
STANCE: APPROVE — this is the standard heuristic-evaluation / design-system-component-audit-before-rebuild move (Nielsen expert audit; Storybook visual-regression baseline review). The per-family failure taxonomy is proper: flat-but-correct vs broken-layout vs wrong-encoding (a Bostock/D3 mark-channel error) are different fixes; conflating them wastes cycles. The can-it-animate probe directly confronts the real finding — 3B1B/Manim "click" = interpolated transitions + staged moves (enter/update/exit, eased tweens) a static once-verified 480×360 SVG can't express; VisuAlgo/Galles get their punch from stepped state-to-state animation, not still frames. KEY CONCERN: the rubric must encode MOTION criteria (enter-update-exit, easing, what-changed highlighting), not just static composition — else Alex signs static-SVG taste and reopens the "static can't carry click" flaw.

## ⚙️ Pragmatist
STANCE: CONDITIONAL — diagnosis-before-design is the highest-leverage + cheap move (hours, delegated). But the action PRE-COMMITS a rubric without proving it's needed; whether a rubric is the right artifact DEPENDS on the diagnosis — if failure is "broken layout," it's a bug fix, no rubric/taste-arbitration at all. KEY CONCERN: gate the rubric BEHIND the diagnosis; ship only the one-page diagnosis + can-it-animate finding first, let that result pick the next move.

## 🧱 First Principles
STANCE: CONDITIONAL — the irreducible goal: make ONE algorithm click for ONE learner who can't say why but knows it on sight. The can-it-animate finding IS the load-bearing empirical unblock (30-min fact-check vs artifacts on disk) — do it. But "build a rubric Alex signs once" is the wrong thing: a written rubric is a textual proxy a visual learner can't validate — he signs words, rejects pixels = manufactured false agreement. The real upstream move: he ALREADY gave a positive reference ("selection sort, old stuff" = good) — extract WHAT makes that one click, reproduce that quality on the worst family, show him the RENDER not a document. KEY CONCERN: replace "Alex signs a rubric" with "Alex reacts to one rebuilt render benchmarked against the selection-sort figure he already endorsed."

## ⚠️ Risk Analyst
STANCE: CONDITIONAL — correctly sequenced (look before designing), but two breaks. (1) The known false-green pathology applies: a delegated workflow can emit a confident-but-WRONG per-family classification or can-it-animate verdict, and there is NO CI gate to catch a wrong DIAGNOSIS — a misclassification silently mis-aims the entire downstream redesign. (2) The rubric has the same no-stop-condition disease unless expressed as checkable, motion-inclusive criteria. Lesser: can-it-animate is a code-fact the PM can and MUST verify directly — trusting the subagent's word is the inadmissible-self-report trap. KEY CONCERN: CRITICAL — diagnosis is not gate-backed → false-confident classification mis-aims all downstream design with no automated catch. Mitigations: (a) PM personally re-opens the renders at Alex's real viewport + confirms each per-family label; (b) PM independently reads the scrubber shell / render contract to confirm the can-it-animate verdict against actual code; (c) rubric = concrete motion-aware checkable criteria, sign-off on those not on one render. HIGH — sort/merge rubric may not transfer; defer generalization until a 2nd family checked.

---

## Sanity Check
SANITY Devil's Advocate: PASS — committed CONDITIONAL, concrete caveat (rubric needs falsifiable kill condition); aligned with clean agents.
SANITY Domain Expert: PASS — named real patterns (Nielsen, Storybook, Manim, VisuAlgo, Galles, Bostock/D3); only APPROVE but with a binding motion-criteria condition.
SANITY Pragmatist: PASS — concrete cost reasoning + specific gate-the-rubric condition.
SANITY First Principles: PASS — stripped to the irreducible goal, found the rubric=proxy failure, named the concrete replacement (react to a render vs sign a doc).
SANITY Risk Analyst: PASS — ranked CRITICAL, named the not-gate-backed + inadmissible-self-report traps, concrete mitigations.

---

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: FLAWED (proceed with the diagnosis HALF; cut/replace the rubric half)

CORE FINDING:
The DIAGNOSIS half is unanimously endorsed and is the correct next move — and the can-it-animate probe is the load-bearing unblock (it decides whether the whole static-SVG-family architecture can ever produce 3B1B "click," and it's a 30-minute code-fact, not a design opinion). But the action as framed is FLAWED on two unanimous points: (1) it PRE-COMMITS to building a rubric before the diagnosis that determines whether a rubric is even needed exists — if the failure is "broken layout," it's a bug fix with zero taste-arbitration; (2) "Alex signs a written rubric" is a proxy a visual learner cannot validate — he signs words then rejects pixels = manufactured false agreement. AND the whole diagnosis is NOT gate-backed, so per this project's inadmissible-self-report rule the PM (Claude) must do the looking + the shell-reading PERSONALLY — a delegated workflow's classification cannot be trusted to aim the downstream redesign.

AGENT CONSENSUS: 1 APPROVE, 4 CONDITIONAL — 0 flagged. Unanimous: do the diagnosis; the can-it-animate code-fact is decisive; the rubric step is premature/proxy.

KEY ISSUES:
- DIAGNOSIS NOT GATE-BACKED → PM-DONE, NOT DELEGATED (CRITICAL, Risk): a wrong classification mis-aims everything with no CI to catch it. The looking-at-renders + reading-the-shell IS PM verification work (not build work), so PM-delegation does NOT apply — Claude does it directly. (Aligns with active-constraints rule 0.)
- RUBRIC IS PREMATURE (HIGH, Pragmatist): gate it behind the diagnosis. If failure = broken layout → bug fix, no rubric. Let the diagnosis result pick the next move.
- RUBRIC-AS-DOC IS A PROXY (HIGH, First-Principles + Devil): a visual learner can't validate prose. Replace with: extract what makes the already-endorsed selection-sort figure click → reproduce on the worst family → Alex reacts to the RENDER. If any rubric exists it must be concrete, MOTION-aware (enter/update/exit, easing, what-changed highlighting), checkable, with a kill condition — and per-family (a sort/merge rubric may not transfer; defer generalization until a 2nd family checked).

RECOMMENDED PATH (this is what Claude does next):
1. PM (Claude) PERSONALLY does the diagnosis — not a delegated agent:
   (a) Locate + open the actual rejected render frames at Alex's real viewport (1536×648 & ×730); label each family's failure mode: flat/static · broken-layout · wrong-encoding.
   (b) Read the SVG scrubber shell + keyframe render contract DIRECTLY; answer yes/no: can it express motion/transition today, or does that need a contract change? (Found on disk: `motion-helpers.tsx` + `AlgoStepperShell.tsx` — read these.)
   (c) Extract WHAT makes the one endorsed figure (selection sort) click — that becomes the positive reference, not a written rubric.
2. STOP after the diagnosis. Bring the one-page result (per-family label + can-it-animate verdict + the selection-sort "what works" extraction) to Alex. The diagnosis result picks the next move:
   - broken-layout → bug fix, no taste loop.
   - flat/static + shell-can't-animate → the fix is a motion-layer/contract change (escalate as a real design decision, tensions the locked static-SVG family contract).
   - flat/static + shell-can-animate → renderer-redesign benchmarked against the selection-sort reference, motion-aware checkable criteria per family.
3. Do NOT pre-build the rubric; do NOT ask Alex to sign a document.

CONFIDENCE: 9/10
What would raise it: nothing material — the council is near-unanimous and the next move is a cheap, reversible, fact-finding step. The only residual is whether the rejected render PNGs are still on disk (BRIDGE-HEAD lists stray r-*.png/cmp-*.png/bars-render*.png; if gone, PM re-renders from the dirty working tree at the two viewports).
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[COUNCIL CONTEXT SAVED — supersedes prior next-action; see council-1781532034 for the architecture verdict]
