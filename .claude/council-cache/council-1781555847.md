# Council review — 1781555847

**Problem:** Alex wants to FINISH the tutor — "built perfect ... as soon as possible without compromises" — but is losing the thread ("i don't really know what's going on anymore"). The last ~2 sessions were spent hand-editing one merge-sort figure's "front/frontier representation" on a throwaway prototype route, while the locked v2 master plan (Phase 0→A→B→C→SPIKE→D→E→F→G→H→I→J, deploy-last) sits barely-started at Phase A and nothing is deployed.

**Proposed approach (critiqued):** Stop hand-iterating the merge front-rep (off critical path, likely off-plan per A3/F18 which folds sort/merge into seq-array; standalone `SortMergeFamily` = off-spec 7th family). Fastest no-compromise path to FINISH = execute the locked v2 plan strictly in order via the two-lane workflow (agents build under PM oversight, never hand-edits on main): Phase 0 exit + A0 (commit viz WIP) → A1 (trace→keyframe engine) → wire semantic frame gates to CI → fold sort/merge into seq-array → onward. Taste ("does the front read") solved STRUCTURALLY (seq-array grammar + frame gates + Phase-D premium shell + one human experience-checkpoint), not by hand-tuning a demo.

**Project context:** jarvis-kotlin tutor (single-user ADHD CS-student learning his own univ subjects). Locked roadmap = `docs/superpowers/plans/2026-06-15-end-to-end-master-plan-to-100-v2.md`. Binding spec = `docs/superpowers/specs/2026-06-11-one-pass-digestion-teaching-engine-design.md`. Grounded state: 13 commits ahead/unpushed, ~247-dirty tree (221 untracked), `SortMergeFamily.tsx` 1554 lines UNTRACKED + registered standalone at `familyRegistry.ts:38`, two real gate tools (frame-conjunction 17KB + svg-overlap 24KB) untracked + manual (not CI-wired), DB 8 kc_verification rows / 828 fsrs_cards / 0 non-null kc_id, off-box VPS backup stale (2026-06-03), live VPS ~140 commits stale.

**Timestamp:** 2026-06-15T20:37:27Z

**Grounding note:** all 5 advisors independently OPENED the artifacts and verified (git, registry, spec §5.2, the diagnosis doc) before opining — the standing "ground councils in artifacts, not prose" rule. No advisor relied on the brief's summary.

---

## 🔴 Devil's Advocate
AGENT: Devil's Advocate
STANCE: CONDITIONAL
REASONING: Grounded every claim and they hold — `SortMergeFamily.tsx` (1554 lines) is untracked, registered as a standalone `"sort-merge"` family alongside `"seq-array"` (literally the off-spec split A3/F18 forbids), and `MergeCompareDemo.tsx` self-labels "Throwaway quality vehicle (NOT a production lesson)." The proposed approach is directionally correct — stop hand-polishing a throwaway — but it is self-contradictory: it says "execute the locked plan STRICTLY in order, A0 commit the WIP," yet the WIP it wants committed is the standalone SortMergeFamily that the very next step (A3/F18) says must NOT exist. You cannot both "commit the full untracked set as a green foundation" (A0) AND "fold sort/merge into seq-array, a standalone family is off-spec" (A3) without first resolving the §5.2 family-split fork that the diagnosis itself escalated to the spec-owner as "NOT an executor call." The approach hand-waves this as "solved STRUCTURALLY inside seq-array's visual grammar" — but the diagnosis's own evidence is that the seq-array grammar produces GOOD bars and BAD boxes for merge, i.e. the fold is the unproven hypothesis under test, not a settled answer.
KEY CONCERN: A0 ("commit the full WIP incl. SortMergeFamily as a green foundation") and A3 ("a standalone sort/merge family is off-spec — fold it") collide, and the approach orders A0 first. The §5.2 fork (merge-gets-bars-grammar-inside-seq-array vs merge-warrants-own-family) is an UNRESOLVED spec-owner decision; that ruling — not "commit the WIP" — is the real critical-path blocker.
Q1: STOP the hand-polish — but the work is NOT discardable; the bars-grammar + the two gates are load-bearing and must be PORTED into seq-array.
Q2: Off-plan confirmed (registry has `"sort-merge"`≠`"seq-array"`); PORT bars/interleave grammar + gates into seq-array and DELETE the standalone family — but only after Alex rules the §5.2 fork; discard the demo route + boxes skin, keep gates + bars motion.
Q3: Strict-in-order is the right spine but NOT executable today because A0⇄A3 collide on the unresolved §5.2 fork; insert a one-line PM/Alex spec-owner ruling on the family-split fork as the literal first unit.
Q4: Get Alex's §5.2 ruling, then commit ONLY the spec-faithful slice via explicit-path `git add` (gates + resolved-grammar family, never `git add -A`), delegated to the workflow under PM review.

## 📚 Domain Expert
AGENT: Domain Expert
STANCE: APPROVE
REASONING: This is a textbook "polishing a prototype instead of solving structurally" trap, and the proposed approach escapes it correctly. The merge-figure work is hand-authored keyframes (`MERGESORT_DATA_JSON` frozen at `FRAME_INDEX=23`) on a route the file itself stamps "throwaway... NOT a production lesson" — the exact artifact A1's generate-from-execution mandate (the Manim/algorithm-viz known-good pattern: figures = a deterministic function of `run(algorithm,input)`, object-continuity across transitions) says to RETIRE. Hand-tuning the front-rep is iterating on Layer-2 taste (the diagnosis says "NOT broken-layout... TASTE/QUALITY path, not a bug"), which by definition cannot be closed by eye — it's closed structurally by the seq-array grammar + the two semantic frame gates (both already RED→GREEN-verified) + the Phase-D premium shell + one human experience-checkpoint. And `SortMergeFamily` is genuinely registered as a standalone 7th family key, which A3/F18 declares off-spec; the plan is DETERMINATE that the executor folds, not debates. The ~2 sessions are NOT wasted — the verified divide/overlap fixes and the two gate tools are the durable Phase-A asset; only the demo route + standalone-family packaging are throwaway.
KEY CONCERN: Don't let "fold into seq-array" silently delete the merge-specific motion grammar the diagnosis proved is load-bearing. The merge concept's defining dynamic is two sorted runs interleaving by comparing fronts, which a bare bars renderer does NOT show. The fold is correct as packaging (one seq-array family key per spec §5.2 "sorting states"), but seq-array must GAIN a merge-interleave motion variant + front-pointer markers, gated by `concept_type→family FITNESS` (fail-closed). Folding that drops the interleave to satisfy the registry would re-create the never-sorts class under a green test.
Q1: Right — stop it; Layer-2 taste hand-tuning on a self-declared throwaway, off critical path, structurally unclosable by eye.
Q2: Off-plan — fold (don't discard): PORT trace-oracle + interleave motion + front markers into seq-array, KEEP the two gate tools, DISCARD the standalone family wrapper + the demo route.
Q3: Strict in-order IS the fastest no-compromise path precisely because "ASAP" without an oracle is what produced never-sorts/ghost-component; only change is operational — commit A0 first (WIP is one `git clean` from deletion), then proceed.
Q4: A0 — explicit-path commit of the full viz WIP set (families + helpers + the two gate tools), full viz vitest foreground-streamed, treat green-tracked as foundation; unblocks A1.

## ⚙️ Pragmatist
AGENT: Pragmatist
STANCE: CONDITIONAL
REASONING: Directionally right; approve its core moves, with hard conditions, because two of its claims contradict the plan it cites. (1) Stopping hand-iteration is correct: the diagnosis itself says the bars-vs-boxes / "frontier doesn't land" question is a §5.2 spec-owner fork — NOT an executor call — yet two sessions burned PM time hand-tuning a throwaway route past the point a gate could adjudicate it; taste-iteration with no exit gate runs forever. (2) But "execute the FULL v2 plan strictly = fastest to DONE/ASAP" is the contradiction: 12 phases, Phase A alone A0-A4 + 4 CI frame gates, Phase E = 7 sub-phases of ZERO-code digestion engine (~50% of spec), Phase F = up to 140 sequential checkpoint cycles across 4 subjects that have never produced a single KC, B/C net-new oracles requiring a SECOND independent LLM family. That is not "ASAP" — it is the full no-compromise build. "Perfect, ASAP, no compromises" is internally contradictory at this scope; the honest move is to make the tradeoff visible to Alex, not bury it inside "just execute the plan." (3) The fold (A3/F18) is unambiguous: standalone family is off-spec, executor is READ-ONLY, so the 1554-line untracked SortMergeFamily does NOT get committed as a 7th production family.
KEY CONCERN: The proposal's "strict full v2 = fastest" framing hides the scope reality from an owner who already said "i don't really know what's going on anymore." The plan is the spine, but A0 must commit the WIP (the gates are real load-bearing work), NOT discard it; the genuine gate tools are keepers, the prototype route is throwaway. Don't let "STOP hand-editing" become "delete the verified gates and rebuild."
Q1: Proportionate to STOP — taste-iteration is off-gate and unbounded; salvage the load-bearing output (two gates + divide/overlap fixes), discard only the prototype-route hand-tuning.
Q2: Off-plan; FOLD — PORT bars grammar + interleave motion into SequenceArrayFamily, keep trace/invariant helpers + gate tools, DISCARD the 1554-line standalone family + throwaway route.
Q3: No — strict full v2 is the no-compromise path, not the ASAP path; they conflict at this scope. Minimum honest path = one real end-to-end vertical slice (PA only, the 6-7 existing real KCs, through A→B→C→D and DEPLOYED once) with no fake-green — a finished user-visible product Alex can SEE — while B/C/E/F-breadth continue behind the oracle; surface this tradeoff explicitly rather than auto-committing to all 12 phases.
Q4: A0 — explicit-path commit of the full viz WIP, run full vitest viz foreground-streamed, confirm green; unblocks everything + ends the dirty-tree landmine.

## 🧱 First Principles
AGENT: First Principles
STANCE: CONDITIONAL
REASONING: Stripped to the raw goal — one ADHD student wants a tutor that teaches HIS subjects correctly, finished ASAP without compromises — the hand-iteration on the merge "frontier" is a symptom, not the work: there is no oracle for "does this figure teach," so a human keeps re-judging taste on a throwaway route and loses two sessions to it. The proposed approach is directionally CORRECT and the diagnosis already routes the fork to the spec-owner, but reject two pieces of its framing: (a) the locked v2 plan is genuinely over-built for a user-base of one — 16 dimensions, 5 residuals, two-layer PROOF/ESTIMATOR tagging, a generator-capability spike, deploy-last across 0→J is process scaled for a product team; for a single learner the load-bearing 80% is "Layer-1 truth is never wrong" + "every concept covered" + "it actually teaches me," not the gate-coverage-registry meta-machinery; (b) "taste solved STRUCTURALLY" is itself residual #1 the plan ADMITS no machine can prove — you cannot fully gate "does the frontier land," so the honest move is to TIME-BOX the look, not promise structure eliminates the human judgment. Net: do the plan's order, but the single next unit is A0 (commit the verified WIP so two sessions of real gate work can't be `git clean`'d off main), NOT another frontier iteration.
KEY CONCERN: A0 is not done; 13 commits unpushed while 246 dirty files — including two genuinely-good verified gates + the divide/overlap fixes — sit one `git clean` from deletion on main. Every hour hand-tuning the frontier is an hour verified work stays unprotected. Protect it first.
Q1: The actual goal is "a tutor that teaches Alex's subjects, truth-first"; hand-polishing the front-pointer is a symptom of the missing teaching-oracle (residual #1), structurally unwinnable by hand, off critical path.
Q2: Off-plan — spec §5.2 (line 316 "sorting states"→sequence/array; line 603 lists exactly 6 families) gives no standalone family; merge folds into sequence/array, and the i-vs-ii grammar fork is a spec-owner amendment, not a hand-tuning loop.
Q3: The plan's SEQUENCE (oracle-first, deploy-last) is right, but its WEIGHT is gold-plated for one user — keep Layer-1 purity + coverage + the honest teaching-checkpoint, treat the registry/matrix/residual ceremony as the lightest thing that keeps you honest, not work to perfect.
Q4: A0 — explicit-path commit (never `git add -A`/`clean`) of the verified gates + family WIP + divide/overlap fixes, run viz vitest foreground-streamed, confirm green; protects two sessions of real work and unblocks A1.

## ⚠️ Risk Analyst
AGENT: Risk Analyst
STANCE: CONDITIONAL
REASONING: Verified the situation directly; the approach is correct on the merits — but it inverts the most urgent risk. The largest live risk is NOT taste-iteration drift; it is that ~1554 lines of genuinely-good SortMergeFamily work plus two real gate tools (17KB+24KB, verified on disk) sit UNTRACKED in a 221-untracked tree on `main`, where BRIDGE-HEAD and the plan both warn a single `git clean`/`git add -A`/branch-switch destroys them — unbacked locally AND off-box (stale 2026-06-03). That is the only CATASTROPHIC, IRRECOVERABLE risk; everything else (oracles missing, deploy gap, fake-green) is recoverable. A0-commit must precede all building, but A0 alone is insufficient: the work isn't durably safe until committed AND the 14 resulting commits are PUSHED off-box.
KEY CONCERN: [CRITICAL] Irrecoverable loss of the uncommitted figure+gate work. SortMergeFamily.tsx (untracked), MatrixGridFamily.tsx, the invariant/trace helpers, both gate tools are unversioned, unpushed, off-box-stale. Blast radius = days of council-grounded work; recoverability = none after a clean/reset. Outranks every plan risk (those are "build later"; this is "gone forever").
Q1: Stopping the hand-iteration NOW is LOW risk and correct; continuing to hand-tweak taste is higher risk (unbounded, ungated, no exit — the "losing the thread" pattern).
Q2: Mitigation ORDER is strict: (1) explicit-path `git add` of ONLY verified family/gate/test files (NEVER `git add -A` — 221 untracked door/demo landmines), (2) commit (A0), (3) viz vitest foreground+streamed green, (4) PUSH all 14 commits to origin/main, (5) fresh off-box backup of `~/.jarvis/tutor.db` BEFORE any later DB mutation. Push-after-commit is the load-bearing step the brief under-weights.
Q3: Biggest path risk = [HIGH] fake-green from long autonomous agent runs (never-sorts passed every gate); mitigation = Phase-0 gate-self-test-on-natural-seeds + CI-wiring of the two now-manual gates BEFORE A1's output is trusted.
Q4: A0 scoped to the verified subset: explicit-path `git add` SortMergeFamily/MatrixGridFamily/helpers/tests + the two gate tools (NOT door/demo), commit, viz vitest foreground-streamed, then PUSH — converts the one irrecoverable risk into a recoverable one; the plan's stated hard precondition for everything after.

---

## Sanity Check
SANITY Devil's Advocate: PASS — grounded (verified registry/untracked/spec); the A0⇄A3 "collision" slightly overstates "impossible" (commit-then-fold is a normal refactor with history, and protection is the point of A0), but the underlying §5.2-fork-is-unresolved-and-executor-can't-decide claim is true and substantive.
SANITY Domain Expert: PASS — clean; named real patterns (Manim/generate-from-execution, polish-prototype trap), grounded, the don't-drop-the-interleave caveat is the sharpest technical catch.
SANITY Pragmatist: PASS — clean; counted the plan, the ASAP⇄no-compromise contradiction is real and the vertical-slice alternative is concrete + specific.
SANITY First Principles: PASS — clean; correctly stripped the framing to the missing-teaching-oracle root and the n=1 gold-plating; the time-box-the-taste point is honest about residual #1.
SANITY Risk Analyst: PASS — clean; independently verified untracked status via `git ls-files --error-unmatch`; the CRITICAL irrecoverable-loss ranking is correct and the mitigation order is actionable.

No agent flagged. All five grounded independently; near-total convergence.

---

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: FLAWED

CORE FINDING:
The approach is RIGHT on every immediate move — stop the merge front-rep hand-polish (it is Layer-2 taste on a self-declared throwaway, ungated, structurally unclosable by eye), fold sort/merge into seq-array (the standalone `SortMergeFamily` is a confirmed off-spec 7th family per spec §5.2 + A3/F18), and treat A0 as the next unit. It is WRONG on one framing the owner must not be allowed to miss: "execute the FULL 12-phase v2 plan strictly = fastest to DONE/ASAP" is internally contradictory. Two clean agents proved the scope (Phase E = ~50% of spec at zero code; Phase F = up to 140 checkpoint cycles on 4 subjects with zero KCs; B/C = net-new oracles needing a second LLM family) is the full no-compromise build, NOT an ASAP path. "Perfect + ASAP + no-compromise" cannot all hold at full breadth; that tradeoff is a decision Alex owns, and burying it inside "just run the plan" is the failure mode.

AGENT CONSENSUS: 1 APPROVE, 4 CONDITIONAL — 0 flagged. Unanimous: stop the hand-polish; fold (not discard) sort/merge into seq-array; A0 is the next unit; the two gate tools are the durable keepers.

KEY ISSUES:
- [CRITICAL — Risk Analyst, echoed by First Principles] ~1554 lines of verified figure work + two real gate tools are UNTRACKED, unpushed, and off-box-backup-stale on `main` — one `git clean`/`reset --hard` from irrecoverable loss. This is the only catastrophic risk on the board and it is live RIGHT NOW. Mitigation order: explicit-path `git add` (NEVER `-A`) → commit → viz vitest foreground-streamed-green → PUSH → fresh off-box DB backup.
- [HIGH — Pragmatist + First Principles] "Full strict v2 = fastest" hides a scope decision from a lost owner. Honest alternative: one real end-to-end vertical slice (PA only, the ~6-7 existing real KCs, through A→B→C→D, DEPLOYED once, zero fake-green) delivers a FINISHED, user-visible product fast, while breadth (E/F) continues behind the oracle. The v2 sequence (oracle-first, deploy-last) stays the spine; its full breadth-weight is the negotiable.
- [HIGH — Domain Expert] The fold must PORT the merge-interleave motion grammar (two runs converging by comparing fronts) + front markers into seq-array, gated by the fail-closed `concept_type→family FITNESS` check. A fold that drops the interleave to satisfy the registry re-creates the never-sorts class under a green test.
- [MEDIUM — Devil's Advocate] The bars-grammar-inside-seq-array vs own-family detail is a real §5.2 spec-owner sub-decision; the plan is determinate that it folds, but the grammar shape is Alex's to ratify, not an executor's.

RECOMMENDED PATH:
1. NOW (no decision needed, protects the only irrecoverable asset): A0 — explicit-path commit of the verified viz WIP + the two gate tools (never `git add -A`; door/demo files excluded), viz vitest foreground-streamed to a log, confirm green, PUSH all commits, fresh off-box DB backup. Do this via the workflow/PM discipline, zero hand-edits on main.
2. THEN put ONE decision to Alex: full-breadth strict v2 (no-compromise, long) vs PA-only end-to-end deployed vertical slice first (finished product fast, breadth behind the oracle). This is the real "ASAP vs no-compromise" fork and only he can rule it.
3. The merge figure: stop hand-tuning; its motion grammar + the two gates PORT into seq-array as a FITNESS-gated interleave variant; the standalone family + demo route are retired. Taste is time-boxed + judged by the one human experience-checkpoint, never an open-ended hand-polish loop.

CONFIDENCE: 8
All five advisors grounded against the real artifacts and converged; no flags. The 2 points below 10: (a) whether Alex wants the vertical-slice or full breadth is a genuine preference, not a fact the council can settle; (b) the exact seq-array merge-interleave grammar is unproven until built + FITNESS-gated.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output saved to: .claude/council-cache/council-1781555847.md
