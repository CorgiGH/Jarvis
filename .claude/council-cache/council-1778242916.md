# Council review round 3 — 1778242916

**Round:** 3 of N (user-authorized iteration loop)
**Prior:** council-1778241788.md (round 1: FLAWED, 2 REJECT 3 CONDITIONAL) → council-1778242458.md (round 2: FLAWED, 1 APPROVE 3 CONDITIONAL 1 REJECT, bilingual issue surfaced).
**Spec under review:** `docs/superpowers/specs/2026-05-08-study-companion-design.md` v2 with paths X (bilingual seed) / Y (catalog inversion) / Z (Pragmatist cut) on the table. User asked council to inspect ALL files + decide.

**This round different:** agents had full file access. **All 5 spotted `tools/concepts_real/` (276 hand-curated bilingual concepts, commit `4091c15`)** that prior rounds + my audit missed.

---

## 🔴 Devil's Advocate
STANCE: REJECT
RECOMMENDED PATH: **OTHER** — adopt `tools/concepts_real/*.md` as seed, retire `seed_concepts.sh`, ship W1+W2+W3+W5 (no [[quiz]] this session).
Cited PS.md sample (`## Formula lui Bayes`, `## Schema bilei neîntoarse`, source-tagged). Round-2 "Lanțuri Markov" was directionally right; actual mismatch is `Bayes theorem` (seed) vs `Formula lui Bayes` (real). Path X bilingualizes WRONG SOURCE; concepts_real already won. Path Y broken: pdftotext on Beamer-style slide PDFs gives `Probabiliti i Statistic` repeated 22× per page, diacritic-mangled `condi�ionat`. Heading regex on real PDFs hits zero matches.
KEY CONCERN: W4b heading-match against PDF body text is unsolved regardless of path — PDFs are OCR-mangled slide format, not markdown. Defer [[quiz]] honestly; don't ship a heading-regex matcher that silently matches nothing.

## 📚 Domain Expert
STANCE: APPROVE
RECOMMENDED PATH: X (bilingual seed)
Note: DE inspected seed_concepts.sh + PDFs but did NOT spot tools/concepts_real/. Recommendation premise is on stale data. Reasoning: W4b match would 0% hit because PDFs are 100% Romanian and seed is English. Anki uses alternative-spellings + RemNote synonym portals — Path X is the standard pattern. Path Y dead because PDFs are slide nightmares.
KEY CONCERN: W4b heading-match must search BOTH EN and RO sides of `## EN | RO` against ingested PDF text.

**Note:** DE's APPROVE Path X is on incomplete information. With concepts_real visible, recommendation effectively converges with DA + FP + RA on OTHER — concepts_real IS the bilingual catalog DE wants Path X to produce. So treat DE's stance as aligned with the OTHER finding (just lacking the data point).

## ⚙️ Pragmatist
STANCE: REJECT
RECOMMENDED PATH: **OTHER** — ship-and-watch. Stop building, let user use what already exists.
PDF ingestion shipped 6h ago (commit `4091c15`): 276 real concepts (ALO 65, PA 36, POO 58, PS 69, SO_RC 48). Path X bilingualizes a seed already replaced. Path Y deletes a seed already moot. Path Z ships [[quiz]] surface on top of catalog the user hasn't yet used in production. User shipped ~7 commits in 12h capping a 6h LLM-provider session; memory `feedback_ship_and_watch.md` says "default to ship-now-watch-usage." Building any of X/Y/Z tonight is the failure-mode the council pattern is meant to catch.
KEY CONCERN: Proposed paths solve a problem `4091c15` already solved. Hard-stop, let user run `[[plan: today]]` against the real 276-concept catalog tomorrow, let usage drive next iteration.

## 🧱 First Principles
STANCE: REJECT
RECOMMENDED PATH: **OTHER** — UNION read: `concepts_real/*.md` + Second brain PDFs + os-study-guide React app, with provenance tracking ("you wrote this in lab2").
`concepts_real/` (835 lines, 276 concepts) sits next to `seed_concepts.sh` (180 stubs) — two parallel catalogs, the real one has won. PDF heading extraction is heterogeneous: SO has parseable Romanian TOC, PS extracts as garbage overlapping LaTeX, PS laborator/ has no PDFs at all (only R + CSV). Single-source extraction breaks. Right shape: union with per-source provenance.
KEY CONCERN: Two parallel catalogs in same `tools/` dir; whichever wins must delete the loser in the same commit. Leaving both is the actual bug rounds 1 + 2 missed.

## ⚠️ Risk Analyst
STANCE: REJECT
RECOMMENDED PATH: **OTHER (Path W)** — deploy `concepts_real/` to archival/ + fix FSRS TOCTOU.
Verified: ConceptCatalog reads `Config.archivalDir` (`/opt/jarvis/data/archival/<subject>/concepts.md`); NO script (populate.ps1, deploy.sh, seed_concepts.sh) copies `tools/concepts_real/*.md` to that path. Commit `4091c15` says "pushed to VPS" but mechanism not visible in checked-in scripts → likely manual scp. Deployment gap means in some VPS states, seed_concepts.sh's 187 stubs win silently. Plus: KnowledgeFsrs.recordReview lines 71-101 still have read→compute→append unsynchronized; LOCK only covers appendTo. Round-2 TOCTOU remains. SO_RC.md vs SO&RC archival dir naming reconciliation needed.
KEY CONCERN: Deployment-path mismatch + still-broken FSRS TOCTOU. "Path W" = deploy concepts_real, fix recordReview mutex (5 lines), reconcile SO_RC→SO&RC naming. Reuse existing 276 concepts; add multi-day [[catchup]]; ship.

---

## Sanity Check round 3

SANITY DA: PASS
NOTE: clean. Concrete file citations (PS.md sample, OS PDF text dump). Bilingual finding refined (Bayes theorem vs Formula lui Bayes is the actual canonical mismatch). Honest about W4b unsolved.

SANITY DE: PASS with caveat
NOTE: Reasoning sound for the data DE saw, but DE missed `tools/concepts_real/` so the Path X recommendation premise is incomplete. Effectively DE's vision converges with OTHER — concepts_real IS the bilingual catalog Path X aspires to.

SANITY Pragmatist: PASS
NOTE: Strong evidence-based pivot. Cites commit `4091c15` directly. ship-and-watch invocation matches `feedback_ship_and_watch.md` rule.

SANITY First Principles: PASS
NOTE: Architectural finding (two parallel catalogs) is correct and was missed in rounds 1+2. UNION reading proposal is constructive.

SANITY Risk Analyst: PASS
NOTE: Verified deployment gap in source. TOCTOU concern persisted across all 3 rounds, still unfixed. SO_RC vs SO&RC naming reconciliation is a real implementation detail.

---

## Judge round 3

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT (round 3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: **WRONG APPROACH** (the 3 paths I asked council to choose between are all wrong; the right answer was already in the repo)

CORE FINDING:
**`tools/concepts_real/` already exists** (commit `4091c15`, 276 hand-curated bilingual concepts across all 5 subjects with source citations) and supersedes `seed_concepts.sh`. All 3 paths I drafted (X bilingual seed, Y catalog inversion, Z Pragmatist cut) are misframed. Four of five agents converged on OTHER; DE recommended X but on incomplete data (didn't spot concepts_real). The catalog problem is solved; the unsolved problems are (a) deploying `concepts_real/` cleanly to `/opt/jarvis/data/archival/` (no script does this, commit message says "pushed to VPS" but mechanism opaque), (b) round-2 FSRS TOCTOU still unfixed in `KnowledgeFsrs.recordReview` lines 71-101, (c) `SO_RC` vs `SO&RC` naming reconciliation, (d) PDF body-text grounding for [[quiz]] is unsolved on slide-style PDFs (regex won't work) — defer honestly.

AGENT CONSENSUS round 3: 4 OTHER (all REJECT-with-better-path), 1 X-on-stale-info (DE) — converging on OTHER.

KEY ISSUES:
1. **Deploy concepts_real → archival/** (RA + DA + FP). Need a deploy script (`tools/deploy-concepts.sh` or extend `deploy.sh`) that copies `tools/concepts_real/*.md` → `/opt/jarvis/data/archival/{subject}/concepts.md`. Subject-name reconciliation: `SO_RC` source → `SO&RC` archival dir.
2. **Retire `seed_concepts.sh`** (FP). Two parallel catalogs is the actual bug. Keep `seed_concepts.sh` only as fallback if user deletes archival, OR delete entirely.
3. **FSRS TOCTOU fix** (RA, persistent across 3 rounds). 5-line fix: extend `LOCK` in `KnowledgeFsrs.recordReview` to span the full read→compute→append.
4. **Defer [[quiz]] surface** (DA + Pragmatist + RA). PDF body grounding via heading regex is theater on slide PDFs. Either disable `[[quiz]]` from CHAT_SYSTEM_PROMPT this session, OR ship with visible STUB warning AND no claim of grounding quality. Pragmatist + DA prefer disable.
5. **Pragmatist's harder ask: stop tonight.** Ship-and-watch: user runs the existing 276-concept catalog tomorrow against `[[plan: today]]`, usage data drives next iteration. Building MORE tonight (even Path W) is the failure mode `feedback_ship_and_watch.md` warns against.

RECOMMENDED PATH:

**Path W (round 3 convergent, condensed):**
- Replace W1: deploy `concepts_real/*.md` → `archival/{subject}/concepts.md` via new `tools/deploy-concepts.sh`. SO_RC.md → SO&RC/. Retire seed_concepts.sh.
- W2 unchanged: schedule with placeholder finals.
- W3 unchanged: [[catchup: N]] + multiDay.
- NEW W6 (FSRS mutex fix): wrap `recordReview` read-compute-append under `LOCK`. 5 lines + 1 test.
- W4 + W4b: **SKIPPED.** concepts_real already does the bilingual + curated work; PDF body grounding is theater on slide PDFs.
- W5 unchanged: tests + deploy + smoke. Smoke includes `[[quiz: PA]]` to verify [[STUB - no material grounding]] degradation visible (or disable [[quiz]] if going Pragmatist-pure).

Total effort: ~3h. Pragmatist + RA aligned this is shippable today.

**Pragmatist's pure ship-and-watch:** Don't ship Path W tonight either. Verify concepts_real is on VPS via `[[stats]]` from phone, populate schedule.json with placeholder exam dates, and call it done. User uses tomorrow. ~30min.

CONFIDENCE: 8
What would change rating: confirm whether `tools/concepts_real/` actually deployed to VPS archival currently (commit `4091c15` claim vs no script visible to do it) — quick `ssh root@VPS "ls /opt/jarvis/data/archival/"` resolves.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
