# Council review — 1781565177

**Problem:** Ratify the refined figure-judge design (SAME claude-max model made independent by fresh-context + checker-role + checklist + grounded-vs-source — NOT a different family) + decide the concrete NEXT executable step, grounded in the ACTUAL CI state.

**Timestamp:** epoch 1781565177. **Grounding:** all 5 verified `.github/workflows/test.yml`, the vitest harness, the 2 `.mjs` gates, `ClaudeMaxLlm.kt`, git state.

---

## GROUNDED FACT CORRECTIONS (verified by ≥4 advisors — several stale beliefs overturned)
1. **The deterministic SEMANTIC floor is ALREADY a hard CI blocker:** `traceMatchHarness.test.tsx` runs under `npm test` (vitest) in the `frontend` job (test.yml:71-72) — GENERAL (per-family, globs all `content/*/viz/*.yaml`, totality self-check, INV-5.1/5.2). "Confirm trace-match in CI" = already done. It proves trace/source-ISOMORPHISM (not taste).
2. **The 2 `.mjs` Playwright gates (frame-conjunction, svg-overlap) are NOT in CI** (absent from `.github/` + `package.json`), are **merge-HARDCODED** (routes `/tutor/lectie-mergesort`+`/tutor/merge-compare`, `MERGE_STEPS`, `sort-merge-*` selectors, `CONTOPESC`/`INTERCLASAT` roles, merge-tuned `PIXEL_EPS`/`MEANINGFUL_PX`/`AREA_FLOOR`), and need a live vite dev server. **THIS is the real gap.**
3. **Git state STALE in BRIDGE-HEAD:** HEAD `2b628df` is PUSHED (0 ahead of origin/main); the A0 commit shipped the prior 13 + the 2 gates are now COMMITTED/tracked (NOT work-at-risk). BRIDGE-HEAD's "13-ahead-unpushed / gates uncommitted" is wrong — fix it.
4. **The vision-judge has a real unbuilt prerequisite:** `ClaudeMaxLlm`/`Llm`/`ChatMessage(content: String)` is text-only — NO image channel in the Llm seam. The bare `claude --print` CLI does vision (proven), but `ClaudeMaxLlm.kt:35,52` sends stdin text only. The judge needs net-new image-path wiring (~day-scale) — the genuine unknown.

## JUDGE DESIGN — RATIFIED (unanimous)
Same claude-max + fresh-context + checker-role + fixed CHECKLIST/text-contract + grounded-vs-source + advisory-behind-deterministic-gate + benchmark-gated = the textbook sound LLM-as-judge independence form (self-preference bias attaches to the same artifact/context, which fresh-context + independent rubric + held-out benchmark neutralize — NIST/HELM/G-Eval). "Different family" is OVER-STRONG — an optional cross-check, **measure-then-decide** (needed only IF the judge's measured precision/recall on BARE frames falls short AND taste is load-bearing for shipping). Keeps it on claude-max, no paid/OpenRouter.

## 🔴 Devil's Advocate — CONDITIONAL
Verified: trace-match harness IS a CI blocker already; gates committed+pushed (`2b628df`, 0 ahead) — BRIDGE-HEAD stale; the judge's delivery vehicle does NOT exist (Llm seam text-only, no image channel). KEY CONCERN: the refined judge ratifies an architecture whose first prerequisite — get a PNG to the model via `ClaudeMaxLlm` — is unbuilt + unmentioned. NEXT: parameterize the 2 gates off a per-family route+seed registry, wire as a NEW hard-blocking test.yml job, prove RED→GREEN on the bare divide defect on ≥2 families; defer the judge until a spike answers "can `claude --print` take an image + what's the Llm-seam change." (Note: this advisor wanted generalization; others warn it's a rewrite trap — see judge.)

## 📚 Domain Expert — CONDITIONAL
Next-step matches practice (deterministic floor as hard regression-lock BEFORE an advisory LLM judge; "LLM proposes, gate disposes"). Judge design = textbook. KEY CONCERN: "generalize over all families×routes" in THIS step is premature scope-creep that delays the RED→GREEN trust event + risks the gates rotting un-wired — wire AS-IS first, generalize after. NEXT: add `gate:figures` npm script + a `figure-gates` test.yml job (modeled on the existing `lesson-gates` job: boot vite → readiness → run both gates AS-IS merge-only). ACCEPTANCE: RED on unfixed renderer (names frozen divide pairs 0→1,1→2,2→3) → GREEN post-fix, in a real Actions run + a committed seeded self-test (mirror the `gate-registry` self-test). Generalization + judge = next 2 units, not folded in.

## ⚙️ Pragmatist — CONDITIONAL
Grounding flips the proposal: the deterministic floor is NOT absent (trace-match in CI today), and the commit/push gap is resolved (`2b628df` pushed 0/0). The genuine gap = the 2 pixel-conjunction gates unwired + merge-hardcoded. Wire AS-IS (not generalize). KEY CONCERN: generalizing before wiring is a yak-shave that delays the trust win + risks fail-open noise (cf. Impeccable `|| true` at test.yml:84-85). NEXT: add ONE blocking step in the `frontend` job that boots vite + runs `node tools/frame-conjunction-gate.mjs` (merge-hardcoded) vs `/tutor/lectie-mergesort`. ACCEPTANCE demoed RED→GREEN: divide bug reintroduced → job RED; fix in place → GREEN; capture both CI logs (the trust artifact Alex wants to SEE). Copy-shaped from `chrome-en-grep.mjs` (test.yml:77-79).

## 🧱 First Principles — CONDITIONAL  ⭐ (the dissent worth heeding)
CI-wiring hardens the CHECK leg before the GENERATION + vision-in-the-loop legs that PRODUCE quality exist — so a green gate proves the gate works on a HAND-CODED figure, NOT that the autonomous approach produces trustworthy figures (which is exactly what Alex doubts, having never watched the checked pipeline work). KEY CONCERN: optimizing the 80%-built leg (deterministic gate) while deferring the two 0%-built legs that carry ALL the uncertainty (vision-in-the-loop generation; independent fresh-context judge). NEXT = a THIN VERTICAL SLICE on the ONE merge-divide figure: (a) commit demo routes + wire the merge gate into CI, RED→GREEN on the bare un-captioned defect [acceptance: CI RED on broken frames → GREEN on fix, auditable]; (b) SAME slice — wire `ClaudeMaxLlm` image-path + run the independent judge on the BARE frame: known-bad + known-good renders, fixed `VERDICT: PASS|FAIL + CHECK_n` checklist, fresh context, concept-spec it did not author [acceptance: flags bad / passes good on the un-captioned single frame, N=3 reproducible, owner reads the reasoning]. The most-informative experiment = the judge on the BARE frame (decides if the vision leg is real or a label-reader).

## ⚠️ Risk Analyst — CONDITIONAL
Order is right (deterministic floor in CI FIRST, judge after) — keep it inviolable. KEY CONCERN ranked: CRITICAL — vision-judge trusted before the deterministic floor is the CI blocker → an AI "looked at it" becomes the only thing between a confident-wrong figure + a non-vetting learner; HIGH — "generalize" smuggled into the wiring step breaks/weakens the merge-tuned detector (calibrated thresholds); HIGH — Playwright/dev-server flake degrades a hard-blocker to `|| true` (Impeccable already is); MEDIUM — the `.mjs` gates have NO seeded-bad self-test in CI (INV-9.4 liveness gap), unlike the trace harness. NEXT: CI-wire `frame-conjunction-gate.mjs` ONLY, as-is, narrow to its 2 routes, hard-blocking job modeled on `lesson-gates` (boot vite, curl-readiness, run; NO `|| true`). ACCEPTANCE: RED on unfixed renderer citing divide pairs 0→1,1→2,2→3 → GREEN post-fix → both in the run log. Defer svg-overlap wiring + any generalize to follow-up PRs each carrying its own seeded-wrong self-test.

---

## Sanity Check
All five PASS — independently verified test.yml + the harness + the gates + git. Key cross-correction the judge carries: 4/5 say "generalize the gates" is a REWRITE trap → wire NARROW/as-is first (Devil's Advocate alone wanted generalization). First Principles' dissent (prove the whole loop, not just the gate) is substantive + folds in as Unit 2. No flags.

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERDICT: APPROVED — judge design ratified; next step = a 2-unit sequence, floor-first.

CORE FINDING:
The refined same-model judge (fresh-context + checker-role + checklist + grounded-vs-source, advisory, benchmark-gated) is the textbook-correct independence form — different-family is measure-then-decide, NOT required. On the next step, grounding overturned the framing: the deterministic SEMANTIC floor (trace-match harness) is ALREADY a hard CI blocker, and the gates are already committed+pushed (BRIDGE-HEAD is stale). The real gap is the 2 merge-hardcoded `.mjs` pixel-gates not being in CI. 4/5 converge: CI-wire `frame-conjunction-gate.mjs` AS-IS / NARROW (generalizing it now is a rewrite that risks breaking the only working detector) as a hard-blocking job, and prove RED→GREEN on the BARE merge-divide defect — that auditable RED→GREEN is the trust-transfer event Alex wants to see. First Principles' dissent is right that this alone only proves the gate on a hand-coded figure, so the SECOND unit (the most-informative experiment) is the independent vision-judge probe on the BARE frame.

AGENT CONSENSUS: 5 CONDITIONAL — 0 flagged. Unanimous: floor-first order inviolable; wire narrow not generalize; judge design sound.

NEXT STEP (2 units, in order):
- **UNIT 1 (low-risk, earns trust):** CI-wire `frame-conjunction-gate.mjs` AS-IS (narrow, its 2 routes) as a HARD-blocking test.yml job modeled on `lesson-gates` (boot vite → curl-readiness → run; NO `|| true`) + a committed seeded-wrong self-test (close the INV-9.4 gap). ACCEPTANCE: a real CI run goes RED on the reintroduced divide defect (names frozen pairs 0→1,1→2,2→3) → GREEN after the computePlacement fix; both logs captured. (Defer svg-overlap wiring + any generalize to follow-up PRs, each with its own seeded-wrong self-test.)
- **UNIT 2 (the key experiment Alex wants to SEE):** wire `ClaudeMaxLlm` to pass an image path (the real unknown; the bare CLI already does vision); run the independent vision-judge on the BARE merge frames (known-bad + known-good), fixed `VERDICT:PASS|FAIL + CHECK_n` checklist, fresh context, concept-spec it did not author, N=3 reproducible; Alex reads the reasoning + confirms it flags bad / passes good. Measures whether the vision leg is real or a label-reader.

DO NOT: generalize the gates in Unit 1; build/trust the judge before Unit 1's floor is the CI blocker; use `|| true`.

CONFIDENCE: 9
Grounded + unanimous. Residual: the judge's precision/recall on bare frames (Unit 2's whole point) + whether `claude --print` cleanly accepts an image via the `Llm` seam (Unit 2's wiring unknown) are unmeasured — those ARE the next evidence.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output saved to: .claude/council-cache/council-1781565177.md
