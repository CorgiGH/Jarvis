# Council review — 1781910590

**Problem:** Should the interactive-tutor fix-set (`build-review/2026-06-19-tutor-interactivity-analysis.md`) be committed to the master plan, as-is?
**Project context:** jarvis-kotlin ADHD-first tutor. Law: machines verify TRUTH, never route correctness to the user, no real-time LLM correctness. Plan is oracle-first / deploy-last; no phase shipped yet. Fix-set = wire the "inner loop" reusing existing rails. A prior independent review (§7 of the doc) verified most claims + refuted the practice-graders-have-no-frontend claim.
**Timestamp:** 2026-06-20 (epoch 1781910590)

---

## 🔴 Devil's Advocate
STANCE: CONDITIONAL
REASONING: §7's corrections check out (practice surfaces ARE route-mounted `main.tsx:278-282`; `next_phase_action` read by no component = "computed then ignored" is real). BUT #1's "small, reuses the existing re-queue + selector" claim does NOT survive grounding: there is **no `requeue`/`reQueue` symbol anywhere in the frontend** (the repo hits are all backend/Android effectors) — no client re-queue rail to "just branch into." Worse, `NextPhaseResolver.resolve` (`NextPhaseResolver.kt:31-36`) emits `remediate` **only on phase REGRESSION (decayed mastery), NOT on the single miss** #1's prose targets. So #1 needs a NEW miss→action mapping + a NEW client re-queue/selector round-trip + reroute UI — mis-sized as wiring.
KEY CONCERN: Committing #1 (the designated keystone, "converts site → tutor") on a false "just wiring an existing re-queue rail" premise. Re-scope #1 (define miss→reroute trigger + build the client re-queue round-trip + UI as explicit net-new tasks) before committing. Secondary: 9+2 inner-loop items inflate scope ahead of the oracle-first foundation — gate the set behind Phase A/B/C, admit the genuinely-small verified items first (#9 nav link, #8 read-only peek).

## ⚠️ Risk Analyst
STANCE: CONDITIONAL
REASONING: Low-risk by construction — every item routes stored, pre-gated, faithful-checked artifacts; no LLM on the real-time correctness path. The two law-risky items are pre-empted (#5 fenced advisory-only; #6 correctly demoted — §7 confirmed `worked_example_ro` is a prose blob). Real exposure = a request-driven spoiler channel + sequencing.
KEY CONCERN: **CRITICAL — the composed request-driven spoiler exploit** (#2 bottom-out + #5 ask + #8 zero-write prereq-peek) is a free answer-extractor. G3 (shared anti-gaming reveal gate, counts/timers only) is **MANDATORY, not conditional** — must land before/with the 2nd of {#2,#5,#8}, never after. Also HIGH: building reveal-bearing UX (#2/#4/#6/#5) over content that hasn't passed the truth oracles lends authority to possibly-wrong answers → gate every reveal behind `kc_verification_status = faithful`, doubt-route on uncertain; build read-only items (#1-render/#8/G1/transparency) first.

## 🧱 First Principles
STANCE: CONDITIONAL
REASONING: The thesis — "act on the decision you already compute" — is the right core; the server knows `remediate`, the client ignores it (`drillGrader.ts:44` → `DrillStack`), and that asymmetry IS the non-tutor. Top-3 is directionally right but over-specified for a first commit: #4 is tiny and travels with #1; #2 carries anti-gaming design risk (correctly demoted to fast-follow).
KEY CONCERN: Committing all 9 + G1/G2/G3 before one inner-loop slice is shipped + FELT risks specifying the full tutor before validating the core loop changes ADHD study behavior. True minimum = **#1 (remediation routing) + the "why am I seeing this" legibility readout** — act on its own decisions, and make them legible to a distraction-prone user who'd otherwise distrust opaque routing. Ship that atomic pair, observe, then unlock the rest. **Approve as a backlog, not a one-pass sequenced plan.**

## 📚 Domain Expert
STANCE: CONDITIONAL
REASONING: Correctly implements VanLehn's two-loop skeleton; the top-3 install the inner loop by routing already-computed signals instead of discarding them. Remediation-routing as highest-leverage is sound (VanLehn: inner-loop step feedback is the largest single ITS effect driver). Hint ladder = Wood & Wood minimum-assistance; confidence callback = textbook Dunlosky (confident-and-wrong = highest-yield); wheel-spin (Beck & Gong) + G2 help-offer = Aleven & Koedinger help-avoidance, thresholds flagged for conservative defaults (right call). #6 demotion = correct vs Sweller expertise-reversal.
KEY CONCERN: **No knowledge-component-level error taxonomy** — the loop branches `remediate` vs `advance/hold` but can't tell WHICH misconception triggered the miss, so hint/re-queue serve the correct KC but not the correct error subtype (Cognitive Tutor / ALEKS error-model level). Not a blocker; name **KC-level error diagnosis as the explicit next layer.**

## ⚙️ Pragmatist
STANCE: CONDITIONAL
REASONING: Size-labeling honest on the small tier, slips on two. Genuinely small (commit as locked Tier-1): #4, #9 (nav link to already-mounted routes), G1 (pure read off queue head). Hidden cost: #2 needs the anti-gaming gate DESIGN (not wiring); #3/#7 thresholds undefined (need a PM "pick N" before any agent executes); #5 medium + UI-enforced advisory guard; #6 net-new digestion (prose blobs). G2/G3 are load-bearing only if #2/#3/#5 ship together = a maintenance dependency cluster.
KEY CONCERN: **G3 is a latent coupling trap** spanning #2/#5/#8 — if they ship across different sessions, G3 gets skipped (gaming hole) or blocks each from shipping independently. Commit **Tier-1 (#1*, #4, #9-nav-link, G1)** as explicit tasks; park **#2/#3/#5/#7/#8/G2/G3 as one named "Inner Loop Phase 2" block** with entry condition: thresholds defined + G3 designed by PM before any agent starts. (*#1 per Devil's Advocate needs re-scoping first.)

---

## Sanity Check
SANITY Devil's Advocate: PASS — grounded a NEW load-bearing fact the prior review missed (no frontend re-queue symbol; `remediate` fires only on regression). Highest-value find.
SANITY Risk Analyst: PASS — CRITICAL correctly scoped to the composed spoiler channel; mitigations concrete.
SANITY First Principles: PASS — true-minimum reframe (#1 + legibility), backlog-not-sequenced.
SANITY Domain Expert: PASS — named ITS canon; the KC-error-taxonomy gap is a real, correctly-non-blocking addition.
SANITY Pragmatist: PASS — honest Tier-1 vs dependency-cluster split; the G3 coupling trap is real.

---

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERDICT: GOOD DESIGN — but DO NOT commit all 11 as sequenced tasks. Commit a small verified tier + park the rest behind hard entry conditions.

CORE FINDING: The fix-set's thesis is right and truth-safe, but two things block a blanket commit. (1) The keystone #1 (remediation routing) is MIS-SIZED — there is no client re-queue rail to reuse, and `remediate` fires only on mastery-decay regression, NOT on the single miss #1 targets; it is net-new, not wiring, and must be re-scoped before commit. (2) The request-driven reveal channels (#2/#5/#8) compose into a free answer-extractor; the shared anti-gaming gate G3 is MANDATORY before the 2nd of them ships.

AGENT CONSENSUS: 5 CONDITIONAL, 0 flagged. Unanimous: commit small + verified, backlog the rest.

KEY ISSUES:
- #1 re-scope: define the miss→reroute trigger + build the client re-queue/selector round-trip + UI as explicit net-new tasks. [Devil's Advocate — grounded]
- G3 anti-gaming gate = MANDATORY (counts/timers, no LLM) before the 2nd reveal channel; gate all reveal-bearing items behind `faithful` KC status, doubt-route on uncertain. [Risk — CRITICAL/HIGH]
- Thresholds for #3/#7 need a PM decision before any agent executes; G3 is a cross-item coupling trap. [Pragmatist]
- Missing ITS layer: KC-level error-subtype diagnosis — name as next layer, not a blocker. [Domain Expert]
- Don't specify the full tutor before one inner-loop slice ships + is felt. [First Principles]

RECOMMENDED PATH: Commit as plan tasks ONLY the verified-small Tier-1: **#4 confident-wrong callback · #9 practice nav-link (routes already mounted) · G1 session re-entry card · the queue-legibility readout** (already added to Phase G this session). Add **#1 remediation routing re-scoped honestly** (net-new trigger + client re-queue + UI). Park **#2 · #3 · #5 · #7 · #8 · G2 · G3** as one named "Inner Loop Phase 2" backlog block with entry conditions: G3 lands before/with the 2nd reveal channel; reveal items gated behind `faithful`; #3/#7 thresholds PM-set first. Name **KC-level error-subtype diagnosis** as the next ITS layer. All of it stays sequenced AFTER the Phase A/B/C truth oracles (deploy-last unchanged).

CONFIDENCE: 9 — grounded in the absent frontend re-queue symbol + the resolver's regression-only `remediate`.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
