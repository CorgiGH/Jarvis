# Council review — 1781563401

**Problem:** Owner corrected 3 premises his AI got wrong: (1) QUALITY is the sole primary ("impossibly perfect"), speed is NOT the driver; (2) the content model is CLAUDE MAX (relay), NOT free AI (free = other users only); (3) he does NOT trust the AI to build visuals autonomously — he's never seen good visuals from it without hand-holding, and has NOT seen the overseer-checked visual pipeline actually working. Re-reason the figure/overseer + autonomy plan under these.

**Timestamp:** epoch 1781563401. **Grounding:** all 5 verified the model setup + figure pipeline in SOURCE (not prose).

---

## GROUNDED FINDINGS (verified in source by ≥3 advisors independently)
1. **The "Claude Max relay" = `ClaudeMaxLlm`/`RelayLlm` = `claude --print --output-format text` — TEXT-ONLY by construction.** No `tool_use`; `responseFormat` (structured/JSON) IGNORED; **NO VISION** — cannot accept images. `VisionLlm.kt:159` verbatim: "relay + claude-max + copilot are all text-only." The ONLY vision path = `OpenRouterVisionLlm` (default `anthropic/claude-3.5-sonnet`, OpenRouter `:free`/paid). ⇒ **Claude Max CANNOT SEE a rendered figure.** An AI "does this visual look right / symbolize the concept" overseer CANNOT run on the mandated model; vision needs a different (OpenRouter) model.
2. **The "overseer that checks the figure symbolizes the concept" is NOT BUILT** — zero implementation in `tutor-web/src/components/viz`; the FITNESS gate is plan-prose only (§145). The 2 gates that exist (`frame-conjunction-gate.mjs`, `svg-overlap-gate.mjs`) are **purely geometric** (frozen-frame-over-changed-state; element overlap) — neither checks MEANING. They are **uncommitted historically + NOT in `test.yml`** (not CI-wired); Impeccable still `|| true` fail-open. ⇒ The "overseer-checked visual pipeline" the owner says he's never seen work **literally does not exist as a pipeline** — his distrust is well-founded.
3. **Second-independent-family check (anti-self-grading for depth/pedagogy) is WEAKENED** by the model correction: if Claude Max is mandated + no paid APIs, "family B" collapses toward the same claude-family or a weak `:free`/NLI model → FAMILY_COLLAPSE / low-quality disagreement.

---

## 🔴 Devil's Advocate — REJECT
The figure overseer is a PHANTOM under the corrected model. Quality-absolute + Claude-Max-relay means the visuals the owner distrusts most are gated by deterministic SVG/trace checks ONLY — no model ever LOOKS at the rendered figure (relay text-only; vision = paid OpenRouter, collides with the no-paid floor). So "merge boxes drop magnitude / blink-not-morph" (the exact 2026-06-15 taste failures) sail through every named gate (trace-match + no-clip + overlap are blind to "grammar doesn't match concept"). KEY CONCERN: the plan mislabels figure-TASTE as dim-5 = L1·PROOF, but the real undecidable-without-a-human leg IS figure taste — that mislabel is the trust gap made concrete. Q1 strong model shrinks-not-eliminates silent-wrong; the REAL changed constraint = relay text-only/no-vision guts the figure-overseer. Q2 "semi-digest fast" is now wrong as a DRIVER; quality-maximal = SPIKE→oracle-first kept, SPIKE re-scoped to the strong relay + prove vision-overseer feasibility OR accept figures as a human-checkpoint residual. Q3 "just trust the pipeline now" trusts standalone scripts a human must remember to run (not CI-wired). Q4 the second-independent-family rigor weakens (FAMILY_COLLAPSE) under Claude-Max + no-paid.

## 📚 Domain Expert — CONDITIONAL
Verified the relay is text-only/no-tool_use/no-vision; the plan's SPIKE + Risk#5 still say "free-tier" (wrong primary). What IS built: 2 deterministic Playwright gates + the trace-match harness — genuinely good, self-tested RED→GREEN, but NOT CI-wired AND only liveness + non-occlusion + trace-isomorphism — none checks that the figure SYMBOLIZES the concept (that leg is prose, zero code). KEY CONCERN: the weakest leg (symbolizes-the-concept) is unbuilt AND can't be a vision check on a text-only relay; with a strong-but-BLIND model the only trustworthy form = **trace-isomorphism (figure = deterministic projection of verified execution/source), bar oracle-less families from VERIFIED, never a free-form "looks right" LLM verdict.** Q1 a strong model REDUCES generation-error but independent verification stays load-bearing (strong models hallucinate confidently). Q2 prove-it-works = staged autonomy gated on a golden-set + seeded-wrong RED + per-family graduation (owner sees the gate catch a known-bad before trusting it). Q3 reframe the overseer as trace/source ISOMORPHISM (LLM proposes, deterministic gate disposes, allow-list reasons logged/auditable). Q4 no core re-arch, but 2 corrections must propagate: SPIKE→Claude-Max-primary; figure-overseer = trace-isomorphism not vision/free-text critic.

## ⚙️ Pragmatist — CONDITIONAL  ⭐ (the concrete trust-earning move)
The overseer-checked pipeline does NOT exist as a loop: figures are hand-coded React families; the only checks are 2 manually-run, merge-hardcoded gates (NOT CI-wired); L2 overseer is plan-spec not built; high-quality path = Claude Max relay (text-only, no tool_use). Building full autonomous generation BEFORE one figure survives the checked pipeline LIVE is backwards. KEY CONCERN: do NOT front-load the heavy L2 LLM overseer / POO's net-new family before one figure goes generate→gate→verdict→render LIVE in front of the owner; until he watches the loop catch a real defect + pass a real figure with an auditable verdict, every new line of pipeline is unearned trust. Q1/Q4 **the demonstration: take the ONE figure with a known defect on record (merge-sort divide) → run the real gates LIVE so the owner WATCHES the gate go RED on the broken frames → watch the fix → watch it go GREEN → add the overseer's written, source-grounded verdict; one figure, fully auditable, before anything scales.** Q2 fully backwards — build the CHECK loop first (mostly exists for one figure), prove it bites, THEN generalize generation. Q3 "overseer replaces hand-holding" credible only as a SECOND independent family (never the author), grounded vs source, fail-OPEN to verifică-sursa, after visibly catching ≥1 real defect; until then keep the per-figure experience-approve.

## 🧱 First Principles — CONDITIONAL
The plan's central move is correct: relocate the problem from "trust the AI" to "trust the CHECK" (the only coherent answer when the owner can't vet content). But 2 generator assumptions are FALSE: (a) the high-quality path is text-only (no tool_use, no vision — confirmed in source); (b) the "overseer" the owner demands to SEE does not exist (only geometric gates; SPIKE not run). KEY CONCERN: the word "overseer" has no built artifact behind it; the gates are geometry/state checks; the one vision path is paid (violates no-paid) and the relay can't do vision. Either build a grounded visual check the owner watches go RED-then-GREEN on a real seed (the frame-conjunction divide-seed self-test is the closest existing thing — make THAT the demoable trust-transfer event), or honestly downgrade "overseer" to "deterministic gate set + one-time human eyeball per pattern" and stop implying a semantic visual judge exists. Q1 "perfect" = the plan's §0 four-part definition (provably-faithful where decidable + honestly-flagged where not + teaching-estimator-residual + owner ratifies EXPERIENCE once per pattern, never truth); literal perfection is unfalsifiable. Q2 the check earns trust only when independent + auditable + self-tested RED on a natural known-bad seed — that demoable RED→GREEN replaces his eyes. Q3 with Claude Max the bottleneck is VERIFICATION not generation. Q4 the simpler primitive ("AI never asserts a visual is good; ships only when an independent auditable check passes AND, until the check has a track record, the owner sees it live") is STRICTLY BETTER than a 3-layer overseer — adopt it; drop any layer that is the model grading its own output.

## ⚠️ Risk Analyst — CONDITIONAL
Confirmed in source: high-quality path = `claude --print` relay (text-only, ignores responseFormat, no tool_use, can't accept images; `VisionLlm.kt:159`); only vision = `OpenRouterVisionLlm`. The semantic overseer is NOT built (no viz impl; FITNESS gate prose-only); live gates are geometric; the 2 newest are uncommitted + absent from test.yml. KEY CONCERN ranked: **CRITICAL #1 — bad-visual-passed-by-unproven-checker, both AI-authored, shipped to a non-vetting learner** (verifier model can't see the image; semantic overseer unbuilt; new gates not in CI → a confident-wrong figure has no live tripwire — the Hermes silent-self-amplification + the [[feedback_multiframe_conjunction_gate]] "shipped dead 4 frames" lesson). HIGH#2 text-only relay ⇒ unreliable structured figure-spec (no tool_use/JSON → free-text-parsed → malformed specs render plausible-but-wrong figures geometric gates pass). HIGH#3 overseer must run on a DIFFERENT model than the generator (vision=OpenRouter) — extra part, free-tier unknown, collapse risk. MEDIUM#4 gates not CI-wired = decorative. Q1 NO relaxation — strong model still confidently hallucinates, n=1 can't catch → VERIFIED-bar + independent ground-truth + visible flag stay MANDATORY. Q4 **TOP MITIGATION: make the deterministic, source-grounded conjunction/fitness gate the HARD blocker and put it in test.yml NOW** — so "AI built it solo" is always backstopped by a non-AI check; add the vision overseer as ADVISORY on a separate model; gate the first N artifacts behind the owner's render-checkpoint until a clean track record, THEN relax.

---

## Sanity Check
All five PASS; the model-text-only + overseer-unbuilt + gates-not-CI-wired findings were independently source-verified by ≥3 advisors (file:line cites: VisionLlm.kt:159, RelayLlm/ClaudeMaxLlm, no viz overseer impl, test.yml grep clean). Devil's REJECT is the strongest-grounded and the judge weights it. No flags.

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERDICT: FLAWED — and the owner's distrust is VINDICATED by the source.

CORE FINDING:
Two hard, source-verified facts reshape everything. (1) The "overseer-checked visual pipeline" the owner says he's never seen work LITERALLY DOES NOT EXIST: the semantic "does the figure symbolize the concept" check is zero-code prose; the only built figure checks are 2 geometric gates (state↔pixels, overlap) that are NOT CI-wired and check no MEANING. (2) Claude Max — the model the owner mandates for his content — is TEXT-ONLY and CANNOT SEE a rendered image, so an AI "look at the figure and judge it" overseer cannot run on it at all (vision needs a separate OpenRouter model, free or paid). Therefore figure TASTE / symbolic-fidelity — the exact thing that keeps failing visually — is the hardest leg, is unbuilt, and is NOT machine-provable on the mandated model. The honest architecture transfers trust from "the AI" to "the CHECK," where the CHECK is: deterministic trace/source-isomorphism (figure = provable projection of verified execution) as the HARD blocker, an optional vision model as ADVISORY only, and the owner's own eye (once per pattern) for "is it intuitive" — never an AI grading its own visual taste.

AGENT CONSENSUS: 1 REJECT, 4 CONDITIONAL — 0 flagged. Unanimous: the semantic overseer is unbuilt + can't run on the text-only relay; the deterministic source-grounded gate must be the hard blocker + CI-wired NOW; trust is earned by a demoable RED→GREEN on a real defect, not by building more.

KEY ISSUES:
- [CRITICAL] No live tripwire for a confident-wrong figure today (semantic overseer unbuilt; the 2 geometric gates not in CI; the verifier model can't see the image). Bad-visual-passed-by-unproven-checker → shipped to a learner who can't catch it.
- Figure TASTE is a HUMAN residual (or an advisory blind-spot-prone vision model), NOT machine-PROOF — the plan's dim-5 = L1·PROOF mislabel must be corrected; the AI must never assert its own visual is good.
- The mandated text-only relay also means figure-SPEC output is free-text-parsed (no tool_use/JSON) → spec reliability is itself a risk; and the depth/pedagogy second-independent-FAMILY check weakens (FAMILY_COLLAPSE) under Claude-Max + no-paid.
- "Quality is primary, speed is not" ⇒ drop "semi-digest fast to learn sooner" as a DRIVER; keep oracle-first; re-scope the SPIKE to measure Claude Max (not free-tier) + prove the figure-check feasibility.

RECOMMENDED PATH (trust-earning, quality-first):
1. NEXT (earns trust, needed regardless): commit + CI-wire + GENERALIZE the 2 existing gates; make the deterministic state↔figure conjunction + family-fitness gate the HARD blocker in test.yml. Demonstrate it LIVE on the known merge-divide defect: owner WATCHES it go RED on the broken frames → fix → GREEN, with the auditable log. That RED→GREEN on a real defect is the trust-transfer event.
2. Reframe the figure-overseer as deterministic trace/source-ISOMORPHISM (LLM proposes, gate disposes, auditable), + an OPTIONAL vision model as advisory-only on a separate provider, + the owner's once-per-pattern eyeball for intuitiveness. Bar any family lacking its own oracle + seeded-wrong self-test from VERIFIED.
3. Only after the CHECK has a visible track record: point generation at it; keep the owner's per-figure experience-approve until then, relax to sampled spot-checks at his opt-in.
4. Correct the plan: SPIKE/Risk → Claude Max primary; figure-taste = honest human/advisory residual not PROOF; name the FAMILY_COLLAPSE risk for the depth/pedagogy independent check.

CONFIDENCE: 9
Source-grounded + unanimous on the load-bearing facts. Residual: whether a separate vision model (OpenRouter) is acceptable as advisory under the no-paid floor, and how much the text-only relay limits structured figure-spec generation — both measurable, both currently unmeasured.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output saved to: .claude/council-cache/council-1781563401.md
