# Council review — 1781564195

**Problem:** PASS THE CORRECTION TO COUNCIL — prior council-1781563401's load-bearing finding "Claude Max can't see figures" is PROVEN FALSE (`claude --print` reading an image path exactly described a real render, no paid API). Re-reason the figure-overseer design now that an AI vision-overseer IS feasible on the approved model.

**Design re-evaluated:** 3-leg figure overseer — (1) deterministic gates (hard CI blocker), (2) AI vision-overseer on claude-max (LOOKS at render, judges vs concept/source, advisory/fail-open, independent of generator, demonstrated-before-trusted), (3) Alex's eye once-per-pattern until (2) earns a track record.

**Timestamp:** epoch 1781564195. **Grounding:** ALL 5 advisors independently RE-RAN the `claude --print` vision test on `look-merge-compare-overview.png` + read `ClaudeMaxLlm.kt`/`VisionLlm.kt`/the prior council. (Triggered by the lesson: the AI parroted a stale comment instead of testing.)

---

## CONFIRMED (all 5, re-tested)
claude-max SEES figures via the Claude Code CLI file-path — exact descriptions of the real render (numbers [5,8]/[3,9]→[1,2], boxes vs bars panels, frame 24/30). Prior "can't see" = a WIRING gap (`ClaudeMaxLlm.kt:35,52` sends stdin text only, never an image arg) + a stale 2026-05-09 comment (`VisionLlm.kt:159` — which actually describes the in-process HTTP `complete()` path, NOT the CLI subprocess). The vision-overseer leg is FEASIBLE on the approved model, no paid API. **The prior council's REJECT collapses; leg-2 reopens.**

## 🔴 Devil's Advocate — CONDITIONAL
Ran it 3×: neutral probe correctly said the boxes drop magnitude; an adversarial leading prompt ("a senior reviewer approved this as perfect") was REFUSED — non-sycophantic, strong. BUT KEY CONCERN: the test PNG is RIGGED-EASY — a side-by-side BOXES-vs-BARS comparison that prints its own verdict IN-IMAGE ("toate celulele la fel de înalte"/"all cells equally tall") AND has the good control beside the bad; the model quoted that caption as its evidence. Production figures ship ALONE, un-captioned, no good twin. So feasibility is overturned, but the demo does NOT yet show the overseer catches a defect from PIXELS ALONE in the deployed framing — until the seeded-wrong self-test feeds the BARE production frame and it still goes RED, "demonstrated-before-trusted" is unearned and the verdict risks being a label-reader masquerading as a pixel-judge. Q1 most damaging = non-reproducibility-under-framing (verdict swings with how much the image/prompt pre-loads the answer). Q2 kills "can't see" but only PARTIALLY overturns "taste is human-only" — moves risk from blind→sees-but-unproven-on-production-framing. Q3 not a trap IF strictly advisory + fail-open + backstopped by the deterministic isomorphism HARD gate (CI-wired first). Q4 free-text both ways is a real reliability tax — make the deterministic gate the blocker, LLM prose advisory only.

## 📚 Domain Expert — CONDITIONAL
Reproduced the test; correction confirmed (the prior council parroted the stale comment without testing the binary). That single correction collapses the prior REJECT + reopens leg-2 on the mandated model, no paid API. BUT feasible ≠ trustworthy: a free-form vision "does this look right" verdict is the LEAST reliable LLM-as-judge form (positional/verbosity/self-preference biases, ~0.6-0.8 human-agreement ceilings, poor reproducibility); sound ONLY as a rubric-grounded, reference-anchored, N-sample, ADVISORY judge that clears a labeled precision/recall benchmark before it gates. KEY CONCERN: the SAME model authoring the figure-spec is now the candidate judge = self-preference/self-grading (the most-documented LLM-judge failure) — leg-2 must be an INDEPENDENT pass (different context, ideally a different model family / OpenRouter :free vision cross-check, never the generator scoring itself), advisory behind the deterministic gate until it clears the benchmark. Q1 right fit = vision-grounded rubric CHECKLIST + reference(golden)-figure comparison, N-sample — not free-form rating. Q2 vision-LLMs reliably catch GROSS/semantic defects (dead frame, wrong encoding, grammar↔dynamic mismatch) but are weak/non-reproducible on fine magnitude/geometry → pair with deterministic gates for the fine stuff; temp≈0 + fixed checklist + N-vote. Q3 labeled held-out benchmark (seeded-wrong tests + the merge-divide defect as starter negatives) → pre-committed precision/recall, visibly catch a real defect RED→GREEN before gating; shadow/advisory until then. Q4 no-JSON hurt is recovered by a TEXT-CONTRACT: `VERDICT: PASS|FAIL` + numbered `CHECK_n: PASS|FAIL — reason`, strict regex parse, fail-closed on unparseable.

## ⚙️ Pragmatist — CONDITIONAL
Ran it — precise correct description, vision via file path at zero paid cost. But the gating reality holds: `ClaudeMaxLlm` only runs `--print` over stdin text (no image arg, line 35); the 2 real gates are good + RED-on-the-merge-defect but NOT CI-wired. The 3-leg design is right ONLY if the deterministic gates go in FIRST and the vision-overseer stays advisory until it earns a MEASURED track record — building it as shipping authority before proven repeats the "trust the AI's eye" pattern Alex distrusts. KEY CONCERN: do NOT let the vision-overseer influence what ships before it's benchmarked on labeled known-bad/known-good figures; a confident-wrong verdict is indistinguishable from a correct one to a learner who can't vet, and the only non-AI backstop (the gates) isn't in CI yet. Q1 smallest demo: feed claude-max the KNOWN-BAD merge-divide render AND a KNOWN-GOOD render, fixed prompt "does the figure's motion match the stated state change? FAIL/PASS + one sentence," Alex reads its reasoning + confirms it flags bad / passes good — n≥1 each, auditable, no infra. Q2 SMALL not heavyweight: ~one method overload on `ClaudeMaxLlm` to append image path(s) on argv + a thin render→path→verdict→parse loop — day-scale, no paid API/JSON needed (verdict free-text-parsed). Q3 order: CI-wire the 2 done gates as HARD blocker FIRST → vision advisory/fail-open → benchmark on labeled set → only influence shipping after measured precision/recall. Q4 NO it does not reduce babysitting yet — until measured precision/recall, Alex eyeballs once-per-pattern; the deterministic gates are what reduce babysitting today; the overseer earns off his plate only after a clean track record.

## 🧱 First Principles — CONDITIONAL  ⭐ (the architecture upgrade)
Live test passed. The verified fact changes the irreducible constraint, not the spine: the generator CAN now perceive its own output. KEY CONCERN: self-grading collapse — vision-in-the-loop self-correction and the independent vision-judge must NOT be the same model instance grading the same artifact, or leg-2 becomes the author approving its own work with a confidence boost; the judge must be a FRESH-context invocation with a concept-spec it did NOT author, and must demonstrate catching a known-bad seed (merge-divide) RED before any GREEN is trusted. Q1 YES — **vision-in-the-loop GENERATION (generate→render→model LOOKS→self-corrects) is the truest primitive + the missing piece** — it directly attacks blink-not-morph/magnitude-dropped AT AUTHORING TIME (you can't iterate on what you can't see), with the deterministic gate as HARD blocker + a SEPARATE fresh-context vision-judge as external check (three distinct roles, not one model in three hats). Q2 figure quality IS fundamentally a PERCEPTION problem, genuinely unsolvable blind, now tractable-but-not-certified; perception makes it ATTACKABLE, isomorphism-to-verified-trace makes the truth-claim PROVABLE — different guarantees. Q3 the irreducible human shrinks to CALIBRATING THE JUDGE (Alex confirms once-per-pattern the AI's visual taste matches his), not per-figure, until the judge has a track record. Q4 from-scratch-best = (1) deterministic trace/source-isomorphism+geometry = HARD blocker CI-wired; (1.5 NEW) vision-in-the-loop self-correction inside generation (cheap, free, better drafts); (2) independent fresh-context vision-judge, advisory, demonstrated-RED-on-known-bad first; (3) Alex calibrates per-pattern then relaxes to spot-checks — DROP the prior "vision needs paid OpenRouter."

## ⚠️ Risk Analyst — CONDITIONAL
Verified the correction (accurate content-specific description). But "can see" ≠ "can be trusted to judge" — a seeing judge that confidently approves a subtly-wrong figure is MORE dangerous than a blind one (manufactures false confidence "the AI looked at it" for a learner who can't vet), and it's the SAME model that generated it (Hermes autophagy). The deterministic gates that should backstop are STILL NOT CI-wired (only test.yml; grep .github/ = zero hits) → no hard tripwire today. KEY CONCERN ranked: CRITICAL#1 seeing-judge false-confidence on a self-generated figure (launders unchecked→"checked"); HIGH#2 no hard tripwire live (advisory on a non-existent floor); HIGH#3 non-deterministic advisory (no JSON, temp variance) → flag-soup/false-pass; MEDIUM#4 free-text figure-spec parses plausible-wrong specs the geometric gates pass. Q1 seeing judge NOT inherently safer — net-safer ONLY as fail-open advisory behind a deterministic hard blocker. Q2 same-model gen+judge IS self-grading; two invocations share weights/priors → correlate on the subtle errors that matter; make the judge structurally independent, ground vs SOURCE/verified-trace, never ship on its verdict alone. Q3 bound non-determinism with N-sample majority on a fixed rubric/checklist, any-fail-to-flag, log every verdict. Q4 **single top mitigation: CI-wire the deterministic source/trace-isomorphism gate as the HARD blocker NOW so "an AI looked at it" is never the only thing between a wrong figure and the student;** add vision advisory-only, require it clear a labeled known-bad set before its verdict carries weight, keep Alex's eye until then.

---

## Sanity Check
All five PASS — each INDEPENDENTLY re-ran the vision test (the whole point) + cited source (ClaudeMaxLlm.kt:35,52; VisionLlm.kt:159; gates absent from .github/). Devil's Advocate's "rigged-easy test (caption names the defect + control beside it)" catch is the sharpest + load-bearing for the demonstration design. No flags. Near-total convergence.

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERDICT: APPROVED (architecture) — with binding conditions; the correction is confirmed and reopens the vision-overseer, but "can see" ≠ "trust its eye."

CORE FINDING:
The correction holds — all 5 re-tested: claude-max sees figures via the CLI file-path, free, so the AI vision-overseer is FEASIBLE on the approved model and the prior REJECT collapses. The vision-overseer is a REAL leg AND vision unlocks a powerful new generation primitive (the model LOOKS at its own render and self-corrects — directly attacking the diagnosis defects: magnitude-dropped, blink-not-morph). BUT a SEEING AI judge that confidently approves a subtly-wrong figure is MORE dangerous than a blind gate — it launders an unchecked artifact into a "checked" one for a student who can't catch it — and the generator judging its own output is self-grading (autophagy). So vision strengthens the design without retiring the deterministic check or Alex's calibration: perception makes quality ATTACKABLE, trace/source-isomorphism makes truth PROVABLE — different guarantees.

AGENT CONSENSUS: 5 CONDITIONAL → judge reads as APPROVED-architecture-with-conditions; 0 flagged. Unanimous: vision feasible + advisory-only behind a CI-wired deterministic hard blocker; independent fresh-context judge (not self-grading); benchmark on labeled known-bad/good before it gates; Alex's eye until measured track record.

KEY ISSUES / BINDING CONDITIONS:
- [CRITICAL] CI-wire the deterministic gates as the HARD blocker FIRST — "an AI looked at it" must never be the only thing between a wrong figure and the student. (They're built + RED-on-the-merge-defect; just not in CI — grep .github/ = zero.)
- [CRITICAL] The vision-JUDGE must be INDEPENDENT of the generator — fresh context, a concept-spec it did NOT author, grounded against the SOURCE/verified-trace (not its own intent), ideally a different model family as cross-check; never the generator scoring its own work; its verdict alone never ships. (Hermes "never grade your own output.")
- [HIGH] The trust demo must use the BARE production-framed render — NOT the rigged `look-merge-compare-overview.png` (which has a caption naming the defect + a good control beside it; the model read the answer off the label). Feed it the seeded-wrong single figure, un-captioned, and require RED.
- [HIGH] Bound reproducibility: temp≈0 + a fixed structured CHECKLIST (text-contract: `VERDICT: PASS|FAIL` + numbered `CHECK_n`, strict regex parse, fail-closed unparseable) + N-sample majority (any-fail⇒flag) + log every verdict.
- Vision-in-the-loop GENERATION (model self-corrects its own draft by looking) is a powerful, cheap, free NEW primitive — adopt it; it is the author improving itself, distinct from the independent judge.

RECOMMENDED PATH:
1. CI-wire + generalize the 2 deterministic gates = the hard floor (do FIRST). Demonstrate RED→GREEN on the BARE merge-divide defect.
2. Wire `ClaudeMaxLlm` to pass an image path (small, ~day-scale; the CLI already does vision). Add the vision-overseer as an INDEPENDENT, rubric-checklist, N-sample, fail-open ADVISORY judge.
3. Build a labeled benchmark (known-good + seeded-bad, bare-framed); measure the judge's precision/recall; it stays shadow/advisory until it clears the bar + visibly catches a real defect. Alex calibrates per-pattern, then relaxes to spot-checks.
4. Adopt vision-in-the-loop self-correction inside generation as a quality lever (separate from the judge).

CONFIDENCE: 9
Grounded + unanimous + the correction independently re-verified 5×. Residual: the judge's actual precision/recall on bare production figures is unmeasured (that benchmark IS the next evidence); and whether a different-family cross-check is worth adding vs two independent claude-max passes is open until measured.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output saved to: .claude/council-cache/council-1781564195.md
