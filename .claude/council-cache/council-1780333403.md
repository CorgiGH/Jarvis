# Council review — 1780333403

**Problem:** Am I (Claude) on track, or did I drift from the locked RESUME RECIPE this session? Alex feels "you started doing things you weren't supposed to."
**Proposed approach:** This session's trajectory — skipped step-1 plan-inputs; wrote DESIGN.md + self-see.mjs + reworked grounded-ui-design (foundation a+b); botched the door render once (recycled old DoorMockups), Alex caught it; then per RENDER-HANDOFF.md rendered the REAL ranked proposals p1/p2/p3, self-saw (fixed 1 contrast bug), presented 3 real renders; Alex picked p1 THRESHOLD; about to proceed to (e) drill surface.
**Project context:** AI tutor for ONE ADHD visual-learner student (Alex). React 19 + Tailwind v4 + Vite frontend, Kotlin backend. No paid APIs. Locked recipe governs ordering. Documented ghost-component scar. Only student-reachable viz (RecursionTree) is correctness-suspect. Door backend (/api/v1/learn/*) does not exist.
**Timestamp:** 2026-06-01 (local)

---

## 🔴 Devil's Advocate
STANCE: CONDITIONAL
REASONING: The recycled-door botch is direct evidence the operator acted before reading RENDER-HANDOFF.md, meaning (a)+(b) were built before fully understanding the handoff contract (c) depends on. Step-1 "read plan-inputs FIRST" exists precisely to prevent this: gap-ledger + viz-score-backlog carry failure modes that should have grounded DESIGN.md, but DESIGN.md was authored from the viz playbook alone. The foundation could be missing constraints the skipped inputs would impose — you cannot know what you didn't read. p1 now sits on a foundation built without the full input set; (e) inherits whatever gaps the skipped readings would have caught.
KEY CONCERN: DESIGN.md authored from an incomplete input set (playbook + index.css only; skipped gap-ledger/roadmap/viz-score-backlog/mount-audit) — the pick is real but the ground under it is unverified against the full recipe inputs.

## 📚 Domain Expert
STANCE: CONDITIONAL
REASONING: Matches a known anti-pattern — "context-skipping before artifact creation": read the playbook (tool docs) but skipped the plan-inputs (project ground truth), then generated a foundational artifact grounded in the wrong layer ("stale-context artifact" — looks right, tokens match, prose coherent, but under-constrained). The recycled-door botch is the "read-handoff-skip / LGTM-without-reading" pattern: act on the most recent in-context artifact (old DoorMockups) rather than loading the named source of truth — identical to this project's documented ghost-component scar. Foundation is structurally sound (self-see loop, token sync, real renders, self-caught contrast bug, faithful components) but latent-risk: if gap-ledger/roadmap contradict/extend the playbook, DESIGN.md needs revision AFTER (e) — the expensive order.
KEY CONCERN: Read gap-ledger + roadmap now and verify DESIGN.md covers every gap BEFORE starting (e).

## ⚙️ Pragmatist
STANCE: CONDITIONAL
REASONING: Foundation work (DESIGN.md, tokens, self-see) is a genuine asset for a visual-learner project where render quality is load-bearing — not gold-plating. The recycled-door botch was a real cost (wasted Alex attention + re-render + trust hit) caused by the step-1 skip. The SKILL.md rework only pays off if actually used in (e). Proceeding to (e) NOW is the problem: backend doesn't exist, the staged plan isn't written, viz correctness bugs unresolved — building drill/teaching surfaces without a written plan + backend contract risks the wrong surface shape and ghost-component rework like the 3 discarded door components.
KEY CONCERN: writing-plans + plan-inputs read haven't happened; proceeding to (e) means designing against a non-existent, uncontracted backend — highest-probability source of wasted effort.

## 🧱 First Principles
STANCE: REJECT
REASONING: Alex's actual goal is to stop learning from falsehoods and learn effectively — now, under finals pressure. Highest-leverage action = fixing the known-wrong RecursionTree viz, not designing doors for a house with no plumbing. Designing the drill surface before backend endpoints exist and before viz-correctness has a detector is designing interfaces for broken machinery. Skipping gap-ledger/roadmap wasn't because UI+playbook grounding is sufficient — it's because reading is slower than producing visible artifacts (comfort bias, not principled dependency resolution). The design-then-plan ordering is inverted: a staged plan accounting for backend reality + viz correctness should CONSTRAIN surface design, not follow it.
KEY CONCERN: The known-wrong RecursionTree is actively teaching falsehoods to the one student this exists for; every hour on surfaces instead of fixing/quarantining it is an hour the harm continues.

## ⚠️ Risk Analyst
STANCE: CONDITIONAL
REASONING: #1 CRITICAL risk = lock-in compounding: about to design a drill surface on a door pick on a non-existent backend, none proven end-to-end vs the actual roadmap. Exact reproduction of the ghost-component scar (the /learn/:id/drill 404 is the named failure mode of this very pick). Skipping plan-inputs before DESIGN.md is HIGH — DESIGN.md may encode assumptions contradicting roadmap ordering, unknown until the plan is read, by which point two surfaces may need revision. Locking p1 while its backend is unbuilt is MEDIUM in isolation (reversible on paper) but escalates to HIGH the moment a second surface is designed on it (revision cost doubles).
KEY CONCERN: Designing the drill surface now locks a second layer of unvalidated UI debt on an unbuilt backend + unread roadmap — the ghost-component pattern, one surface deeper.

---

## Sanity Check
SANITY Devil's Advocate: PASS — clean; CONDITIONAL with a concrete condition (read plan-inputs + verify DESIGN.md before e).
SANITY Domain Expert: PASS — named the project's own ghost-component scar concretely; pattern labels are descriptive, reasoning grounded.
SANITY Pragmatist: PASS — distinguishes genuine-asset from premature-proceed; condition specified.
SANITY First Principles: PASS — genuinely strips the framing (challenges the recipe's design-then-plan order), does not rephrase; REJECT is justified by the falsehood-harm argument.
SANITY Risk Analyst: PASS — ranks risks, concrete blast radius, ties to the documented scar.

---

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: FLAWED

CORE FINDING:
The artifacts you produced are good and the door pick is real — but the SEQUENCING drifted in two ways, one yours and one possibly the recipe's. Yours: you skipped step-1 (read plan-inputs) and authored DESIGN.md from the playbook + existing CSS alone, so the foundation Alex just picked on top of has never been checked against the roadmap/gap-ledger it was supposed to be grounded in. The recipe's: 4 of 5 agents (and First Principles outright) say designing MORE surfaces (step 2e) before writing-plans, before the backend exists, and before the correctness-suspect viz is fixed is inverted — that is the ghost-component scar reproduced one layer deeper, and it defers the only thing actively harming the student (a viz that may teach a falsehood).

AGENT CONSENSUS: 4 CONDITIONAL, 1 REJECT — 0 flagged. Unanimous on: do NOT proceed to (e) yet.

KEY ISSUES:
- DESIGN.md + the p1 pick were grounded in the playbook + index.css, NOT the skipped plan-inputs (roadmap, gap-ledger, viz-score-backlog, mount-audit). Latent contradiction risk; verify before building further.
- Proceeding to (e) drill surface compounds unvalidated UI debt on an unbuilt backend + unwritten plan (ghost-component class). 3 throwaway door components already show the rework cost.
- First Principles' escalation: the correctness-suspect RecursionTree teaches the one student potential falsehoods NOW; fixing/quarantining it is higher-leverage than any surface design.

RECOMMENDED PATH:
1. STOP before (e). 2. Read the skipped step-1 plan-inputs (roadmap spec + gap-ledger at minimum); verify DESIGN.md + the p1 pick do not contradict them — amend if they do. 3. Surface to Alex that the council challenges the recipe's OWN ordering (design-all-surfaces → then plan → then execute): the staged plan + Stage-0 (viz-correctness fix, verification-net, backend contract) arguably should GATE surface design, not follow it. He owns the recipe — do not unilaterally reorder; present the reorder as his decision. 4. The foundation (DESIGN.md, self-see, the p1 render) is a keeper — not wasted.

CONFIDENCE: 8
Would rise to 9-10 with confirmation of whether the roadmap/gap-ledger actually contradict DESIGN.md (unknown until read) — that single read resolves the largest open question.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output saved to: .claude/council-cache/council-1780333403.md
