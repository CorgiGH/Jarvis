# Council review — 1778155110 (DEPLOY STRATEGY for Step B)

**Problem:** Decide deploy approach for jarvis-kotlin Step B Letta-split (5 commits on local main, last = e87425c). User stated old wiki.md content is "useless" — rollback urgency LOW, migration NOT load-bearing.

**Decisions:** A migrate-or-skip / B migrator-invocation / C deploy-mechanics / D rollback / E pre-deploy-gate / F shipment.

**Project context:**
- jarvis-kotlin Kotlin 2.0.21 / JDK 21 / Gradle 8.10 (PC only, no gradle on VPS) / Ktor 3.0.1
- VPS: corgflix.duckdns.org Ubuntu 22.04, JVM at -Xmx512m, MC server eats 4.2GB of 7.8GB → 3.6GB free
- systemd unit jarvis.service active. State at /opt/jarvis/data/
- Council 1778105576 caught: WebMain never feeds VectorStore on VPS → semantic already starved
- User in finals window — time scarce

**Timestamp:** 2026-05-07 (Unix 1778155110)

---

## 🔴 Devil's Advocate

```
AGENT: Devil's Advocate
STANCE: REJECT
KEY CONCERN: Semantic-starvation bug is the load-bearing fact and your plan does not name it.

PICKS:
A) SKIP migration entirely — HIGH
B) SKIP — HIGH (follows from A)
C) installDist + scp — MEDIUM
D) Versioned symlink — HIGH
E) VPS dry-run + post-restart health — CRITICAL
F) Claude commands user copy-pastes — HIGH

DEPLOY SEQUENCE: 1. STOP, do not deploy Step B. 2. Council on VectorStore-starvation bug first. 3. If forced to ship Step B anyway: skip migration, installDist+scp, versioned symlink, VPS dry-run, copy-paste, journalctl 5min. Document in commit msg that you are shipping write-path improvements onto a starved read-path knowingly.
```

## 📚 Domain Expert

```
AGENT: Domain Expert
STANCE: CONDITIONAL
KEY CONCERN: Installing Gradle on VPS is a known anti-pattern — no serious JVM shop builds on prod (Pivotal, Netflix, Spring Boot ecosystem).

PICKS (named precedent):
A) Skip migrator (Letta MemGPTConfig reset / Mem0 drop-and-reseed / Khoj regenerate)
B) Gradle installDist or Shadow fat-jar built on PC, scp tree/jar (Spring Boot bootJar standard, Ktor deployment docs)
C) installDist rsync to versioned dir + atomic symlink swap + systemctl restart (Capistrano releases pattern)
D) Versioned-symlink with N=2 retained releases (Capistrano current → releases/<ts>)
E) gradle check + installDist + smoke --version invocation (every Spring Boot CI)
F) Idempotent shell script tools/deploy.sh (Spring Boot/DigitalOcean/Ktor reference docs converge on 30-line deploy.sh at single-host scale)

DEPLOY SEQUENCE: gradle check → installDist → rsync to releases/<ts> → healthcheck staging binary → atomic ln -sfn → systemctl restart → curl /healthz → on failure flip symlink back. Migration NOT in sequence.
```

## ⚙️ Pragmatist

```
AGENT: Pragmatist
STANCE: CONDITIONAL
KEY CONCERN: Building "proper" deploy infra for single-user life-OS during finals is the textbook cost-vs-value gap.

PICKS (cost rationale):
A) SKIP — saves 30-60min, zero retained value
B) N/A (skip A makes it free)
C) installDist + rsync — already produced by gradle locally, zero new tooling, one-line memorable redeploy
D) mv current current.bak — 10x less cognitive load than symlink-versioned releases
E) local test green + local installDist boots without crash + git log -5 eyeball — single-user app, you ARE the canary
F) Claude provides commands, user runs — 5min user time vs 20min building SSH harness; pause-able under fatigue

DEPLOY SEQUENCE: gradle test → gradle installDist → smoke-run 10s → ssh stop+mv-bak → rsync → ssh start+status → curl /healthz → done in <15min. Rollback = ssh stop && rm new && mv .bak back && start.
```

## 🧱 First Principles

```
AGENT: First Principles
STANCE: CONDITIONAL
KEY CONCERN: The whole "deploy strategy" framing is oversized for a single-user single-host system. This is `scp; systemctl restart`. The five-decision matrix is itself the smell.

PICKS (or "wrong framing because..."):
A) Skip — "migrate" is a word that snuck in from a world where data has stakeholders
B) Wrong framing — no migrator should exist
C) Build locally, scp, restart — installDist vs fat-jar is a taste call with no operational difference at n=1
D) mv-old — "Rollback" is overkill but mv is one command and free; versioned symlinks are infrastructure cosplay
E) systemctl is-active + journalctl 30s + one HTTP endpoint hitting new code path — re-running tests on VPS is theatre
F) Copy-paste 6-line shell block first time. If you deploy twice, write the script. Automation before second instance is speculative.

DEPLOY SEQUENCE: gradle installDist → ssh stop → mv app app.prev → scp -r build → delete old wiki/conv (schema new, data useless) → ssh start && journalctl -f 30s → hit endpoint. Rollback = mv app.prev app.
```

## ⚠️ Risk Analyst

```
AGENT: Risk Analyst
STANCE: CONDITIONAL
KEY CONCERN: Migrator + live writer racing on conversations.jsonl — mutex is in-process; cross-process it does not protect.

PICKS (severity + scenario + mitigation):
A) MIGRATE — HIGH if skipped (no recovery from .bak), MEDIUM if done with service running. Mitigation: stop → migrate → start, NOT "service stays up during migrate."
B) FAT-JAR — MEDIUM. Install gradle = HIGH (~150MB on tight disk). Mitigation: build fat-jar locally, scp single file.
C) FAT-JAR mv — MEDIUM (atomic rename(2)). installDist = HIGH (rsync mid-flight + systemd auto-restart = ClassNotFound). git-pull = HIGH (EBUSY).
D) Versioned-symlink — LOW severity, MEDIUM-MISS-RATE. Operator-error rate dominates. Mitigation: jarvis.prev.jar next to jarvis.jar.
E) Smoke gates catch DIFFERENT failures: --version (link), /healthz (bind), /api/chat fake provider (context assembly). Real-provider smoke = HIGH cost, LOW marginal coverage. Critical gap: smoke does NOT exercise migrator output (wc -l before/after).
F) SCRIPT + USER RUNS — MEDIUM. Claude SSHes = HIGH blast radius (typo in rm hits bak+activity+archival). Copy-paste = HIGH operator-error rate. Script in repo = bounded blast + version-controlled.

PC LOGGER DURING DEPLOY:
LOW + benign. Verified ActivityCapture.kt:55-69 — appendLocal is terminal, NO retry queue, NO replay-on-success. Each tick independent: succeeds-and-posts OR fails-and-writes-locally-and-forgets. Net data loss in 60s deploy window = 0–1 entries written to PC's local activity.jsonl that will never reach VPS. NO thundering herd possible.

DEPLOY SEQUENCE (15 steps): build fat-jar + test → scp to incoming/ staging → df -h /opt abort if <500MB → systemctl stop → snapshot conversations.jsonl → mv old jar to .prev → mv new jar in place → migrator --dry-run → migrator real → wc -l → systemctl start → journalctl 30s → curl /healthz → curl /api/chat fake provider → wait one PC-logger tick (5min) → if 502s persist, mv .prev back. Keep .prev + snapshot 24h.
```

---

## Sanity Check

SANITY [Devil's Advocate]: PASS
NOTE: clean. Persona-correct REJECT. Concrete: drives single most damaging flaw home (semantic starvation on VPS not named in deploy plan).

SANITY [Domain Expert]: PASS
NOTE: clean. Names Pivotal/Netflix/Spring Boot bootJar pattern, Capistrano releases-symlink, Letta/Mem0/Khoj migration patterns, Ktor deploy docs, Spring Boot Actuator. Concrete throughout.

SANITY [Pragmatist]: PASS
NOTE: clean. Hard cap "15min user time". Cost rationale per pick. Specific commands.

SANITY [First Principles]: PASS
NOTE: clean. Strips framing aggressively ("infrastructure cosplay", "the five-decision matrix is itself the smell"). Stays in lane.

SANITY [Risk Analyst]: PASS
NOTE: clean. Verified ActivityCapture.kt source code before answering F. Caught nuance: rename(2) atomic only when target absent; PC logger has NO replay queue → no herd.

No flags.

---

## Judge

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: APPROVED with named follow-up

CORE FINDING:
4-of-5 agents converge: SKIP migration, build locally with installDist, rsync to VPS, mv-old as rollback, copy-paste commands for first deploy. Devil's Advocate's REJECT raises a real follow-up (VectorStore on VPS is starved, separate from this deploy) but does NOT block Step B — the chat recency rewire, mutex hardening, and privacy scanner all deliver value on a starved-VectorStore stack. Risk Analyst's lone dissent on A (migrate-anyway) is overruled by user's explicit "useless" directive plus the .bak preserves the option indefinitely.

AGENT CONSENSUS: 1 REJECT, 4 CONDITIONAL, 0 APPROVE — 0 flagged.

KEY ISSUES (ruled):

A) SKIP migration. 4-of-5 converge. User explicitly said useless. wiki.md.bak.1778090717 (7.3MB) preserves recovery option. Conversations.jsonl starts empty on VPS.

B) N/A. SKIP A → no migrator on VPS → no fat-jar/gradle-on-VPS question.

C) installDist + rsync. Domain + Pragmatist + DA + FP converge against fat-jar (Risk's atomicity argument is real but the service is STOPPED during swap, so atomic-rename advantage is moot). git-pull rejected (no gradle on VPS). 4-of-5.

D) mv-old (NOT versioned symlink). Pragmatist + FP + Risk converge — "infrastructure cosplay at this scale". Domain + DA argue for versioned symlinks but rollback urgency is LOW per user. mv /opt/jarvis /opt/jarvis-prev = one cmd, one cmd to roll back. Keep ONE prev.

E) Local test green + local installDist boot smoke + post-restart curl /healthz + journalctl 30-60s. 5-of-5 reject "re-run tests on VPS" (no gradle) and "real-provider smoke" (cost, eats Copilot quota). Critical gate elements: catch ClassNotFound at startup + bind/port collision + LLM-call doesn't throw.

F) FIRST deploy = copy-paste with output visible per command (DA + Pragmatist + FP first-time). SECOND+ deploy = extract to tools/deploy.sh (Domain + Risk + FP second-time). User runs commands; Claude provides them.

DEVIL'S ADVOCATE REJECT, RULED:
DA's "semantic starvation on VPS = load-bearing blocker" is REAL but does NOT apply here. Step B value is independent of VectorStore: (1) chat recency rewire fixes the 988-file collapse class of bug regardless of semantic search, (2) mutex hardening prevents byte-interleaving regardless, (3) privacy scanner catches identifier-paste regardless, (4) Letta-shaped foundation enables future work including the VectorStore wiring. SHIP STEP B. File "wire VectorStore from WebMain on VPS" as separate follow-up after deploy.

RECOMMENDED PATH (synthesized):

```
# === LOCAL ===
0. Tests green confirmed (already done — gradle :test passed on e87425c).
1. PC: gradle :installDist
   → produces build/install/jarvis-kotlin/{bin/jarvis-kotlin, lib/*.jar}

2. PC: smoke-boot artifact briefly (catches ClassNotFound)
   JARVIS_DATA_DIR=/tmp/jarvis-smoke JARVIS_AUTH_TOKEN=smoke ./build/install/jarvis-kotlin/bin/jarvis-kotlin web &
   sleep 5; curl -s http://localhost:8080/healthz; kill %1

# === VPS PRE-CHECKS ===
3. ssh root@46.247.109.91 "df -h /opt && free -h"
   abort if /opt has <500MB free OR free -h shows <300MB available

4. ssh root@46.247.109.91 "test -f /opt/jarvis/.env && echo env-exists"
   confirm .env survives the mv (next step preserves it)

# === STOP + SWAP ===
5. ssh root@46.247.109.91 "systemctl stop jarvis"

6. ssh root@46.247.109.91 "cp /opt/jarvis/.env /tmp/jarvis.env && mv /opt/jarvis /opt/jarvis-prev"
   .env staged in /tmp; old dir kept as -prev for rollback

7. PC: rsync -avz --delete build/install/jarvis-kotlin/ root@46.247.109.91:/opt/jarvis/

8. ssh root@46.247.109.91 "mv /tmp/jarvis.env /opt/jarvis/.env && cp -r /opt/jarvis-prev/data /opt/jarvis/data"
   restore .env + state dir (data/ was OUTSIDE the dist, but be defensive about /opt/jarvis layout)

# === START + VERIFY ===
9. ssh root@46.247.109.91 "systemctl start jarvis && sleep 3 && systemctl is-active jarvis"

10. curl -s https://corgflix.duckdns.org/healthz   # expect: ok

11. ssh root@46.247.109.91 "journalctl -u jarvis -n 80 --no-pager"
    eyeball for stack traces, ClassNotFoundError, bind failures

12. PC: curl -s -X POST https://corgflix.duckdns.org/api/chat \
        -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
        -d '{"msg":"smoke test from deploy"}'
    expect: {"reply":"...","model":"copilot"}

13. wait ~5min for one PC logger tick → confirm /api/activity 200 in journalctl
    AND verify /opt/jarvis/data/conversations.jsonl gained 2 rows from step-12

# === ROLLBACK if any step 9-13 fails ===
ssh root@46.247.109.91 "systemctl stop jarvis && rm -rf /opt/jarvis && mv /opt/jarvis-prev /opt/jarvis && systemctl start jarvis"

# === CLEANUP after 24h soak ===
ssh root@46.247.109.91 "rm -rf /opt/jarvis-prev"
```

CONFIDENCE: 8
[Would change to 9+ with: confirmed `/etc/systemd/system/jarvis.service` ExecStart matches the installDist layout `/opt/jarvis/bin/jarvis-kotlin`. Would change to 6 if smoke-boot in step 2 reveals an unanticipated runtime dep (e.g. JNA native lib path on Linux differs from Windows local).]

FOLLOW-UP TICKETS (after deploy):
1. Wire VectorStore.add into appendChatTurn so semantic recall works on VPS, not just local CLI (DA's REJECT scope, valid but separate).
2. Codify deploy.sh on second deploy (Domain + Risk + FP-second-time).
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
