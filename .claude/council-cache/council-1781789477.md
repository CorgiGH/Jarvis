# Council review — 1781789477

**Problem:** Is my session reasoning + the proposed fix on track? Diagnosis: tutor checkers are wired+running but aimed at student-answers/claims, not the QUESTION; SymPy leg PA-only; donor lesson bypasses pipeline. Proposed fix: "point the existing numeric oracle at the question at authoring time (small, reuse)."
**Project context:** jarvis-kotlin AI tutor. First deploy target = POO (OOP), family = class-diagram (structural, structure-isomorphism gate, NOT numeric); content/POO/kcs/ = only .gitkeep. Live VPS months stale (deploy gap). No paid APIs.
**Timestamp:** 2026-06-18 (epoch 1781789477)

**Judge's independent grounding (verified against code):** ContentValidator.kt = 13 structural checks (cycles/orphans/weights/bilingual/verbatim/viz-ref/grounding/enums/tautology/garble/figure-binding/language), ZERO question-keyed-answer checks (grep "option|correct|answer|unique" → none). NonLlmLegs.kt:204 `"PA" -> SymPyLeg(); else -> NoneLeg` (POO unchecked → UNCERTAIN floor). GraderRouting: structural archetypes (PROOF/PROSE) → `[RUBRIC, LLM_JUDGE]`, no NUMERIC_ORACLE. DrillGenerator.kt:135-137 = an LLM SELF-SOLVE keyed-answer check (generator re-solves its own stem), fires only when canonicalAnswer != null — i.e. an LLM-alone author-time check that doesn't cover structural items.

---

## 🔴 Devil's Advocate — REJECT
The fix solves neither the bug it names nor the subject it deploys. (1) cuts-vs-depth is LINGUISTIC: both readings are valid in-options integers, key=3 IS correct for the depth-reading, so a numeric "key correct + unique + in-options" check returns GREEN — SymPy can't see the prose/key gap. (2) Doesn't touch POO: NonLlmLegs.kt:204 `else -> NoneLeg`, content/POO/kcs empty, class-diagram is structural — no numeric surface. (3) "small/reuse" is dishonest: ContentValidator (486 lines) has no options/key/uniqueness notion; question-validation is net-new with a hard LLM-or-human core. KEY CONCERN: effort hardens a PA path not on the critical path while the actual first-deploy (POO) ships with NO question-correctness gate.

## 📚 Domain Expert — CONDITIONAL
Diagnosis grounded and matches practice (PrairieLearn computes the key in code so it can't drift; NBME single-best-answer item validation). But: a numeric oracle catches only numeric/symbolic mismatch, NOT linguistic ambiguity/under-determination; the existing keyed check (DrillGenerator.kt:135-137) is an LLM self-solve = the "LLM-alone at authoring" the principle forbids; and SymPy is structurally irrelevant to POO (class-diagram → needs a structure/graph-isomorphism authoring oracle, not SymPy). KEY CONCERN: approve the PRINCIPLE (machine-key-at-authoring + best-of-N + LLM-for-taste + human spot-check); reject the mis-scope — require a structure-isomorphism authoring oracle for POO + a non-LLM single-best-answer/options check; don't let PA-only SymPy stand in as the POO gate.

## ⚙️ Pragmatist — REJECT
Not "small/reuse": SymPyLeg only checks "do both sides of a GIVEN lhs=rhs simplify equal" — invariant-checking, not question-validation; validating a question needs item parsing + per-type answer generation + uniqueness logic none of the files contain, and GraderRouting/SymPyLeg run at GRADE-time on a student submission, not authoring-time on the item. Sequencing upside-down: first deploy is POO/structural (structure-iso gate), content/POO/kcs empty, real blocker is DEPLOY (VPS months stale). KEY CONCERN: mis-scoped AND mis-sequenced — net-new validator sold as "reuse," hardening a numeric path the deploy-first subject never exercises.

## 🧱 First Principles — REJECT
Actual goal = lessons correct-by-construction so the student is never QA. The pipeline AUTHORS from corpus with the answer derived from the span; a separate post-hoc question-validator is largely redundant with doing authoring right. The donor bug exists ONLY because a lesson was hand-written outside the pipeline → correct fix = "route donor content through the pipeline / stop shipping hand-written lessons," not bolt a numeric oracle on. POO concept_types are structural (definition-taxonomy/comparison/proof); SymPyLeg is PA-only; the numeric fix touches zero lines of the deploy path. KEY CONCERN: it patches one hand-authored symptom of a bypass, aimed at a numeric surface the structural deploy target doesn't have.

## ⚠️ Risk Analyst — CONDITIONAL
[CRITICAL] coverage/false-confidence inversion: a numeric oracle validates the non-deployed subject (PA) and structurally cannot touch POO (non-numeric, NoneLeg-floored, RUBRIC/LLM-routed) — "questions are now validated" overstates coverage to ~0% on the live corpus and re-creates the cuts-vs-depth silent gap under a green label. Most-likely-to-bite failure = shipping POO structural content with NO loud author-time correctness check (NoneLeg UNCERTAIN is silent). [HIGH] sequencing: content/POO/kcs empty + VPS stale. KEY CONCERN: gate the "validated" claim to real coverage; add a structural-correctness check (or a surfaced "UNVERIFIED structural" badge) for POO before deploy.

---

## Sanity Check
SANITY Devil's Advocate: PASS — grounded (NonLlmLegs.kt:204, ContentValidator line count); reasoning holds.
SANITY Domain Expert: PASS — names real patterns (PrairieLearn, NBME), cites DrillGenerator self-solve; clean.
SANITY Pragmatist: PASS — correct on SymPyLeg semantics + sequencing; clean.
SANITY First Principles: PASS — strongest reframe (correct-by-construction redundancy); grounded in concept_type + routing.
SANITY Risk Analyst: PASS — the false-confidence inversion is the sharpest single finding; grounded.
(No flags — all five independently grounded in the same code and converged. Verified the load-bearing new fact, DrillGenerator.kt:135-137, by hand.)

---

## Judge

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: FLAWED

CORE FINDING:
The DIAGNOSIS (A–D) is correct and grounded — checkers are wired and run, but aimed at student answers + claim-grounding, not at the question; SymPy is PA-only; the donor bypasses the pipeline. The FIX/DECISION (E) is wrong on every axis: a numeric oracle cannot catch the cuts-vs-depth LINGUISTIC ambiguity (key=3 is a valid in-options integer → green); it is structurally inapplicable to the FIRST deploy target POO (class-diagram = structural, NoneLeg, RUBRIC/LLM, content empty); "small/reuse" is mislabeled (net-new validator, and a partial LLM-self-solve version already exists and is itself LLM-alone); and it risks a false-confidence inversion that relabels the same silent gap "validated."

AGENT CONSENSUS: 3 REJECT, 2 CONDITIONAL, 0 flagged — unanimous in substance (fix mis-scoped). Judge agrees; independently verified.

KEY ISSUES:
- Numeric ≠ linguistic. The bug is a prose↔key mismatch; catching it needs computing MULTIPLE defensible readings (or best-of-N / human), not "is the key correct."
- Wrong subject. POO (deploy-first) is structural; its oracle is the EXISTING structure-isomorphism gate. Verify THAT runs at authoring; numeric covers ~0% of POO.
- Correct-by-construction > post-hoc validator. The donor bug exists only because a lesson was hand-written outside the pipeline. Author from corpus (answer derived from source) + don't ship hand-written lessons.
- False-confidence + sequencing. Never show a global "questions validated" badge; surface per-subject honest status. The real blocker is DEPLOY + authoring the first POO KC, not perfecting a PA checker.

RECOMMENDED PATH:
1. POO deploy: author the first POO KC correct-by-construction from a real lecture; rely on the EXISTING structure-isomorphism gate — VERIFY it runs at admission/authoring (don't assume). 2. Treat the donor question-bug as "route through the pipeline / don't ship hand-written," not as a reason to build a numeric question-checker. 3. Defer numeric question-validation to when PA numeric items re-enter authoring; design the answer-in-options/uniqueness part NON-LLM, and accept linguistic ambiguity needs best-of-N or a thin human spot-check. 4. Honest per-subject coverage labels, never a global green.

CONFIDENCE: 9
All five grounded independently in the same code and converged; the one load-bearing new fact (DrillGenerator self-solve) was judge-verified.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output saved to: .claude/council-cache/council-1781789477.md
