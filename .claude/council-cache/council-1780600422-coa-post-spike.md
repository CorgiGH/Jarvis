# Council — best course of action post-spike — 1780600422

**Question:** Given the B5 NLI spike went GREEN, what is the best course of action? (paths A=full B5 now, B=corpus dry-run first, C=degraded-badge+Phase-3-first, D=other)
**Method:** grounded workflow (coa-council-post-spike, run wf_5ad7c831-e41) — 6 lenses each read the repo, then a judge.

---
## devils-advocate  — APPROVE_B
**Recommended path:** Do the corpus dry-run / calibration FIRST (path B): run the NLI leg offline over the REAL faithful-target KCs that actually reach the runner's faithful branch (pa-kc-005/006, the only ones with a source.span), and measure the true faithful-rate before building the full leg+gate+monitor. Block full B5 until the dry-run proves the faithful path is reachable on a span-bearing KC; in parallel, replace the tautological invariants and treat the spike result as not-yet-evidence.

**Reasoning:** The spike is green on the wrong KC. pa-kc-001 (the spike target) has NO source.span (content/PA/kcs/pa-kc-001.yaml:11-17), so VerificationRunner.decideOutcome case-1 (VerificationRunner.kt:293) floors it to DEFINITIONAL_NO_GOLD_SPAN → uncertain — it can NEVER be certified faithful regardless of how sharp NLI was. The ONLY KCs that can reach the faithful branch are pa-kc-005/006, which have spans but carry tautological invariants ("1 + 1 + 1 = 3", "t + t + t = 3*t"; pa-kc-005.yaml:16, pa-kc-006.yaml:16). The faithful path (VerificationRunner.kt:295) demands nonLlm.ran && nonLlm.pass — but SymPyLeg checks claim.invariant, not the lecture (NonLlmLegs.kt:49-57), so it rubber-stamps any tautology and contributes ZERO source-faithfulness signal for the exact KCs B7 must certify. Worse, those math KCs are NLI's known-weak domain, and after D6 family-B (NLI) plus family-A (relay) both score the same (quote, claim) entailment the round-trip already covers structurally — so post-B5 a faithful verdict on 005/006 rests on two correlated NL-entailment opinions plus a tautology, not the independent non-LLM machine check the LOCKED invariant promises. correctness-engine.md:122 already flags the tautology as unfixed. Full B5 now would build the leg, monitor, and gate on top of an unmeasured (and structurally weak) faithful path, risking a B7 acceptance that either fails to reach faithful or reaches it via the tautology hole — exactly First Principles' and the Risk Analyst's worries, still unanswered by this spike.

**Key concern:** The spike measured NLI on pa-kc-001, which the runner floors to uncertain (no source.span) and never certifies; the actual faithful-target KCs (005/006) pair tautological SymPy invariants that prove nothing with NLI's weakest domain — so whether ANY real KC can honestly reach faithful is still completely unmeasured.

**Grounding checked:**
- VerificationRunner.kt:293 decideOutcome case-1: source.span==null → DEFINITIONAL_NO_GOLD_SPAN (uncertain)
- VerificationRunner.kt:295 faithful path requires bothSupported && nonLlm.ran && nonLlm.pass && roundTrip.pass && !anyThrew
- content/PA/kcs/pa-kc-001.yaml:11-17 — spike KC has NO span and NO invariant, so cannot reach faithful
- content/PA/kcs/pa-kc-005.yaml:16 invariant '1 + 1 + 1 = 3'; pa-kc-006.yaml:16 't + t + t = 3*t' — tautologies
- NonLlmLegs.kt:49-63 SymPyLeg checks claim.invariant (tautology), not the lecture — passes regardless of source
- TrustRoutes.kt:159-160 family-B is OpenRouterChatLlm() (a 2nd free LLM) — confirms the false-independence trap D6 must fix
- TwoFamilyDeriver.kt:75-92 + SpanClaimRoundTrip.kt:29-49 — NLI family-B and the relay both score (quote,claim) entailment, overlapping the round-trip's structural check
- correctness-engine.md:122 explicitly flags pa-kc-005/006 invariants as SymPy tautologies contributing no independent signal, still unfixed

---
## domain-expert  — APPROVE_B
**Recommended path:** Do the offline corpus dry-run/calibration FIRST (Path B): run the local NLI leg over every real PA KC claim with NO wiring and NO gate, log raw 3-class scores per (quote, claim) pair, and measure the actual SUPPORTED/UNCLEAR/REFUTED distribution plus the would-be faithful-rate. Use that gold set to set the UNCLEAR band, THEN build the B5 leg + coverage monitor with the gate default-OFF. Adopt DeBERTa-v3-MNLI (the proven spike model) as family-B now; treat MiniCheck as a fast-follow swap behind the same NliLeg adapter, not a blocker.

**Reasoning:** A corpus dry-run before shipping an NLI consistency gate is established practice, not gold-plating: SummaC, AlignScore, and MiniCheck papers all calibrate the decision threshold on a held-out set and report per-domain balanced accuracy before deployment, because NLI/fact-check thresholds do NOT transfer across domains or summary styles. The spike proved the IPC + a sharp 3-band verdict on ONE clean definition; it did not measure the faithful-rate distribution on the corpus, which is the one number that decides whether a default-OFF→ON gate is shippable or bricks the 100%-uncertain corpus (exactly Risk Analyst's worry). Crucially, the repo architecture defuses First Principles' and the spike's 'NLI is weak on math' caveat: VerificationRunner splits the legs (VerificationRunner.kt:295) — SymPyLeg deterministically checks the arithmetic invariant (1+1+1=3), while the NLI/LLM family only checks whether the natural-language claim is entailed by its natural-language source quote, which is NLI's core competency, not its weak domain. So the dry-run's real job is calibrating the UNCLEAR band and revealing where prose-grounding genuinely fails (e.g. pa-kc-005's claim 'distinguish three size measures' vs a thin one-line quote), not measuring math. On the model choice: DeBERTa-v3-MNLI is the right pick to PROCEED — it is the proven, installed, 3-class model and the neutral/UNCLEAR class is load-bearing for the FAIL-LOUD band; MiniCheck is a 2-class support-prob checker (no native UNCLEAR/neutral) and would force you to synthesize an abstention band anyway, so deferring it behind the NliLeg adapter is correct and low-cost.

**Key concern:** The faithful-rate on the real corpus is unmeasured. If the dry-run shows most KCs land in UNCLEAR even after band calibration (likely when claims assert more than their thin source quote literally says — e.g. pa-kc-005's 'distinguish three size measures'), then building the full leg+gate before the dry-run wastes effort and re-validates First Principles' 'is faithful even achievable' worry; the dry-run resolves this with evidence cheaply and must precede the gate.

**Grounding checked:**
- TrustRoutes.kt:158-160 — family-A=RELAY RelayLlm, family-B=OPENROUTER OpenRouterChatLlm() confirmed (second :free LLM, false-independence trap real)
- VerificationRunner.kt:292-300 decideOutcome — faithful requires bothSupported && nonLlm.ran && nonLlm.pass && roundTrip.pass && !anyThrew (genuine AND-gate, FAIL-LOUD); NONLLM NONE → uncertain floor; else → DISAGREE_OR_…
- content/PA/kcs/pa-kc-005.yaml invariant='1 + 1 + 1 = 3' but source quotes are NL ('the size of representation must be mentioned'); pa-kc-006.yaml invariant='t + t + t = 3*t' with NL quotes — confirms math vs prose split
- NonLlmLegs.kt:38-83 SymPyLeg handles the equational invariant via splitEquation+pythonEquals; non-equational/DEFINITION claims → kind=NONE ran=false (uncertain floor) — so the LLM/NLI family verifies prose entailment, NOT the arithmetic
- TwoFamilyDeriver.kt buildPrompt — verifies claim.content+invariant against source.quote, classify maps SUPPORTED/REFUTED/else→UNCLEAR; the NLI premise=quote, hypothesis=claim text
- correctness-engine.md D6 — locked decision names MiniCheck-class / DeBERTa-v3-NLI as family-B local model; coverage monitor mandated (council R2)

---
## pragmatist  — APPROVE_B
**Recommended path:** Run the corpus dry-run FIRST: take the already-green spike harness (DeBERTa-v3-MNLI + the JARVIS_PYTHON3 ProcessBuilder bridge that just worked) and run it OFFLINE over every real PA KC claim — no LegFamily slot, no gate wiring, no schema edit. Produce one number per KC: would this claim's (span, claim) pairs clear SUPPORTED, and the corpus-wide faithful-rate split by claim kind (DEFINITION/prose vs the math invariants on 005/006). Only after that measurement decide DeBERTa-MNLI vs MiniCheck and commit the full B5 build (adapter contract -> leg -> coverage monitor -> gate default-OFF).

**Reasoning:** I verified family-B is genuinely a 2nd OpenRouter :free LLM (TrustRoutes.kt:159-161), that no NLI leg exists yet, that F1 is shipped (VerificationRunner.kt:208-215 bothSupported), and that the two locked faithful-target KCs are SymPy tautologies (pa-kc-005 "1+1+1=3", pa-kc-006 "t+t+t=3*t"). That last fact is the crux: the spike proved NLI works on a clean definition (pa-kc-001) but explicitly did NOT measure the math KCs, which are NLI's known-weak domain AND are the only KCs the locked acceptance points at. Full B5 (adapter contract + leg + LegFamily slot + coverage monitor + gate + gradle env-forwarding + report_wrong lifecycle) is a multi-day build that, per the post-fix audit, lands on a corpus that today aggregates 100% uncertain — if the math KCs also can't reach faithful via NLI, the whole build buys an unchanged badge. The dry-run reuses the exact harness that just went green, so its marginal cost is hours not days, and it converts two council members' worries (First Principles' "is faithful achievable" + Risk Analyst's "gate denies everything") from speculation into a measured faithful-rate. This is maximum de-risking per unit of work and it does not violate the plan-first or verification-first locks — it is a measurement, not a build, and it precedes the build the plan already orders.

**Key concern:** The locked faithful-targets (pa-kc-005/006) are tautological math invariants in NLI's weakest domain; if NLI can't certify them, full B5 ships a leg that still can't reach faithful on the acceptance KCs, and the gate (even default-OFF) plus the report_wrong/coverage/sync scaffolding is dead weight built before its premise was tested. The dry-run must report the math-KC faithful-rate explicitly, or it fails its only purpose.

**Grounding checked:**
- TrustRoutes.kt:158-161 — family-B IS a 2nd OpenRouter :free LLM (LegFamily.OPENROUTER, OpenRouterChatLlm()), confirming the false-independence trap is live
- VerificationRunner.kt:208-215 — F1 fix shipped: bothSupported is distinct from agree, both-REFUTED no longer certifies faithful
- content/PA/kcs/pa-kc-005.yaml:16 — invariant is the tautology '1 + 1 + 1 = 3'; pa-kc-006.yaml:16 — invariant is 't + t + t = 3*t'; both are the locked faithful-targets and both are NLI-weak math
- grep across src/ — zero NliLeg/NliAdapter/MiniCheck implementation exists; the leg is entirely unbuilt
- correctness-engine.md:99,118 — Bundle 5 requires the NLI adapter contract written before wiring, gate at default-OFF, and a coverage monitor; matches the council's 3 conditions
- correctness-engine.md:115 — post-fix audit confirms current PA content aggregates to 'uncertain', real faithful needs the D6 NLI leg

---
## first-principles  — APPROVE_B
**Recommended path:** Run B as ordered: an OFFLINE corpus dry-run/calibration of the NLI leg over ALL real PA KCs (no LegFamily wiring, no gate, no DB writes) that measures the actual faithful-rate split by KC kind (definition vs math/invariant), AND calibrates the UNCLEAR band on a small gold set of Alex's OWN KCs. The dry-run output is the gate that decides B5-proper vs my degraded-badge path. Fold the cheap independent win in parallel: surface the deterministic SpanClaimRoundTrip result as the honest "matches your lecture" badge now, since that leg already exists and is what the LOCKED badge actually asserts.

**Reasoning:** My REJECT softens but does not flip, and the spike is why: it proved the IPC plumbing (JVM ProcessBuilder -> real py3.12 -> NLI verdict read back), not that NLI raises the faithful-rate on Alex's corpus. The spike KC (pa-kc-001) is a clean natural-language DEFINITION and — verified — has claims with NO span, so it currently floors to uncertain via DEFINITIONAL_NO_GOLD_SPAN (VerificationRunner.kt:293), not via a missing leg; NLI does not even change its verdict. The KCs that block the corpus are the math ones: pa-kc-005/006 carry invariants "1 + 1 + 1 = 3" and "t + t + t = 3*t" that already pass the deterministic SymPy leg, and NLI would be asked to entail those from prose quotes — its known-weak symbolic domain, which the spike explicitly did NOT measure. So "is faithful achievable on the corpus" is still unproven; building the full leg+LegFamily+gate before measuring violates the LOCKED plan-first/verification-first rule and risks Risk Analyst's "gate denies everything" outcome. The deepest point stands: SpanClaimRoundTrip already computes the exact span-location claim the badge is LOCKED to make ("matches your lecture," never "verified correct"), independent of NLI — so the honest badge is achievable today without any model, and that should ship in parallel regardless of how the dry-run lands.

**Key concern:** The faithful path requires bothSupported (both LLM families SUPPORTED) AND the non-LLM leg pass AND round-trip; swapping family-B to NLI means a low NLI score on a math KC silently caps the whole corpus at uncertain. Committing to the full B5 leg+gate build before the offline dry-run measures NLI's faithful-rate on the math KCs (its weak domain) risks shipping an engine that, on Alex's real corpus weeks before finals, certifies nothing — the same 100%-uncertain bricking the prior council warned about, just with a heavier model.

**Grounding checked:**
- TrustRoutes.kt:158-161 — confirmed legB=OPENROUTER free LLM (false-independence trap real, family-B is a 2nd LLM not a non-LLM leg)
- content/PA/kcs/pa-kc-005.yaml:16 invariant '1 + 1 + 1 = 3' + grader_rule sympy simplify(...)==0 — pure tautology, SymPy-checkable, NLI-weak domain
- content/PA/kcs/pa-kc-006.yaml:16 invariant 't + t + t = 3*t' — same: math KC, NLI symbolic-reasoning weak spot, unmeasured by spike
- content/PA/kcs/pa-kc-001.yaml:11-17 — claims have quote but NO span field; so floors via DEFINITIONAL_NO_GOLD_SPAN not a missing leg
- VerificationRunner.kt:292-301 decideOutcome — case1 no-span=>uncertain; case3 faithful requires bothSupported && nonLlm.ran&pass && roundTrip.pass && !anyThrew
- SpanClaimRoundTrip.kt:22-49 — deterministic span<->quote round-trip, NO LLM/NLI, proves quote verbatim-locates in live source = exactly the 'matches your lecture' badge claim
- TwoFamilyDeriver.kt:75-92 buildPrompt — NLI/LLM is fed (quote + invariant + content) and judges entailment; for math KCs that is prose-quote => symbolic-invariant entailment, NLI's weak case
- correctness-engine.md:99,118,120 — B5 spec already mandates writing the NLI SUPPORTED/REFUTED/UNCLEAR contract + load-bearing UNCLEAR band + coverage monitor BEFORE wiring; report_wrong REVERIFY lifecycle flagged HIGH/deferrable

---
## risk-analyst  — APPROVE_B
**Recommended path:** Run a CORPUS DRY-RUN first: execute the NLI leg offline over ALL real PA KCs (no LegFamily wiring, no gate, no DB writes) and measure the actual faithful-rate per claim-kind — especially the math KCs (pa-kc-005/006) — calibrating the UNCLEAR band on Alex's own KCs. Only then commit to the full B5 leg+gate build, and keep the F2 gate default-OFF/observe-only until the dry-run proves a non-trivial faithful-rate exists.

**Reasoning:** The spike proved the IPC plumbing and NLI on a clean DEFINITION — it did NOT measure the one number B5's success depends on: the faithful-rate on Alex's real corpus, dominated by math KCs that are NLI's known-weak domain. I traced the certify path (VerificationRunner.kt:295): faithful needs bothSupported (two LLM families agree SUPPORTED) AND nonLlm.pass AND roundTrip.pass AND !anyThrew. The SymPy leg on pa-kc-005/006 is a tautology (1+1+1=3, t+t+t=3*t) that passes trivially and adds ZERO independent signal — so on the math KCs the entire faithful verdict rests on the NLI family-B agreeing SUPPORTED, which is exactly where NLI is weakest. If the dry-run shows ~0% faithful on math, then building the full leg+gate buys nothing and Path A wastes the runway; if it shows a usable rate, A proceeds with calibrated thresholds instead of model defaults. B is the cheapest probe that collapses the First-Principles-vs-Risk split with evidence before any irreversible wiring, fully honoring plan-first/verification-first.

**Key concern:** CRITICAL: faithful-rate-near-zero-on-math. On pa-kc-005/006 the non-LLM leg is a tautology contributing no signal, so faithful collapses to "does the NLI model say SUPPORTED" on algorithmic invariants — NLI's known-weak domain. If that rate is ~0%, B5 ships an engine that still certifies nothing, and Phase 3 stays blocked. This is unmeasured and the spike did not touch it.

**Grounding checked:**
- TrustRoutes.kt:159-161 — family-B IS OpenRouterChatLlm (OPENROUTER family), false-independence trap confirmed
- VerificationRunner.kt:292-301 decideOutcome — faithful (case-3) requires bothSupported && nonLlm.ran && nonLlm.pass && roundTrip.pass && !anyThrew; so a thrown/429 family-B blocks faithful via anyThrew
- VerificationRunner.kt:209-215 — agree vs bothSupported split (F1 fix holds: both-REFUTED is agree but not bothSupported)
- VerificationRunner.kt:118-123 aggregateKc — KC faithful iff EVERY per-claim verdict faithful (conjunction, order-independent)
- content/PA/kcs/pa-kc-005.yaml:16 invariant '1 + 1 + 1 = 3' and pa-kc-006.yaml:16 't + t + t = 3*t' — both SymPy tautologies, zero independent signal
- NonLlmLegs.kt:38-68 SymPyLeg — DEFINITION/non-equational claims floor to kind=NONE/ran=false (uncertain floor), only equational claims invoke the sympy seam
- VerificationGate.gate has ZERO production callers (grep src/main): only referenced in CitationGuard.kt:27 doc + its own def VerificationGate.kt:24 — F2 confirmed: net labels but enforces nothing
- correctness-engine.md:99 + :118 — B5 ordering: NLI adapter contract with load-bearing UNCLEAR band BEFORE wiring; gate default-OFF; coverage monitor from verification_audit cols
- realSymPyEquals NonLlmLegs.kt:100 — reads JARVIS_PYTHON3 env (the spike's fix); gradle test worker env-forwarding is the surfaced fragility

---
## sequencer-pm  — APPROVE_B
**Recommended path:** Insert a CORPUS DRY-RUN/calibration step as the immediate next action: build only the NLI adapter (DeBERTa-v3-MNLI, not MiniCheck) + a throwaway offline batch runner, run it over ALL real PA KCs with NO wiring and NO gate, and record the actual SUPPORTED/REFUTED/UNCLEAR distribution — explicitly broken out for the math KCs (pa-kc-005/006) vs the prose definitions. The measured faithful-rate then decides whether full B5 (leg + coverage monitor + gate-default-OFF) proceeds as ordered, or whether the corpus needs real non-tautological invariants / re-authoring first. This is a strict prefix of B5, so it parallelizes cleanly — see below.

**Reasoning:** The spike proved the IPC path and that NLI gives sharp verdicts on a clean definition — but it measured n=1 on NLI's strongest domain, and I confirmed in-repo that the only faithful-eligible math KCs (pa-kc-005/006) carry tautological SymPy invariants (`1+1+1=3`, `t+t+t=3*t`) that contribute zero independent signal, so the NLI leg IS the real check precisely where NLI is weakest. Path A (full build) commits the leg + coverage monitor + gate before anyone knows if faithful is achievable on the corpus; if the answer is "math KCs never reach SUPPORTED," I've built infrastructure around an unreachable acceptance and burned finals-window time. Path C re-litigates a LOCKED decision (verification-first sequencing + the keystone framing) — running Phase 3 teaching on real content first directly inverts the locked order and the spike was specifically commissioned to resolve that split, which it did in NLI's favor. Path B is the cheapest evidence that resolves BOTH live worries (First Principles' "is faithful achievable on math" and Risk Analyst's "gate denies everything") and it is a strict prefix of B5 — the NLI adapter built for the dry-run IS Bundle-5 sub-task (1)+(2), so nothing is thrown away. Critically, the prior council's condition (a) was "SPIKE before bundle"; the spike is one clean KC, not the corpus, so B is the natural completion of that condition, not a detour. DeBERTa-v3-MNLI should be the pick over the locked-named MiniCheck because it is already proven green on this exact box and MiniCheck is binary (no UNCLEAR band) — but that model choice is exactly what the dry-run's calibration on Alex's gold set must confirm.

**Key concern:** If the dry-run shows the math KCs cannot reach SUPPORTED under NLI (likely, given they are arithmetic tautologies, not natural-language entailment pairs), then "≥1 KC faithful with page_anchor_status=LIVE" is unreachable on the current corpus regardless of how well B5 is built — the real blocker is the tautological invariants, not the leg. That must be discovered by measurement now, not after the full leg+coverage+gate is wired.

**Grounding checked:**
- TrustRoutes.kt:158-161 — family-B is LegFamily.OPENROUTER + OpenRouterChatLlm() (2nd free LLM, false-independence trap confirmed); no NliLeg exists in src/main/kotlin (grep returned zero)
- content/PA/kcs/pa-kc-005.yaml:16 invariant '1 + 1 + 1 = 3' and pa-kc-006.yaml:16 invariant 't + t + t = 3*t' — both SymPy tautologies, zero independent signal, NLI-weak math domain
- VerificationRunner.kt:292-301 decideOutcome — faithful (ALL_AGREE_ROUNDTRIP_NONLLM_PASS) requires bothSupported && nonLlm.ran && nonLlm.pass && roundTrip.pass && !anyThrew; agreed-REFUTED falls to else=failed (F1 holds, honest-safe)
- Phase1Tables.kt:82,90 — report_wrong resolution enum OPEN|REVERIFIED_FAITHFUL|RETRACTED declared only in comments; grep found REVERIFIED_FAITHFUL/RETRACTED in zero executable code (one-way trap confirmed)
- correctness-engine.md:99,118-122 — locked B5 order already mandates writing the NLI adapter contract + UNCLEAR band BEFORE wiring, gate default-OFF/LAST, and flags pa-kc-005/006 tautologies as needing real invariants

---
## JUDGE
**VERDICT:** Path B — run the offline corpus dry-run / calibration FIRST, as a strict prefix of Bundle 5, before building the leg+monitor+gate.

**CORE FINDING:** All six specialists land on B, and the grounding is verified, not rhetorical: the spike went green on pa-kc-001 — but pa-kc-001 has NO source.span (content/PA/kcs/pa-kc-001.yaml:11-17), so VerificationRunner.kt:293 floors it to DEFINITIONAL_NO_GOLD_SPAN and NLI never changes its verdict. The spike measured the IPC plumbing on a KC that can never be certified faithful. The ONLY span-bearing faithful-eligible KCs are pa-kc-005/006, and their SymPy invariants are tautologies (1+1+1=3, t+t+t=3*t; pa-kc-005.yaml:16, pa-kc-006.yaml:16) that pass trivially and add zero independent source signal — so on the exact KCs the locked B7 acceptance points at, the entire faithful verdict collapses onto NLI entailing prose→claim in its known-weak math domain, which the spike did NOT measure. First Principles' degraded-badge/Phase-3-first path (C) is DENIED: it inverts the LOCKED verification-first sequencing and the keystone framing, the spike was commissioned to resolve exactly that split and did so in NLI's favor for the plumbing, and FP itself only reached APPROVE_B. The cheap, honest part of C (surface the existing deterministic SpanClaimRoundTrip as the "matches your lecture" badge) is real and should ship in parallel — but it rides alongside B, it does not replace the order.

**CONSENSUS:** 6/6 APPROVE_B (devils-advocate, domain-expert, pragmatist, first-principles[softened from REJECT], risk-analyst, sequencer-pm). No dissent on the path; the only live disagreement is the SymPy-leg interpretation (rubber-stamps tautology vs clean math/prose split) and the model choice — both resolved below.

**CONFIDENCE:** 9

**KEY ISSUES:**
- The spike is green on the wrong KC. pa-kc-001 floors to uncertain via DEFINITIONAL_NO_GOLD_SPAN (VerificationRunner.kt:293, no span) regardless of NLI sharpness. The faithful-rate on the real span-bearing corpus — dominated by the two math KCs — is still completely unmeasured. The dry-run's ONE required output is that rate, broken out math-KC vs prose.
- On pa-kc-005/006 the non-LLM leg is a tautology that contributes no independent signal (the plan's own line 122 flags this unfixed; F6 at line 92 confirms 'SymPyLeg falsely passes' on KC-invariant prose). So post-D6 a faithful verdict on the acceptance KCs rests on family-A relay + family-B NLI both entailing prose→claim — two NL-entailment opinions, NLI's weakest domain, overlapping the round-trip structurally. If that rate is ~0%, the full leg+gate+monitor build buys an unchanged 100%-uncertain badge.
- Model choice: take DeBERTa-v3-MNLI (the proven, installed, 3-class model whose neutral class IS the load-bearing UNCLEAR band the plan at line 118 mandates) as family-B NOW. MiniCheck is binary (no native UNCLEAR) and would force synthesizing an abstention band — defer it behind the NliLeg adapter as a fast-follow swap, not a blocker. This does NOT re-litigate D6 (which named the MiniCheck/DeBERTa-NLI class, not MiniCheck exclusively).
- Gate safety: VerificationGate.gate has ZERO production callers today. Never wire F2 in DENY-mode against a 100%-uncertain corpus or it bricks card creation. Gate stays default-OFF/observe-only and wired LAST, exactly per locked order step 5 and the council's condition (b).
- The real blocker may be the tautological invariants, not the leg. If the dry-run shows the math KCs can't reach SUPPORTED, the fix is re-authoring real non-tautological invariants (plan line 122), discoverable now by measurement — not after the full build.

**RECOMMENDED PATH:** 1) Build ONLY the NLI adapter (DeBERTa-v3-MNLI) + a throwaway offline batch runner that reuses the already-green JARVIS_PYTHON3 ProcessBuilder bridge. No LegFamily slot, no gate, no schema edit, no DB writes — this adapter IS B5 sub-tasks (1)+(2), so nothing is thrown away. 2) Run it offline over EVERY real PA KC's (quote, claim) pairs; log raw 3-class scores; report the would-be faithful-rate split explicitly by claim kind (prose/definition vs the math invariants pa-kc-005/006). Calibrate the UNCLEAR band on a small gold set of Alex's OWN KCs, not model defaults; low-confidence entailment must map to UNCLEAR, never silent SUPPORTED. 3) GATE DECISION on the measured rate: if a non-trivial faithful-rate exists, proceed with full B5 as ordered (leg + coverage monitor + gate default-OFF) and lock DeBERTa-v3-MNLI, MiniCheck deferred behind the adapter; if the math KCs land ~0% (likely), the real next step is re-authoring non-tautological invariants (plan line 122) BEFORE the gate. 4) In PARALLEL and regardless of the rate: surface the existing deterministic SpanClaimRoundTrip result as the honest 'matches your lecture' badge — it already computes exactly the span-location claim the badge is LOCKED to make, needs no model, and is First Principles' one cheap independent win. 5) Wire the gradle test-worker JARVIS_PYTHON3 env-forwarding (the surfaced fragility) as part of the dry-run harness. Top mitigation throughout: the gate is never armed in DENY-mode until the dry-run proves faithful is reachable.

**WHAT WOULD CHANGE IT:** If the dry-run had ALREADY been run as part of the spike and showed a healthy faithful-rate on span-bearing math KCs, Path A (proceed direct to full B5) would win — the measurement is the whole point of B. Conversely, if the dry-run comes back ~0% faithful on the math KCs, the immediate next action shifts from 'build the leg+gate' to 'author real non-tautological invariants for pa-kc-005/006', and the leg build waits on that re-authoring. Evidence that SpanClaimRoundTrip already feeds the served badge (rather than just the audit row) would let the parallel honest-badge win ship with zero extra work.