# Council review — 1781535790 (EVIDENCE-GROUNDED re-run of 1781535094)

**Problem:** Recurring failure — Claude (PM) judges multi-frame algorithm figures from a PARTIAL view, misses defects the user catches (this session: first ~4 merge "divide" frames are visually inert; declared a prototype done off 3 stills). Make it NEVER recur. This council was RE-RUN grounded: each of 5 agents OPENED the actual rendered divide frames + read the renderer code + data trace + plan gate spec before judging (the prior council 1781535094 ran on Claude's prose only — Alex: "give them the actual situation").
**Proposed approach (under review):** an unbypassable per-frame gate: render every frame to rasterized pixels via the real renderer at real viewport/DPR; perceptual diff (SSIM/pixelmatch) between consecutive non-hold frames + no ≥2 consecutive inert; load-bearing (not a human contact-sheet); machine-derived holds; runs on the prototype path; locked in constraints+memory; corrects the plan's state-vector gate.
**Timestamp:** 2026-06-15T (epoch 1781535790) UTC

## Evidence (PM-verified, not agent-trusted)
PM opened r-divide-0.png, r-divide-2.png, r-divide-3.png directly: the 6-cell row [5,2,8,1,9,3] + "POZIȚII 0…5" is PIXEL-IDENTICAL across all divide frames; ONLY the callout chip text + the PAS counter change. Confirmed. The divide subdivision (runs 1→2→4→6, present in MERGESORT_DATA_JSON) is drawn NOWHERE — computePlacement's divide branch (SortMergeFamily.tsx ~L407-414) places every token by array position and never reads st.runs.

---

## 🔴 Devil's Advocate — STANCE: REJECT
The proposed whole-frame pixel-diff gate would NOT catch this: the callout chip text changes on frames 0→1 and 2→3, so SSIM/pixelmatch sees real motion there; ONLY 1→2 is truly inert → "no ≥2 consecutive inert" is SATISFIED → GREEN → ships the rejected figure. The callout is a "liveness alibi." KEY CONCERN: diff must be REGION-SCOPED to the algorithm figure (exclude callout/chrome) — and even then a motion gate only proves not-frozen, NOT that the divide split is drawn at all; the true fault is a content-COMPLETENESS bug (renderer draws no subdivision) that no motion gate detects. summaryMissed: prior council assumed frames are pixel-identical (FALSE — callouts differ) so its threshold passes; and the plan ALREADY rejected perceptual hash + tags divide frames hold:true, so "machine-derive holds" would re-classify the inert frame AS a hold and exempt it — fix and plan are in unreconciled contradiction.

## 📚 Domain Expert — STANCE: CONDITIONAL
Verified with own eyes; root-caused in computePlacement divide branch. Pixel-delta is the right layer (matches Playwright toHaveScreenshot / Chromatic / sprite-sheet QA) BUT a naive WHOLE-FRAME SSIM/pixelmatch FALSE-PASSES here — the callout went from "Vectorul întreg" to "ÎMPARTE…" = lots of changed text pixels. KEY CONCERN: must be REGION-SCOPED (mask callout chip, PAS counter, progress dots, chrome — pin a stable bbox per family) + TWO thresholds (floor epsilon for AA noise + a meaningful-change notion) + a seeded-inert fixture asserted RED (INV-9.4 self-test). summaryMissed: the state vector DOES mutate every divide frame (runs 1→2→4→6) — the bug is "state mutates in a field the divide renderer never reads," and BOTH prototype skins share computePlacement so both inherit it → gate must run per-skin.

## ⚙️ Pragmatist — STANCE: CONDITIONAL
Confirmed defect + root cause. The fix is the only mechanism that goes red here, cheap + durable (~30-50 line raster diff, no attention to rot) IF wired into the SAME path that produced the PNGs (the demo path the prototype used) and IF it blocks "done." KEY CONCERN: (1) epsilon — whole-canvas diff trips on the callout + "1/30→2/30" counter while the body is frozen → mask those regions or measure motion in the FIGURE region only; (2) holds must derive from a CAP on consecutive identical RENDERS, never the phase label (plan L140 tags divide=hold → re-whitelists these exact frames). summaryMissed: the plan green-lights these on BOTH legs (state-advance passes AND hold:true exempts) — a self-refuting gate that names the exact failure it can't catch.

## 🧱 First Principles — STANCE: CONDITIONAL
Irreducible failure: the only oracle for the perceived pixel sequence was an un-anchored glance; no machine artifact represents it. Verified two ways (pixels identical; data shows runs advancing while computePlacement ignores runs). The plan's L139 gate is self-defeating-by-design ("over the STATE delta, NOT a perceptual hash" + divide=hold). Minimum fix = the proposed one, with corrections. KEY CONCERN: rasterize the SAME output the user sees (NOT the state→SVG-attribute layer — that layer is exactly what's non-frozen); machine-derive holds (author hold:true launders the bug). summaryMissed: the real defect is narrower + fixable — a single renderer bug (divide branch discards runs[]); the SEMANTIC fix exists (draw the split). The pixel gate is the class-killer, but the figure must actually render the splits, not merely pass a motion check on churning callout text.

## ⚠️ Risk Analyst — STANCE: CONDITIONAL
Opened the frames; confirmed. Endorse the fix BUT the plan it "corrects" is self-contradictory in a way that re-opens the hole: L139 state-advance is GREEN (runs mutated) AND L140 divide=hold SKIPS these frames. KEY CONCERN — CRITICAL: author-derived hold whitelisting. A hold frame is never pixel-checked; even a correct raster gate is bypassed on exactly the 4 failing frames. AND "machine-derive holds = identical-pixels⇒hold" is CIRCULAR — it auto-classifies the inert frames as holds and passes them. The rule must be SEMANTIC: **a frame whose STATE changed but whose FIGURE pixels did NOT is a HARD FAIL, never reclassifiable as a hold.** HIGH: epsilon false-POSITIVE on callout-only churn → mask the callout region. HIGH: the demo file is slated for Phase-D deletion → a prod-CI-only gate never touches it → run on the demo harness or ban prototypes from "done." summaryMissed: (1) the plan's hold:true is a SECOND independent bypass the prose council never saw; (2) the root cause in code (computePlacement L407-414 discards runs) — cleanest fix is making the renderer DRAW the split; a pixel gate alone with the renderer unchanged just fails CI forever (correct, never ships); (3) the real risk is the false-POSITIVE (chrome churn launders a frozen figure green), not the AA false-negative the prior council focused on.

---

## Sanity Check
All 5 PASS — each listed the real artifacts it opened (frames + code + data + spec + prior council), grounded reasoning in specific files/pixels, committed a stance. Devil's REJECT is the strongest finding and the 4 CONDITIONALs converge on the identical corrections. 0 flagged.

---

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: FLAWED — the fix as I (Claude) framed it last turn would have SHIPPED THIS EXACT FIGURE. Grounding flipped it. Corrected mechanism below.

CORE FINDING:
A whole-frame pixel-diff gate goes GREEN here: the callout chip + step counter change every frame and supply "motion" while the algorithm figure is frozen (only the 1→2 transition is truly identical, so "no ≥2 consecutive inert" passes). Worse, the plan's existing gate is DOUBLY self-defeating — its state-advance leg passes (runs DID mutate 1→2→4→6) AND it tags divide frames hold:true, which EXEMPTS them. And a "machine-derived hold = identical-pixels⇒hold" rule is circular: it would auto-classify the inert frames as holds and pass them. The root cause is a content-COMPLETENESS bug: the renderer's divide branch discards the run-partition, so the split is drawn nowhere — no motion gate (state/pixel, whole/region) detects a completeness bug by itself.

AGENT CONSENSUS: 1 REJECT, 4 CONDITIONAL — 0 flagged. All converge on: region-scope the diff, kill the hold-whitelist, the real invariant is a state-vs-pixel CONJUNCTION, and fix the renderer too.

KEY ISSUES:
- WHOLE-FRAME DIFF IS FOOLED BY CHROME (CRITICAL, all 5): the diff must be REGION-SCOPED to the algorithm figure — mask out the callout chip, PAS counter, progress dots, chrome (stable bbox per family). Otherwise text-only churn launders a frozen figure green (false-POSITIVE — the opposite of the AA false-negative the prose council worried about).
- THE HOLD-WHITELIST IS A SECOND BYPASS (CRITICAL, Risk + Devil + Pragmatist + FP): the plan tags divide frames hold:true → never pixel-checked. Author/phase-derived holds AND "identical-pixels⇒hold" both launder the bug. Holds must be SEMANTIC.
- THE LOAD-BEARING INVARIANT (the real fix): **state-delta(frame N→N+1) ≠ 0 AND figure-region pixel-delta(N→N+1) ≈ 0 ⇒ HARD FAIL** (the renderer dropped a state field the algorithm advanced). This is a machine-decidable CONJUNCTION that catches THIS bug precisely; it is neither a pure state gate (fooled by render-blindness) nor a pure pixel gate (fooled by chrome) — it couples them. A frame that genuinely holds (no state delta) is exempt; a frame whose state moved but whose figure didn't is never a hold.
- FIX THE RENDERER TOO (HIGH, FP + Risk): the pixel gate is the class-killing CI invariant, but with computePlacement unchanged it just fails CI forever (correct, never ships). The divide branch must actually DRAW the run subdivision (brackets/splitting) so the frames differ for the right reason.
- COVER EVERY SKIN + THE PROTOTYPE PATH (HIGH, Domain + Pragmatist + Risk): both boxes and bars share computePlacement → both inherit the defect → gate runs per-registered-variant; it must run on the demo/prototype harness (slated for Phase-D deletion) or prototypes are BANNED from "done" claims. Gate self-test: the divide-frame seed asserted RED.
- MOTION ≠ PEDAGOGY (note, FP): green proves not-frozen-for-the-right-reason, NOT that it teaches. Layer-2 estimator, honestly labeled.

RECOMMENDED PATH:
1. Build the CONJUNCTION gate: per consecutive frame pair, compute (a) the typed STATE delta and (b) the perceptual pixel delta of the FIGURE REGION ONLY (masked bbox, real renderer, real viewport/DPR, two calibrated thresholds). Assert: NOT (state-changed AND figure-frozen). Holds = state-unchanged frames only; a state-changed-but-figure-frozen frame is a HARD FAIL and can never be reclassified. Self-test on the divide-frame seed → RED.
2. Delete the plan's divide=hold whitelist + the state-only every-frame-advances leg for this class; replace with the conjunction gate. Run per-skin + on the prototype/demo harness; until it exists, NO prototype carries a "done" claim.
3. Fix the renderer: computePlacement divide branch must draw the run subdivision so divide frames genuinely differ.
4. Lock in active-constraints + memory: "no figure is shown/claimed-done until the conjunction gate is green on every skin; whole-frame or state-only checks are insufficient." 
5. META: this is the 2nd time grounding a council in real artifacts flipped its answer (the merge "dynamic absent" call was also a partial-view error). Councils on concrete artifacts MUST inspect the artifacts, never the PM's summary.

CONFIDENCE: 9/10
What would raise it: the figure-region bbox must be stable across frames for the mask — verify the family renders the figure in a fixed SVG region (it does: viewBox is fixed; the callout chip is a known sub-rect) so the mask is reliable. Implementation detail, not a design risk.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
