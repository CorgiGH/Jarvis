# Council review — 1781781668

**Problem:** A blind multi-lens LLM audit of the Claude-Design donor mergesort lesson was meant to catch content/pedagogy defects FOR the user (so he isn't the QA), specifically the user's own catch: beat-1 PREDICT question ambiguity. The audit's verifier refuted that finding.
**Proposed approach (under review):** (1) the user's bug is REAL — beat-1 "de câte ori poți tăia, pentru 6 elemente?" opts [2,3,4,6] is ambiguous (literal cuts n−1=5 absent; intended depth ⌈log₂6⌉=3); (2) the audit found it but a refute-biased verifier killed it; (3) fix = computational test for the correctness leg, then re-run. Deeper: is LLM-audit-of-hand-written-donor-content the right path, or should correctness be deterministic + corpus-grounded authoring?
**Project context:** jarvis-kotlin AI tutor; donor mined for look + 5-beat form only (content won't ship — "zero free-form" invariant); overseer-check trust = user demands RED→GREEN demonstration; no paid APIs; token-frugality; PM-delegates-to-workflows.
**Timestamp:** 2026-06-18 (epoch 1781781668)

**Artifact-grounded fact check (judge, verified against donor directly):** beat-1 = "Dacă tai mereu vectorul în jumătate până rămân **elemente singure** — de câte ori crezi că poți **tăia**, pentru 6 elemente?" opts [2,3,4,6]. beat-5 = "câte **niveluri** de împărțire... de câte ori tai 8 în jumătate până **ajungi la 1**?" opts [2,3,4,8]. Beat-1's "elemente singure" (plural, whole-tree) + "de câte ori tăia" (cut-count) ⇒ 5 is a legitimate reading and is ABSENT from options ⇒ defective. Beat-5's "niveluri" + "ajungi la 1" (singular chain) ⇒ unambiguous depth 3.

---

## 🔴 Devil's Advocate
STANCE: REJECT. Argues beat-1's "vectorul" (singular) halved-to-singletons is unambiguous depth (=3), the "5" reading imports a tree-wide split-count the sentence never invokes, the verifier was correct (3 agents independently is_real=false), and the proposed computational test would manufacture false positives on every D&C question; deeper, INV-5.5 means the donor content never ships so the checker is calibrated against throwaway. KEY CONCERN: the whole path rests on a misread; real fix = deterministic correctness over corpus-grounded authoring.

## 📚 Domain Expert
STANCE: REJECT. Same factual inversion; calls the refute-biased verifier the standard LLM-as-judge confirmation-bias-inversion mitigation working correctly; says forcing the finding through is reward-hacking the auditor. KEY CONCERN: real defect class (ambiguous wording where two valid quantities differ) belongs in a deterministic authoring-time invariant binding each numeric question to a named formula + computed key — never post-hoc LLM re-judging of disposable content. (Note: itself observed the verifier "imported Beat-5's clean reading back into Beat-1's looser wording" yet still sided with it.)

## ⚙️ Pragmatist
STANCE: CONDITIONAL. Re-derived independently: beat-1 IS ambiguous (5 vs 3, log₂6 non-integer, 5 not an option); beat-5 fine (subtitle pins single chain). Narrow fix (deterministic numeric checker: compute n−1 and ⌈log₂n⌉, flag divergence when wording doesn't pin one) is a few dozen lines, ms runtime, would've flagged beat-1 without 28 agents / 1.57M tokens. KEY CONCERN: don't fund a general LLM-audit-of-prose project on throwaway donor content; spend on (a) deterministic numeric-claim checker, (b) corpus-grounded authoring; reserve the LLM overseer for non-decidable pedagogy/UX (the 6 real findings).

## 🧱 First Principles
STANCE: REJECT (factual) but reframes. Claims beat-1 = repeated halving of one quantity = ⌈log₂n⌉, "5" imported from outside, refuted 6-to-1. KEY CONCERN (the strongest strategic contribution): move correctness UPSTREAM — generate the question + its checkable answer together from the verified KC (n, operation-semantics, expected count) with a deterministic author-time gate, so "ambiguous question" becomes structurally impossible rather than something an LLM might catch later.

## ⚠️ Risk Analyst
STANCE: CONDITIONAL. Confirms bug real + planted ambiguity (5 absent from options). KEY CONCERN [CRITICAL]: the "caught=true" self-report is a FALSE positive on the exact metric the user conditioned trust on — a confidently-wrong verifier overrode a correct catch and the harness rebranded it a pass. An LLM judge is structurally unfit as the SOLE correctness oracle (confidence uncorrelated with correctness; refute-bias is asymmetric — can only kill findings; silent suppression looks like a clean bill). Fix necessary-but-insufficient: computational test covers only math-decidable findings; the other 6 (deferred feedback, dead CTA, wrong-answer-unlocks, color collisions) still need a NEUTRAL (not refute) LLM verifier + human escalation on blind-vs-verifier disagreement.

---

## Sanity Check
SANITY Devil's Advocate: FLAG — factual claim ("bug not real, verifier correct") rests on reading beat-1 as "single chain to 1," but the artifact says "elemente singure" (plural, whole-tree) + "de câte ori tăia" (cut-count); 5 is a valid reading absent from options. Reproduces the very refute-bias under review. (Strategy half — deterministic over corpus — is sound.)
SANITY Domain Expert: FLAG — same factual inversion; internally inconsistent (notes the verifier imported beat-5's reading yet endorses it). Strategy (deterministic authoring-time invariant) is excellent.
SANITY Pragmatist: PASS — clean; independent re-derivation correct, fix correctly scoped.
SANITY First Principles: FLAG — "refuted 6-to-1" cites the biased verifiers as authority (circular: the output under review used as evidence it's correct). Reframe (upstream deterministic author-time generation) is the best strategic contribution.
SANITY Risk Analyst: PASS — clean; correctly identifies the critical trust-failure mode and the limits of the proposed fix.

---

## Judge

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: FLAWED

CORE FINDING:
The read is RIGHT, the fix is MIS-AIMED. Alex's bug is real — verified against the donor directly: beat-1's "elemente singure" (plural) + "de câte ori tăia" makes 5 (=n−1) a legitimate answer that is absent from the options, while beat-5's "niveluri / ajungi la 1" is unambiguous depth. The 3 REJECT members reproduced the EXACT refute-bias under review (importing beat-5's clean single-chain wording into beat-1), so the council itself re-demonstrated the failure mode live — which is the strongest possible evidence that the fix must be DETERMINISTIC, not "a better LLM prompt re-run."

AGENT CONSENSUS: 3 REJECT (all flagged), 2 CONDITIONAL (both clean). Judge overrides the reject-majority on "bug not real" — their reasoning is the bias under audit; the 2 clean agents + independent artifact re-derivation settle it.

KEY ISSUES:
- The correctness mechanism must be DETERMINISTIC (compute n−1 and ⌈log₂n⌉; flag divergence unless wording pins one), NOT a re-run LLM verifier. Cheap, ms, zero false-negative-by-rationalization.
- Move correctness UPSTREAM: corpus-grounded authoring generates question + computed answer + options from one verified source, with a deterministic author-time gate — "ambiguous numeric question" becomes structurally impossible. Auditing throwaway donor content is low-leverage.
- The LLM judge is unfit as the SOLE oracle; it stays ONLY for non-decidable pedagogy/UX findings, with a NEUTRAL verifier + human escalation on disagreement. The self-reported "caught=true" must be derived from per-finding verdicts, never separately asserted.

RECOMMENDED PATH:
Do NOT "harden the LLM correctness leg + re-run on the donor." Instead: (a) build a small deterministic numeric-claim checker; (b) wire correctness into the corpus-grounded AUTHORING path (answer computed from the KC), not post-hoc audits; (c) keep the LLM audit only for non-decidable pedagogy/UX with a neutral verifier + human escalation; (d) treat the donor's confirmed bugs (dead CTA, wrong-answer-unlocks, deferred feedback) as a failure taxonomy to design against, not content to QA. Fix the donor bugs only insofar as they inform the authored form.

CONFIDENCE: 9
Settled by direct artifact verification; the only thing that would move it is discovering the authored corpus pipeline can't bind a computed numeric key per question — unlikely given the KC schema already carries source spans + formulas.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output saved to: .claude/council-cache/council-1781781668.md
