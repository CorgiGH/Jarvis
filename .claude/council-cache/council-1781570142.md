# Council review — 1781570142 (UNIT 1 approval)

**Problem:** Approve (or not) the shipped UNIT 1 work — commit `01763c6` on main: frame-conjunction figure gate CI-wired + hard-blocking; permanent vitest liveness self-test; pure core extracted without breaking the detector; live RED→GREEN CI demo.
**Type:** Approval review. All 5 agents OPENED the committed artifacts AND re-verified the 3 CI runs (27585267302 main GREEN, 27585406595 PR RED, 27585539848 revert GREEN) THEMSELVES — not from the PM's prose.
**Timestamp:** 2026-06-16 (epoch 1781570142)

---

## 🔴 Devil's Advocate — APPROVE
All four claims hold against real artifacts. Seam: detector bodies pure-deleted from the gate, byte-identical in core.mjs, run()/captureFigure/route-walking untouched, no leftover dupes. Self-test 7/7; figC leg genuinely runs the per-pixel counting loop (note lacks "reflow", changedPx ≥80 AND < w*h sentinel). CI job runs under `bash -eo pipefail` with no `|| true`/continue-on-error; 52 live pairs in the RED log prove the page rendered (not blank). Demo: success→failure→success on distinct commits; RED log names FROZEN pairs 0→1,1→2,2→3 changedPx=0 on BOTH skins.
KEY CONCERN: could not confirm GitHub branch-protection lists `frame-gate` as a *required* check — hard-failing within the workflow (verified) but a future PR could merge while it's red. Main push run is overall-red from unrelated pre-existing failures (designMdSync drift, lesson-gates).

## 📚 Domain Expert — APPROVE
Extraction verifiably clean (diffPng/decodePng/unfilter byte-identical, only `export` added; thresholds verbatim; gate imports + uses classifyPair at the verdict site). Self-test follows trace-match.self.test.ts precedent, covers the conjunction properly (frozen=FAIL, moved=live via reflow AND per-pixel, hold=exempt, oracle pin, seed-sync 30) — 7/7. frame-gate job correctly modeled on lesson-gates (vite :5173, 60×2s curl, hard-blocking). Registry flip honest (backingGate names two real artifacts, accepted-debt recorded; checker exits 0, zero UNMAPPED). CI defect run shows frame-gate=failure.
KEY CONCERN: none — the only residual is the self-disclosed accepted-debt (captureFigure route-walk covered only by the live job), an honest scoping choice.

## ⚙️ Pragmatist — APPROVE
frame-gate job GREEN, ~94s wall (gate step 50s, vite-ready 2s), nowhere near the 20-min timeout. Readiness loop bounded (60×2s) + explicit exit 1 + log dump; gate independently re-probes. Self-test browserless/deterministic/fast (7 tests ~156ms CI). The two other RED jobs in that run (frontend vitest designMdSync drift, lesson-gates) are unrelated — the self-test passes inside vitest and the frame-gate job itself is green.
KEY CONCERN: two soft rot vectors, neither blocking — vite `port:5173` lacks `strictPort` (runner-improbable collision → silent 5174 rollover → dead probe); seed-sync pin guards COUNT only, blind to same-length content edits. Follow-up: `strictPort:true` + a content-hash pin.

## 🧱 First Principles — APPROVE
The two-half split is drawn HONESTLY: registry marks LIVE-GREEN but records coverageNote NAMED ACCEPTED-DEBT that captureFigure is exercised only by the live job; the unit test matches that boundary exactly and does NOT over-claim. The route-walking half IS covered by a real hard-blocking job. RED→GREEN chain genuinely proves broken⇒RED, fixed⇒GREEN with the SAME gate (RED job 81554521812: 6 HARD FAILS, frozen 0→1/1→2/2→3, changedPx=0, stateFields=[runs,frontOf], both skins; revert + main jobs GREEN 58/0). The run-level red is unrelated pre-existing failures; the frame-gate JOB is success in both. The defect is genuine renderer logic (computePlacement ignoring st.runs), not contrived.
KEY CONCERN: the captureFigure half has no deterministic in-process test — a subtle callout-crop regression (re-admitting the "liveness alibi") would not be caught by the permanent self-test; correctly disclosed as accepted-debt but an uncovered seam, not a closed one.

## ⚠️ Risk Analyst — CONDITIONAL
Core byte-identical (classifyPair maps the old branch order exactly, no off-by-one, strict `<` against 80). 7-leg self-test passes; figC forces the per-pixel path figB's reflow leg can't. Both demo runs reproduce from CI logs — RED is a real freeze (vite HTTP 200, 6 pairs changedPx=0/maxΔ=0, full GATE RED + Exit 1), not a boot failure. No landmine files swept in (commit touched only the 11 intended files); self-test in the vitest include glob.
KEY CONCERN: `main` has NO branch protection (`/branches/main/protection` → 404) — "hard-blocking" holds at the JOB level (no `|| true`/continue-on-error) but the job is NOT a required status check, so a red frame-gate does not actually block a merge/push to main. Add it as a required check (or accept as named debt) for the claim to be true end-to-end. Secondary nits: readiness loop tolerates 4xx/5xx (but the gate's own probe.ok preflight still bites — not a false-GREEN); no strictPort.

---

## Sanity Check
SANITY Devil's Advocate: PASS — byte-diffed the seam, fetched the RED job log, named the branch-protection caveat correctly.
SANITY Domain Expert: PASS — precedent comparison + registry checker run grounded.
SANITY Pragmatist: PASS — pulled real CI timings from the run JSON; rot vectors concrete.
SANITY First Principles: PASS — sharpest on the honest two-half split; verified per-job conclusions vs run-level red.
SANITY Risk Analyst: PASS — branch-protection 404 independently confirmed; the one CONDITIONAL is an enforcement-layer point, not a defect in the work.

---

## Judge
VERDICT: APPROVED (4 APPROVE / 1 CONDITIONAL). UNIT 1 is genuinely shipped and works.
CONFIDENCE: 9

The four claims are all verified against reality by independent agents: (1) the core extraction is byte-identical and the detector still runs GREEN 0/58/0; (2) the permanent self-test genuinely proves liveness of the decode+diff+verdict path (frozen=FAIL, per-pixel moved=live via figC, reflow via figB, hold=exempt, oracle pin, seed-sync) — not synthetic/trivia; (3) the frame-gate CI job is hard-blocking at the job level (no soft-fail) and runs the gate as-is over the real renderer; (4) the live RED→GREEN demo is real and RED for the documented reason (frozen pairs 0→1,1→2,2→3, changedPx=0, both skins).

The ONE shared material caveat (Devil's Advocate + Risk Analyst): the gate is hard-failing WITHIN the workflow but `main` is unprotected, so frame-gate is not a *required status check* — a future red could be merged/pushed without enforcement. This is an enforcement-layer gap, not a defect in the delivered work, and in a solo direct-to-main repo its stakes are low (the value delivered is that the run goes visibly red).

FOLLOW-UPS (none block approval):
1. (Decision for Alex) Add `frame-gate` as a required status check via branch protection on main — closes the enforcement gap, but would also gate Alex's own direct pushes on CI. Otherwise accept as named debt.
2. (Cheap hardening) `strictPort: true` in vite.config.ts so a port collision fails loud instead of rolling to 5174.
3. (Cheap hardening) Upgrade the seed-sync pin from length-only to a content-hash of the renderer data_json (catch same-length content edits).
4. (Already named debt) The captureFigure screenshot/callout-crop half remains covered only by the live job; a future per-family generalization should add a deterministic crop-regression guard.

Output saved to: .claude/council-cache/council-1781570142.md
