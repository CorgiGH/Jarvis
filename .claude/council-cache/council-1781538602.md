# Council review — 1781538602 (evidence-grounded; overlap in the divide-fixed merge figure)

**Problem:** After the divide-split fix, Alex reports elements OVERLAP on the live merge figure. Render ALL frames, locate it, escalate to a council grounded in the real renders (his standing rule: give the council the artifacts, not my word).
**Evidence (PM-verified):** rendered all 30 frames both skins + contact sheets. PM opened r-contact-bars.png, r-contact-boxes.png, r-af-bars-13.png: **BARS skin (merge-compare) overlaps on every 2-line "take" callout** — the callout chip bottom occludes the "DOUĂ LISTE SORTATE → SE CONTOPESC" run-band label (frames 5,8,10,13,16,20,22,24,26,28). **BOXES skin clean all 30.** Divide frames 0-3 clean in both.
**Timestamp:** 2026-06-15T (epoch 1781538602) UTC

## 5 verdicts (all opened the renders + code)
- 🔴 **Devil's Advocate — REJECT:** static geometry collision — bars chip top hardcoded `boxTop=6` grows down by line count; run-band label hardcoded `RUN_BAND_BASE - BARS_MAX_H - 24 = 48`; nothing couples chip height to label y; 2-line chip bottom ≈50-56 > 48. Boxes clears by luck (label at y≈71). PLUS: sort-merge in ZERO e2e specs; assertNoClip only checks interactive elements — blind to SVG text-on-text.
- 📚 **Domain Expert — REJECT:** violates the family's own "reserve space ⇒ no-clip by construction" contract (it measures WIDTH via measureLabelWidth but never feeds chip HEIGHT into the label y). The no-clip gate is a DOM-bbox check; every viz token is SVG → the gate has NEVER been able to catch an in-SVG overlap. Needs registry-driven per-family specs over every route × skin × frame.
- ⚙️ **Pragmatist — CONDITIONAL:** cheap local fix (compute chip floor from layoutCallout's boxH, set label y = max(current, chipBottom + ~12)) kills the whole class. But insufficient alone — the gate gap is the real defect (no sort-merge spec; assertNoClip SVG-blind). Make the family registry the source of truth so a prototype can't ship un-gated.
- 🧱 **First Principles — CONDITIONAL:** pure layout-math bug in the bars top band, NOT a viewBox-room problem (divide frames clean, 432px ample). Derive label y from the computed callout boxH. The deeper hole: assertNoClip skips SVG namespace + only compares interactive elements; assertLegibility checks only contrast/truncation — an SVG chip over an SVG label is invisible to BOTH gates by construction.
- ⚠️ **Risk Analyst — REJECT (CRITICAL):** the no-overlap gate is SVG-blind by construction → the entire merge figure (and every SVG-text family) has ZERO machine coverage for text-on-text. AND the MOTION (conjunction) gate DELIBERATELY CROPS OUT the callout band (to defeat the liveness alibi) — so it is engineered to ignore exactly the region this overlap lives in. Three-way blind seam. Point-fix closes one instance; the class stays open.

## Sanity Check
All 5 PASS — each listed the renders + code files opened, confirmed the overlap independently, root-caused to the same constants, stayed in lane. 3 REJECT + 2 CONDITIONAL converge on identical fix + gate corrections; 0 flagged.

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT — FLAWED (real overlap; fix is clear; the GATE GAP is the actual defect)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CORE FINDING: The overlap is real and deterministic (bars 2-line take callouts occlude the run-band label) — a layout-math bug: the callout chip grows downward unbounded while the run-band label y is a hardcoded constant that assumed a 1-line chip; nothing couples them. Boxes clears only by luck. The fix is trivial-correct: derive the label y from the chip's ACTUAL measured height (the family already measures width for no-clip — it just never fed height into the label). BUT the deeper finding (all 5): this shipped because BOTH gates are blind here — the no-clip `assertNoClip` is SVG-blind by construction (skips SVG namespace, only checks interactive-element overlap), sort-merge has NO no-clip spec at all, no gate enforces "every frame / both skins," and the motion gate deliberately crops out the callout band. "Motion-green + a few eyeballed frames" = the SAME partial-check trap as the original, rotated to the vertical (SVG-text) axis.

CONSENSUS: 3 REJECT, 2 CONDITIONAL — 0 flagged.

KEY ISSUES:
- LAYOUT FIX (do first): couple the bars (and boxes) run-band label y to the computed callout `boxH` — label y = max(current baseline, chipBottom + min-gap) — reserve-space-by-construction. No viewBox change.
- assertNoClip IS SVG-BLIND (CRITICAL): it `continue`s on SVG-namespace text-clip and only compares interactive elements for overlap → it has never caught, and cannot catch, in-SVG text-on-text. Add an SVG `<text>`/`<rect>` pairwise bbox-intersection check (getBBox / data-cx/cy stamps already emitted) inside the figure root.
- COVERAGE GAP (HIGH): sort-merge is in ZERO no-clip specs; the prototype routes are gate-uncovered; existing specs step a subset of frames. Need a sort-merge no-clip spec over BOTH skins × ALL 30 frames at 1536×648 AND ×730.
- REGISTRY-DRIVEN ENFORCEMENT (HIGH): a parametrized spec iterating EVERY registered family × skin/variant × frame, so a new figure/prototype cannot ship without inheriting BOTH the motion AND the (now SVG-aware) overlap gate. The sort-merge family existing un-gated is what let motion-only ship.

RECOMMENDED PATH:
1. Fix the bars label-y coupling (and audit boxes for the same latent reliance-on-luck) → re-render all 30 frames both skins, PM eyeballs → no overlap.
2. Extend assertNoClip with an SVG text/rect bbox-intersection pass; add a sort-merge no-clip spec (both skins, all frames, both heights); assert it goes RED on the current overlap, GREEN after the fix.
3. Make the no-clip + conjunction gates registry-driven over every family×skin×frame, incl. prototype routes — close the seam where the motion gate crops the callout and the overlap gate didn't run.
4. Lock: the overlap gate must SEE SVG, run every frame/skin/prototype — add to active-constraints rule 9 (it already says "both gates"; make explicit the overlap gate is SVG-aware + all-frames).

CONFIDENCE: 9/10 — overlap + root cause + fix verified against code and pixels; only open question is whether other families harbor the same latent chip-vs-label luck (the registry-driven gate would surface it).
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
