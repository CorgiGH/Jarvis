# Council review — 1781567474

**Problem:** UNIT 1 — add a PERMANENT committed seeded-wrong self-test proving the frame-conjunction figure gate (`tutor-web/tools/frame-conjunction-gate.mjs`) is alive (closes INV-9.4 liveness gap), runnable in CI. Self-test must feed a deliberately FROZEN figure over a state-that-moved and assert the gate HARD-FAILS.
**Proposed approach (the fork):** A = extract pure verdict math into `frame-conjunction-core.mjs`, vitest the threshold rule (route-walking untouched). B = keep gate untouched, CI git-applies a `divide-defect.patch`, runs the gate, asserts exit 1, reverts, asserts exit 0 (verifier-deps-loud-red idiom).
**Project context:** jarvis-kotlin tutor; figure-overseer trust thread; prior strategy council warned "don't break the only working detector"; baseline proven GREEN today (58 live pairs / 0 fails / exit 0) against live vite :5173.
**Timestamp:** 2026-06-16 (epoch 1781567474)

All five agents OPENED the six real artifacts before voting (grounded, not prose-fed).

---

## 🔴 Devil's Advocate
STANCE: CONDITIONAL · PICK: C
Verified no `divide-defect.patch` exists (`git ls-files`) — kill shot for B. Verified `computePlacement` (SortMergeFamily.tsx:460-474) already reads `st.runs` → divide defect is ALREADY FIXED → running the gate against today's renderer is GREEN not RED. B would re-break production source every CI run (one bad revert ships the bug) + relies on the exact HMR flake the gate's SETTLE_MS/networkidle plumbing exists to fight. A's instinct (pure core, matches trace-match.self.test.ts + seeded-clip-legs precedent) is right, but A's "frozen leg against live renderer" inherits the false premise — must feed the core a SYNTHETIC/real frozen pair over `stateDelta(MERGE_STEPS[0],[1])` (changed:true, fields runs/frontOf) and assert `changedPx<MEANINGFUL_PX => FAIL` (gate 196-197).
KEY CONCERN: premise "gate goes RED on current renderer" is FALSE — defect already fixed; any liveness proof driven from live figure pixels won't fire.

## 📚 Domain Expert
STANCE: CONDITIONAL · PICK: C
B disqualified: needs nonexistent patch, depends on live :5173 + HMR, and verifier-deps-loud-red (test.yml:174-216) is hermetic env-var with NO browser/HMR/patch-revert — so B does NOT actually mirror it. A follows repo precedent and the math IS cleanly extractable (diffPng/decodePng/unfilter 242-343 pure; FAIL = single conjunction `sd.changed && changedPx<MEANINGFUL_PX` line 196). BUT A with SYNTHETIC changedPx tests a hollow core (proves only `80<80` false) — never exercises decodePng/diffPng/PIXEL_EPS where calibration rot hides. Fix: commit two REAL cropped PNG fixtures (frozen pair + moved pair), round-trip through the EXTRACTED diffPng+conjunction.
KEY CONCERN: neither closes the route-walking half (captureFigure callout-crop 142-177, scrubber-stepping) — vitest can't reach it; must record that as named accepted-debt covered only by the live gate run, not let a green core imply whole-gate self-proven.

## ⚙️ Pragmatist
STANCE: CONDITIONAL · PICK: A
A decisively cheaper to keep green over a year: one-time refactor against a stable PNG/diff contract, runs in ms in the existing `frontend` vitest job, mirrors trace-match.self.test.ts. B's `.patch` is guaranteed rot (computePlacement 458-468 is live churning code — patch anchored near 460 fails-to-apply next edit; a silently-non-applying self-test is worse than none) + needs booted vite + HMR + two full gate runs (2 skins × ~30 pairs × 650ms ≈ minutes). CONDITION: A proves math not renderer-wiring — pair with a thin smoke that `MERGE_STEPS.length` matches the rendered scrubber step count, else risks the Slice-1 ghost-green (bundled-but-unmounted).
KEY CONCERN: A verifies math not conjunction-against-real-renderer; without seed-sync smoke INV-9.4 only half-proven.

## 🧱 First Principles
STANCE: CONDITIONAL · PICK: C
INV-9.4's two halves have asymmetric trust. STATE half pure + already testable (stateDelta seed.mjs:32-42). Load-bearing fragile half = FIGURE: captureFigure (142-177) callout-band crop heuristic (151-164, 22% fallback) → screenshot → decodePng/diffPng. A silent break there masks a real freeze = false-GREEN = exactly what the invariant exists to prevent; synthetic changedPx proves none of it (pins line 196, least likely to rot). C: refactor gate into a thin run() shell over exported `evaluatePairs({getFigurePng,steps})`, drive it (1) committed PNG-fixture pair (frozen→RED, moved→GREEN) through the WHOLE decode/diff path, no browser; AND (2) one full-pipeline run that INJECTS the freeze at render-time (seeded-clip-legs page.evaluate pattern, since the renderer is no longer natively defective) → exit 1 then revert → exit 0. A is a strict subset of (1).
KEY CONCERN: the seed fixture's premise is stale — divide defect NOT present in current computePlacement; any test built on "renderer natively frozen on frames 0→3" won't go RED for the named reason. Liveness MUST inject the freeze or feed a committed frozen-PNG pair.

## ⚠️ Risk Analyst
STANCE: CONDITIONAL · PICK: C
Pure-A is a FALSE-GREEN factory: pins the trivial `changedPx<MEANINGFUL_PX`/`sd.changed`, proves nothing about liveness — the whole bug class lives in captureFigure (callout-crop 151-164, SVG boundingBox, goToFrame React-setter, hand-rolled decodePng/unfilter). If captureFigure silently returns identical buffers the gate goes GREEN-while-blind and a threshold-only vitest never notices. B genuinely exercises the pipeline but is the flaky-RED/dirty-tree hazard: no vite/HMR job exists in test.yml today (lesson-gates boots Ktor+networkidle, not vite dev); git apply rots when computePlacement (448-474) is touched; mid-CI reverse-apply failure leaves tree dirty. Borrow the repo's own precedent (seeded-clip-legs runtime injection): commit a self-test feeding diffPng a FROZEN buffer over a MOVED stateDelta pair, assert HARD-FAIL through the real verdict path; keep the live gate as the route-walk arm.
KEY CONCERN: threshold-only extraction pins the part that cannot break, leaves the part that DOES break (capture/crop/decode) unguarded — must drive a real frozen-PNG-vs-moved-state pair through diffPng+verdict.

---

## Sanity Check
SANITY Devil's Advocate: PASS — verified no patch + renderer-fixed against real lines; sound.
SANITY Domain Expert: PASS — hollow-core critique + fixture-PNG recommendation grounded; verifier-deps-loud-red hermeticity correctly characterized.
SANITY Pragmatist: PASS — picked A but with a seed-sync condition that folds into C; under-weights the synthetic-core false-green that 3 peers raised (note, not flag — reasoning itself is valid).
SANITY First Principles: PASS — asymmetric-trust analysis is the sharpest; C well-reasoned.
SANITY Risk Analyst: PASS — false-green-factory consistent with FP; failure modes concrete and real.

---

## Judge
VERDICT: REJECT Option B. ADOPT Option C (= Option A's core-extraction seam, hardened).
CONFIDENCE: 9

RULING:
1. **Option B is dead** (5/5). No `divide-defect.patch` exists; it would re-break production source every CI run; it rots the moment `computePlacement` near line 460 is edited; it depends on a vite-dev+HMR settle that has NO CI job today and is the exact flake class the gate fights; a mid-CI reverse-apply failure dirties the tree. It also does NOT mirror verifier-deps-loud-red (that idiom is hermetic env-var, no browser/HMR/patch).
2. **Pure Option A is insufficient** (3/5 + judge): feeding SYNTHETIC `changedPx` integers pins only `80<80` — the one line that cannot rot — and leaves the actual bug-class surface (captureFigure callout-crop + decodePng/diffPng) a false-green blind spot.
3. **The premise was stale and the council caught it** (load-bearing): the divide defect is ALREADY FIXED in the live renderer (SortMergeFamily.tsx:460-474 reads st.runs) — confirmed by the PM's own baseline run (GREEN 58/0). The seed header's "MUST go RED on current renderer" is stale.

ADOPTED DESIGN (C):
- Extract pure core `frame-conjunction-core.mjs` (decodePng/unfilter/diffPng + the conjunction verdict `evaluatePair`/`evaluatePairs` + PIXEL_EPS/MEANINGFUL_PX); gate imports it, route-walking 100% untouched; standalone gate re-run after to prove byte-identical GREEN (58 live/0 fail).
- PERMANENT vitest (runs in existing `frontend` job, no browser) feeds **committed REAL cropped PNG fixture pairs** through the extracted decode+diff path: (a) FROZEN pair (byte-identical figure crop) over a state-that-moved `stateDelta(MERGE_STEPS[a],[b]).changed===true` → assert HARD-FAIL; (b) MOVED pair → assert live; (c) state-UNCHANGED pair + identical figures → assert exempt hold. Also pin `stateDelta` directly (a known-changed pair true, a known-equal pair false).
- Seed-sync smoke (Pragmatist): assert `MERGE_STEPS.length` equals the rendered scrubber's step count so the seed can't silently desync from SortMergeFamily (anti-ghost-green).
- **Named accepted-debt** in the gate registry: the screenshot+callout-crop+scrubber route-walking half is NOT vitest-covered; it is covered by the new end-to-end CI frame-gate job running the real gate against the real renderer. Record explicitly so a green core test never implies whole-gate self-proven.
- End-to-end CI `frame-gate` job (boot vite :5173 → curl-wait → run gate AS-IS, hard-block, no `|| true`) = the route-walk live arm.
- RED→GREEN trust demo (separate, one-time, NOT a committed patch): on a throwaway branch reintroduce the real computePlacement divide defect → CI frame-gate RED naming pairs 0→1,1→2,2→3 → revert → GREEN; capture both real CI logs for Alex.
- OPTIONAL follow-up (First Principles/Risk, defer unless cheap): a runtime-injection route-walk drill (seeded-clip-legs page.evaluate pinning figure geometry while stepping the scrubber → assert RED) to prove the capture wiring still feeds the pipeline without re-breaking source.

Output saved to: .claude/council-cache/council-1781567474.md
