# Council review — 1781561962

**Problem:** Alex challenged the AI's "PA-first is faster than OOP-first" recommendation: "We're building a subject-AGNOSTIC machine; PA's lessons are hand-authored and get reprocessed anyway, so PA's content is no real head-start — the only real subject-dependent cost is the missing OOP structure-figure-family." He asked: "am I tripping?"

**Project context:** single-user RO tutor; scope = build the subject-agnostic digestion machine, deploy ONE subject end-to-end first; owner picked OOP-first; PA has 6 hand-authored trace KCs + working trace-figure families; OOP has 0 content + needs a net-new static-structure figure family (prior council 1781561170, confidence 9).

**Timestamp:** epoch 1781561962. **Grounding:** all 5 advisors independently opened the v2 plan (A1/E/F/GATE-OF-GATES/SPIKE), the content tree, the families/registry, the prior council — verified (spec §5.2 = exactly 6 trace families, zero OOP/UML/isomorphism hits; POO/SO-RC = 0 KCs; ALO=1, PS has viz+problem; gates NOT CI-wired; PA concept_types all fit existing families).

---

## 🔴 Devil's Advocate — CONDITIONAL — VERDICT ON ALEX: PARTLY RIGHT
Alex's load-bearing half is grounded-WRONG: Phase A1 retires hand-authored keyframe JSON for figure STATE GEOMETRY ONLY; callout prose, KC explanations, beats, misconceptions stay generated/authored content. And the plan's near-term path (JUDGE "semi-digest the ~5 subjects through gated templates"; SPIKE fallback "semi-manual authoring of the ~7 real KCs") does NOT assume a machine-only firehose — PA's KCs + figures + sources + misconceptions are a real reusable corpus OOP lacks. (Also: AI over-claimed — ALO and PS are NOT empty.) KEY CONCERN: Alex treats "geometry regenerated by A1" as dissolving the WHOLE PA head-start, but only geometry is reprocessed; the gated KC/pedagogy corpus + a fitting figure FAMILY survive — exactly what OOP-first must author from zero PLUS a net-new isomorphism-gated family that doesn't exist. OOP-first stacks two unsolved firsts; PA-first stacks zero.
Q1 illusory ONLY for figure geometry; source-grounded KC text/beats/misconceptions/CHECK + matching trace-family survive. Q2 delta is bigger than "just the family": OOP = 0 KCs + net-new family + no concept_type fit. Q3 real ordering-cost not extra work; OOP-first forces it onto the critical path. Q4 AI's conclusion still holds: PA end-to-end → deploy first, then OOP.

## 📚 Domain Expert — CONDITIONAL — VERDICT ON ALEX: PARTLY RIGHT
Established practice = prove a generic engine on the EASIEST representative input first (walking skeleton / path of least resistance) to de-risk the ENGINE. PA = easiest (real KCs + trace families + a real decidable oracle). OOP = hardest (0 KCs + net-new static-structure family, isomorphism-gated, no oracle/fixtures/precedent). OOP-first inverts the de-risk order: you debug the generic pipeline AND invent+gate a new figure paradigm AND author a subject from zero at once, with no baseline to diff. KEY CONCERN: OOP-first couples 3 independent risks PA-first separates — (1) engine works, (2) never-built structure family + isomorphism oracle works, (3) free-tier generator clears the bars. PA-first isolates #1 with #2 solved + 6 KCs for #3. Q2 RIGHT but incomplete: PA prose IS superseded, but the figure-ORACLE + trace families + a known-good diff baseline are the real (non-content) head-start. Q4 PA-first faster-to-engine-PROOF; if the true goal is OOP-deploy specifically, the family is on the critical path either way → it's sequencing not speed: prove on PA, then build OOP family + author POO behind the proven engine.

## ⚙️ Pragmatist — CONDITIONAL — VERDICT ON ALEX: PARTLY RIGHT
Alex is structurally right: the machine is subject-agnostic and PA's 6 KCs get reprocessed by E/F → PA hands you no finished-lessons shortcut into the real machine. BUT PA's concept_types (definition-taxonomy/proof/formula-application/code-trace) all fit the 6 EXISTING families; OOP static structure is structurally homeless → net-new isomorphism-gated family that doesn't exist. PA-first is faster ONLY for the bring-up/debug window (engine code, 4 graders, 6 families, trust-net, 7 KCs to gate against, exam corpus — all PA/ALO-shaped), but doesn't reduce total work to 100% (OOP family needed regardless). KEY CONCERN: the OOP family+gate is real, unbuilt, unvalidated; OOP-first puts the FIRST shipped figures on it with zero precedent → green-badge-over-oracle-less-family risk. Q2 the only "PA shortcut" = shipping hand lessons WITHOUT the engine, which the plan forbids; PA's real advantage = content to bring up + gate the engine AGAINST. Q4 PA-first as bring-up/debug subject, then OOP behind a working pipeline; if owner-priority hard-requires OOP visible first, it's "pick by priority" WITH the OOP-family-before-any-POO-VERIFIED bar enforced, accepting slower/riskier first deploy.

## 🧱 First Principles — CONDITIONAL — VERDICT ON ALEX: PARTLY RIGHT
The deliverable splits in two: the MACHINE (ingest→classify→digest→generate→gate→render→track) is genuinely subject-agnostic — for machine-readiness "which subject first" is IRRELEVANT, Alex is right. But the machine's OUTPUT is gated by figure families, and families are NOT subject-agnostic: all 6 are algorithm-execution-trace renderers. PA content is 100% trace-shaped + already authored → zero net-new families. OOP is static-relational → NO family can render it + zero KCs. So "only the missing OOP family" is right in KIND but wrong in MAGNITUDE: that one family is a new figure ONTOLOGY (trace vs structure) + a new ORACLE (isomorphism not frame-diff) + the first content corpus. PA-first is cheaper-to-first-PROOF; OOP-first is more VALUABLE to Alex — different axes. KEY CONCERN: "PA content reprocessed ⇒ no head-start" hides that PA's head-start isn't the text — it's that PA exercises ZERO net-new families + the oracle's whole validation history is trace-shaped; OOP-first runs the machine's first real output through a family that doesn't exist, an oracle never built, no self-test. Q1 MACHINE readiness is subject-independent; CONTENT readiness (families + first corpus) is the only subject-shaped cost. Q4 "PA-vs-OOP first" is MOSTLY A DISTRACTION — the real gate = "machine + all REQUIRED families exist + oracle-gated (incl the missing static-structure family, barred-from-VERIFIED until self-test)"; prove the machine on the cheapest trace corpus (PA), THEN deploy OOP for value — the two decisions are ORTHOGONAL, don't collapse them.

## ⚠️ Risk Analyst — CONDITIONAL — VERDICT ON ALEX: PARTLY RIGHT
Spec §5.2 = exactly 6 trace/state families; zero hits for inheritance/UML/isomorphism. POO 0 KCs; gates NOT CI-wired (no workflow references them — L1 enforced locally for ONE figure today); free-tier generator capability UNMEASURED (SPIKE/residual #3). OOP-first proves a brand-new engine against FOUR unproven variables at once (net-new figure paradigm + zero content + first-time extraction + unmeasured generator) = the breadth-before-oracle trap + the council-1781561170 CRITICAL green-badge risk. PA-first is lower-risk because PA fits built+about-to-be-gated families → a first failure isolates to engine-vs-content with the figure paradigm CONSTANT. But Alex is right the speed advantage is real-yet-MODEST (engine subject-agnostic, PA content reprocessed); the dominant factor is FAILURE-ISOLATION, not speed. KEY CONCERN ranked: CRITICAL green-badge over the structurally-homeless OOP family (first thing shipped, zero precedent); HIGH tangled-failure across 4 uninstrumented variables + unmeasured spike; MEDIUM gates not CI-wired (L1 local-only today). Q4 lowest-risk: Phase 0 (CI-wire+generalize gates) → A/B/C EARLY-green on PA/ALO KCs → run the SPIKE → first honest deploy on PA → build OOP structure-family + author POO behind the proven oracle. TOP MITIGATION: structurally BAR any family lacking its own oracle + seeded-wrong self-test from VERIFIED — never silent-green.

---

## Sanity Check
All five PASS, grounded independently (spec §5.2 family count, content census, A1 scope, gates-not-CI-wired all verified). One cross-correction the judge must carry: Devil's Advocate caught the AI's overstatement that "PA is the only subject with content" — ALO has 1 KC + viz + problem, PS has a viz + problem; PA is the only subject with a full KC SET, not the only with any content. No flags.

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERDICT: FLAWED (the AI's "PA-first is faster because PA has content" framing) — Alex is PARTLY RIGHT (unanimous, 5/5).

CORE FINDING:
Alex correctly punctured the AI's reasoning: the MACHINE is subject-agnostic, and PA's hand-authored content is NOT a machine head-start — A1 reprocesses figure geometry and Phase F re-runs all content through the engine, so "PA has lessons" justifies nothing for building the machine. The AI conflated "fast to a deployed PA product" with "fast to the general machine." BUT Alex understates the real delta: it is not "only the missing OOP family." The figure FAMILIES are subject-shaped and GATE the machine's servable output, so the machine is not subject-agnostic-in-practice until every needed family exists + is oracle-gated. PA's concepts fit the 6 existing trace families (zero net-new); OOP static structure is structurally HOMELESS — it needs a net-new figure ontology + a new isomorphism oracle + its first content + a self-test. So PA-first's true value is not content — it's FAILURE-ISOLATION and a fitting+gated figure paradigm, letting the engine be proven with the figure-type held constant; OOP-first detonates four unproven variables at once.

AGENT CONSENSUS: 5 CONDITIONAL — 0 flagged — 5/5 "Alex PARTLY RIGHT."

KEY ISSUES:
- The two decisions are ORTHOGONAL and were wrongly collapsed (First Principles): (1) MACHINE-readiness = subject-agnostic, build regardless; (2) CONTENT-readiness = which figure families exist+gated + the first KC corpus = the ONLY subject-shaped cost. "Which subject first" matters only for (2).
- PA needs ZERO net-new figure families; OOP needs ONE net-new (static-structure/isomorphism) — a whole new paradigm + oracle, on the critical path for OOP regardless of order. It is sequencing, not extra-vs-not.
- [CRITICAL, both councils] Bar any family lacking its own oracle + seeded-wrong self-test from VERIFIED (verifică sursa, excluded from mastery) — so OOP whenever it ships can't wear an unearned badge.
- The capability SPIKE (free-tier generator vs the oracles) is UNMEASURED; OOP-first amplifies it (OOP depth/pedagogy is the input most likely to defeat a free model); PA gives a known-good baseline to read the spike against.

RECOMMENDED PATH:
Separate the decisions. Build + PROVE the subject-agnostic machine using the cheapest corpus to gate against = PA (needs no new family, has KCs to gate the oracle). Concretely: Phase 0 (CI-wire + generalize the 2 gates) → A/B/C EARLY-green on the existing PA/ALO KCs → run the SPIKE → then DEPLOY by owner value. If "OOP visible first" is a hard requirement, that's legitimate — but it costs building the OOP structure-family + isomorphism gate + digesting POO from zero BEFORE the first deploy, all behind the VERIFIED-bar; it does not become faster by dismissing PA's role, it just moves the net-new family onto the first-deploy critical path. Pure-fastest-to-a-usable-deploy = PA first; OOP-first = legitimate-by-value but slower + riskier, mitigated by the VERIFIED-bar.

CONFIDENCE: 9
Grounded, unanimous. Open: the only thing the council can't decide is Alex's actual DRIVER for OOP-first (priority/value vs an imminent exam) — that, not any technical fact, picks between "deploy PA first" and "pay the OOP-family cost to deploy OOP first."
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output saved to: .claude/council-cache/council-1781561962.md
