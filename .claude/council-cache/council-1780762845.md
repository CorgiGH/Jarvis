# Council review — 1780762845

**Problem:** jarvis-kotlin Phase 2 trust-net. `caf02c0` (D1–D4 false-faithful fix + migration guard; tests green; pa-kc-001..004 faithful under v2) is LOCAL/UNPUSHED. Planned next = LIVE v2 re-key/re-audit on the irreplaceable prod DB, gated on audit env UP + pre-flip zero-row abort + faithful gate, THEN push, THEN lock docs. BLOCKER: offline NLI env (`JARVIS_PYTHON3`/local DeBERTa) is DOWN. `content_hash` IS the D9 sync key. Unrelated uncommitted "door" landmine in tree.
**Proposed approach:** A) revive NLI env → re-audit → push. B) decouple: push caf02c0 now (code-only) + env-independent work (lock docs, missing FKs on 3 Gate-2 tables, F2 VerificationGate default-OFF). C) something else.
**Project context:** solo-dev tutor app; Kotlin BE + React/TS FE; SQLite single-user irreplaceable prod DB; no paid APIs; FAIL-LOUD; off-box dump before any live mutation; PM-delegation; plan-first. Locked: two-hash sha256_8; COMMIT_CODE_ONLY.
**Timestamp:** 2026-06-06 (epoch 1780762845)

---

## 🔴 Devil's Advocate
STANCE: REJECT
REASONING: Both A and B step on the uncommitted "door" landmine sitting next to caf02c0 — one reflexive `git add -A`/branch-switch/audit-write leaves the flipped state unreconstructable and the off-box dump ambiguous. B is worse: pushing caf02c0 BEFORE the live re-audit publishes the v2 `content_hash` as canonical — and since content_hash IS the D9 sync key, you've broadcast a key never validated against the real DB; if the re-audit later flips pa-kc-001..004 or surfaces a collision you can't quietly revert. "Human reviews the diff" can't catch a hash-distribution problem that only manifests against 828 live rows the diff never touches.
KEY CONCERN: caf02c0 must not be pushed until the live v2 re-key/re-audit runs green on prod, because the push publishes v2 content_hash as the D9 key. Correct ordering = C: quarantine the door landmine, revive NLI, dump off-box, run the gated live re-audit, and ONLY on green push.

## 📚 Domain Expert
STANCE: CONDITIONAL — approve B (decouple/push code-only) with one hard gate added
REASONING: "Migration code shipped + tests green, live DATA migration not yet executed" is the expand/contract (parallel-change) migration discipline — specifically a dark-launched migration where cutover is an explicit operator step, not a deploy side effect. Verified real: the v2 re-key is fenced behind `assertNoLiveContentHashRowsBeforeV2Flip()` (DELTA-4) and a flip flag that defaults false on normal boot, so pushing caf02c0 cannot touch `~/.jarvis/tutor.db` (untouched since Jun 3). gh-ost / pt-online-schema-change formalize exactly this deploy-code-first/migrate-data-later split. A needlessly couples a reversible code artifact to a down NLI env and inverts safe ordering; B is correct PROVIDED the re-key keeps its off-box dump + zero-row abort, and the diff review confirms no boot path flips the flag without operator intent.
KEY CONCERN: The real risk is the D9 sync key being content_hash under a CHANGED v2 formula — the moment the live re-key runs, every VPS row keyed on the v1 hash silently orphan-matches (rekey-without-dual-read split-brain). Cutover must be additive (dual-write/backfill-verify) OR gated on verified dump + DELTA-4 zero-row assertion + a post-flip VPS↔PC content_hash parity reconcile before re-enabling the serve gate.

## ⚙️ Pragmatist
STANCE: CONDITIONAL
REASONING: B is right because it splits the one irreversible step (live re-key on the irreplaceable DB) from the merely-annoying one (flaky local ML env). Reviving DeBERTa to gate a push is a false dependency — the push backs up code, the audit validates content; conflating them holds a local-only commit (real bus-factor risk) hostage to an env down since last session. Pushing caf02c0 code-only is safe IF the diff is genuinely code/test/schema-only with no rows re-keyed; a human-reviewed diff covers that. But first quarantine the door landmine (stash/branch) so it can't ride along or corrupt the push.
KEY CONCERN: Don't let "push" imply "trusted/faithful." Push caf02c0 strictly as an unaudited code backup (say so in BRIDGE-HEAD, keep docs UNLOCKED); the live re-audit stays a hard gate before any prod re-key or doc-lock — never run that mutation while NLI is down or the tree is dirty.

## 🧱 First Principles
STANCE: CONDITIONAL
REASONING: Stripped of framing, the goal is "D1–D4 is correct and won't get lost," not "mutate the irreplaceable prod DB now." The live re-key exists only to feed D9's sync key — and D9 is not being built — so doing the risky live mutation now is solving a future problem early, against plan-first/PM-delegation. Correct sequence: durably protect the work (push code-only after diff review) + env-independent hardening, then defer the live re-key to immediately-before-D9 when the audit env is actually required and proven up. Neither A nor B as written: A wastes effort reviving NLI for a not-yet-needed mutation; B is right to decouple but pushing before any re-audit is the live trap unless marked.
KEY CONCERN: Pushing caf02c0 ("faithful under v2") publishes a faithfulness guarantee never validated by NLI (env DOWN), proven only by the code that makes the claim — so push behind an explicit "code-only, NLI-unverified, VerificationGate default-OFF, no live DB touched" marker, never as a faithful-verified milestone; keep the live re-key off the table until it's the real D9 prerequisite with env up + fresh dump.

## ⚠️ Risk Analyst
STANCE: CONDITIONAL — adopt B, with the live re-key explicitly NOT in this batch
REASONING: Verified: caf02c0 is HEAD, on no remote, +0/-1 vs origin (push is a pure fast-forward, no merge hazard), and it is the ONLY copy — disk loss or a botched `git add -A`/clean/branch-switch over the 31-entry dirty tree (untracked door/ dirs + a 0-byte ./tutor.db decoy) destroys it. `JARVIS_PYTHON3` empty → a re-audit now FAIL-LOUDs into false-reds and proves nothing. A is wrong: it chains the single unbacked commit's survival to a hazardous tree AND front-loads an irreversible content_hash rewrite on a DB with no fresh off-box backup. The push is code-only (COMMIT_CODE_ONLY) and touches no DB → zero risk pushing before the live re-key. Push via explicit pathspec (`git push origin HEAD`), never `git add -A`.
KEY CONCERN: CRITICAL — the live v2 re-key is irreversible mutation on an irreplaceable DB whose newest off-box dump is STALE (no `.bak`/`.dump`/`backups/` found; prod tutor.db mtimed Jun 3). Must NOT run this batch. Hard gate before it ever runs: (1) FRESH off-box dump immediately pre-flip, (2) NLI env verified UP (smoke-test one known-faithful row), (3) zero-row abort + faithful gate confirmed firing on a COPY first. Secondary (HIGH): never `git add -A` while door work is uncommitted — push via explicit ref only.

---

## Sanity Check
SANITY [Devil's Advocate]: FLAG
NOTE: Central claim — that pushing caf02c0 "broadcasts the v2 content_hash as the D9 sync contract on the wire" — is a non-sequitur: D9 is not built and the push is code-only (verified by Domain Expert + Risk Analyst; the v2 flip defaults false, no live DB touched). The door-quarantine instinct is valid and aligns with the clean agents.
SANITY [Domain Expert]: PASS — verified code against repo; named concrete patterns (expand/contract, gh-ost, pt-online-schema-change); split-brain concern is substantive.
SANITY [Pragmatist]: PASS — clean; condition specified (code/schema-only diff + door quarantine + docs stay unlocked).
SANITY [First Principles]: PASS — genuinely strips framing (live re-key serves not-yet-built D9); guard specified.
SANITY [Risk Analyst]: PASS — claims verified against repo (single copy, stale backup, NLI down, door landmine, fast-forward push); severity ranked.

---

## Judge
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COUNCIL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: FLAWED

CORE FINDING:
Option A is wrong; Option B is right but under-specified. All four clean agents converged: the push of caf02c0 is code-only and — verified against the repo — cannot touch the live DB (the v2 re-key is fenced behind a flip-flag that defaults false + the DELTA-4 zero-row abort), so coupling the push to a down NLI env (A) is a false dependency that strands your ONLY copy of caf02c0 next to a fragile tree. Do B's core (push now), but the live re-key must NOT be in this batch and the push must not be dressed up as a "faithful" milestone.

AGENT CONSENSUS: 4 CONDITIONAL (clean) backing decouple+push-code-only-now+defer-re-key, 1 REJECT (Devil's Advocate — FLAGGED, sync-key-broadcast claim is a non-sequitur).

KEY ISSUES:
- CRITICAL (Risk Analyst): the live v2 re-key is an irreversible mutation on an irreplaceable DB whose newest off-box dump is STALE (no fresh local .bak/.dump). Must not run until: FRESH off-box dump + NLI env up & smoke-tested + zero-row abort proven firing on a COPY first.
- HIGH: don't let "push" imply "verified/faithful" — caf02c0's faithfulness was proven only on test fixtures, never by live NLI. Push as an explicit code-only/NLI-unverified backup; keep lock-docs UNLOCKED until the live re-audit runs green.
- HIGH (First Principles): the live re-key only feeds D9, which is not being built yet — defer it to immediately-before-D9; do not revive NLI just to run it now.
- HIGH: door landmine — quarantine it and push via explicit ref (`git push origin HEAD`); never `git add -A`/clean/branch-switch over the 31-entry dirty tree (incl 0-byte ./tutor.db decoy).
- (Domain Expert, for when re-key DOES run): make the cutover additive — post-flip VPS↔PC content_hash parity reconcile — to avoid v1↔v2 rekey split-brain.

RECOMMENDED PATH (ordering):
1. Quarantine the door landmine (stash or branch it out) → clean tree.
2. Push caf02c0 via explicit ref as a code-only, NLI-unverified backup (Alex reviews diff = confirms code/schema-only, no boot path flips the v2 flag). Second copy off-machine.
3. Env-independent hardening: missing FKs on 3 Gate-2 tables, F2 VerificationGate wiring (default-OFF). Lock-docs stay UNLOCKED.
4. DEFER the live v2 re-key/re-audit to the real D9 prerequisite — only behind fresh off-box dump + NLI up & smoke-tested + zero-row abort + post-flip VPS↔PC parity reconcile.

CONFIDENCE: 9
Claims (NLI down, caf02c0 single-copy fast-forward, stale backup, door landmine, flip-flag defaults false) were verified against the live repo by two agents. Would drop only if the caf02c0 diff contains a non-code change or an auto-firing flip path — which step 2's diff review checks.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
