# ACTIVE CONSTRAINTS -- jarvis-kotlin (LIVE; injected every turn; updated 2026-06-20 / SESSION-88)

Hard, currently-live lines. They override momentum. Re-read before EACH reply. (Maintained by /wrap; edit the instant Alex states a new hard line. /wrap MUST retire any constraint BRIDGE-HEAD marks `resolved:` — a stale constraint injected every turn poisons resume.)

## 🧭 NORTH-STAR (read once): jarvis ≠ the tutor

**Jarvis = a personal "life-OS"; the tutor (`jarvis-kotlin`) is its FIRST + ONLY-active vertical.** Planned siblings (DEFERRED — don't build/propose unless Alex opens that door): nutrition · finance · career · life-aware scheduler · +more. Topology: `C:\Users\User\Jarvis\`=abandoned brainstorm vestige · `jarvis-kotlin\`=the app · `~/.life-os\`=dormant Python-prototype data · `~/.jarvis\`=tutor runtime DB. Keep the tutor cleanly wire-able into big-Jarvis; don't hard-couple, don't build integration scaffolding now. ([[project_jarvis_north_star]])

<!-- BEGIN auto:resume - this block is REGENERATED by /wrap from BRIDGE-HEAD each session (resume-recipe + locked decisions + git SHA). Do NOT hand-edit; edits get overwritten. Everything OUTSIDE these markers (north-star, the numbered hard-rules, PROPOSAL-GATE) is human-authored and /wrap NEVER touches it. -->

## ▶ RESUME RECIPE (next session — do in order)

0. **Read `BRIDGE-HEAD.md` first**, then `MEMORY.md`. **Re-verify git + live state NEWEST-FIRST** (`git fetch` + diff origin) before acting. **Agent/PM "verified" AND my-own-summary are INADMISSIBLE** — councils inspect ARTIFACTS, I re-run gates myself, only green MACHINE-gates ship. Never judge a multi-frame figure from a few frames. ([[feedback_ground_councils_in_artifacts]], [[feedback_multiframe_conjunction_gate]])
1. **🧭 THE FRAME:** the project = the whole-system **one-pass DIGESTION teaching engine** per the BINDING spec `docs/superpowers/specs/2026-06-11-one-pass-digestion-teaching-engine-design.md` — ingest ANY media → classify → digest → machine-verify → serve 5-beat lessons/drills/mock-exams → mastery/FSRS → revise, **for ANY/ALL subjects.** "Ship OOP first" is **NOT** the frame ([[project_scope_oop_first]] DE-EMPHASIZED).
2. **⚖️ THE DIVISION (spec §1.4/§1.2):** **MACHINES verify TRUTH**; **the USER approves EXPERIENCE**; the user is NEVER asked to judge correctness — gaps ask for **FILES, not judgments.** ([[feedback_no_oracle_inversion]])
3. **🔎 VERIFICATION LAW:** LLM-as-judge CANNOT be trusted for correctness — **NON-LLM oracle** (compute/test/prove) + **correct-by-construction** + **best-of-N**; LLM only for taste + a thin human spot-check — and **the USER cannot be that human (withhold instead)**. ([[feedback_verifier_refute_bias_kills_truth]]) Research: `build-review/2026-06-18-trustworthy-ai-verification-research.md`.
4. **🎯 LIVE DIRECTION (SESSION-88): Phase-A first slice SHIPPED + council-cleared (SHIP-WITH-FOLLOWUPS).** Committed: self-sizing housing + `figure-noclip-gate` (`cf31120`; CI-wired `78a0666`, RED→GREEN proven by my hand), `pa-kc-002` rebind onto sort-merge (`8b598ce`), stale-plan amend (`668cf70`). **TWO decisions are ALEX'S, both UNRESOLVED — surface, don't tunnel ([[feedback_stay_strategic]]):** (i) **sort-merge §5.2** fold-vs-carve — blocks the Phase-A exit gate, NO fold/carve code until he rules; (ii) **flagship figure visual intent** — `AlgoStepperShell.tsx:380` `maxWidth:viewBoxW` shrinks the mergesort lesson figure to 480px in its 760 container (undisclosed regression-or-intended); SHOW him the LIVE `/tutor/merge-compare`, his eyes not mine. **Then Phase-A tail:** CI-wire `svg-overlap` + `class-diagram-layout` gates; build the 2 missing A2 families (timeline/protocol, state-machine/code-trace). **Then B+C greenfield oracles — GATE-OF-GATES: A+B+C EARLY-green TOGETHER before any breadth (do NOT start B until A's tail closes).** The "tutor not a content site" inner-loop work (Phase G/H) stays slated AFTER the oracles. Paused: wellposedness gate SPEC (`council-1781797515`).
5. **Git: `main @ 9626f73` — 9 AHEAD of origin, unpushed; SESSION-88 added 5 commits** (`cf31120`/`8b598ce`/`78a0666`/`668cf70` + post-wrap `9626f73` = project `CLAUDE.md` audit; relocated the stale C/C++ home doc out of the parent dir). Dirty ~50: residue = rejected gauss2 demo (`LectieGaussDemo.tsx` + VizDemoPage gauss2 section), orphan shadcn scaffold, HELD `tutor-dist` bundle, door/junk landmine, untracked council/build-review docs. `viz-alo-gauss-002.yaml` is now COMMITTED (green keeper); the SESSION-87 housing + Phase G/H plan edits all landed this session. Repo-root `*.png` = junk; **never `git add -A` / `git clean` on main** (tutor-dist door/png un-gitignored). **`material/` + `.scratch/donor/` gitignored.** Landmine stays GONE.
6. **HARD RULES (Alex):** PM-delegation (workflows/agents build, NOT me); MACHINE gates ship NOT agent/PM "verified"; viz gated @ **1536×648 AND ×730** VIEWPORT (`fullPage:false`), ZERO overlap/clip; **SHOW Alex the LIVE page, never screenshots I consume**; learning=RO / jarvis=EN; **≤5-line lead, ONE concept at a time, plain words before jargon**; processing-first; context-governor (hand off at a CLEAN boundary BEFORE quality degrades); build via WORKFLOWS. **Ground subject facts from material/findings/official-fișe, NEVER parrot code subject-label wiring** ([[reference_subject_natures]] — PA≠math, PS≠just-R). **Don't ASK when the instruction is explicit — just do it** (stalled on "pull from the sites" SESSION-83). **Don't invent structure Alex never asked for** (SESSION-83). **🆕 SESSION-85 design reckoning: LEAD with VISUAL quality, not just gates — a verified-but-UGLY figure is a FAIL; USE the design skills/MCPs (impeccable LIVE), NEVER hand-code visuals; NEVER ship half-finished + ask Alex to grade it (oracle-inversion); the rigid part is the figure HOUSING (fixed-480), not the figure rules; bar = EXTRAORDINARY.**

## LOCKED decisions — CITE, do NOT re-litigate

- **THE FRAME = the digestion spec (above).** Build the FULL system to spec; **quality is the SOLE primary** ("impossibly perfect"); deploy is LAST (Phase J). ([[project_scope_oop_first]] de-emphasized)
- **LOCKED MASTER PLAN = `docs/superpowers/plans/2026-06-15-end-to-end-master-plan-to-100-v2.md`** (oracle-first / two-layer / deploy-last: 0→A→B→C→[SPIKE]→D→⛔GATE-OF-GATES→E→F→G→H→I→J). PRISTINE.
- **MATERIAL = two SEPARATED layers (SESSION-83, Alex hard line):** `material/current/` = UP-TO-DATE material pulled from official subject sites (Alex's BScIA program); `material/archive/` = the old CS-scrape (BScINFO), deduped, `archive/<SUBJ>/<year|nedatat>/<type>/`. **Decision A: archive keeps SO + RC SEPARATE** (faithful to CS, where they ARE two subjects); Alex's combined **SORC** lives ONLY in `current/`. Year from PARENT FOLDERS only (no download-date/"2026" concept). Dedup by CONTENT (non-LLM hash), keep BOTH pdf+md.
- **SUBJECT TAXONOMY (grounded SESSION-83):** Alex = **BScIA** (Inteligență Artificială, `AI####`); archive = **BScINFO** (CS, `CS####`) — DIFFERENT programs. Only **PA/PS/POO** genuinely shared. **SORC** = AI1202 (new IA-only combined, no archive). **ALO** = AI1203 Linear Algebra & Optimisation ≠ CS "Fundamente Algebrice". Sites: PA `/proiectarea-algoritmilor`, PS `/probabilitati-si-statistica`, ALO `/algebra-liniara`, SORC `/operating-systems-and-computer-networks`, **POO = `gdt050579.github.io/poo_course_fii`** (not on edu.info). ([[reference_subject_natures]])
- **"100% to spec" = FOUR things:** (1) system to spec; (2) full COURSE COVERAGE; (3) **Layer-1 TRUTH 100% PURE** (faithful, HARD-gated PROOF; unverified behind a non-dismissible "verifică sursa" warning, never silently withheld); (4) **Layer-2 TEACHING** = honest ESTIMATOR, named-not-faked.
- **GOVERNING PRINCIPLES (law):** P1 NO-SPEC-CUT — PROVEN (decidable gate) OR ESTIMATOR + `neverificat / verifică sursa` checkpoint, never silently dropped, never faked-green. P2 EASY ⇒ GATED — typed input → renderer verified ONCE → auto-gates ride free.
- **MODEL + VISION:** content model = **CLAUDE MAX (relay/CLI)**; claude-max SEES images via CLI file-path (`Llm.complete` `imagePath`, `f5182ba`); free `:free` = other users only; **no paid APIs.** ([[feedback_no_paid_apis]])
- **MACHINE GATES live:** trust-net faithful-check; grader chain (machine-leg-first, `LlmNeverAlone`); `frame-conjunction-gate.mjs` + **`figure-noclip-gate.mjs`** (SESSION-88, 168 checks, RED→GREEN proven) BOTH CI-wired + HARD-BLOCKING; **TWO gates built + green but STILL NOT CI-wired** = `svg-overlap-gate.mjs` + `class-diagram-layout-gate.mjs` (Phase-A tail); trace-match. Trust = a demoable RED→GREEN (figure-noclip teeth re-proven live SESSION-88). ([[feedback_quality_primary_visual_trust]])
- **INNER-LOOP + UX work SLATED (SESSION-87, council-vetted): "make it a tutor not a content site."** Phase H = the inner-loop fix-set (Tier-1: confident-wrong callback · practice nav-link · G1 re-entry · queue-legibility; **#1 remediation routing RE-SCOPED net-new** — no client re-queue rail, `remediate` fires only on decay; parked "Phase 2" needs anti-gaming gate G3 + PM thresholds). Phase G = pomodoro (lesson-aware, NOT the parked `BlockReminder` buzzer) + subject-focus bias (VIEW-LAYER only + due-debt banner + auto-expire) + guided funnel (/oggi default landing, nav demoted not removed). ALL after the A/B/C oracles. Doc: `build-review/2026-06-19-tutor-interactivity-analysis.md`.
- **DONOR:** metered human-in-loop **LOOK + 5-beat FORM** reference — NOT a code donor, NOT in the pipeline, bypasses gates. **INV-5.5 HOLDS — distill skin = tokens/chrome/motion only, NEVER geometry.** Phase-D fast-follow. ([[feedback_lesson_layered_depth]])
- **FILE ORG = `docs/CONVENTIONS.md`**: raw material → `material/` (gitignored), ephemera → `.scratch/`, dated naming, `.gitignore` enforces; door landmine DISARMED (door files committed — still never `git add -A` / `git clean` on main). `2026-06-02-interface-signatures-lock.md` canonical-on-conflict for frozen signatures; off-box DB dump before ANY live-DB mutation; trust-net badge "corespunde cursului" NEVER "corect".
- **🎨 DESIGN STACK (ADOPTED SESSION-86):** **impeccable** INSTALLED + INITIALIZED (`.claude/skills/impeccable/`; `PRODUCT.md` + `.impeccable/live/config.json` written; DESIGN.md pre-existing). **shadcn/ui** ADOPTED (`tutor-web/components.json` + `@/*` alias + `cn` + brutalist token compat layer in `index.css` [radius→0, primary→yellow + #000 ink]; deps cva/clsx/radix-ui/tailwind-merge; `button.tsx` proof; manual setup — NOT `init`, which would clobber the theme). **Tailwind v4** wired at base (`@import "tailwindcss"`); figures/lesson surfaces STILL inline `style={}` (un-migrated). **AIDesigner MCP** SCAFFOLDED only — `.mcp.json` (`api.aidesigner.ai`) + `/aidesigner` cmd + skills; **host arg = `claude` NOT `claude-code`** (memory was wrong); **Alex MUST restart Claude Code → `/mcp` connect `aidesigner` (OAuth, FREE tier)** before live. NEW pending: DESIGN.md drift-gate reconcile (do WITH the dark-reskin revert, not piecemeal). Donor `.scratch/donor/` = DIAGNOSIS-ONLY, not a baseline. ([[feedback_no_paid_apis]])

<!-- END auto:resume -->

---

1. **NEVER show Alex ASCII/sketch mockups** — visual learner; REAL rendered screenshots only. **Render-before-claim = LOOK at the WHOLE artifact, not just the top.** ([[feedback_design_by_proliferation]], [[feedback_render_before_claim_done]])
2. **NEVER route content-verification to Alex** — he can't vet subject content; he hands over a FILE; the system verifies. ([[feedback_no_oracle_inversion]])
3. **Oversight / direction-setting mode → surface the decision, don't tunnel into build/spec/author.** ([[feedback_stay_strategic]])
4. **Before proposing next work, grep plans/roadmap/gap-ledger FIRST** — cite path:line or "net-new because <reason>." ([[feedback_get_it_right_first_time]])
5. **Repeat-back + confirm; ONE THING AT A TIME / short responses.** Alex (ADHD): lead simple, ≤5 lines, let him pace ("next"). Explain concepts in plain words, intuition before jargon (the cowork-tutor lesson applies to Claude too). ([[feedback_short_responses]])
6. **Viz correctness gate** (`trace-match` + `self-see`) for any new viz.
7. **ALO study is a PARALLEL track, NOT the project.** Alex learns WHILE building — never tell him to "stop building to study." Cowork-tutor handoff lives on Desktop `jarvis-tutor-handoff/` (hardened SESSION-52 to a **recipe-card extraction gate**: tutor must extract method/variant/prereqs/steps from HIS files, each tied to a quote, before teaching; translate jargon to plain words; never teach from memory). ALO = "Algebră Liniară și Optimizare" = numerical methods; CN overlaps heavily.
8. **Context-budget GOVERNOR (Alex-ratified SESSION-74; tworkflow's "40%-rule" adapted, council w6itsmll5).** Memory fires at session BOUNDARIES; this governs MID-session. Don't push a long autonomous stretch past the point quality degrades — hand off at the next **CLEAN boundary** (green commit / phase edge) BEFORE the budget tightens, not at the hard limit. **Symptom-override (any one = stop at next boundary, write BRIDGE-HEAD, continue fresh):** catching yourself trusting agent "green" without re-checking · batching large unseen work · losing the thread · re-deriving settled facts. The SESSION-73 "verified 30 frames after checking 3" was degradation-before-limit. A pwsh budget-watch hook (to auto-nudge) is the build half. ([[feedback_context_budget_governor]])
9. **MULTI-FRAME figures: the CONJUNCTION gate ships, NOT my eyes (council-1781535790, SESSION-75).** No step-through figure is claimed done OR shown to Alex until `tutor-web/tools/frame-conjunction-gate.mjs` is GREEN on EVERY skin. The invariant: per consecutive frame pair, **STATE-changed AND figure-region pixels frozen = HARD FAIL** (the renderer dropped a field the algorithm advanced). A whole-frame pixel diff is INSUFFICIENT (callout/counter churn = false-green "liveness alibi" → mask the chrome, diff the figure region only); a state-only diff is INSUFFICIENT (data moved, render blind); an author/phase `hold` whitelist is FORBIDDEN (a hold = a state-UNCHANGED frame only). NEVER judge a multi-frame figure from start+end or a few stills — that partial-view sin shipped the merge figure DEAD for 4 frames (Alex caught it, not me). CI-wiring of the gate into `test.yml` + the gate-registry is the open follow-up. ([[feedback_multiframe_conjunction_gate]])
   **Companion = the SVG ELEMENT-overlap gate `tutor-web/tools/svg-overlap-gate.mjs` (SESSION-75):** every painted figure element (rect/bar/cell/chip · text/value/label/callout · path/line = brackets/chevrons/rules) pairwise bbox-checked, cross-group, EVERY frame × every skin × 1536×648 AND ×730, with an explicit allow-list for INTENDED nesting only (a value on its OWN token group). "Overlap" is NOT just text-on-text — Alex's correction: bars-on-bars, bracket-crossing-a-cell, collapsed group gaps all count. The standing `e2e/helpers/assertNoClip.ts` was SVG-BLIND (only checked interactive DOM) → it never caught any in-figure collision; this gate replaces that hole. Same discipline as the conjunction gate: gate ships, not my eyes; covers prototypes; CI-wiring pending.
10. **SHOW = the LIVE page in Alex's browser, NOT screenshots (Alex hard line, SESSION-75).** When showing Alex any rendered artifact (figure / page / UI / lesson), open the ACTUAL live page he can interact with — `Start-Process <url>` for the route I take screenshots FROM — never present static PNGs as the thing he reviews. Screenshots stay for MY OWN verification only (eyeball-before-claim, the inadmissible-self-report re-check); they are not what Alex sees. Sharpens rule 1 for the SHOWING step. ([[feedback_show_live_page_not_screenshots]])

---

## PROPOSAL-GATE (mandatory)

Before ANY sentence proposing next-step work — verbs like author / build / spec / generate / deploy / "let's" / "we need" / "should we" — OR any "gaps we need" list, emit exactly ONE line FIRST:

PROPOSAL-GATE: [against-constraints: ok | CONFLICT#n] [in-plan: path:line | not-in-plan, net-new because reason | n/a]

If CONFLICT, or you cannot cite the plan for slated work, STOP and ask. A missing gate line before a proposal IS the violation.
