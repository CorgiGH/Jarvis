# ACTIVE CONSTRAINTS -- jarvis-kotlin (LIVE; injected every turn; updated 2026-06-15 / SESSION-74)

Hard, currently-live lines. They override momentum. Re-read before EACH reply. (Maintained by /wrap; edit the instant Alex states a new hard line. /wrap MUST retire any constraint BRIDGE-HEAD marks `resolved:` — a stale constraint injected every turn poisons resume.)

## 🧭 NORTH-STAR (read once): jarvis ≠ the tutor

**Jarvis = a personal "life-OS"; the tutor (`jarvis-kotlin`) is its FIRST + ONLY-active vertical.** Planned siblings (DEFERRED — don't build/propose unless Alex opens that door): nutrition · finance · career · life-aware scheduler · +more. Topology: `C:\Users\User\Jarvis\`=abandoned brainstorm vestige · `jarvis-kotlin\`=the app · `~/.life-os\`=dormant Python-prototype data · `~/.jarvis\`=tutor runtime DB. Keep the tutor cleanly wire-able into big-Jarvis; don't hard-couple, don't build integration scaffolding now. ([[project_jarvis_north_star]])

<!-- BEGIN auto:resume - this block is REGENERATED by /wrap from BRIDGE-HEAD each session (resume-recipe + locked decisions + git SHA). Do NOT hand-edit; edits get overwritten. Everything OUTSIDE these markers (north-star, the numbered hard-rules, PROPOSAL-GATE) is human-authored and /wrap NEVER touches it. -->

## ▶ RESUME RECIPE (next session — do in order)

0. **Read `BRIDGE-HEAD.md` first**, then `MEMORY.md`. **Re-verify git + live state NEWEST-FIRST** (`git fetch` + diff origin) before acting. Lesson re-proven SESSION-75 (3×): a council fed my PROSE approved a fix that was WRONG; grounding it in the real renders FLIPPED it. **Agent/PM "verified" AND my-own-summary are INADMISSIBLE — councils inspect the ARTIFACTS, I re-run gates myself, only green gates ship.** Never judge a multi-frame figure from a few frames ([[feedback_multiframe_conjunction_gate]], [[feedback_ground_councils_in_artifacts]]).
1. **Git: `main @ 2ebd48b` — 13 AHEAD of `origin/main` (`94b4082`), 0 behind, NOT pushed.** **NOTHING committed SESSION-75.** Tree dirty **246** = SESSION-74 carry (door/demo + dark reskin + earlier viz WIP) + SESSION-75 uncommitted: the two gate tools `tutor-web/tools/{frame-conjunction-gate,svg-overlap-gate}.mjs`, the `SortMergeFamily.tsx` rewrite (bars variant + divide-split + two-lists/fronts + chip↔label coupling), NEW `MergeCompareDemo.tsx` + its `/tutor/merge-compare` route, `shoot.*.mjs`, many `r-*.png`, `active-constraints` rules 9+10, 5 council files, 3 memories, the diagnosis doc. **NEVER `git add -A` / `git clean` on main** (explicit-path only). `../jarvis-kotlin-lane-b` worktree reusable.
2. **HARD RULES (Alex):** PM-delegation (workflows/agents do the work, not me); **MACHINE gates ship, NOT agent/PM "verified"**; **viz quality MACHINE-gated** — EVERY frame at **1536×648 AND ×730**, VIEWPORT screenshots `fullPage:false`, ZERO overlap/nothing under chrome; **rule 9 = the CONJUNCTION gate (state-moved+figure-pixels-frozen=FAIL) + the SVG ELEMENT-overlap gate (ALL painted elements, not just text) ship not my eyes, cover prototypes**; **rule 10 = SHOW Alex the LIVE page, never screenshots I consume**; rule 8 = context-governor; learning=RO/jarvis=EN; ≤5-line lead; processing-first; executors NEVER edit the plan. **PROCESS HYGIENE: suites FOREGROUND from `tutor-web` cwd + stream-to-log; NEVER `| tail`/`Select-Object -Last`; never two vitest at once.**
3. **STATE: SESSION-75 = the figure-quality thread (Phase-A visual half).** Diagnosed merge → bars-vs-boxes prototypes → Alex caught dead divide frames + overlap (twice) → grounded councils → built+self-verified the conjunction gate + element-overlap gate, fixed divide-split + ALL overlaps, redesigned merge to two labeled lists + fronts + play button (gates GREEN, 38/38 vitest, re-run by me). **DEPLOY GAP: served VPS = SESSION-66; nothing live until Phase J.**
4. **NEXT (Alex-set) = the FRONT / "frontier" representation** — the two-lists layout reads, but the head-as-THE-candidate + compare-the-two-fronts must DELIVER (lift+ring+"vs" was insufficient). Continue the merge-figure LOOK pass, **shown LIVE** (rule 10). Then: **bars-vs-boxes pick** · **CI-wire BOTH gates** into `test.yml`+registry (manual `tools/*.mjs` today; `assertNoClip` is SVG-blind) · carried SESSION-74 items (**PUSH** 13 · `#1` calibration · D-A leg-2 · **REVERT** dark reskin · verify context hook). Build via WORKFLOWS, oracle-first deploy-last (0→A→B→C→SPIKE→D→⛔GATE→E→F→G→H→I→J).

## LOCKED decisions — CITE, do NOT re-litigate

- **🆕 FIGURE ENGINE (D-A, council-1781489738) = B2 (TS-oracle-as-generator) + a 3-LEG field-scoped gate:** leg 1 STATE re-derivation `===oracle(seed)` (PROOF/L1) · leg 2 INDEPENDENT per-frame invariants + postcondition vs a TRUSTED reference, NOT the oracle (PROOF/L1 — kills the never-sorts tautology) · leg 3 callout/prose → language + Phase-C pedagogy (ESTIMATOR/L2), never byte-pinned. One shared oracle module (codegen + harness). **Leg 2 currently NOT-INDEPENDENT** (fix queued). Beats Kotlin-backend (two-impl drift) + hybrid (sig churn). Golden-file codegen, frozen §NEW-V, families/scrubber unchanged.
- **🆕 CONTEXT-BUDGET GOVERNOR (Alex-ratified SESSION-74; tworkflow-adapted, council w6itsmll5):** mid-session, hand off at a CLEAN boundary BEFORE quality degrades; symptom-override on trusting unchecked agent "green" / batching unseen work / losing the thread. Build half = the pwsh `context-budget-hook.ps1` (verify before trusting). ([[feedback_context_budget_governor]])
- **LOCKED MASTER PLAN = `docs/superpowers/plans/2026-06-15-end-to-end-master-plan-to-100-v2.md` (PRISTINE).** v1 → 8-angle audit (37 holes closed) → v2 + two-layer model → re-audit → second-independent-LLM-family on Phase B → PRISTINE. SUPERSEDES the old V→5→7 ledger.
- **"100% to spec" = FOUR things (Alex):** (1) system to spec; (2) full COURSE COVERAGE; (3) **Layer-1 TRUTH 100% PURE** — course material faithful, HARD-gated PROOF; unverified = rare, behind a non-dismissible "verifică sursa" warning (NEVER withheld), on a `flagged-servable` surface INV-1.1 excludes, counted via INV-1.1b; (4) **Layer-2 TEACHING** = generated scaffolding, an HONEST ESTIMATOR named-not-faked. *"course content 100% pure but taught with additional stuff; spec NOT downsized."*
- **ORACLE-FIRST, DEPLOY-LAST (locked):** Phase 0 (trustworthy gates — DONE except #1) → A (figures) → B (depth, 2nd-LLM-family) → C (pedagogy estimator) → [SPIKE] → D (premium UI) → ⛔ GATE OF GATES → E (digestion) → F (content, 5 subjects) → G (guidance/practice) → H (EC1–5) → I (hardening) → J (ONE verified deploy).
- **MACHINE GATES ship, NOT agent/PM "verified" (reinforced HARD).** Figures GENERATE FROM the instrumented algorithm + CI gates; a concept→figure FITNESS gate. Councils `1781471050`/`1781475889`.
- **🆕 VIZ MOTION + OVERLAP GATES (SESSION-75, BUILT + self-verified RED→GREEN):** (1) `tutor-web/tools/frame-conjunction-gate.mjs` — per consecutive frame pair, **STATE-delta≠0 AND figure-region pixels frozen = HARD FAIL** (the renderer dropped a field the algorithm advanced); a whole-frame pixel diff is insufficient (callout churn = false-green alibi → mask chrome), a state-only diff is insufficient, holds = state-UNCHANGED frames only (council-1781535790). (2) `tutor-web/tools/svg-overlap-gate.mjs` — ALL painted elements (rect/text/path/line) pairwise bbox, cross-group, every frame×skin×648+730, intended-nesting allow-list only (council-1781538602; the standing `assertNoClip` is SVG-BLIND). BOTH manual `tools/*.mjs` → **CI-wiring into `test.yml`+registry is the open follow-up**; until then "fixed" is provisional ([[feedback_multiframe_conjunction_gate]]).
- **GOVERNING PRINCIPLES (Alex, SESSION-71) — law.** P1 NO-SPEC-CUT: PROVEN (decidable gate) OR ESTIMATOR + checkpoint w/ `neverificat / verifică sursa` — never silently dropped, never faked-green. P2 EASY ⇒ GATED: typed input → renderer verified ONCE → auto-gates ride free; no bespoke un-gated content.
- **EC1–EC5 (SESSION-72; spec §15)** = Phase H: live-help · cold-start gate · recovery · goal/intent · provider-health (learner zero-setup `free`). Guidance core (`/oggi` queue, readiness, interventions) EXISTS.
- **Binding spec wins;** `interface-signatures-lock.md` canonical-on-conflict; plans just-in-time; per-task two-stage review; escalations halt for PM rulings. Spec = `docs/superpowers/specs/2026-06-11-one-pass-digestion-teaching-engine-design.md`.
- **Viz architecture = CONSTRAINED FAMILIES, LOCKED (G4/INV-5.5)** — arch SOUND, keep; figure = `(family_id, typed data)`, verified ONCE/family, SVG 480×360. Gap = the concept→figure FITNESS gate + (Alex SESSION-74) the VISUAL-QUALITY bar, NOT the family pattern.
- **TWO-LANE MODE:** Lane B own worktree; ZERO file intersection (machine-checked); shared files PM-merge-only; delta-recon first.
- **Lesson FORM + skeleton-trace councils** unchanged. Bare-chrome lesson route ratified. **Depth = a SEPARATE concept-keyed gated artifact** (two render-skins ≠ depth) → Phase B.
- **Trust-net LIVE + RUN;** 4 faithful KCs PC+VPS; badge "corespunde cursului" NEVER "corect"; no paid APIs; off-box dump before ANY live-DB mutation; explicit DB path on VPS.
- **Impeccable:** post-process filter pinned @2.3.2; blocking-flip ATTEMPTED + REVERTED SESSION-74 (25 real findings → needs calibration before fail-CLOSED). Alk = fetch-only (pinned sha256). Token frugality = the 2 adopted levers (viz carved out). Gate-coverage REGISTRY now LIVE (`tools/gate-registry-check.mjs`, CI-wired).

<!-- END auto:resume -->

---

1. **NEVER show Alex ASCII/sketch mockups** — visual learner; REAL rendered screenshots only. **Render-before-claim = LOOK at the WHOLE artifact, not just the top.** ([[feedback_design_by_proliferation]], [[feedback_render_before_claim_done]])
2. **NEVER route content-verification to Alex** — he can't vet subject content; he hands over a FILE; the system verifies. ([[feedback_no_oracle_inversion]])
3. **Oversight / direction-setting mode → surface the decision, don't tunnel into build/spec/author.** ([[feedback_stay_strategic]])
4. **Before proposing next work, grep plans/roadmap/gap-ledger FIRST** — cite path:line or "net-new because <reason>." ([[feedback_get_it_right_first_time]])
5. **Repeat-back + confirm; ONE THING AT A TIME / short responses.** Alex (ADHD): lead simple, ≤5 lines, let him pace ("next"). Explain concepts in plain words, intuition before jargon (the cowork-tutor lesson applies to Claude too). ([[feedback_short_responses]])
6. **Viz correctness gate** (`trace-match` + `self-see`) for any new viz.
7. **ALO study is a PARALLEL track, NOT the project.** Alex learns WHILE building — never tell him to "stop building to study." Cowork-tutor handoff lives on Desktop `jarvis-tutor-handoff/` (hardened SESSION-52 to a **recipe-card extraction gate**: tutor must extract method/variant/prereqs/steps from HIS files, each tied to a quote, before teaching; translate jargon to plain words; never teach from memory). ALO = "Algebră Liniară și Optimizare" = numerical methods; CN overlaps heavily.
8. **Context-budget GOVERNOR (Alex-ratified SESSION-74; tworkflow's "40%-rule" adapted, council w6itsmll5).** Memory fires at session BOUNDARIES; this governs MID-session. Don't push a long autonomous stretch past the point quality degrades — hand off at the next **CLEAN boundary** (green commit / phase edge) BEFORE the budget tightens, not at the hard limit. **Symptom-override (any one = stop at next boundary, write BRIDGE-HEAD, continue fresh):** catching yourself trusting agent "green" without re-checking · batching large unseen work · losing the thread · re-deriving settled facts. The SESSION-73 "verified 30 frames after checking 3" was degradation-before-limit. A pwsh budget-watch hook (to auto-nudge) is the build half. ([[feedback_context_budget_governor]])
9. **MULTI-FRAME figures: the CONJUNCTION gate ships, NOT my eyes (council-1781535790, SESSION-75).** No step-through figure is claimed done OR shown to Alex until `tutor-web/tools/frame-conjunction-gate.mjs` is GREEN on EVERY skin. The invariant: per consecutive frame pair, **STATE-changed AND figure-region pixels frozen = HARD FAIL** (the renderer dropped a field the algorithm advanced). A whole-frame pixel diff is INSUFFICIENT (callout/counter churn = false-green "liveness alibi" → mask the chrome, diff the figure region only); a state-only diff is INSUFFICIENT (data moved, render blind); an author/phase `hold` whitelist is FORBIDDEN (a hold = a state-UNCHANGED frame only). NEVER judge a multi-frame figure from start+end or a few stills — that partial-view sin shipped the merge figure DEAD for 4 frames (Alex caught it, not me). CI-wiring of the gate into `test.yml` + the gate-registry is the open follow-up. ([[feedback_multiframe_conjunction_gate]])
   **Companion = the SVG ELEMENT-overlap gate `tutor-web/tools/svg-overlap-gate.mjs` (SESSION-75):** every painted figure element (rect/bar/cell/chip · text/value/label/callout · path/line = brackets/chevrons/rules) pairwise bbox-checked, cross-group, EVERY frame × every skin × 1536×648 AND ×730, with an explicit allow-list for INTENDED nesting only (a value on its OWN token group). "Overlap" is NOT just text-on-text — Alex's correction: bars-on-bars, bracket-crossing-a-cell, collapsed group gaps all count. The standing `e2e/helpers/assertNoClip.ts` was SVG-BLIND (only checked interactive DOM) → it never caught any in-figure collision; this gate replaces that hole. Same discipline as the conjunction gate: gate ships, not my eyes; covers prototypes; CI-wiring pending.
10. **SHOW = the LIVE page in Alex's browser, NOT screenshots (Alex hard line, SESSION-75).** When showing Alex any rendered artifact (figure / page / UI / lesson), open the ACTUAL live page he can interact with — `Start-Process <url>` for the route I take screenshots FROM — never present static PNGs as the thing he reviews. Screenshots stay for MY OWN verification only (eyeball-before-claim, the inadmissible-self-report re-check); they are not what Alex sees. Sharpens rule 1 for the SHOWING step. ([[feedback_show_live_page_not_screenshots]])

---

## PROPOSAL-GATE (mandatory)

Before ANY sentence proposing next-step work — verbs like author / build / spec / generate / deploy / "let's" / "we need" / "should we" — OR any "gaps we need" list, emit exactly ONE line FIRST:

PROPOSAL-GATE: [against-constraints: ok | CONFLICT#n] [in-plan: path:line | not-in-plan, net-new because reason | n/a]

If CONFLICT, or you cannot cite the plan for slated work, STOP and ask. A missing gate line before a proposal IS the violation.
