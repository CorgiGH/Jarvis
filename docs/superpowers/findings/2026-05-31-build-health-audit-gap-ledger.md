# Build-health audit — gap ledger (2026-05-31)

**Durable copy of the SESSION-48 build-health audit (workflow `wal22ju9i`, code-verified) + the completeness council (`wd2vlzks1`) open must-resolves.** This is the load-bearing INPUT to `writing-plans` for the staged plan — the audit transcript itself ages out, so this file is the source of truth. Verdict: **SOUND foundation, no teardown — but foundation-level fixes must LEAD the plan, not trail it.**

## Health scorecard
- **Grader + Mastery + FSRS** — 🔴 fragile (3 internally-clean ledgers that don't JOIN).
- Content + grounding · Generation · Retrieval · Web/auth/relay · Tests/build/ops — 🟠 needs-work.
- Overall: needs-work, fragile at the seams. Pieces are well-built; the JOINS + real-model verification are where it breaks.

## The 6 BLOCKERS — must lead the plan (code-verified file:line)
- **B1 — phantom grade→FSRS join.** Confident grade calls `KcMasteryRepo.record()` (`TutorRoutes.kt:2024-2031`) but NEVER seeds/updates an `FsrsCard`; `FsrsSource.RUBRIC_CRITERION` has zero prod callers. Mastery + the review queue are disjoint ledgers. **Fix:** on confident grade, upsert an `FsrsCard` (RUBRIC_CRITERION) in the same txn as the mastery write.
- **B2 — dropped citation handle (= the KEYSTONE).** `generate-drills` flattens `sources = kc.source.map{it.quote}` (`TutorRoutes.kt:1459`); `Problem`/`DrillContentDto` have no `SourceRef`/`model_tag` field; the observability envelope is built AFTER `call.respond` (`:2045`→`:2051`). **Fix (build FIRST after the E2 root):** add `SourceRef[]`+`model_tag` to `Problem`/`DrillContentDto`; thread the full SourceRef through generation; one `emit()` chokepoint that HARD-BLOCKS un-cited/cheap-model BEFORE respond. B2 is what #2-re-verify / #8-citation / #9-retraction all attach to.
- **B3 — retrieval searches the WRONG corpus.** Every tutor `HybridRetriever.search` callsite searches `Config.archivalDir` (the personal chat tree); the KC oracle corpus at `content/` is indexed/searched NOWHERE; 5 prod routes pass `semanticEmbed=null` (semantic pass dead). **Fix:** new producer indexing `content/**/_sources` + KC text; repoint every tutor callsite's `archivalRoot` at `content/`.
- **B4 — no `content/` indexer exists.** The only `VectorStore.add` feeders are `EmbeddingsPipeline` (chat turns) + `ReindexMain` (legacy MemoryWiki). "Activate dormant infra" = a near-rewrite of `ReindexMain`, not an extend.
- **B5 — auth-allowlist drift (blocks multi-user).** `WebMain.kt:91-106` allowlists plural `/api/v1/tasks` but the real mutating LLM routes are singular `/api/v1/task/{id}/...` (+ `/task-detect/run`, `/edit/{id}/status`) → magic-link-only users get 401 before per-route auth. Only works in dogfood because the owner also holds `jarvis_auth`. **Fix:** reconcile the allowlist to the real route surface (or collapse to one composable guard); add the missing WebMain-interceptor test.
- **B6 — generation SPOF masked by green tests.** generator+grader = free Llama (429'd); critic = bare `RelayLlm()` that `error()`s if `JARVIS_RELAY_URL` unset, NO fallback wrapper → `generate-drills` 502s when the relay PC is down. F1 shows the free generator false-rejects a high fraction; every green test injects a fake gen returning "42". **Fix:** fallback/retry wrapper on the critic relay; real-model (or cassette) generation tests measuring the false-reject rate.

## Remediation backbone — the plan must absorb this (dependency-ordered)
- **Phase 0 — foundation joins (BLOCKS everything):** B1 grade→FSRS join · B2 citation handle (keystone) · B3/B4 `content/` indexer + repoint · B5 auth-surface fix.
- **Phase 1 — safe embedder swap (D2):** local 384-dim multilingual embedder (current embedder is paid OpenAI, **1536-dim** — store is a wipe/migration, NOT drop-in); add a dim guard on `VectorStore.add()`; lock-snapshot all read paths (CME hazard); batch the bulk index; decide `WikiPage` userId scoping (multi-user).
- **Phase 2 — trust the signal:** UTC timestamp normalization (FsrsCards.kt clock skew) + learner-tz anchor; grader CALIBRATION vs a gold set (+ fix canonical-vs-rubric mastery-score bug; fix same-family self-solve → distinct solver seam); resolve the model-routing inversion (D1 Claude-everywhere) + wrap the relay in fallback/retry/throttle.
- **Phase 3 — provenance + grounding integrity:** real critic model_tag (kill hardcoded `criticUsed="relay/claude"` :1520) + generate-path envelope + pre-respond hard-block; BOM strip + `.gitattributes`; referential-integrity validation (misconception/edge→real KC); validator as a WRITE-TIME gate; author real spans (or scope the grounding gate to what exists); decide the pdftotext→`_sources` pipeline.
- **Phase 4 — serving hardening:** `install(StatusPages)` + one composable ctx/session/CSRF/ownership guard; `runBlocking` LLM/retriever → suspend; **clamp `count` 1..10** + per-user rate limits (DoS).
- **Phase 5 — verification net (GATING, not cleanup):** real-server E2E (no stubs) asserting the live prep payload; real-model loop test (real grade records mastery + seeds a card); CI runs `check`+`validateContent`+Playwright+`test:tools`; **gradle wrapper** pinned; `deploy.sh` builds+ships the SPA, drops the Android hard-req, deepens the smoke (SPA-paints + corpus-loaded + relay-reachable).
- **Parallel cleanup (non-gating):** dead `retrievability` column · incoherent gap-promotion seeding · doc-id namespacing · JSON-by-string-concat → `@Serializable` · supervised event-log writer · the 2 flaky tests · `.tmp-*` litter.

## 4 OPEN completeness must-resolves (decide BEFORE/DURING writing-plans)
1. **Slice-1 KC** — must be **computational** (pa-kc-001 is *definitional* = wrong; only `apply`-bloom KCs qualify, ~pa-kc-005/006 — verify one has a canonical answer `answerMatches` grades sync + a real source span; else author one). Decides grading/oracle/UX.
2. **Source-of-record** — `_sources/pa-lecture-01.md` is a hand-typed **paraphrase** (0 form-feeds), NOT raw pdftotext; the PDF isn't in the repo. Alex chose **real-PDF**: obtain `Curs 1 PA.pdf`, re-extract raw, fix `SourceOfRecord`'s page/offset contract.
3. **Schema-migration + backfill + BACKUP** over the live **871-card** DB (no Flyway; `sourceRef` varchar(32) too narrow for a claim handle). The keystone ALTERs live tables during finals. Decide: nullable cols vs defaulted-backfill; new dedicated claim→card column (not reuse the occupied sourceRef); **a pre-deploy `sqlite3 .dump` off-box as a HARD precondition** of the first schema-touching deploy. The one spot where a mistake = unrecoverable loss.
4. **Truth-oracle independence under D1 (Claude-everywhere)** — Claude-confirms-Claude is correlated; textbook PDFs don't exist. Scope slice-1 oracle to compute-verify (#3a) + flag-all-definitional-as-unverified; defer textbook-KB (#3b) to breadth.

## Already-decided (locked — don't re-litigate)
multi-user · real-PDF source · add the concept "door" · **D1 Claude-everywhere** (pre-gen+cache, deterministic/async grade) · **D2 local multilingual embedder** · **staged-with-trust-gates, NOT end-to-end** (Stage 0 net+DB-backup+clean-baseline → Stage 1 walking skeleton on one computational KC → Stage 2+ breadth) · **build the verification-net FIRST**. Full reasoning: roadmap `docs/superpowers/specs/2026-05-31-north-star-roadmap-design.md` + BRIDGE-LOG SESSION-48.
