# Tutor Overhaul — Backlog

Created: 2026-05-10 (Phase 1).

Source-of-truth doc for MED/LOW findings produced by each phase's UX-Playbook gate. HIGH severity findings block phase advancement and are fixed in-place — they should never appear here. Phase 8's final audit rolls everything up; entries can be deleted as they ship.

Format per entry:
- `[phase] [category] [principle] [severity med|low] [finding] — [recommended action]`

## Phase 1 — Critical bugs

- `[1] [backend] [idempotency] [done] Closed by 2fbe85f (Tasks: dedup unique index + race-loser handling). TaskRepo gained the unique index on (user_id, subject, title) WHERE status='ACTIVE'; race losers now receive the existing row via dedup path. Closes the original Phase 6 deferred fix.`
- `[1] [frontend-test] [scroll] [done] Documentation-only. Phase 2 typography tokens shipped without changing the class contracts that scroll.test asserts; vitest stayed green across the overhaul. No further action.`
- `[1] [layout] [mobile-first] [stale] Old TutorWorkspace was split-pane PDF + chat (2-col). Slice 1.5 replaced it with DrillStack + Sidekick + ResourceRail + ProgressStrip. PDF is now lazy-opened in a ResourceRail drawer, not stacked above chat. Mobile-stack-order concern doesn't apply to the new layout.`
- `[1] [layout] [gestalt-grouping] [stale] Old layout (PDF + Scratchpad + Chat stack) replaced by the Slice 1.5 single-column drill workspace. Scratchpad lives in ResourceRail drawer now; no stacking concern.`
- `[1] [layout] [responsive-breakpoint] [stale] The 2-col PDF/chat split this entry described doesn't exist in the new TutorWorkspace; main is full-width single-column with a 320px ResourceRail on desktop.`
- `[1] [interaction] [feedback-response-time] [done] TaskQuickStart preset cards already disable while busy + show inline "creating…". TasksScreen CREATE button shows a "creating…" sibling spinner via task-create-spinner data-testid. ActiveTaskDashboard RUN DETECTION now also has its own busy + last-run timestamp from the polish sweep. All real task-create surfaces covered.`
- `[1] [layout] [scroll-containment] [stale] PdfPane is no longer stacked next to chat — it opens in a ResourceRail drawer on demand. The "0 height when chat dominates" failure mode doesn't apply.`

## Phase 2 — UX foundation

- `[2] [test-infra] [playwright-zoom] [deferred] Phase 8 final-audit territory — Playwright MCP zoom calibration. axe-core is the load-bearing check and passes clean; the strict-literal px assertion is the only brittle bit. No current backlog action.`
- `[2] [responsive] [touch-target] [done] Action buttons bumped py-1 → py-2 sm:py-1 across KnowledgeGapCard (5 buttons: INSERT/RESOLVE/FLAG WRONG/SHOW DOCS/PROMOTE→FSRS), SuggestedEditCard (APPLY/REJECT), TasksScreen (subject preset/CREATE/OPEN/DELETE), TrustSettings (GRANT + REVOKE — latter was the worst at py-0.5). Mobile-only mobile bump matches ChipRow canonical pattern. Computed ~32-36px tall on mobile (text-xs + py-2 + 1-2px border) vs prior ~20-24px. vitest 305/305 green; Playwright 375px verification deferred (rests on the same ChipRow precedent).`
- `[2] [a11y] [form-labeling] [done] ChatPane gained <label htmlFor="chat-input" className="sr-only">Message Jarvis</label> + id="chat-input" on the input. Scratchpad textarea aria-labelledby="scratchpad-heading" wires it to the visible <h2> header from the a11y-headings sweep.`
- `[2] [responsive] [header-overflow] [done] App.tsx outer header now uses flex-wrap + gap-2 so the title/taskId block and nav block reflow vertically on narrow viewports. min-w-0 on the inner container preserves the truncate behavior for the taskId chip.`
- `[2] [visual-polish] [typography-tokens] [deferred] The --type-sm/--type-lg/--type-h2 vars stay (test asserts them) but no migration path is currently load-bearing. Revisit when a real reskin demands a unified scale — at that point the @theme --text-* aliases option is the cleanest.`
- `[2] [a11y] [aria-current-value] [done] Sidebar task buttons now use aria-current={active ? "page" : undefined}. ARIA 1.2 page-context semantics.`
- `[2] [a11y] [section-headings] [done] Sidebar subject group headers promoted to <h3> inside the <nav aria-label="Tasks by subject">. Scratchpad header promoted to <h2 id="scratchpad-heading"> with the textarea now carrying aria-labelledby="scratchpad-heading". SR users can jump via H key. KnowledgeGap + SuggestedEdit chips intentionally stay as <div> per the original entry's guidance (inline message-attached, not navigable sections).`
- `[2] [a11y] [redundant-role] [done] Documentation-only — Sidebar <ul role="list"> stays as-is. Tailwind preflight sets list-style:none which strips the implicit list role in Safari reader; the explicit role is therefore load-bearing. Documented so future cleanup doesn't drop it.`
- `[2] [visual-polish] [page-h1-missing] [done] TutorWorkspace.tsx now renders <h1 className="sr-only">Tutor workspace · task {taskId}</h1> at top. SR users get an explicit landmark; visual brutalist-mono header bar unchanged.`

## Phase 3 — Hygiene

- `[3] [ops] [pii-scrub] [done] core_memory.md PII scrubbed on VPS 2026-05-10. Removed line "- Matricol: 31091001031ROSL251002" from /opt/jarvis/data/core_memory.md via single sed -i; restarted jarvis service; journalctl -u jarvis --since restart shows 0 CoreMemory WARN. Closes spec § 3.5.`
- `[3] [test-infra] [log-tail-stale] [deferred] Phase 8 final-audit territory — Playwright gate scenario refactor to use journalctl --since. No current backlog action; the substantive PII-scrub fix shipped 2026-05-10.`
- `[3] [interaction] [feedback-response-time] [done] Scratchpad (now in ResourceRail's SCRATCHPAD drawer via the new ScratchpadDrawer component) renders a status pill in the SCRATCHPAD header that transitions idle → saving → saved (auto-fades after 1.5s) on success or idle → saving → error on failure, with the HTTP error message surfaced via the pill's `title=` attribute. Replaces the silent `.catch(() => {})`. PUT also now actually debounced (500ms — the prior inline code PUT on every keystroke; the backlog had the debounce claim wrong). Pattern matches Notion/Google Docs auto-save UX.`
- `[3] [interaction] [state-visibility] [done] Scratchpad lives in ResourceRail's SCRATCHPAD drawer now (post Slice 1.5 C1), not TutorWorkspace. ScratchpadDrawer.tsx uses a hydratedRef.current single-shot flag: GET fires exactly once on mount, never re-fires when the user intentionally clears the field. Closes the prior clobber race where the inline code re-fetched whenever scratch === "" and could overwrite a deliberate clear with stale server data. ScratchpadDrawer.test.tsx asserts the no-re-fetch contract directly.`
- `[3] [error-prevention] [error-recovery] [done] SuggestedEditCard now renders a RETRY button (data-testid="suggested-edit-retry") when status === "failed" AND edit.type === "clipboard" AND !readOnly. Click resets status to "pending" + clears error + re-invokes apply(). Transient clipboard rejections (Safari prompt-deny) recoverable in-place.`
- `[3] [error-prevention] [error-recovery] [done] Scratchpad textarea now carries maxLength={SCRATCHPAD_MAX_CHARS} (50000, mirrors the server-side cap). Visible counter (data-testid="scratchpad-counter") renders "N / 50000" below the textarea, switching to bold danger-red when value.length > 90% of cap. Silent 400s eliminated — browser blocks input at the cap.`
- `[3] [interaction] [state-visibility] [done] PdfPane's <Document> already renders <div className="p-6 text-xs">loading PDF…</div> via react-pdf's loading= prop while the underlying PDF fetch is in flight. The earlier "blob size check" sibling fetch is silent but covered by the same visual gate — the user sees the loading placeholder until react-pdf resolves the document. No additional indicator needed.`
- `[3] [error-prevention] [error-recovery] [done] ChatPane.send() now checks res.ok and throws `Error('HTTP {status}: {body preview}')` before res.json(). 500/502 responses with text bodies now surface the actual HTTP status + first 200 chars of the body, instead of a misleading JSON-parse-error message. Matches the pattern in ScreenshotCapture + TaskQuickStart.`
- `[3] [interaction] [feedback-response-time] [done] Sidebar tracks `refreshing` separately from `loaded`. TASKS header renders a "refreshing…" pill (data-testid="sidebar-refreshing", aria-live="polite") during in-flight re-fetch triggered by jarvis:task-created. Initial-mount loading still uses the existing "loading…" placeholder; only the re-fetches got the new indicator.`
- `[3] [interaction] [progressive-disclosure] [done] KnowledgeGapCard now mirrors the content-expansion pattern for exampleCode: exampleExpanded state, preview slice + "show more" button (data-testid="knowledge-gap-example-show-more") when exampleCode.length > 320. Long code samples no longer permanently elided.`
- `[3] [error-prevention] [undo] [deferred] Informational not a defect per the entry text. Future "undo last action" toast deferred — needs a toast component + a server-side undo route. Tracked here for visibility but no current backlog action.`

## Phase 4 — Layer B §4 close

- `[4] [responsive] [touch-target] [done] PdfPane selection tooltip button bumped py-1 → py-2 sm:py-1 in the same sweep as the P2 [2] touch-target entry. px-2 kept (px-3 would only widen, height is the load-bearing dim).`
- `[4] [interaction] [state-visibility] [deferred] PDF selection → tooltip path is partially dormant in the Slice 1.5 architecture: ResourceRail's PDF drawer passes `onPdfSelectionGap={async () => {}}` (no-op), so the tooltip's click doesn't POST any gap today. PdfPane itself still has the tooltip + click handler ready. When the feature is re-enabled (ResourceRail's PDF drawer hooked up to /api/v1/gap), the confirmation chip + scroll-into-view flash described here should ship in the same patch.`
- `[4] [interaction] [state-visibility] [done] KnowledgeGapCard.postStatus now dispatches a window-level "jarvis:gap-resolved" CustomEvent (detail: {id, status}) on successful server POST. ChatPane's historicalGaps useEffect listens for both jarvis:gap-created AND jarvis:gap-resolved and re-fetches /api/v1/gaps?taskId=... on either. Combined with the resolvedBy==null filter from 90c0533, resolved gaps disappear from the rail immediately after the user marks them done. Closes the local-UI-vs-remount divergence loop.`
- `[4] [interaction] [error-recovery] [done] KnowledgeGapCard sync-error banner now renders a RETRY button inline (data-testid="knowledge-gap-sync-retry") when `resolved` is non-null. Click calls postStatus(resolved) again, clearing syncError on success or re-setting it on repeat failure. RETRY is suppressed on PROMOTE-failure (resolved is null in that case); error message still surfaces.`
- `[4] [interaction] [feedback-response-time] [done] ChatPane tracks `historicalGapsRefreshing` alongside historicalGaps. PREVIOUSLY FLAGGED header renders "refreshing…" pill + 60% opacity on the rail block during in-flight re-fetch (triggered by jarvis:gap-created or jarvis:gap-resolved per 49c3094).`
- `[4] [error-prevention] [error-recovery] [deferred] Needs backend change (ApiSearchDocsReply.error field). Frontend-side already surfaces docsError when HTTP non-2xx; the empty-vs-search-broken-with-200 distinction requires the route to throw the error up explicitly.`
- `[4] [interaction] [direct-manipulation] [done] PdfPane selectionchange now debounces 100ms via tooltipDebounceRef. Continuous mid-drag events cancel the prior timer; tooltip materialises only after selection stabilises. pdfSelection.test.tsx updated to use a string-body fetch mock (deterministic blob.size across jsdom versions) + explicit waitFor timeout to accommodate the debounce window.`
- `[4] [layout] [edge-clipping] [done] PdfPane tooltip x now clamped via Math.max(GUTTER, Math.min(rawX, containerW - TIP_W - GUTTER)); y flips above the selection when rawY + TIP_H > containerH. TIP_W=180, TIP_H=26, GUTTER=8 constants documented inline. Common popover edge-detection pattern.`
- `[4] [interaction] [state-visibility] [done] App.tsx now derives missingPinnedTask = !isDefault && taskExists === false. When true, a yellow banner (data-testid="missing-pinned-task", role="status" aria-live="polite") renders above the main content: "couldn't open last task ({taskId}) — pick another below". Distinct from the deduped-notice banner.`
- `[4] [a11y] [keyboard-nav] [done] PdfPane tooltip button is now auto-focused when it appears via a useEffect on the tooltip state. aria-describedby="pdf-tooltip-hint" wired to a sr-only <span> announcing "press Enter to flag this selection as a knowledge gap". Keyboard users selecting via shift+arrow now get an immediate focusable affordance.`

## Phase 5 — Corpus expansion

- `[5] [ops] [reingest-runbook] [done] Post-crawl knowledge-graph rebuild on VPS: ssh root@46.247.109.91 "set -a; source /opt/jarvis/.env; set +a; /opt/jarvis/jarvis-kotlin/bin/jarvis-kotlin ingest-corpus". Idempotent on existing nodes; new files in _extras/<subject>/extracted/ become new nodes + edges. Run after each crawler invocation that fetches new PDFs.`
- `[5] [crawler] [off-site-content] [deferred] Per-source adapters (GoogleDriveFolderScraper, GitHub releases) are substantive backend work that needs OAuth + per-source quirks. Defer until the user signals corpus-growth is actually blocked by this — current ICS + manual upload paths are sufficient.`

## Phase 6 — Task autonomy

- `[6] [interaction] [recognition-over-recall] [done] ActiveTaskDashboard now filters tasks.filter(t => t.status === "ACTIVE" || t.status === "TODO" || t.status == null) before ranking. SUBMITTED / GRADED / ARCHIVED rows no longer leak into the "ACTIVE TASKS" header. Server-side ?status= param deferred — client-side filter is sufficient for the visible-set semantics.`
- `[6] [interaction] [feedback-honesty] [done] Dashboard header copy is now "ACTIVE TASKS · ranked by urgency × weight" — readiness mention dropped per option (a) until KnowledgeRepo.confidence is wired into rankTask. No longer promises a signal the math doesn't compute.`
- `[6] [layout] [goal-gradient] [deferred] Tied to Phase 7 KnowledgeRepo.confidence wiring through rankTask. Until readiness is a real signal (not constant 0.5), a per-row readiness pill would be visual-only — explicitly NOT what feedback-honesty MED wanted closed. Re-open when confidence query lands.`
- `[6] [interaction] [hicks-law] [done] Dashboard caps to VISIBLE_CAP=7 rows by default. When ranked.length > 7, a "+ N more" toggle (data-testid="active-task-show-all") sets showAll=true and renders the full list. Hick's Law gate restored once cron-triggered FiimaterialsSource starts populating.`
- `[6] [interaction] [direct-manipulation] [stale] The materials-rail described here doesn't exist in the post-Slice-1.5 TutorWorkspace. ResourceRail now renders per-item buttons that dispatch by item.type (PDF / SCRATCHPAD / CONCEPT / PRIOR_GAP / FSRS_DUE) and open drawers or navigate. The "clickable materials" spec intent is met by the new architecture, just through a different surface.`
- `[6] [layout] [edge-clipping] [stale] TutorWorkspace.tsx no longer renders a materialPaths rail directly (the data was lifted into ResourceRail's per-item buttons under the Slice 1.5 rewire). The truncate-without-title concern doesn't apply to the new ResourceRail item buttons, which use full label rendering.`
- `[6] [interaction] [state-visibility] [done] ActiveTaskDashboard detect-result panel now appends "· last run {Ns/Nmin/Nhr} ago" computed from a new detectAt: number | null state set when the detection run completes. Format helper formatRelativeAgo handles s/min/hr granularity. User now knows whether they should click again or wait.`
- `[6] [interaction] [recognition-over-recall] [deferred] Needs backend change (ApiTaskView.sourceId JOIN through DetectedTaskMappingTable). UI half is trivial once the field exists. Re-open when the backend lands the JOIN.`
- `[6] [interaction] [progressive-disclosure] [deferred] TaskQuickStart refactor to accept a tasksHidden prop. Mechanically simple but cosmetic — wait until a user actually expands "+ Manual entry" on a populated dashboard and complains, since the duplicate inventory only surfaces on power-user dashboards.`
- `[6] [a11y] [button-semantics] [done] Dashboard rows refactored: the whole-card button is gone. Each row is now a non-interactive <li> with subject + title content + an explicit OPEN button (data-testid="active-task-open-btn", aria-label="Open task {title}"). Matches spec § 6.5 verb chip. Future per-row secondary actions (archive, snooze) can co-exist without nesting interactive elements.`
- `[6] [error-prevention] [feedback-response-time] [done] ActiveTaskDashboard now tracks detectError separately from detectResult. 502/503/exception paths surface in a distinct red "detection failed: {msg}" banner (data-testid="active-task-detect-error", role="alert") instead of being swallowed silently. HTTP error message preserved through the throw path.`

End: TOTAL: 0 high, 5 med, 6 low.

## Phase 7 — Layer C

- `[7] [a11y] [accessibility-fundamentals] [done] ConceptInline button now carries aria-label="Open references for {name}" + aria-haspopup="dialog" + aria-expanded={open}.`
- `[7] [a11y] [keyboard-nav] [done] Both drawers now (a) auto-focus the × close button on mount via closeBtnRef.current?.focus(), (b) dismiss on Escape via a document-level keydown listener that calls onClose, (c) dismiss on backdrop click via an aria-hidden overlay div (z-29 for ConceptDrawer, z-19 for KnowledgeLedger) one z below the drawer itself. Full focus-trap (tab cycling pinned inside the dialog) deferred — current scope handles the dominant a11y failure modes (no keyboard escape, no visual click-outside affordance). Native <dialog> migration also deferred — the manual approach lets the ResourceRail drawer pattern stay homogeneous. Test coverage: ConceptDrawer.test.tsx + KnowledgeLedger.test.tsx assert all three behaviors.`
- `[7] [a11y] [aria-current-value] [done] KnowledgeLedger filter buttons now carry aria-pressed={filter === f}. SR users hear pressed/unpressed state on the active filter.`
- `[7] [responsive] [touch-target] [done] KnowledgeLedger filter buttons (ALL/OPEN/RESOLVED via map) + drawer × close bumped py-1 → py-2 sm:py-1 in the same sweep as P2/P4 touch-target. Sidebar LEDGER trigger (already py-2) left as-is per the action-list scope (the backlog entry's explicit fix-list named filter+close only).`
- `[7] [interaction] [feedback-response-time] [done] Both drawers now track loadError: string | null and render distinct error UI (data-testid="concept-drawer-load-error" / "ledger-load-error") with the HTTP status / network message when the GET fails. Legitimate empty results still render the existing empty-state copy. Test coverage in ConceptDrawer.test.tsx + KnowledgeLedger.test.tsx asserts the distinction.`
- `[7] [layout] [z-index-stacking] [partial] Both drawers now render an aria-hidden backdrop div one z below themselves (z-29 / z-19) with onClick=onClose. Stacked-drawer scenario (Ledger open → click concept → ConceptDrawer opens on top) now visually communicates the layering AND clicking the backdrop dismisses the top drawer cleanly. Focus restoration to the underlying trigger (returning focus to the Ledger after ConceptDrawer closes) is NOT yet implemented — would need a lastFocusedRef on each open. Native <dialog> migration deferred.`
- `[7] [layout] [chunking] [done] KnowledgeLedger caps to LEDGER_VISIBLE_CAP=50 rows after sorting by reusedCount DESC. When filtered.length > 50, a notice (data-testid="ledger-cap-notice") renders "showing top 50 of N by reuse count · N more hidden". Grouping by subject/type deferred (current cap is sufficient until usage shows reuse-only sort hides relevant rows).`
- `[7] [interaction] [state-visibility] [done] KnowledgeLedger's gap-list useEffect now subscribes to both jarvis:gap-resolved and jarvis:gap-created window events and re-fetches /api/v1/gaps on either. Stale "open" status no longer persists across resolve-elsewhere actions. Test coverage in KnowledgeLedger.test.tsx asserts the re-fetch contract for both event types.`
- `[7] [security] [input-sanitization-deferral] [deferred] Backend wiring in JarvisToolset.chat(). Substantive security work; revisit when prompt-injection actively becomes a vector (e.g. someone exploits a Telegram message path).`
- `[7] [backend] [implicit-gap-detection-deferral] [done] Per the active-roadmap memory (commit c234290): /api/chat already invokes ImplicitGapDetector + upserts implicit gap on trigger phrases. The Phase 7.4 deferral was closed in the post-overhaul polish wave.`
- `[7] [a11y] [section-headings] [done] Both drawers' headers promoted: KnowledgeLedger header → <h2 id="ledger-heading">, ConceptDrawer header → <h2 id="concept-drawer-heading">, with aria-labelledby on the respective dialog divs. Pairs with the aria-modal + focus-mgmt additions to complete the modal-dialog contract.`
- `[7] [interaction] [direct-manipulation] [done] KnowledgeLedger rows that carry g.taskId are now wrapped in a button (data-testid="ledger-row-open") that navigates to /?taskId={taskId} + calls onClose to dismiss the ledger. Rows without taskId (orphan / pre-task gaps) render non-interactive as before. KnowledgeLedger.test.tsx covers the click→navigate+close contract.`
- `[7] [backend] [progressive-disclosure] [deferred] Backend regex tweak in ImplicitGapDetector. Low-impact; defer until topic-truncation actually surfaces in real gap data.`
- `[7] [interaction] [feedback-response-time] [deferred] Backend perf — token-set index or embedding shortcut. Premature; current single-user workload (n_gaps < 100) is well under the O(N=500) cliff. Re-open if gap latency becomes user-visible.`
- `[7] [backend] [reuse-affordance] [deferred] Backend ApiGapView.reused boolean + UI ribbon. Mechanically small but cross-cuts envelope; bundle with a future ApiGapView v2 if other fields motivate a change.`
- `[7] [interaction] [error-recovery] [done] Closed jointly with the feedback-response-time MED entry above — ledger now renders ledger-load-error block when GET fails, distinct from the "no gaps yet" empty state.`
- `[7] [security] [defense-in-depth] [deferred] Backend scrubber hardening (HTML-decode + NFKC-normalize + whitespace-collapse pre-pass). Bundle with the input-sanitization-deferral wire-up; both are touching PromptInjectionScrubber.`
- `[7] [visual-polish] [credibility-trust] [done] 📒 emoji prefix dropped from Sidebar LEDGER button. Pure-text "LEDGER" with the same tracking-widest treatment matches the rail's other entries + the brutalist-mono register.`
- `[7] [interaction] [progressive-disclosure-deferral] [done] Verified already shipped: ConceptInline.tsx lines 22-37 fetch GET /api/v1/concept-confidence?name=..., set linked=false when server returns linked:false (i.e. confidence >= threshold), and render <span data-testid="concept-plain"> for plain text. Failure-mode default ("over-surface") preserved.`
- `[7] [interaction] [chatpane-integration-deferral] [done] Verified already shipped in current code: ChatPane.send() calls parseConcepts() after chip parsing (line 148), threads the concepts array into the jarvis message, and renderJarvisProse() at line 89 maps CONCEPT{N} sentinels to <ConceptInline name=...>.`

## Phase 8 — Layer D + ops + final audit

- `[8] [ops] [cron-probe-installed] [done] /etc/cron.d/jarvis-probe weekly Mon 06:30 UTC. Logs to /var/log/jarvis-fallback-probe.log. Smoke-tested 2026-05-10 11:15Z: chain 3 models OK (llama 429 transient, z-ai 200, openai 200). Probe script lives at /opt/jarvis/tools/ on VPS — note: deploy.sh doesn't scp tools/, so any tools/ updates need explicit scp before they take effect on the next cron tick.`
- `[8] [ops] [deploy-tools-rsync] [deferred] deploy.sh scp-tools block. Manual scp suffices for current cron-script update cadence (rarely). Add when the manual scp habit slips into an outage.`
- `[8] [navigation] [jakobs-law] [done] Workspace tab Link.to is now dynamic: explicitTaskId && !pickMode → /?taskId=${explicitTaskId} (no-op); else /?pick=1. Clicking workspace while pinned no longer destroys the pinned-task state.`
- `[8] [interaction] [chunking] [done] ChatPane now filters historicalGaps.filter(g => g.resolvedBy == null) before render. PREVIOUSLY FLAGGED count reflects open gaps only. Top-N cap not added — open-only filter alone keeps the rail bounded in practice; revisit if a single task accumulates >10 unresolved gaps.`
- `[8] [interaction] [chatpane-input-disabled] [done] Dropped `disabled={sending}` from the textarea — user can compose a follow-up while a reply is in flight. SEND button keeps its disabled-while-sending semantics (no queue + no cancel implemented; the in-flight send still wins). Closes Jakob's-Law deviation.`
- `[8] [interaction] [plotlyembed-chatpane-deferral] [done] Verified already shipped in current code: ChatPane.send() calls parsePlotly() after parseConcepts (line 149), and renderJarvisProse() at line 95 maps PLOTLY{N} sentinels to <PlotlyEmbed figure=... indexLabel="FIG N+1" />.`
- `[8] [a11y] [aria-pressed] [done] TasksScreen subject preset buttons already carry aria-pressed={subject === s} (post-shipped). TaskQuickStart preset cards are not a single-select group (each card creates a different task) so aria-pressed does not apply there.`
- `[8] [a11y] [aria-expanded] [done] Sidebar LEDGER button gained aria-expanded={ledgerOpen} + aria-haspopup="dialog". ActiveTaskDashboard "+ Manual entry" already had aria-expanded={showManual} (post-shipped).`
- `[8] [a11y] [aria-modal] [done] aria-modal="true" added to both ConceptDrawer + KnowledgeLedger. Focus-trap from the Phase 7 [7] keyboard-nav entry still open; aria-modal alone is incomplete without it (announces inert background but background is still actually-clickable).`
- `[8] [visual-polish] [token-coverage] [done] PlotlyEmbed now uses className="w-full max-w-[800px]" instead of inline style={{ maxWidth: 800 }}. Tailwind-class form is at least more discoverable for grep-based refactors than the inline literal. Future token migration (--plot-max-width: 50rem) remains an option but no longer hidden inside a style prop.`


## Audit 2026-05-17 — LOW findings catalogued (deferred)

51 LOW findings from the Slice-1.5 audit (`docs/standin-findings/audit-slice15-2026-05-17.md`). Mostly LLM-judge subjective UX nits (concatenation noise, label ambiguity, etc.). Full table in the audit findings doc; not duplicated here to avoid drift. Re-run audit with `node tools/audit-slice15.mjs ...` to regenerate.

Highlights worth triaging manually:
- LLM judge "closeworkspacetasksreviewtrustREADY" appears across many states — innerText artifact (no space between sibling nav links in DOM). Real visual gap? Or audit-tool artifact? Needs human verify.
- Sidekick `?` icon ambiguity flagged repeatedly — "Select text or click ? to ask the sidekick" — `?` not visually visible.
- Concatenation in rail item labels: "PDFTema_A.pdf p.1SCRATCHPADdraft answers..." — same innerText artifact.
- "☐③ DRILL · YOUR TURN[PRACTICE]" — mixed checkbox/number/bracket label not self-explanatory.

Real follow-up work (HIGH bucket, not in LOW):
- ~~`[audit-2026-05-17] [HIGH] [backend]` POST /api/v1/task-detect/run returns 500~~ — **FIXED 2026-05-17**: 2 root causes (prod chmod on archival dirs + TaskRepo.findByUserSubjectTitle upsert wired into /api/v1/task-detect/run). Live probe HTTP 200 with `{inserted:0, existing:4, total:4}`. See commit 57fbe9c + `docs/standin-findings/backend-recon-2026-05-17.md`.
- ~~`[audit-2026-05-17] [HIGH] [backend]` POO C1 task prep 404~~ — **FALSE POSITIVE 2026-05-17**: endpoint returns 200 with empty drills when fully authenticated. Audit caught the lazy-generation race; closed via prep pre-warm in `tools/audit-slice15.mjs`. See commit 92ed2db.
- ~~`[audit-2026-05-17] [HIGH] [css]` axe AA color-contrast~~ — **FIXED 2026-05-17**: bumped `text-[9px] text-page-fg/40` → `text-[11px] text-page-fg/75` in DrillStack; analogous in App.tsx + DaemonHealthPill. Post-audit S-07/S-08/S-10/S-11 AA findings cleared (AAA on rubric heading remains, lower priority).
- ~~`[audit-2026-05-17] [HIGH] [feature]` Sidebar component never mounted~~ — **CLOSED 2026-05-17 via Option C**: per council `1778988899-sidebar-dead-code` (5/5 CONDITIONAL on C), Sidebar.tsx + Sidebar.test.tsx deleted; LEDGER reachable via new `<button data-testid="header-ledger-btn">ledger</button>` in App.tsx header nav between `trust` and READY status. Audit spec S-21 testid renamed accordingly; S-22..S-26 chain-resolve. **Deferred (Sidebar deletion 2026-05-17):** per-subject task list grouping + subject confidence % badges (Sidebar was the sole consumer of `/api/v1/subject-confidence`; endpoint stays live for future re-surfacing). **Carried risk:** if LEDGER itself sees no user usage within 2 weeks, next move is delete KnowledgeLedger too (Option B fallback per council Devil's Advocate KEY CONCERN).
- ~~`[audit-2026-05-17] [HIGH] [spec]` parseStateMatrix picks up extra rows~~ — **FIXED 2026-05-17**: `inFence` tracking added in commit 92ed2db.
- ~~`[audit-2026-05-17] [MED] [audit-tool]` snake-case-leak false positives~~ — **FIXED 2026-05-17**: `detectSnakeCase(text, {skipFilenameTokens: true})` option added; audit-slice15 passes it; `intro_print` in `.pdf` filename dropped. Commit 92ed2db.

