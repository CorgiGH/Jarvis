# Tutor Overhaul — Backlog

Created: 2026-05-10 (Phase 1).

Source-of-truth doc for MED/LOW findings produced by each phase's UX-Playbook gate. HIGH severity findings block phase advancement and are fixed in-place — they should never appear here. Phase 8's final audit rolls everything up; entries can be deleted as they ship.

Format per entry:
- `[phase] [category] [principle] [severity med|low] [finding] — [recommended action]`

## Phase 1 — Critical bugs

- `[1] [backend] [idempotency] [med] POST /api/v1/tasks read-then-write race under truly-concurrent clients — Phase 1 Playwright gate (2026-05-10) used synthesized JS dispatchEvent x3 to fire 3 simultaneous identical POSTs and got 3 distinct task rows. The pre-INSERT lookup runs in a separate transaction from the INSERT, so concurrent requests can all miss the lookup and all INSERT. Real-user UX is protected by TaskQuickStart's disabled={busy != null} gating (one click sets busy → other preset buttons disabled before second click registers); the natural browser_click x3 in the same gate run fired only once (page navigated after first click, breaking locator). Race window for real users requires a non-disabled-button entry path (e.g. raw curl, future custom task UI bypassing busy). — Phase 6 closes this with TaskDetector + unique index on (user_id, subject, title) WHERE status='ACTIVE'. Comment at TutorRoutes.kt:589-591 already documents the race; no Phase-1 fix expected.`
- `[1] [frontend-test] [scroll] [low] TutorWorkspace.scroll.test.tsx asserts class contracts only (jsdom can't measure layout). Real scroll-engaged behavior is verified by Playwright gate, not vitest. — When Phase 2 lands typography tokens that change line-height + min-h propagation, re-run Playwright gate scenario 1 to make sure scroll math still holds. No backlog action needed unless Phase 2 regresses.`
- `[1] [layout] [mobile-first] [med] Mobile stack order is PDF-on-top / chat-below. Chat is the primary action; on phones user must scroll past full PDF height before reaching the input. — Cap PDF height on mobile (e.g. max-h-[40vh] sm:max-h-none) instead of flex-col-reverse, so reading order stays top→down without DOM-reverse semantic confusion. Phase 2 touch audit territory; revisit there.`
- `[1] [layout] [gestalt-grouping] [med] On mobile the column stack reads PDF + Scratchpad (no internal divider) → thick black bar → Chat. PDF↔Scratchpad visually merge. — Add border-t-4 border-black sm:border-t-0 above Scratchpad so three regions read as three on mobile and two columns on desktop. Phase 2 sweep.`
- `[1] [layout] [responsive-breakpoint] [med] Two 50/50 columns at 640-820px (small tablets / landscape phones) produce ~310px each — ChatPane SEND button + PdfPane both cramped. — Bump column-split breakpoint to md (768px): change sm:flex-row sm:w-1/2 sm:border-b-0 sm:border-r-4 → md: variants. Phase 2 reflow pass.`
- `[1] [interaction] [feedback-response-time] [med] Submit/preset buttons today disable while busy (TaskQuickStart line 140) but other surfaces creating tasks (custom task page, future curl/API) lack the in-flight signal. Dedup-200 path also returns silently fast enough that users may not notice the inline busy state. — Phase 2.5 already commits to spinner/disabled-state polish; ensure all task-create entry points get covered there.`
- `[1] [layout] [scroll-containment] [low] PdfPane sibling has overflow-hidden on its wrapper. On mobile when PDF is stacked, the iframe can clip to 0 height if chat content dominates first paint. — Add min-h-[50vh] sm:min-h-0 on the PDF wrapper for mobile breakpoint. Phase 2 mobile sweep.`

## Phase 2 — UX foundation

- `[2] [test-infra] [playwright-zoom] [low] Phase 2 Playwright gate strict outlineWidth==='2px' assertion is brittle to harness viewport zoom. The Playwright MCP session rendered at effective 80% zoom; all px-valued border/outline widths returned 0.8x in computed styles (probe controls confirmed: literal outline:3px → 2.4px, border:10px → 9.6px). Real-user browsers at 100% see the authored 2px ring. — Phase 8 final audit: either pin Playwright session to 100% zoom OR change strict-literal assertion to a numeric ratio check (e.g. width >= 1.5px). axe-core a11y scan was unaffected and is the load-bearing check; passed clean on both /tutor/ and /tutor/tasks with zero AA violations including scrollable-region-focusable.`
- `[2] [responsive] [touch-target] [done] Action buttons bumped py-1 → py-2 sm:py-1 across KnowledgeGapCard (5 buttons: INSERT/RESOLVE/FLAG WRONG/SHOW DOCS/PROMOTE→FSRS), SuggestedEditCard (APPLY/REJECT), TasksScreen (subject preset/CREATE/OPEN/DELETE), TrustSettings (GRANT + REVOKE — latter was the worst at py-0.5). Mobile-only mobile bump matches ChipRow canonical pattern. Computed ~32-36px tall on mobile (text-xs + py-2 + 1-2px border) vs prior ~20-24px. vitest 305/305 green; Playwright 375px verification deferred (rests on the same ChipRow precedent).`
- `[2] [a11y] [form-labeling] [med] ChatPane chat input (line 142) has placeholder-only labeling — no <label htmlFor> and no aria-label. Placeholder disappears on focus and has weaker contrast than label text per [[Accessibility Fundamentals]]; SR users may hear nothing. — Add <label htmlFor="chat-input" className="sr-only">Message Jarvis</label> + id="chat-input" on the input. Same pattern for Scratchpad (currently aria-label only is acceptable but visible label binding via htmlFor on the SCRATCHPAD header would be stronger).`
- `[2] [responsive] [header-overflow] [med] App.tsx header at <420px viewport: "JARVIS · TUTOR" + taskId + "× close" + workspace/tasks/trust nav fits via flex but truncates the taskId chip and crowds the nav. No flex-wrap, no burger. — Add flex-wrap on the outer header div + min-w-0 already present on inner; consider hiding the taskId chip below sm:. Or move nav to a second line below sm: via flex-col sm:flex-row.`
- `[2] [visual-polish] [typography-tokens] [med] index.css defines --type-sm (12px), --type-lg (18px), --type-h2 (20px) but no component references them — every component still uses tailwind text-xs/text-sm/text-lg literals. Token contract is half-applied: colors flow through tokens (post Phase-2 fixup), font sizes don't. — Either drop the unused --type-* vars OR migrate text-xs → text-[length:var(--type-sm)] (less ergonomic) OR define matching @theme --text-* aliases so Tailwind's default text-* utilities map onto our scale. Lowest-friction: drop the dead vars; revisit when a real reskin demands them.`
- `[2] [a11y] [aria-current-value] [low] Sidebar.tsx line 98: task buttons use aria-current={active ? "true" : undefined}. These are routing buttons inside a <nav>, so aria-current="page" is the better-specified value per ARIA 1.2; "true" is the unspecified-context fallback. — Change "true" → "page". Behaviorally identical for SR; better semantics.`
- `[2] [a11y] [section-headings] [low] Sidebar.tsx subject group headers (line 80) and Scratchpad.tsx panel header are <div>s, not <h-N> tags. Screen-reader users navigating by heading (H key) can't jump to subject sections or scratchpad. — Promote subject headers to <h3> inside the <nav>; promote SCRATCHPAD label to <h2 id> + bind the textarea via aria-labelledby. KnowledgeGap "GAP · TYPE · TOPIC" and SuggestedEdit "SUGGESTED · TYPE" labels are inline message-attached chips, not navigable sections — leave as <div>.`
- `[2] [a11y] [redundant-role] [low] Sidebar.tsx line 83: <ul role="list">. Implicit role on <ul> is already "list"; redundant unless the parent CSS sets list-style:none which can strip the role in Safari reader. Tailwind preflight does set list-style:none, so the role IS load-bearing here. — Keep as-is. Documenting so future cleanup doesn't strip it.`
- `[2] [visual-polish] [page-h1-missing] [low] Workspace page (/tutor/?taskId=...) has no <h1>. The chat header "JARVIS · TASK X" is a <div>, the PDF panel uses a <div>, etc. /tasks and /settings/trust each provide an <h1>. — Add visually-hidden <h1>Tutor workspace · {taskId}</h1> at TutorWorkspace top. Helps SR landmark navigation; one h1 per page is the wiki [[Visual Hierarchy]] expectation.`

## Phase 3 — Hygiene

- `[3] [ops] [pii-scrub] [done] core_memory.md PII scrubbed on VPS 2026-05-10. Removed line "- Matricol: 31091001031ROSL251002" from /opt/jarvis/data/core_memory.md via single sed -i; restarted jarvis service; journalctl -u jarvis --since restart shows 0 CoreMemory WARN. Closes spec § 3.5.`
- `[3] [test-infra] [log-tail-stale] [low] Phase 3 Playwright gate scenario 4 used "tail -200 /var/log/jarvis.log | grep -c CoreMemory.WARN" which counts pre-restart lines (the log file is append-only across restarts; tail looks at a fixed window). The substantive "any new WARN since restart" check needs journalctl with a since-time bound. — Phase 8 final audit: change the gate's stale-WARN check to "journalctl -u jarvis --since <restart-time> | grep -c CoreMemory.WARN" so tail's window doesn't masquerade as recent activity.`
- `[3] [interaction] [feedback-response-time] [med] TutorWorkspace.tsx scratchpad debounced PUT (500ms) is fully silent: no "saving…/saved/save failed" indicator, and the .catch(() => {}) swallows errors. User has no signal that their notes are persisted. If the network drops, they continue typing and lose work on next reload (server still holds stale text). — Add a small status pill near the SCRATCHPAD header (idle/saving/saved/error). Replace .catch(() => {}) with setSaveError so a failure surfaces inline. Pattern matches Notion/Google Docs auto-save UX.`
- `[3] [interaction] [state-visibility] [med] TutorWorkspace.tsx scratchpad mount sequence has a clobber race: localStorage hydration setScratch(v) runs first effect, server-fetch effect runs and overwrites with setScratch(data.text ?? ""). If user types between local hydration and server response, their keystrokes are wiped. Window is short (~100-500ms) but real on slow networks. — Track a `mounted` ref or use a single-shot "hydrate-once" flag; once user has typed (scratch state changed locally), don't overwrite from server fetch. Or fetch BEFORE rendering the textarea (gate Scratchpad render on hydration complete).`
- `[3] [error-prevention] [error-recovery] [med] SuggestedEditCard.tsx — once apply() fails (clipboard permission denied, type unsupported), status flips to "failed" and the COPY/REJECT button row disappears (line 109 `status === "pending"` gate). User has no retry affordance; a transient clipboard rejection (Safari prompt-deny) leaves the card permanently dead. — Add a "RETRY" button when `status === "failed"` AND `edit.type === "clipboard"` AND error is not READ-ONLY. Reset status back to "pending" on retry click.`
- `[3] [error-prevention] [error-recovery] [med] TutorRoutes.kt PUT /api/v1/tasks/{id}/scratchpad enforces 50_000 char cap server-side but TutorWorkspace.tsx textarea has no visible counter or client-side cap; user typing beyond 50k gets a silent 400 (swallowed by .catch). — Add a char-count indicator below the textarea (e.g. "12345 / 50000") that turns red as it approaches limit; soft-clamp via maxLength={50000} on the textarea so the browser blocks input past the cap.`
- `[3] [interaction] [state-visibility] [low] PdfPane.tsx (jarvisFetch swap) has no loading indicator during initial fetch — only error or final size-badge states. Per [[Feedback & Response Time]] anything >400ms warrants a spinner; cold-cache PDF fetches over slow networks routinely exceed that. — Render "loading PDF…" placeholder while size==null && error==null (i.e. before .then resolves).`
- `[3] [error-prevention] [error-recovery] [low] ChatPane.tsx send() doesn't check res.ok before parsing res.json(). A 500/502 from /api/chat with text body throws inside res.json() and surfaces as a parse-error message rather than the actual HTTP status. Pre-existing; Phase 3 only added AbortController so this is adjacent territory. — Add `if (!res.ok) throw new Error(\`HTTP ${res.status}: ${(await res.text()).slice(0, 200)}\`)` before res.json(). Mirrors the pattern already in ScreenshotCapture.tsx and TaskQuickStart.tsx.`
- `[3] [interaction] [feedback-response-time] [low] Sidebar.tsx jarvis:task-created listener re-fetches but doesn't reset `loaded` — the post-creation refresh is silent (no "loading…" indicator on subsequent fetches, only on first mount). User who creates a task far from sidebar (custom-task page in a future iteration) sees the list update with no transition. — Track refreshing state separately from loaded; show a small refresh indicator (e.g. opacity-60 + spinner near TASKS header) during in-flight re-fetch.`
- `[3] [interaction] [progressive-disclosure] [low] KnowledgeGapCard.tsx truncates exampleCode at 320 chars (line 58) but provides NO show-more toggle for it — only gap.content has progressive expansion. Long code samples become permanently elided with "…". — Add an `exampleExpanded` state mirroring `contentExpanded`; render preview slice + "show more" button when exampleCode.length > 320.`
- `[3] [error-prevention] [undo] [low] KnowledgeGapCard / SuggestedEditCard "MARK RESOLVED" / "REJECT" / "FLAG WRONG" buttons commit immediately with no confirmation and no UI undo path. The action-buttons block disappears on resolution; user must refresh / wait for next chat turn to re-surface a gap they accidentally dismissed. Server log is append-only (CardActionLog), so a future "undo" route is feasible. Per spec § Phase 3 Gates the local-apply-then-best-effort-POST pattern is intentional, so this is informational not a defect. — Phase 4 territory: when tutor_gaps table replaces the inline-only state, expose an "undo last action" affordance for ~5s post-click (toast pattern).`

## Phase 4 — Layer B §4 close

- `[4] [responsive] [touch-target] [done] PdfPane selection tooltip button bumped py-1 → py-2 sm:py-1 in the same sweep as the P2 [2] touch-target entry. px-2 kept (px-3 would only widen, height is the load-bearing dim).`
- `[4] [interaction] [state-visibility] [med] PDF selection → tooltip click POSTs the gap and immediately closes the tooltip with NO confirmation. ChatPane rail re-fetches via jarvis:gap-created, but on mobile the chat column is below the PDF — user sees the tooltip vanish and nothing else. Direct-manipulation gate intent (selection→tooltip→action chain feels immediate) is partially met: action is immediate, RESULT is invisible without scrolling. — Add a transient inline confirmation (e.g. floating chip "added to gaps ↓" anchored at the click point for 1.5s) OR scroll-into-view + flash the new card in the chat rail when jarvis:gap-created fires. Either resolves the closed-loop signal.`
- `[4] [interaction] [state-visibility] [med] KnowledgeGapCard local resolution (markResolved/dismiss) flips the card's UI to hide actions, but the historicalGaps rail in ChatPane caches the GET result on mount + re-fetches on jarvis:gap-created — it does NOT re-fetch on resolution. After a resolve action, a refresh / remount re-renders the same gap with full action row again because the parent never propagates the resolution back into historicalGaps state. Local card UI says "resolved", remount says "open". — Either (a) listen for a new "jarvis:gap-resolved" custom event in ChatPane and re-fetch / soft-remove from historicalGaps, or (b) plumb a parent-level onResolve from rail cards that filters historicalGaps by id. Server has resolvedBy persisted; serverside-only the next GET /gaps already returns it, so just wiring the re-fetch closes the loop.`
- `[4] [interaction] [error-recovery] [low] KnowledgeGapCard postStatus failure surfaces sync-error inline (good — fail-loud per spec) but the local UI has already flipped to resolved=USER_MARKED_DONE; the action row is gone so there's no retry button. User is stuck with "resolved + sync failed" until next remount. Mirrors P3 SuggestedEditCard retry-affordance gap. — Add a RETRY button next to the syncError banner that re-runs postStatus(resolved!) and clears syncError on success.`
- `[4] [interaction] [feedback-response-time] [low] ChatPane jarvis:gap-created listener re-fetches /api/v1/gaps?taskId=… silently — no "loading PREVIOUSLY FLAGGED…" indicator during in-flight refresh. Mirrors P3 [3] sidebar refresh-state entry. — Track refreshing alongside historicalGaps; show opacity-60 + small spinner near the "PREVIOUSLY FLAGGED" header during fetch.`
- `[4] [error-prevention] [error-recovery] [low] POST /api/v1/gap/{id}/search-docs returns {results: []} both when HybridRetriever throws (catch falls through to emptyList()) AND when search legitimately found nothing. UI shows "no docs found" in both cases — user can't distinguish "search broken" from "search ran, zero hits". — Wrap retriever call to surface a distinct ApiSearchDocsReply.error field; UI renders "search failed: {error}" instead of "no docs found" when error is non-null.`
- `[4] [interaction] [direct-manipulation] [low] PdfPane selectionchange handler fires on every range change with no debounce — tooltip recomputes position + setTooltip on every mouse-drag pixel during selection. On mobile / older devices the tooltip flickers and "follows" the selection in real time, which can flicker out before user releases. — Debounce setTooltip by 80-120ms OR only update once selection is "settled" (mouseup / touchend within container). Lighter-touch fix: track selection-active in a ref and only setTooltip once when sel.toString() stabilizes for one frame.`
- `[4] [layout] [edge-clipping] [low] PdfPane tooltip is positioned absolute at rect.left + rect.bottom with no edge-detection. A selection ending near the right edge of the PDF pane positions the tooltip past the container's right border (clipped); a selection at the very bottom puts it below scrollable content. — Clamp tooltip x to (containerWidth - tooltipWidth - 8) and flip y above selection (rect.top - tooltipHeight - 4) when rect.bottom + tooltipHeight > containerHeight. Common popover pattern.`
- `[4] [interaction] [state-visibility] [low] App.tsx /last-task cross-device restore can redirect to a taskId that the server returns but that has been deleted on this device's view — the subsequent GET /api/v1/tasks check (line 117) fails to find it and silently shows TaskQuickStart. User sees QuickStart with no signal "the task you last opened on another device no longer exists". — When showQuickStart fires because of taskExists===false (not isDefault), surface a one-shot toast / banner "couldn't open last task — pick another below". Distinct from the dedupedNotice banner which fires for a different reason.`
- `[4] [a11y] [keyboard-nav] [low] PdfPane tooltip is reachable only via mouse selection — keyboard users selecting text via shift+arrow can't trigger it (no aria-popup, focus doesn't move to the button). The button has tabindex (default for <button>) but never gets focus because tooltip auto-renders post-selection without focus management. — On selectionchange that produces a tooltip, optionally call tooltip.current?.focus() (or render an aria-describedby hint announcing "press T to flag"); document the keyboard path in copy.`

## Phase 5 — Corpus expansion

- `[5] [ops] [reingest-runbook] [done] Post-crawl knowledge-graph rebuild on VPS: ssh root@46.247.109.91 "set -a; source /opt/jarvis/.env; set +a; /opt/jarvis/jarvis-kotlin/bin/jarvis-kotlin ingest-corpus". Idempotent on existing nodes; new files in _extras/<subject>/extracted/ become new nodes + edges. Run after each crawler invocation that fetches new PDFs.`
- `[5] [crawler] [off-site-content] [med] fiimaterials.web.app dry-run discovers 0 .pdf links because the site links almost exclusively to off-domain hosts (drive.google.com folders, github.com repos, mega.nz folders, profs.info.uaic.ro/~sd, edu.info.uaic.ro/...). The crawler ships + works correctly (40 anchors discovered post-hydration, 0 are direct .pdf URLs); real yield needs per-source adapters: Google Drive folder API enumerator (with OAuth or shared-link fetch), GitHub release downloader, optional fac-page-specific HTML scrapers. — Phase 6+ work: write GoogleDriveFolderScraper that takes a shared-link folder URL + lists+downloads PDFs; same pattern for GitHub releases. Hook into the CourseScraper interface from Phase 5.3. The current crawler stays as a fallback for any direct fiimaterials.web.app static PDFs (none today, may change).`

## Phase 6 — Task autonomy

- `[6] [interaction] [recognition-over-recall] [med] ActiveTaskDashboard renders all tasks from /api/v1/tasks regardless of status — TaskRepo.listForUser returns SUBMITTED / GRADED / ARCHIVED rows alongside ACTIVE / TODO. The dashboard header says "ACTIVE TASKS" but the underlying query doesn't filter. Recognition gate intent (one glance, see what's open) leaks because completed work sits in the same list. — Filter ranked.filter(t => t.status === "ACTIVE" || t.status === "TODO") in ActiveTaskDashboard before sort, OR add a server-side filter param to GET /api/v1/tasks?status=open. Same fix unblocks the Zeigarnik signal — open loops only count if completed loops aren't visually competing.`
- `[6] [interaction] [feedback-honesty] [med] Dashboard header "ranked by urgency × weight × readiness" advertises a 3-axis composite, but taskRanking.ts hard-codes readiness=0.5 default (no KnowledgeRepo.confidence call). The visible signal is urgency × weight only; readiness is a constant. Per [[Credibility & Trust]] the copy makes a promise the math doesn't keep. — Either (a) drop "× readiness" from the header until Phase 7 wires KnowledgeRepo.confidence to readiness, or (b) ship a per-row 3-dot readiness pill (still 0.5 = ●●○) so the rank component is visible even at the placeholder value.`
- `[6] [layout] [goal-gradient] [med] Spec § 6.5 calls for "readiness bar (3 dots filled out of 3)" per row — Goal Gradient signal making progress-toward-completion visible per task. Live ActiveTaskDashboard renders subject + dueTag + title only; no readiness affordance, no progress dots, no submission status. User can't see at-a-glance which task is closest to ship. — Add a per-row readiness chip: ●●○ (2/3) or similar, derived from rankTask's readiness component (currently constant 0.5 → ●○○ until Phase 7 plumbs real signal). Makes the math visible AND prepares the surface for Phase 7's real signal.`
- `[6] [interaction] [hicks-law] [med] No upper bound on visible tasks. With 5 manually-created tasks today the list is fine; once /task-detect/run + cron land + FiimaterialsSource starts flagging metadata.json files (one task per fetched PDF, deadline = now+14d), the dashboard could surface 30+ rows on first run. > 7 visible options breaks the Hick's Law gate. — Cap to top-7 ranked rows by default; render "+ N more" button that expands the rest. Or page at 10. Spec § 6.5 is silent on the cap so this is a correctness gap to plug before the cron starts populating.`
- `[6] [interaction] [direct-manipulation] [med] Reference materials rail (TutorWorkspace.tsx:99-108) renders materialPaths as plain <li> text, not clickable. Spec § 6.3 explicitly calls for "clickable; opens that PDF in PdfPane via the existing per-task PDF route, repurposed to accept ?materialPath=". User sees relevant material paths but cannot act on them — the affordance signals "look, here are docs" but the docs aren't reachable from the rail. — Wrap each path in a <button> that POSTs PdfPane reload with ?materialPath=<path> OR opens a side route /api/v1/archival/file?path=<path>. Repurpose /api/v1/tasks/{id}/pdf to accept materialPath query per spec; PdfPane gains an onClick handler that updates its url prop.`
- `[6] [layout] [edge-clipping] [low] Reference materials rail truncates long paths via truncate class (TutorWorkspace.tsx:105) with no title attribute — user sees "_extras/PS/ps_hw/…" cut at column width with no way to read the full path on hover/long-press. — Add title={p} to the <li> so desktop hover shows the tooltip; on mobile, consider word-break-all OR a "…" expansion affordance. Mirrors the existing Sidebar.tsx pattern (line 109 — title={t.title}).`
- `[6] [interaction] [state-visibility] [low] RUN DETECTION button (ActiveTaskDashboard:82-88) has no "last run at" timestamp. After clicking once and seeing 0 new rows, user has no signal whether the button "did anything" or whether they can wait for cron. Mirrors P3 [3] sidebar refresh-state pattern — silent re-fetch is hostile to the user's mental model. — Show a small "last: 2 min ago · 0 new" line under the button after a detection run completes. POST /api/v1/task-detect/run already returns {inserted, existing, total} (TutorRoutes.kt:886) — surface it inline.`
- `[6] [interaction] [recognition-over-recall] [low] Detection-discovered tasks (POST /task-detect/run path) insert without setting any "auto-detected" flag — DetectedTaskMappingTable carries the source mapping but ApiTaskView surfaces only id/subject/title/deadline/status. Dashboard rows look identical to manually-created tasks; user can't tell "the system discovered this for me" from "I made this last week". Loses Recognition signal that detection IS doing work. — Add a sourceId chip (e.g. "AUTO" / "fiimaterials" / "manual") to ApiTaskView pulled from DetectedTaskMappingTable JOIN; render in ActiveTaskDashboard row as a small tag right of subject.`
- `[6] [interaction] [progressive-disclosure] [low] "+ Manual entry" toggle (ActiveTaskDashboard:90-95) inlines the full TaskQuickStart component which itself renders YOUR TASKS list (line 109-134) on top of dashboard's ranked list. Two task lists stack vertically — once expanded the page renders ranked tasks, then "+ Manual entry", then YOUR TASKS (same data, different sort), then NEW TASK PRESETS. Duplicate inventory. — Either (a) pass a tasksHidden prop to TaskQuickStart that suppresses the YOUR TASKS section when used as fallback (presets-only mode), or (b) inline only the preset grid + custom-task copy, not the full component.`
- `[6] [a11y] [button-semantics] [low] ActiveTaskDashboard row buttons (line 65-75) wrap the entire card in <button> with onClick=navigate. Per spec § 6.5 "Each row shows: subject · title · days · readiness bar · 'open' button" — the explicit "open" verb is missing. Whole-card click works, but discoverability of the action is lower than an explicit verb chip; also makes future per-row secondary actions (archive, snooze) harder to add without nesting interactive elements. — Adopt an explicit "open" button as spec requires, OR document the deviation. Mirrors TaskQuickStart's same pattern (line 121-129) — both surfaces drift from spec the same way.`
- `[6] [error-prevention] [feedback-response-time] [low] runDetection in ActiveTaskDashboard (line 32-46) swallows errors with a bare catch (//best-effort comment). On 502/503 (e.g. fiimaterials walk fails) user sees "RUNNING…" flip back to "RUN DETECTION" with no error surfaced. Compare to TaskQuickStart's setError/render pattern. — Add error: string | null state; surface inline error banner under the buttons row when detection fails.`

End: TOTAL: 0 high, 5 med, 6 low.

## Phase 7 — Layer C

- `[7] [a11y] [accessibility-fundamentals] [med] ConceptInline button has dotted-underline visual but no aria-label / aria-haspopup / role description. SR users hear "<concept name>, button" with no signal it opens a drawer of past references. — Add aria-label={`Open references for ${name}`} + aria-haspopup="dialog" + aria-expanded={open} on the button.`
- `[7] [a11y] [keyboard-nav] [med] ConceptDrawer + KnowledgeLedger open without focus management: close × button doesn't auto-focus, Escape key doesn't dismiss, no focus trap, no click-outside-to-dismiss. Mouse-only escape route violates [[Accessibility Fundamentals]] keyboard-equivalence. — Add closeBtnRef.current?.focus() on mount, useEffect listener for Escape→onClose, focus-trap (e.g. focus-trap-react) OR migrate to native <dialog> element which gives all three for free.`
- `[7] [a11y] [aria-current-value] [med] KnowledgeLedger filter buttons (ALL/OPEN/RESOLVED) use bg-accent + text-page-fg to denote active filter visually — no aria-pressed state. SR users can't tell which filter is currently applied. — Add aria-pressed={filter === f} on each filter button. Same pattern as toggle-button group.`
- `[7] [responsive] [touch-target] [done] KnowledgeLedger filter buttons (ALL/OPEN/RESOLVED via map) + drawer × close bumped py-1 → py-2 sm:py-1 in the same sweep as P2/P4 touch-target. Sidebar LEDGER trigger (already py-2) left as-is per the action-list scope (the backlog entry's explicit fix-list named filter+close only).`
- `[7] [interaction] [feedback-response-time] [med] ConceptDrawer + KnowledgeLedger fetch failure (.catch sets hits=[] / gaps=[]) renders identical to legitimate empty result. User can't distinguish "search broken" from "0 matches". Mirrors P4 [4] search-docs error-vs-empty backlog entry. — Track loadError: string | null separately; render distinct copy "couldn't load — refresh to retry" when error is non-null.`
- `[7] [layout] [z-index-stacking] [med] ConceptDrawer (z-30) + KnowledgeLedger (z-20) can both be open simultaneously — clicking a concept name from inside Ledger spawns ConceptDrawer ON TOP of Ledger with no backdrop. Closing ConceptDrawer doesn't return focus to Ledger; Ledger remains click-through visible behind. — Add a backdrop overlay with onClick=onClose, OR enforce mutually-exclusive open state via shared modal manager. Better: migrate both to native <dialog> which handles backdrop + focus restoration.`
- `[7] [layout] [chunking] [med] KnowledgeLedger renders flat list of all gaps — no grouping by subject / task / type. With 30+ gaps the list violates [[Chunking]] (3-5 units per level) and [[Hick's Law]] (no cap on visible options). Spec § Phase 7 Gates is silent on cap. — Cap visible rows to top-50 by reusedCount/recency; OR group by taskId or type with collapsible <details> sections. Same pattern as P6 [6] dashboard cap (top-7).`
- `[7] [interaction] [state-visibility] [med] KnowledgeLedger fetches gap list once on mount; if user resolves a gap from KnowledgeGapCard "MARK RESOLVED" in chat rail, Ledger doesn't re-fetch — stale "open" status persists until close+reopen. No jarvis:gap-resolved / jarvis:gap-created listeners. Mirrors P4 [4] historicalGaps re-fetch backlog. — Subscribe to jarvis:gap-resolved + jarvis:gap-created events in useEffect; re-fetch gaps on each.`
- `[7] [security] [input-sanitization-deferral] [med] PromptInjectionScrubber.sanitizeInput is implemented but JarvisToolset NEVER calls it on user-message ingestion or tool-return text. Phase 7 deferral per spec — function is dead code today. Closed-loop prompt-injection vector remains open. — Phase 8 ops cleanup: wire sanitizeInput into JarvisToolset.chat() user-message path AND wrap each tool return through PromptInjectionScrubber.wrap before re-feeding the model.`
- `[7] [backend] [implicit-gap-detection-deferral] [med] ImplicitGapDetector.detect is implemented + unit-tested but route-layer NEVER calls it — chat path doesn't invoke detector before LLM round-trip, so end-to-end persistence is missing. Function is dead code today. — Phase 8 ops cleanup: wire ImplicitGapDetector.detect into the chat handler. On hit, POST /api/v1/gap (LLM_GROUNDED, EXPLICIT_ASK trigger) before/after the model reply so the gap is durable even if the LLM fails to emit a <gap> envelope.`
- `[7] [a11y] [section-headings] [low] KnowledgeLedger header "KNOWLEDGE LEDGER" + ConceptDrawer header "CONCEPT · {name}" are <div>s with font-bold. Both are role="dialog" so they need an h-N for SR landmark navigation + aria-labelledby. Mirrors P2 [2] section-headings backlog. — Promote to <h2 id="ledger-title"> + add aria-labelledby="ledger-title" on the dialog div. Same for ConceptDrawer.`
- `[7] [interaction] [direct-manipulation] [low] KnowledgeLedger rows are non-interactive <li> elements — user sees gap topic + status but can't click to open the source task or expand gap content. Recognition Over Recall is half-met: surfaces past work, doesn't let user act on it. — Wrap each row in a <button> that navigates to /?taskId=<g.taskId> when set, OR opens a detail drawer with full content + exampleCode. Spec § Phase 7 doesn't mandate this so it's enhancement, not gate-fail.`
- `[7] [backend] [progressive-disclosure] [low] ImplicitGapDetector regex (.{3,}) is greedy to end-of-line — "what is X and where is Y" captures topic="X and where is Y", >200 chars truncated, meaning lost when topic is multi-clause. — Terminate the capture group on [?.!\n] OR cap to first sentence boundary; e.g. (.{3,}?)(?=[?.!\n]|$).`
- `[7] [interaction] [feedback-response-time] [low] POST /api/v1/gap similarity reuse path runs findSimilar (loads up to 500 user gaps + tokenizes each + Jaccards inline) on every POST. O(N) per request. No index, no embedding shortcut (deferred per Phase 7 spec). Acceptable while gap count <500 but degrades for power users. — Phase 8 perf review: add SQL-backed token-set index OR embed corpus + cosine path. Phase 7 spec already documents the embedding deferral.`
- `[7] [backend] [reuse-affordance] [low] POST /api/v1/gap similarity-reuse responds with HTTP 200 + the existing gap; UI can only infer reuse from reusedCount > 0 (and a fresh gap could legitimately have a previous user-bumped count). No explicit reused: true flag. — Add `reused: Boolean` to ApiGapView; UI can render a "↻ matched existing gap" inline notice on save when true.`
- `[7] [interaction] [error-recovery] [low] KnowledgeLedger fetch failure renders identical empty state to "0 gaps yet" — user can't distinguish broken from empty. Mirrors the same pattern noted on ConceptDrawer above and on P4 [4] search-docs. Already covered by the "feedback-response-time / med" entry above; this is the duplicate manifestation in Ledger. — Same fix (track loadError separately) closes both.`
- `[7] [security] [defense-in-depth] [low] PromptInjectionScrubber.scrub neutralizes literal `<system>` etc via simple String.replace, but doesn't normalize lowercase-spaced variants ("< system >"), HTML entities ("&lt;system&gt;"), or unicode lookalikes (e.g. fullwidth `<`). — Pre-normalize: HTML-decode + collapse-whitespace + NFKC-normalize before running the scrub passes. Backlog only — current pass covers the common attack surface.`
- `[7] [visual-polish] [credibility-trust] [low] Sidebar LEDGER button uses 📒 emoji prefix; rest of the sidebar is text-only ("+ NEW TASK", "TASKS"). Inconsistent visual register vs the brutalist-mono mockup which avoids emoji. — Drop the 📒 OR use a typographic glyph (▣ / ◾ / [≣]) consistent with the palette. Pure-text "LEDGER" with the same tracking-widest treatment matches the rail's other entries.`
- `[7] [interaction] [progressive-disclosure-deferral] [low] ConceptInline renders ALL concepts as inline links — confidence-gating threshold (env JARVIS_CONCEPT_LINK_CONFIDENCE_THRESHOLD) is deferred per Phase 7 spec until KnowledgeState confidence query is plumbed through the tutor surface. Documented deferral. — Phase 8 (or Layer C extension): add KnowledgeRepo.confidence(userId, name) call inside ConceptInline; render as plain text when conf >= threshold (no need to expand) and as inline-button when below threshold.`
- `[7] [interaction] [chatpane-integration-deferral] [low] ChatPane never calls parseConcepts() — assistant replies render raw <concept>name</concept> XML rather than ConceptInline buttons. parseConcepts + ConceptInline both ship but have no incoming render path. Documented deferral per Phase 7 spec. — Phase 8: wire ChatPane to invoke parseConcepts(reply.text) and replace the CONCEPTn sentinels with <ConceptInline name=... /> at render time.`

## Phase 8 — Layer D + ops + final audit

- `[8] [ops] [cron-probe-installed] [done] /etc/cron.d/jarvis-probe weekly Mon 06:30 UTC. Logs to /var/log/jarvis-fallback-probe.log. Smoke-tested 2026-05-10 11:15Z: chain 3 models OK (llama 429 transient, z-ai 200, openai 200). Probe script lives at /opt/jarvis/tools/ on VPS — note: deploy.sh doesn't scp tools/, so any tools/ updates need explicit scp before they take effect on the next cron tick.`
- `[8] [ops] [deploy-tools-rsync] [low] tools/ directory is not part of build/install/jarvis-kotlin so deploy.sh doesn't ship updates to it. Cron entry depends on /opt/jarvis/tools/probe-fallback-models.sh being current. — Add an scp -rq tools/ root@VPS:/opt/jarvis/ block to deploy.sh so probe-fallback-models.sh + future tools stay synced. Until then: scp manually after editing the script.`
- `[8] [navigation] [jakobs-law] [med] App.tsx header "workspace" Link is hard-coded to /?pick=1, so clicking the workspace tab while pinned to /?taskId=X always unpins the task and returns user to ActiveTaskDashboard. Same-tab-click expectation in Notion / Canvas / MDN: no-op or scroll-to-top. Here it destroys workspace state. — Make workspace tab Link.to dynamic: when explicitTaskId present and not pickMode, link to `/?taskId=${explicitTaskId}` (no-op route); only fall back to /?pick=1 when no task pinned. Or render the Link as <span aria-current="page"> when already on workspace.`
- `[8] [interaction] [chunking] [med] ChatPane historicalGaps GET /api/v1/gaps?taskId= renders ALL gaps regardless of resolvedBy — no client-side filter, no server-side ?status= param. After a few task sessions "PREVIOUSLY FLAGGED (N)" balloons with resolved + dismissed cards stacked above the most recent assistant reply, pushing recency below the fold. Compounds w/ Hick's Law on dense workspaces. Distinct from the [4] cache-stale entry (which covers re-fetch-on-resolve); this is about steady-state list bloat. — Filter `historicalGaps.filter(g => g.resolvedBy == null)` before render OR add ?openOnly=true server-side. Cap to 10 most-recent OR collapse resolved into a "+ N resolved" expandable subsection.`
- `[8] [interaction] [chatpane-input-disabled] [med] ChatPane input is disabled={sending} (line 222) so user can't compose a follow-up while the previous reply is still streaming. Modern chat patterns (ChatGPT, Claude, Slack) keep the input enabled and either queue the next send or cancel the in-flight reply on enter. The disabled flag is a Jakob's-Law deviation + makes long-thinking turns feel hostile. — Drop `disabled={sending}`; let the user type freely. SEND button stays disabled until ac.signal aborts the prior request OR queue the next message after the reply lands.`
- `[8] [interaction] [plotlyembed-chatpane-deferral] [low] PlotlyEmbed + parsePlotly both ship + tested but ChatPane never imports either. Phase 8 task #83 was titled "PlotlyEmbed lazy + ChatPane" — only the lazy half landed. Mirrors Phase 7 ConceptInline / parseConcepts deferral exactly. — Wire ChatPane: invoke parsePlotly(chipParsed.body) before render; replace each PLOTLYn sentinel with <PlotlyEmbed figure={plots[n].json} /> at the same render-time substitution site that ConceptInline will use.`
- `[8] [a11y] [aria-pressed] [low] Subject preset buttons in TasksScreen (PA/PS/POO/ALO/SO line 82-92) and TaskQuickStart use bg-panel-dark-bg vs bg-page-bg to denote active subject — no aria-pressed state. SR users can't tell which subject is currently selected. Same shape as the Phase 7 KnowledgeLedger filter aria-pressed med backlog. — Add aria-pressed={subject === s} on each preset button.`
- `[8] [a11y] [aria-expanded] [low] Sidebar LEDGER button (sidebar-ledger-btn) opens a dialog but lacks aria-expanded={ledgerOpen} + aria-haspopup="dialog". SR users hear "open knowledge ledger button" but get no signal whether it's currently open. Same shape as the Phase 7 ConceptInline aria-haspopup med entry. — Add aria-expanded={ledgerOpen} + aria-haspopup="dialog" on the LEDGER button. ActiveTaskDashboard "+ Manual entry" toggle (line 89-95) has the same gap — add aria-expanded={showManual} there too.`
- `[8] [a11y] [aria-modal] [low] Both ConceptDrawer + KnowledgeLedger render <div role="dialog"> without aria-modal="true". When focus management lands per the Phase 7 backlog entry, aria-modal completes the modal-dialog contract — without it, SR users may not be told that background content is inert. — Add aria-modal="true" to both dialog wrappers. Pairs with the Phase 7 focus-trap fix.`
- `[8] [visual-polish] [token-coverage] [low] PlotlyEmbed hard-codes maxWidth: 800 in inline style (line 28) — only literal-pixel maxWidth in the codebase that bypasses the design tokens. — Move to a CSS var (--plot-max-width: 50rem) or a Tailwind class (max-w-[800px] is also literal but more discoverable than inline style). Marginal — only matters if tokens grow beyond colors into spacing/sizing.`
