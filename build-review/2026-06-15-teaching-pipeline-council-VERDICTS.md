AGENT: devils-advocate
STANCE: REJECT
REASONING: The fatal assumption is sequencing: V→5→7 builds the figure-correctness machinery and digestion BEFORE there is any oracle for "does this teach," yet the brief itself proves the figure councils only closed the GEOMETRY sub-problem — generate-from-algorithm + perceptual-hash gates guarantee a figure that *moves and sorts*, not one that *teaches the right mental model*. You are about to scale generation (§2–3, the missing 50%) on top of a serving layer whose only quality judge is a human who is *definitionally* unable to judge — so digestion will mass-produce all-gates-green lessons that don't teach, exactly the §6 failure multiplied across 6 subjects instead of one figure. Worse, the DEPTH artifact — a ratified Alex requirement — is not named, schema'd, or gated anywhere in the 739-line spec, and the locked figure architecture *structurally cannot host it*, so "reach genuine technical depth" is not a gap in implementation but a hole in the architecture itself. The §10.3 "caught defect → new gate" accretion loop is a dead mechanism here: it requires a learner who catches defects, and the entire reason this tutor exists is that Alex cannot. A pipeline that auto-produces at scale while its only pedagogy oracle is structurally blind is not "architecturally correct, partly built" — it is an unbounded false-mastery generator pointed at a kid three weeks from finals.
KEY CONCERN: There is NO machine pedagogical-quality/depth oracle, and the one human checkpoint is the one person who by hard constraint (no-oracle-inversion) cannot fill it — so scaling generation amplifies undetectable "renders-but-doesn't-teach" failures before any gate exists to catch them.
CORRECT-PATH: Before any digestion scale-up (Plan 5), promote the DEPTH artifact and a pedagogical-quality ESTIMATOR to first-class schema'd pipeline stages: the depth artifact gets its own concept-keyed slot (invariant + complexity DERIVATION + correctness + edge cases + code), its own correctness gate, and a `neverificat` flag until gated; the pedagogy estimator runs an LLM-last-never-alone fitness check that anything machine-uncertain serves flagged `neverificat` rather than fake-green. Re-sequence so the question "does this teach" has a non-human gate BEFORE generation is allowed to produce more than one hand-verified exemplar per concept_type — never let auto-production outrun the only oracle that can catch a lesson that lies.

---

AGENT: domain-expert
STANCE: CONDITIONAL
REASONING: Judged against ITS practice, the SERVING layer is genuinely well-architected — the 5-beat gated deck is a faithful instantiation of productive-failure + worked-example→faded-guidance (PREDICT/ATTEMPT before NAME is exactly the Kapur/VanLehn ordering), the KC graph + R-PREREQ edges are textbook KLI knowledge-component modeling, and FSRS/EWMA is correct spaced-retrieval. That foundation is sound and is the right target to feed. But the brief asks whether the pipeline can AUTO-PRODUCE good lessons for ANY topic, and on that question the architecture is not yet correct because the two load-bearing pieces are absent: (1) the entire digestion/generation half — the system is a hand-authored content hub wearing an auto-generation spec, and hand-authoring is the literal root cause of the never-sorts/dead-frame failures (§6); (2) there is NO pedagogical-quality oracle — every gate verifies truth/geometry/render, none verifies "does this teach," and the one human checkpoint is filled by a learner who by the no-oracle-inversion constraint cannot vet content. The field has a name for this trap: KLI/Bloom-2-sigma systems treat "does the instruction produce learning" as an empirically-gated question (learning-curve/error-rate evidence), and your pipeline currently has no analog — it ships on render-green, which is precisely how a wrong-mental-model figure passed. I APPROVE the direction and the figure-council remedies; I CONDITION approval on promoting depth + a pedagogical-quality estimator to first-class gated stages before claiming "auto-produce for any topic."

KEY CONCERN: There is no machine oracle for instructional quality, and the spec's only fallback (§10.3 "a defect the user catches becomes a new gate") is architecturally invalid here because it assumes a learner who can detect defects — Alex provably cannot. A single-learner ITS with no quality estimator and a blind human checkpoint will silently teach wrong models before an exam; that is the catastrophic-false-mastery failure the Risk Analyst flagged, and it generalizes from figures to every generated artifact.

CORRECT-PATH: Promote the DEPTH artifact (invariant/derivation/correctness/edge-cases/code) and a pedagogical-quality ESTIMATOR to first-class, schema'd, `neverificat`-flagged pipeline stages — the estimator should assert checkable instructional invariants the field already uses (ATTEMPT precedes any answer-bearing content; the CHECK item targets the KC's stated misconception, not a paraphrase; the figure's defining-dynamic + final-frame end-state match concept_type via the fitness gate; depth content is concept-keyed and independently truth-gated, never inheriting the figure's verification), with anything un-estimable served as `neverificat` rather than fake-green. Build digestion (Plan 5) to emit TYPED specs that ride these gates (P2 EASY⇒GATED) so generation and quality-gating co-arrive — never ship a generation path whose output has no quality oracle. Sequence: lock the depth schema + estimator invariants BEFORE/with digestion, not after, so "auto-produce for any topic" is gated-honest the day it first generates rather than accreting gates from defects a blind learner can't catch.

---

AGENT: pragmatist
STANCE: CONDITIONAL
REASONING: Step back from the spec poetry: one learner, finite weeks to finals, and what he actually sees today is ~one subject's worth of figure-less hand-authored lessons. The serving/tracking/figure-correctness layer is the genuinely valuable 80% and it's built — but "auto-produce genuinely good lessons for ANY topic" is the 20% that's eating 80% of the effort and the failure history. The §6 disaster (never-sorts figure, dead frames, false "verified") is direct evidence that scaling generation BEFORE there is a pedagogical/depth oracle just mass-produces gate-green lessons that don't teach — for a learner who definitionally cannot catch it. So full auto-digestion for any topic is the wrong near-term scope; a small set of excellent templated lesson-shapes (the locked 5-beat deck + the generate-from-algorithm stepped figure family + a depth artifact) fed by digested data, applied to Alex's 5–6 real subjects, is the real win and the honest path to "up to spec."
KEY CONCERN: There is NO machine gate on "does this lesson teach," and the only human who could judge it (Alex) is by construction unable to — so every unit of generation breadth added before that gate exists multiplies un-catchable false-mastery risk right before an exam. Breadth is the trap; the depth/pedagogy oracle is the unlock.
CORRECT-PATH: Reorder to depth-and-oracle-first, breadth-second: (1) build the generate-from-algorithm stepped figure family + the concept→family FITNESS gate + the named rendered-frame CI gates (every-frame-advances, final==sorted, monotone-progress) so figures are correct-by-construction, fail-closed; (2) promote the DEPTH artifact to a first-class schema'd concept-keyed stage with its OWN correctness gate, flagged `neverificat` until gated, because the figure architecture structurally cannot host invariant/derivation/proof/code; (3) hand-author or semi-digest the ~5 remaining real subjects through these now-gated templates rather than chasing a general PDF→KC digester — treat full auto-digestion as a later, gated stretch goal (ESTIMATOR + `neverificat`, never fake-green), not the thing that blocks Alex having teachable lessons before finals.

---

AGENT: first-principles
STANCE: REJECT
REASONING: The brief asks whether the *current* architecture is correct to AUTO-PRODUCE good lessons for ANY topic, and on first principles the honest answer is no, because the half of the system that makes "auto" and "any topic" true (§2–3 digestion/generation) is entirely absent — what exists is a hand-authored content hub with an excellent serving/tracking/figure-correctness layer wrapped in a spec that *describes* generation. More fundamentally, the digest→KC→5-beat→figure factoring is the wrong decomposition for "teach a human a concept," because it optimizes the *form* of a lesson (a fixed deck plus a verified geometry figure) while having literally zero representation of the thing that determines whether teaching happened: a model of the *learner's* belief state and whether it moved. The §6 failure is not a figure bug — it is the architecture announcing that every gate it owns measures the artifact, never the effect on a mind, so an all-green lesson that builds the wrong mental model is indistinguishable from a good one. Layering a "pedagogical-quality gate" onto this factoring still measures the artifact, not the learner, so it will catch dead frames and never catch "renders perfectly, teaches a falsehood." The depth gap and the missing pedagogy oracle are not two gaps; they are one symptom — the system has no oracle for *understanding* because understanding was never made a first-class object in the decomposition.

KEY CONCERN: The factoring has no representation of the learner's mental model or of "did the belief change," so every gate it can ever build verifies the artifact (geometry, language, no-clip, faithfulness) and none can verify the only thing that matters — that a specific human now understands something he didn't. For a learner who by construction cannot vet content, an artifact-only verification stack means *nothing* in the loop ever tests teaching; it tests rendering and calls it teaching.

CORRECT-PATH: Make the learner's understanding a first-class object, not the lesson artifact: each KC carries an explicit pre-misconception → target-conception delta and a small bank of *discriminating* check items (questions a learner who holds the wrong model fails and the right model passes), and the pipeline's terminal gate is an ESTIMATOR that generates a lesson, runs it through a learner-simulator (or, minimally, asserts the check items actually discriminate the named misconception) and flags `neverificat` on any KC whose checks don't separate wrong-model from right-model — this is the pedagogy oracle the figure councils' geometry gates structurally cannot be. Promote the depth artifact to a first-class concept-keyed object with its own correctness gate (invariant/derivation/proof/code, machine-checkable against the algorithm, never a figure reskin), and build digestion as the generator that *populates these learner-state objects* — not as a side effort to feed the existing serving layer — so that "any topic" means "extract the misconception-delta and discriminating checks," not "emit beats and a figure."

---

AGENT: risk-analyst
STANCE: REJECT
REASONING: Judged as an auto-produce-for-any-topic pipeline, the architecture is not yet correct because the two stages that determine whether a lesson is *true and teaches* — digestion/generation (~50% of spec, zero code) and any machine oracle for pedagogical fit/depth — are absent, while the only human able to catch a bad lesson is constitutionally unable to vet content. The figure councils already produced the proof of concept for the silent-wrong-teaching failure: every gate was geometry-green and a never-sorts, wrong-mental-model figure passed all of them, was self-certified "READY," and "verified" on 3 of 30 frames. That is not a one-off bug; it is the structural default of a hand-authored corpus served behind correctness gates that verify rendering and faithfulness-to-source but never *adequacy-of-teaching*. Worse, the trust-net faithful-check creates a false-confidence halo: a `faithful` stamp means "matches the source span," not "correctly teaches," yet to Alex a green lesson is indistinguishable from a good one. Generalize this to 5 empty subjects and an open long tail and the expected number of silently-wrong lessons reaching an exam-bound learner is not small — it is the base rate of LLM generation with no teaching oracle, multiplied by every KC. The §10.3 "caught-defect → new gate" accretion model is the single most dangerous assumption in the spec: it presumes a catcher, and this user is definitionally not one.

KEY CONCERN: There is no machine oracle on "does this lesson teach / does its depth artifact say something true," and the human checkpoint that is supposed to backstop it is occupied by a learner who cannot detect wrong content — so silent-wrong-teaching is the default outcome, not the exception, and it lands hardest on the depth layer (invariants, complexity derivations, correctness arguments) which currently inherits *zero* verification and would carry a false `mastery` signal straight into an exam.

CORRECT-PATH: Promote two things to first-class, schema'd, fail-CLOSED pipeline stages before any "any-topic" claim: (1) a concept→family/concept→beat FITNESS + every-frame-advances + final-state gate computed from RENDERED output (per the figure councils), extended so the same generate-then-gate discipline governs beats and the new DEPTH artifact, which must have its own correctness gate (invariant re-derivable, complexity proof checks out, code compiles+passes tests) and ships `neverificat` until it passes — never fake-green; (2) a pedagogical-quality ESTIMATOR-class gate (productive-failure structure present, prereq path complete per EC2, intuition-before-name, depth present-and-gated) that, like P1 mandates, either PROVES-by-gate or marks `neverificat` and is excluded from any `mastery`/`faithful` UI signal — so an ungated lesson can never *look* trustworthy to a learner who can't tell. Build digestion last, only after these gates exist, so the generation firehose is born behind oracles rather than retrofitted after it has already silently shipped wrong lessons across 5 subjects.

---

AGENT: learning-scientist
STANCE: REJECT
REASONING: As an architecture for AUTO-PRODUCING genuinely good lessons for ANY topic, the pipeline is not correct, because the only stages that actually exist serve and track hand-authored content — the generation half that would make "any topic" true is spec-only, so there is currently nothing to evaluate as auto-pedagogy, only a delivery shell with strong form-gating. More fundamentally, the architecture has no oracle for the one thing learning requires: whether a representation builds the correct mental model. Every gate verifies truth, geometry, rendering, and language — the §6 failure proves a faithful, no-clip, trace-matched figure can still teach the wrong dynamic (a process concept rendered as a static structure that never sorts), and the only catch-net is a human who is definitionally unable to vet content. The 5-beat deck is genuinely strong cognitive science — un-skippable PREDICT/ATTEMPT operationalize the generation effect and productive failure, dwell-floors fight ADHD skim, FUSED reveal kills the Khan watch-then-forget decay — but a correct *form* wrapped around a representation no oracle can judge for pedagogical fit is a confident vehicle for misconceptions. And the council-1781433195-ratified DEPTH artifact (invariant, complexity DERIVATION, correctness, edge cases, code) — the thing that converts intuition into durable transferable schema and is the actual exam-relevant knowledge — is unnamed, unschema'd, ungated, and structurally homeless in the figure-family architecture, which means the system can ship false mastery: a learner who "passed" the bars but cannot derive O(n²).

KEY CONCERN: There is no pedagogical-quality oracle anywhere in the pipeline — no machine estimator of "did this build the right mental model / does this reach real depth" — and the sole human judge is, by the load-bearing no-oracle-inversion constraint, incapable of supplying one; "caught defect → new gate" accretion cannot bootstrap from a blind detector, so misconception-laden lessons pass silently and the highest-stakes failure (confident false mastery before an exam) is exactly the one the system cannot detect.

CORRECT-PATH: Promote the DEPTH artifact to a first-class, concept-keyed, separately-budgeted, separately-gated pipeline stage (its own correctness oracle: invariant holds, complexity derivation is sound, code compiles+passes tests, edge cases enumerated), flagged `neverificat` until gated, and never inferred from the figure — depth is prose/proof/code knowledge, not a zoom level of the picture. Add a pedagogical-fitness ESTIMATOR-class gate (per P1: real assertion or `neverificat`, never fake-green) that checks mental-model alignment beyond geometry — minimally a concept_type→family fitness check that the figure's final state reaches the taught end-state and expresses the concept's defining dynamic (visible progress / monotone change), plus a misconception-elicitation check at the CHECK beat (does ⑤ test the predicted-error from ① and re-queue a targeted remediation drill on miss) so the loop measures understanding rather than completion. Sequence-wise, do NOT scale generation across 6 subjects until these two oracles exist, because auto-producing un-judgeable lessons at volume for a learner who can't vet them multiplies false mastery rather than learning.

---

I have the full brief. Answering through the UX/product-design lens only.

AGENT: ux-designer

STANCE: REJECT

REASONING: As a pipeline meant to auto-produce *premium* lessons for any topic, the current architecture is not there — and the brief's own §5/§6 evidence proves it. The only thing that reads as premium today is the demo shell (`/tutor/lectie-selectsort`), which is hand-tuned per lesson; production (`BeatOrchestrator.tsx`) is functional-brutalist with a different pip style, no masthead/trust-badge, and a 272px figure clamp on a 648px-tall screen — i.e. the experience is a patchwork of two surfaces, exactly the "premium vs generated gap" the question fears. More damning for UX: every gate verifies geometry, none verifies that the learner *experiences the concept happening* (the never-sorts mergesort, the dead frames), so the system can ship a polished-looking lesson that teaches the wrong mental model to a learner who cannot detect it. A pipeline whose premium quality depends on per-lesson hand-tuning, and whose only experience-judge is a learner structurally unable to judge, is not architecturally correct for "premium by default."

KEY CONCERN: There is no *experience-quality* contract that travels with generated content. "Premium" currently lives in two places it must not live: (1) a per-lesson demo shell, and (2) a human (Alex) who can't fill that role. Premium-by-default requires the polish to be a property of the family/template/shell that every generated lesson inherits unconditionally — and a machine gate on the *felt* experience (visible progress, motion-toward-end-state, no dead beats), not just CSS geometry.

CORRECT-PATH: Collapse the two surfaces — promote the cinematic Lectie shell (masthead, trust-badge, premium pips, dark figure stage, generous figure height) into the *production* `BeatOrchestrator`/`LessonFigureShell` so polish is structural and zero-per-lesson, then delete the demo-vs-prod split and lift the figure clamp at Alex's real resolution (viewport screenshots, not fullPage). Add an experience-quality gate alongside the geometry gates that asserts the learner-felt properties the figure councils named — every-frame-advances, final==taught-end-state, monotone visible progress, no dead beats — fail-CLOSED to a "needs new family/template" backlog, never a silent fallback. And promote the DEPTH artifact to a first-class, separately-shelled, separately-gated experience surface (its own prose/proof/code template with `neverificat` until gated), because intuition-front-door + one fused NAME sentence is not the "reach real technical depth" the experience promises.