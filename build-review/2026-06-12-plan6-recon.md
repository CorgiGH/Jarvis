# Plan 6 Recon Report — Practice Surfaces + Graders (§6)

> **Purpose:** input for the plan-writer authoring the detailed Plan 6 doc (format template: `docs/superpowers/plans/2026-06-12-plan4a-gate-tooling.md`). Recon date 2026-06-12, against `main` working tree (Plan 4a merged, Plans 1–3 DONE per `docs/superpowers/plans/2026-06-11-one-pass-plan-index.md`). All file paths below verified by Glob/Grep on 2026-06-12 unless marked otherwise.

---

## 1. Spec requirements (numbered, with section refs)

### Arc (§6.1)

**REQ-1. I-do → we-do → you-do arc (§6.1)**
> "All practice follows I-do → we-do → you-do: the lesson's reveal is I-do; structured/step-trace drills with scaffolding are we-do; free-form code practice and mock exams are you-do. The queue (§7.4) moves a KC along the arc as mastery rises."

### Surface 1: Structured Proof Drill (§6.2.1)

**REQ-2. Named sub-steps scored separately (§6.2.1)**
> "Named sub-steps scored separately (not all-or-nothing on the whole proof)."

**REQ-3. Cheat-sheet proof templates become fill-in frames (§6.2.1)**
> "Cheat-sheet proof templates become fill-in frames: the template's structure is given, the learner supplies the named pieces."

Cross-ref: cheat-sheet kind at §2.2 item 7 ("feeds proof templates"); rubric rows sourced from §3.4.

**REQ-4. Required testid selectors — proof drill (§6.2.1)**
> "MUST see: `[data-testid="proof-drill-substeps"]` (the named sub-step list), `[data-testid="proof-drill-frame"]` (the fill-in frame), `[data-testid="proof-drill-substep-score"]` (per-sub-step result after submit)."

### Surface 2: Step-Trace Numeric Drill (§6.2.2)

**REQ-5. Every intermediate state checked, not just the final answer (§6.2.2)**
> "Every intermediate state is checked, not just the final answer — a sign error at step 3 is caught at step 3."

Cross-ref: beat variant for procedure/formula-application at §4.3 (skeleton trace, one formula-line revealed per click); ATTEMPT beat fields include "skeleton rows + trace steps" per §3.2.

**REQ-6. Required testid selectors — step-trace drill (§6.2.2)**
> "MUST see: `[data-testid="trace-drill-skeleton"]` (the dominant skeleton table), `[data-testid="trace-drill-step-input"]` (current step's input), `[data-testid="trace-drill-step-verdict"]` (per-step correctness)."

### Surface 3: Code-Practice Session (§6.2.3)

**REQ-7. Exam-language constraint is mandatory (§6.2.3)**
> "Skeleton-fill or free code in the exam's language (§3.4 exam-language constraint): Alk for PA, R for PS, C++ for POO, bash/POSIX for SO."

Cross-ref: §3.4 defines `exam-language constraint` as a first-class problem-bank field. §8.1: "Practice mirrors the EXAM's language: POO practice uses C++ with EN identifiers because the exam does; PA uses Alk; the tutor's explanations around that code are RO."

**REQ-8. Reference solution is attempt-gated (§6.2.3)**
> "Reference solution is attempt-gated: visible only after a graded attempt."

**REQ-9. Covers 5 of 6 subjects' exam code gap (§6.2.3)**
> "This is the corpus's biggest gap — 5 of 6 subjects have an exam code component with no current practice surface."

**REQ-10. Required testid selectors — code practice (§6.2.3)**
> "MUST see: `[data-testid="code-practice-editor"]`, `[data-testid="code-practice-language-badge"]` (the constrained language, visible), `[data-testid="code-practice-run"]`, `[data-testid="code-practice-verdict"]`, and only post-attempt `[data-testid="code-practice-reference"]`."

(INV-6.6 enforces the attempt-gating in Playwright — see §6.4 list below.)

### Surface 4: Mock-Exam Mode (§6.2.4)

**REQ-11. Timed; mirrors real paper structure (§6.2.4)**
> "Timed; mirrors the real paper's structure: ordered dependent sub-questions (b uses a's result), permitted-materials phases (e.g. ALO's 30-minute docs-allowed phase modeled as a phase switch), rubric-scored via §3.4 rubric rows with a common-error checklist surfaced in the result."

Cross-ref: §3.4 rubric rows = `G1..Gn` items, AP/WP classification, all-or-nothing flags, penalty rules; §7.3 misconception-fired trigger re-queues remediation drills.

**REQ-12. Synthetic-tag honesty requirement (§6.2.4)**
> "Where no real past paper exists, items are synthetic-tagged and the UI says so (honesty over simulation)."

Cross-ref: §3.4 `solution_present` bool + R-SOLPRESENT (Appendix A.2).

**REQ-13. Real-paper structures the mode must reproduce (§6.2.4)**
> "Real-paper structures the mode reproduces (taxonomy §2): PA — 3 questions (Greedy/DP/NP) × ordered a–d sub-parts with printed point brackets, 0.5-pt partial credit; POO course exam — MCQ 'ce se întâmplă la execuția următorului program?' with the barem's step-by-step execution trace as the reveal; SO theory — 18-item grids mixing 0.5-pt radio and 1-pt 'bifați TOATE' checkbox items (the multi-select variant, R-MULTISELECT, lives here and in drills); practical coding tests (SO TP, POO T1/T2) — timed rubric self-assessment with all-or-nothing sub-items and score-halving penalty rules (the app cannot auto-grade these; the value is format familiarity + rubric awareness)."

**REQ-14. PS mock = 15-minute mini-test simulation (§6.2.4)**
> "PS has no sit-down exam: its 'mock' is a 15-minute mini-test simulation matching the live-page format — the taxonomy's 'Test teoretic, format unknown, ask the user' framing is corrected by sweep erratum E3 (format is known: 6 × 15-min in-seminar mini-tests; no gap-record exists for it)."

**REQ-15. Permitted-materials phases modeled (§6.2.4)**
> "permitted-materials phases (e.g. ALO's 30-minute docs-allowed phase modeled as a phase switch)"

**REQ-16. Common-error checklist in result (§6.2.4)**
> "rubric-scored via §3.4 rubric rows with a common-error checklist surfaced in the result"

**REQ-17. Required testid selectors — mock exam (§6.2.4)**
> "MUST see: `[data-testid="mock-exam-timer"]`, `[data-testid="mock-exam-phase"]` (current permitted-materials phase), `[data-testid="mock-exam-question"]` with sub-question ordering visible, `[data-testid="mock-exam-rubric-result"]` (per-G-item breakdown post-submit), `[data-testid="mock-exam-synthetic-tag"]` on synthetic papers."

### Surface 5: Deliverable Tracker (§6.2.5)

**REQ-18. Per deliverable: deadline, points-at-stake, prep drills from pseudocode (§6.2.5)**
> "Per deliverable: deadline (data), points-at-stake per sub-problem, prep drills generated from the spec's pseudocode."

Cross-ref: §3.5 course-meta — deliverables absorbed into existing `tasks` (`Tasks.kt:69`) and `task_prep` (`TaskPrep.kt:14`); `mock_exams` (`MockExamTable.kt:18`) gains §6.2.4 semantics additively.

**REQ-19. App prepares, never grades real submissions (§6.2.5)**
> "The app prepares the user for the deliverable; it never grades real submissions (the professor does that; pretending otherwise is a trust violation)."

**REQ-20. Lab-kind divergence: POO lab = deliverable-tracker entry (§2.2 → §6.2.5)**
> "for POO a lab is a graded code deliverable (GitHub-submitted implementation spec with banned-API constraints; digests to a deliverable-tracker entry + code-practice prep, §6.2.5)"

**REQ-21. Required testid selectors — deliverable tracker (§6.2.5)**
> "`[data-testid="deliverable-card"]` with `[data-testid="deliverable-points"]` (per-sub-problem points), `[data-testid="deliverable-prep-drills"]` (linked drills), `[data-testid="deliverable-deadline"]`."

### Grader Stack (§6.3)

**REQ-22. Grader ordering: numeric oracle first, LLM last (§6.3)**
> "1. Numeric oracle — SymPy / tolerance comparison; local; free; first choice for anything with a computable answer. 2. Execution grader — run the learner's code locally: R, Python, C++ via MinGW, Alk via the alk-language/java-semantics interpreter (github.com/alk-language/java-semantics, URL verified in the sweep); bash/POSIX is constrained (no safe local execution contract) → graded by rubric instead. 3. Rubric grader — structural G1..Gn item-by-item scoring against §3.4 rubric rows (presence/shape of required elements, AP/WP split, penalty rules). 4. LLM-judge — prose only, and always paired with a structural grader (rubric or oracle) so the LLM never solely decides a score."

**REQ-23. Numeric oracle closes audit E16 (§6.3)**
> "Closes audit E16: `DrillGrader.kt:43-61` has no oracle path and `grader_rules` is never read at runtime — ALO numerics, PS fractions, RC subnetting were all LLM-judged."

**REQ-24. Execution grader: R, Python, C++ via MinGW, Alk; bash/POSIX → rubric (§6.3)**
> "run the learner's code locally: R, Python, C++ via MinGW, Alk via the alk-language/java-semantics interpreter (`github.com/alk-language/java-semantics`, URL verified in the sweep); bash/POSIX is constrained (no safe local execution contract) → graded by rubric instead."

**REQ-25. Execution grader closes audit E17 (§6.3)**
> "Closes audit E17: `GRADE_PROMPT_CODE` at `DrillGrader.kt:68-85` says 'You do NOT execute the code' — missing exactly the UB/aliasing/dangling bug classes POO teaches."

**REQ-26. LLM-judge always paired with structural grader (§6.3)**
> "always paired with a structural grader (rubric or oracle) so the LLM never solely decides a score"

**REQ-27. LLM-judge prompts carry per-subject misconception codes (§6.3)**
> "prompts carry per-subject misconception codes mined from the corpus's 'greșeli frecvente' lists (R-ERRORS), so wrong answers map to named misconceptions that feed remediation (§7.3)."

Cross-ref: §7.3: "Misconception fired (LLM-judge or rubric grader maps an error to a misconception code) → a targeted remediation drill for that code is re-queued ahead of new material for that KC." §2.4 R-ERRORS: "'greșeli frecvente' / error lists mined as misconception corpora feeding distractors + scorer checklists."

**REQ-28. Closes audit E18 — misconception taxonomy beyond 3 hardcoded codes (§6.3)**
> "Closes audit E18: today's taxonomy is 3 hardcoded statistics codes — L2_ESTIMATOR / MINIMAX / MODE_CONFUSION — so every PA/POO/SO/RC error files as OTHER. The PA flagship case the codes must catch: the flipped reduction direction, 'students flip the arrow constantly', which an LLM fluency judge passes."

**REQ-29. Existing drill CHECK card wired into grader stack (§6.3)**
> "The existing drill CHECK card becomes a real graded input wired into this stack (audit E8: `DrillStack.tsx:376-391` is read-only text whose 'MARK CHECK DONE' fires without engagement)."

### Acceptance criteria / invariants (§6.4)

- **INV-6.1** — "Grader routing table is total: CI asserts every (subject × problem-archetype) in the real problem bank resolves to a grader chain whose first element is non-LLM. Meaningful only once the problem bank is non-empty; CI marks it pending-not-green until the proof run populates ≥1 row per subject."
- **INV-6.2** — "Execution-grader sandbox test per language against real corpus problems: known-good reference solution → pass; known-bad mutant → fail; for each of R/Python/C++/Alk."
- **INV-6.3** — "LLM-judge is never alone: CI asserts no grader chain consists of LLM-judge only."
- **INV-6.4** — "Golden answer sets (§9.2 gate 7) green per grader per subject before any grader change merges."
- **INV-6.5** — "Playwright suites for all five surfaces (§6.2 selectors + smoke) run against real corpus content."
- **INV-6.6** — "Attempt-gating: Playwright asserts `code-practice-reference` is absent from the DOM pre-attempt and present post-attempt."

### Cross-references §6 → §3 / §7 / Appendix A.2

- §3.4 problem bank: rubric rows (G1..Gn, AP/WP, all-or-nothing, penalty rules) used at §6.2.4 + §6.3; exam-language constraint field used at §6.2.3; `solution_present` drives §6.2.4 synthetic tagging; `archetype + parameter slots` feed §7.3 fresh-values re-lesson.
- §3.5 course-meta: deliverables absorbed into existing `tasks`/`task_prep`/`mock_exams` tables (§6.2.5).
- §7.3: misconception fired → targeted remediation drill re-queued ahead of new material.
- §7.4: queue moves a KC along the I-do/we-do/you-do arc (§6.1).
- Appendix A.2 R-* mapped to §6: R-NEW-SURFACE-PROOF→§6.2.1 · R-NEW-SURFACE-STEPTRACE→§6.2.2 · R-NEW-SURFACE-CODE→§6.2.3 · R-NEW-SURFACE-MOCK→§6.2.4 · R-NEW-SURFACE-DELIV→§6.2.5 · R-RUBRIC→§3.4+§2.4+§6.2.4 · R-SOLPRESENT→§3.4+§6.2.4 · R-MULTISELECT→§6.2.4 (mock + drills) · R-ERRORS→§2.4+§6.3+§7.3 · R-LANG→§3.4+§6.2.3+§8.1 · R-ARCHETYPE→§3.4+§7.3 · R-GRADEMODEL→§3.5 · R-DIFFCAL→§2.2 kind 9.

---

## 2. Current code state (paths verified 2026-06-12)

### Graders (Kotlin)

| File | Role |
|---|---|
| `src/main/kotlin/jarvis/tutor/DrillGrader.kt` | LLM-as-judge grader. Two prompts: `GRADE_PROMPT_TEXT` + `GRADE_PROMPT_CODE` (`:68` — verified: "You do NOT execute the code. Judge from reading the code", line 70). Misconception enum hardcoded at `:50-57`: `L2_ESTIMATOR_CONFUSION` / `MINIMAX_CONFUSION` / `MODE_CONFUSION` / `OTHER` (the E18 trio). `GradeResult`/`GradeAttempt` data classes, `parseGradeJson` two-pass brace-extract fallback, sentinel scrub. Calls `Llm.complete` with optional `responseFormat=json_object`. |
| `src/main/kotlin/jarvis/tutor/GradeScoring.kt` | Deterministic scoring layer (LLM-free): `scoreFromRubric`, `correctFromRubric`, `isConfident`, `answerMatches` (exact + numeric). This is the closest thing to a numeric-oracle leg today. |
| `src/main/kotlin/jarvis/tutor/GraderProviderSetting.kt` | Per-user grader provider enum (`free`/`claude`/`freellmapi`) + repo (upsert via delete+insert). |
| `src/main/kotlin/jarvis/tutor/GradeTeachingPayload.kt` | Wire types for `/drill/grade` reply: `MisconceptionPayload`, `LadderRung`, `NextPhaseAction` (`advance/hold/remediate`), `matchByGraderCode` helper (`:106` mentions free-text codes like `OFF_BY_ONE`). |
| `src/main/kotlin/jarvis/tutor/NextPhaseResolver.kt` | Pure resolver (score, attempts, confidence) → `NextPhaseAction`; no DB writes. |
| `src/main/kotlin/jarvis/tutor/FeedbackLadderBuilder.kt` | Builds the L0–L4 scaffold rung list on the grade reply. |
| `src/main/kotlin/jarvis/tutor/SympyTool.kt` | Python-subprocess SymPy bridge (diff/integrate/solve) for the tutor's `symbolic_math` LLM tool — NOT a student-code runner, but the existing subprocess pattern an oracle/execution grader can mirror. |

**Stage coverage today:** numeric-oracle ≈ `GradeScoring.answerMatches` only (no SymPy in the grade path, no tolerance contract); execution grader ABSENT (no sandbox/runner of any kind); rubric leg = `GradeScoring.scoreFromRubric/correctFromRubric` over **LLM-emitted** rubric booleans (i.e., the structural scoring is deterministic but the per-item verdicts still come from the LLM — not yet a §6.3 "rubric grader" independent of the LLM); LLM-judge = `DrillGrader.grade()`.

### Grader-eval harness + goldens (Plan 4a, merged)

- `src/test/kotlin/jarvis/tutor/GraderGoldenHarnessTest.kt` — JUnit 5 `@ParameterizedTest` over `src/test/resources/fixtures/grader-golden/{subject}/grade-scoring/*.json`; runs real `GradeScoring`; 1e-9 epsilon; seeded-red guard verified at `:74`: `require(files.size >= 12)`.
- `src/test/resources/fixtures/grader-golden/` — 12 fixtures verified present: PA×5 (`pa-all-false`, `pa-all-pass`, `pa-empty-rubric`, `pa-single-pass`, `pa-typical-3of5`), ALO×4 (`alo-all-pass-2`, `alo-half-4of8`, `alo-one-short`, `alo-single-fail`), PS×3 (`ps-all-or-nothing-one-false`, `ps-single-fail`, `ps-typical-2of3`).
- `src/test/resources/fixtures/grader-golden/_README.md` — reserves `{subject}/llm-judge/` + `{subject}/execution-grader/` sub-dirs for Plan 6 (dirs are convention-only, empty).

### Practice surfaces (frontend + backend)

| File | Role |
|---|---|
| `tutor-web/src/components/DrillStack.tsx` | Main practice surface; predict→attempt→grade→reveal; props incl. `language`, `rubricItems`, `referenceSolution`, `vizId`, `provenance`; calls `gradeDrill()`; mounts `FeedbackLadder`/`MisconceptionRibbon`/`TrustBadge`/`GroundedExplanationCard`/`ProvenanceBadge`. Code mode = plain `<textarea>` (no Monaco/CodeMirror). CHECK card at `:376-391` is read-only (audit E8 target, REQ-29). |
| `tutor-web/src/components/DrillCard.tsx` | Stateful single-problem-beat card. |
| `tutor-web/src/components/ProblemStepper.tsx` | Problem nav; `?problem=N` URL param. |
| `tutor-web/src/components/CompileSubmitCard.tsx` | Post-completion stitcher; POSTs `/api/v1/tasks/{id}/submit`. |
| `tutor-web/src/components/TutorWorkspace.tsx` | Mount site wiring Stepper + DrillStack + CompileSubmitCard. |
| `tutor-web/src/lib/drillGrader.ts` | Client wrapper for `POST /api/v1/drill/grade`; types mirror `GradeTeachingPayload.kt` 1:1 (snake_case). |
| `tutor-web/src/components/MockExamShell.tsx` (+ `.test.tsx`) | **EXISTS and is mounted in `main.tsx`** — a prior mock-exam surface the code inventory under-reported. §6.2.4 semantics land ADDITIVELY on top (per §3.5), not as a from-scratch surface. |

**Backend endpoints (verified):** `POST /api/v1/drill/grade` at `src/main/kotlin/jarvis/web/TutorRoutes.kt:2036` (resolves LLM via `productionGraderResolver` at `TutorRoutes.kt:189`); `GET/POST /api/v1/tasks/{id}/prep` (serves `drills_json`); `POST /api/v1/tasks/{id}/submit`; `GET/PUT /api/v1/me/grader-provider` in `src/main/kotlin/jarvis/web/GraderProviderRoutes.kt`; **`src/main/kotlin/jarvis/web/MockExamRoutes.kt` EXISTS** — `installMockExamRoutes()` at `:143` with `POST /api/v1/mock-exam/start` (`:151`), `POST /api/v1/mock-exam/{id}/submit` (`:201`), `GET /api/v1/mock-exam/{id}/result` (`:292`), plus `gradeOpenQuestion` (`:362`). No timer/phase/rubric/synthetic-tag semantics there yet. Route registration hub = `src/main/kotlin/jarvis/web/WebMain.kt`.

### Problem bank (Plan 2)

- `src/main/kotlin/jarvis/tutor/Plan2Tables.kt` — `ProblemsTable` ("problems": subject, archetype, statementJson, parameterSlotsJson, solutionJson, examLanguage, examLanguageConstraintsJson, dataFilesJson, sourceDoc, provenance, syntheticTag), `ProblemRubricItemsTable` ("problem_rubric_items": label G1..Gn, points, kind AP/WP, allOrNothing, penaltyRulesJson, position), `ProblemKcLinksTable`, plus `GradingModelsTable`/`GradeComponentsTable`/`GlossaryTermsTable`/`ExamScheduleRowsTable`.
- `src/main/kotlin/jarvis/tutor/Migration.kt` — all tables in `ALL_TABLES`, created via `SchemaUtils.createMissingTablesAndColumns` (Exposed code-first; no Flyway).
- `src/main/kotlin/jarvis/tutor/Plan2Seed.kt` — seeds grading models/components/exam schedule; does NOT seed `ProblemsTable` (empty in production DB).
- **No `ProblemsRepo`** — no encapsulating repository over the three problem-bank tables.
- Deliverable-absorption targets verified present: `src/main/kotlin/jarvis/tutor/Tasks.kt`, `src/main/kotlin/jarvis/tutor/TaskPrep.kt`, `src/main/kotlin/jarvis/tutor/MockExamTable.kt`.

### LLM provider stack

- `src/main/kotlin/jarvis/OpenRouterChatLlm.kt` — default free LLM; HAS single retry on 429 honoring `Retry-After` ≤5s (`postWithRetry`, `:219-248`).
- `src/main/kotlin/jarvis/RelayLlm.kt` — Tailscale PC relay wrapping `claude --print`; 5s connect / 120s read; **NO retry/backoff** (grep-verified: zero retry hits).
- `src/main/kotlin/jarvis/FreeLlmApiLlm.kt` — OpenAI-compatible client for the (not-yet-deployed) FreeLLMAPI proxy; **NO retry/backoff**.
- `src/main/kotlin/jarvis/tutor/ProviderConfig.kt` — wider per-user provider config (5 types), used outside the drill grader.
- Resolver: `productionGraderResolver()` at `TutorRoutes.kt:189` maps `GraderProvider` → concrete `Llm`. The LLM-last leg should consume this resolver unchanged.

### Code execution sandbox

**Does not exist.** No Alk/R/C++/bash runner, no Docker, no subprocess execution of student answers. `SympyTool.kt` is the only subprocess bridge (symbolic math, not student code). `execution-grader/` golden dirs reserved-but-empty.

### Gaps summary

1. Execution grader: zero infrastructure (no sandbox, no runners, no execution goldens).
2. LLM-judge golden fixtures: dirs reserved, no fixtures, no harness leg.
3. No `ProblemsRepo` over the problem-bank tables.
4. `ProblemsTable` unseeded — empty in production DB (INV-6.1 is pending-not-green by spec until ≥1 row per subject).
5. Code input UX: plain `<textarea>`, no syntax highlighting, no step-trace component, no proof-drill component.
6. No dedicated practice route surface — grading is tied to task+problem via `task_prep`; mock-exam routes exist but lack §6.2.4 semantics (timer/phases/G-item rubric result/synthetic tag).
7. Misconception taxonomy = 3 hardcoded statistics codes inside the prompt string (`DrillGrader.kt:50-57`) — not data, not per-subject (E18).

---

## 3. Proposed file list (CREATE vs MODIFY)

> Lane context: per the index two-lane contract, Plan 6 is the next Lane-B candidate; Lane A = Plan 4b (lesson-gated rendered gates) and later Plan 5 (digestion). Files plausibly touched by Lane A are flagged **SHARED:PM-MERGE-ONLY** — Lane B must not edit them directly (patch-file or PM-applied edit at merge, the Plan-4a `test.yml` mechanism).

### Backend — grader stack (CREATE)

| Path | Purpose |
|---|---|
| `src/main/kotlin/jarvis/tutor/grader/GraderChain.kt` | Chain/router types: ordered grader legs per (subject × archetype); first-element-non-LLM and never-LLM-alone enforced in the type/builder (INV-6.1, INV-6.3, REQ-22/26). |
| `src/main/kotlin/jarvis/tutor/grader/NumericOracleGrader.kt` | SymPy/tolerance oracle leg (reuses the `SympyTool.kt` subprocess pattern + extends `GradeScoring.answerMatches` with a tolerance contract). Closes E16 (REQ-23). |
| `src/main/kotlin/jarvis/tutor/grader/ExecutionGrader.kt` | Local sandboxed runner: compile/run + expected-output compare, per-language adapters, hard timeout. Closes E17 (REQ-24/25). |
| `src/main/kotlin/jarvis/tutor/grader/LanguageRunners.kt` | R / Python / C++ (MinGW) / Alk (alk-language/java-semantics jar) adapters; bash/POSIX explicitly routed to rubric (REQ-24). |
| `src/main/kotlin/jarvis/tutor/grader/RubricGrader.kt` | Structural G1..Gn scoring against `problem_rubric_items` (AP/WP, all-or-nothing, penalty rules) — independent of LLM-emitted booleans (REQ-22 leg 3). |
| `src/main/kotlin/jarvis/tutor/MisconceptionTaxonomy.kt` | Per-subject misconception code registry (data-driven, mined R-ERRORS source; PA flipped-reduction flagship). Replaces the 3 hardcoded prompt codes (REQ-27/28, E18). |
| `src/main/kotlin/jarvis/tutor/ProblemsRepo.kt` | Repository over `ProblemsTable`/`ProblemRubricItemsTable`/`ProblemKcLinksTable` (gap #3). |

### Backend — grader stack (MODIFY)

| Path | Change |
|---|---|
| `src/main/kotlin/jarvis/tutor/DrillGrader.kt` | Becomes the LLM-LAST leg: prompts gain per-subject misconception codes from `MisconceptionTaxonomy`; drop the hardcoded 3-code enum; never invoked solo (REQ-26/27/28). |
| `src/main/kotlin/jarvis/tutor/GradeScoring.kt` | Tolerance-aware numeric matching (shared tolerance source — see carried follow-up #1). |
| `src/main/kotlin/jarvis/tutor/GradeTeachingPayload.kt` | Additive fields for per-sub-step / per-step / per-G-item results (REQ-2, REQ-5, REQ-17) + which grader leg decided. |
| `src/main/kotlin/jarvis/tutor/MockExamTable.kt` | Additive §6.2.4 semantics (timer, phases, synthetic tag, rubric link) per §3.5. |
| `src/main/kotlin/jarvis/tutor/Tasks.kt` + `src/main/kotlin/jarvis/tutor/TaskPrep.kt` | Additive deliverable fields (deadline/points-at-stake/prep-drill links) per §3.5 + REQ-18. |
| `src/main/kotlin/jarvis/tutor/Migration.kt` | **SHARED:PM-MERGE-ONLY** — `ALL_TABLES` additions if any new table lands; Plan 5 (Lane A) also adds digestion tables here. Prefer additive columns on existing tables to avoid touching it at all. |

### Backend — routes

| Path | CREATE/MODIFY | Change |
|---|---|---|
| `src/main/kotlin/jarvis/web/PracticeRoutes.kt` | CREATE | New surface endpoints: proof-drill, step-trace (per-step grade), code-practice (run/grade/reference-gate), deliverable tracker. Keeps Plan 6 out of `TutorRoutes.kt`. |
| `src/main/kotlin/jarvis/web/MockExamRoutes.kt` | MODIFY | Extend existing `installMockExamRoutes` with phases/timer/G-item rubric result/synthetic tag (REQ-11..17) — additive on the live start/submit/result endpoints. |
| `src/main/kotlin/jarvis/web/TutorRoutes.kt` | MODIFY — **SHARED:PM-MERGE-ONLY** | Two surgical edits only: `/api/v1/drill/grade` calls the chain instead of bare `DrillGrader`; CHECK-card grading input (REQ-29, E8). Plan 5 (Lane A) plausibly adds upload routes here — deliver as patch file if lanes overlap in time. |
| `src/main/kotlin/jarvis/web/WebMain.kt` | MODIFY — **SHARED:PM-MERGE-ONLY** | One-line `installPracticeRoutes()` registration (route hub — any concurrent plan touches it). |

### Frontend

| Path | CREATE/MODIFY | Change |
|---|---|---|
| `tutor-web/src/components/practice/ProofDrill.tsx` | CREATE | REQ-2/3/4 selectors (`proof-drill-substeps`/`-frame`/`-substep-score`). |
| `tutor-web/src/components/practice/StepTraceDrill.tsx` | CREATE | REQ-5/6 selectors (`trace-drill-skeleton`/`-step-input`/`-step-verdict`). |
| `tutor-web/src/components/practice/CodePractice.tsx` | CREATE | REQ-7/8/10 selectors incl. attempt-gated `code-practice-reference` (INV-6.6). |
| `tutor-web/src/components/practice/DeliverableTracker.tsx` | CREATE | REQ-18/19/21 selectors (`deliverable-card`/`-points`/`-prep-drills`/`-deadline`). |
| `tutor-web/src/components/MockExamShell.tsx` | MODIFY | Additive §6.2.4: timer, phase switch, sub-question ordering, per-G-item result, synthetic tag (REQ-11..17 selectors). |
| `tutor-web/src/components/DrillStack.tsx` | MODIFY | CHECK card → real graded input (REQ-29, E8 at `:376-391`). |
| `tutor-web/src/lib/practiceApi.ts` | CREATE | Client wrapper for the new practice endpoints (mirror `drillGrader.ts` pattern). |
| `tutor-web/src/lib/drillGrader.ts` | MODIFY | Mirror additive `GradeTeachingPayload` fields. |
| `tutor-web/src/main.tsx` | MODIFY — **SHARED:PM-MERGE-ONLY** | Route/mount additions for the new surfaces (Plan 3 touched it; Plan 4b/W plausibly will again). |

### Tests / fixtures / gates

| Path | CREATE/MODIFY | Change |
|---|---|---|
| `src/test/kotlin/jarvis/tutor/grader/GraderRoutingTotalityTest.kt` | CREATE | INV-6.1 (pending-not-green until ≥1 problem row per subject — encode the pending state explicitly). |
| `src/test/kotlin/jarvis/tutor/grader/LlmNeverAloneTest.kt` | CREATE | INV-6.3 over all registered chains. |
| `src/test/kotlin/jarvis/tutor/grader/ExecutionGraderSandboxTest.kt` | CREATE | INV-6.2: known-good pass + known-bad mutant fail per R/Python/C++/Alk, real corpus problems. |
| `src/test/kotlin/jarvis/tutor/GraderGoldenHarnessTest.kt` | MODIFY | Add `llm-judge/` (recorded-response replay) + `execution-grader/` legs; raise the seeded-red fixture-count guard. |
| `src/test/resources/fixtures/grader-golden/{subject}/llm-judge/*.json` | CREATE | Recorded LLM JSON → deterministic parse/score replay (INV-6.4). |
| `src/test/resources/fixtures/grader-golden/{subject}/execution-grader/*.json` | CREATE | Reference solutions + mutants per language (INV-6.2/6.4). |
| `tutor-web/e2e/practice/proof-drill.spec.ts` | CREATE | INV-6.5 selectors + smoke. |
| `tutor-web/e2e/practice/step-trace.spec.ts` | CREATE | INV-6.5. |
| `tutor-web/e2e/practice/code-practice.spec.ts` | CREATE | INV-6.5 + INV-6.6 attempt-gating assertion. |
| `tutor-web/e2e/practice/mock-exam.spec.ts` | CREATE | INV-6.5 (timer/phase/rubric-result/synthetic-tag). |
| `tutor-web/e2e/practice/deliverable-tracker.spec.ts` | CREATE | INV-6.5. |

### SHARED:PM-MERGE-ONLY (full flag list)

- `.github/workflows/test.yml` — any CI step additions (sandbox toolchain install, INV gates) via patch file, PM applies (Plan 4a precedent).
- `build.gradle.kts` — new deps (Alk interpreter jar, process libs) — Plan 4a edited it; Lane A may too.
- `tutor-web/package.json` + `tutor-web/package-lock.json` — editor dep (CodeMirror/Monaco) + scripts; shared-baseline rule from Plan 4a Task 0 applies (additive keys, PM re-runs `npm ci` at merge).
- `tutor-web/playwright.config.ts` — if a `practice` project/dir is registered; Plan 4b owns rendered-gate config.
- `docs/superpowers/plans/2026-06-11-one-pass-plan-index.md` — status + follow-ups line (always PM-merge-only).
- `src/main/kotlin/jarvis/web/WebMain.kt`, `src/main/kotlin/jarvis/web/TutorRoutes.kt`, `src/main/kotlin/jarvis/tutor/Migration.kt`, `tutor-web/src/main.tsx` — route/schema/mount hubs, flagged above.

---

## 4. Carried follow-ups to fold in

1. **Numeric-ATTEMPT tolerance source** (grader-related → in scope). Source: `docs/superpowers/plans/2026-06-12-plan3-lesson-engine.md:1953-1963` (in-code marker also at the lesson grade route) + follow-up ledger at `:5848`. The lesson route's no-choices ATTEMPT branch is a deliberate placeholder: exact-string compare of `free_input` vs `trace_steps.last().value` (a formula STRING) — "would grade a real numeric answer wrong"; DEAD on the 4 served choice-variant KCs. Fold-in: when Plan 6 builds the numeric-oracle leg, make it THE single tolerance source — add `attempt.numeric_answer` + tolerance to the lesson ATTEMPT path (mirror the numeric CHECK path), route both lesson-ATTEMPT and drill numeric grading through `NumericOracleGrader`, and add the pinning `LessonBeatGradeRouteTest` case the follow-up demands.
2. **Relay retry/backoff in any LLM-grader client.** Index follow-ups line: "relay resilience (timeout retry/backoff in VerificationRunner — the pa-kc-006 false-negative class)". Verified current state: `OpenRouterChatLlm` has a single 429 retry; `RelayLlm.kt` and `FreeLlmApiLlm.kt` have ZERO retry/backoff — and both are reachable from `productionGraderResolver` (`TutorRoutes.kt:189`), so the Plan 6 LLM-judge leg inherits the false-negative class. Fold-in: add bounded retry/backoff (transient timeout/5xx) at the client level (RelayLlm + FreeLlmApiLlm, or a shared retrying `Llm` decorator used by the grader chain), so a transient relay timeout reads as RETRY, not as a failed grade.

---

## 5. Risks + open questions for the PM

1. **Execution-sandbox environment split (biggest risk).** Spec says "run the learner's code locally". PC = Windows (MinGW g++ exists per workspace config); VPS = Linux (g++/MinGW-equivalent unverified, R unverified, Alk jar absent everywhere). INV-6.2 needs all four runners (R/Python/C++/Alk) green in CI too (GitHub Actions = a third environment). **Q: which environment(s) must the execution grader actually work in for Plan 6's acceptance — PC-only first, or PC+VPS+CI? And what isolation level is acceptable (timeout+temp-dir subprocess vs container)? No Docker exists in the stack today.**
2. **Alk interpreter provisioning.** `github.com/alk-language/java-semantics` — a JVM jar; needs fetch/build, license check, and a decision: commit the jar vs fetch-on-CI vs Gradle dep. URL was sweep-verified but the artifact form was not.
3. **Empty problem bank vs INV-6.1/6.5 "real corpus content".** Spec marks INV-6.1 pending-not-green until the proof run (Plan 7) seeds rows, but INV-6.2/6.5 say "real corpus problems / real corpus content" NOW. **Q: does Plan 6 hand-author a minimal real-problem seed (≥1 problem per subject from the real corpus, via `ProblemsRepo`), or do the Playwright/sandbox suites run on fixture problems flagged pending like INV-6.1?** Note the no-oracle-inversion rule: Alex cannot vet seeded problems — provenance must come from the corpus docs themselves.
4. **Misconception mining dependency on Plan 5.** R-ERRORS mining ("greșeli frecvente" lists) is a §2.4 digestion obligation — Plan 5 is not built. Plan 6 can ship the taxonomy MECHANISM (data-driven registry + chain wiring + PA flipped-reduction flagship code hand-seeded from the sweep), but corpus-mined breadth arrives with Plan 5. **Q: confirm mechanism-now / mining-later split.**
5. **INV-6.4 LLM-judge goldens and determinism/no-paid-APIs.** Live LLM calls in CI are non-deterministic and rate-limited (`:free` tiers). Recommended interpretation: goldens = recorded LLM responses replayed through `parseGradeJson` + scoring (deterministic), not live calls. **Q: ratify this interpretation of "golden answer sets green per grader".**
6. **Two-lane intersection pressure.** Plan 6 wants edits in 4 hub files Lane A also plausibly touches (`TutorRoutes.kt`, `WebMain.kt`, `main.tsx`, `Migration.kt`) — more than Plan 4a's single shared file. The patch-file mechanism scales awkwardly past 1–2 files. **Q: either sequence Plan 6 after Plan 4b's lesson-route work lands, or pre-agree the hub-file patch set at plan-write time.**
7. **Existing mock-exam surface is additive ground, not greenfield.** `MockExamRoutes.kt` + `MockExamShell.tsx` already serve start/submit/result; §3.5 mandates ADDITIVE semantics. Plan must not rewrite/break the live surface; needs a delta-recon of their current contracts at plan-write time (this recon verified existence + endpoints only).
8. **bash/POSIX rubric routing must be explicit** — a routing-table row, not an unreachable runner; INV-6.1's totality check must accept "rubric" as the SO code-archetype first leg. (No WSL adventure.)
9. **Code editor dependency.** Plain `<textarea>` today; a syntax-highlighting editor (CodeMirror 6 recommended over Monaco for bundle size) = new npm dep in the SHARED package.json. **Q: is the editor upgrade in Plan 6 scope or deferred (textarea + language badge satisfies the spec selectors)?**
10. **Deliverable data entry.** REQ-18 needs deadlines + points-per-sub-problem as DATA; pre-Plan-5 there is no digestion to produce them. Likely source: `ExamScheduleRowsTable`/course-meta seeds (Plan 2) + manual seed. **Q: confirm acceptable data source for Plan 6's deliverable-tracker acceptance.**

---

*Recon by Lane-B recon subagent, 2026-06-12. All §2 paths Glob/Grep-verified against the live working tree; spec quotes carried verbatim from the §6 extract.*
