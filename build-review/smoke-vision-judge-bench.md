# Vision-Judge Precision/Recall Bench — smoke

**Status:** MEASURE-only. Run mode: E2E smoke (1 family, 1 BAD prop + 1 paired GOOD).

## Honest caveats (carried verbatim from spec §9)

- The deterministic floor (frame-conjunction-gate + svg-overlap-gate) stays **authoritative and hard-blocking**. This bench changes nothing about that.
- The vision leg stays **ADVISORY / SHADOW** regardless of the measured precision/recall. This bench MEASURES; it does not graduate the leg.
- The floor is wired ONLY to sort-merge today; for the other 4 families the "marginal value" question is the WHOLE value (the floor catches nothing there). D1 records porting as separate future work.
- Numbers are reported with CIs; small-N cells are flagged, not point-estimated. The fidelity-validated prop fraction and the label-adjudication disagreement rate are reported so a reader can discount accordingly.

## §6.5 Reconciliation assert (catalog.expectedDetGateBlind === empirical.detGateBlind)

Checked **1** BAD defects with a determined empirical value · **1** matched · **0** mismatches.

## Matrix-2 — ALL defects (full coverage)

Positives = all scored BAD frames; negatives = all scored GOOD (incl. borderline) frames. Precision = TP/(TP+FP), recall = TP/(TP+FN), Wilson 95% CI.

| family | TP | FN | FP | TN | precision | recall |
|---|---|---|---|---|---|---|
| seq-array | 1 | 0 | 1 | 0 | *insufficient sample — n=2* | *insufficient sample — n=1* |
| **OVERALL** | 1 | 0 | 1 | 0 | ***insufficient sample — n=2*** | ***insufficient sample — n=1*** |

## Matrix-1 — empirically det-gate-blind subset (RECALL-led, R8)

Matrix-1 = empirically-detGateBlind BAD positives (the floor misses them) + GOOD negatives SCOPED to the same families. RECALL-led (R8). Empirically-caught (RED) defects are excluded.

Scoped families: seq-array. **Headline metric = RECALL.**

| family | TP | FN | FP | TN | precision | recall |
|---|---|---|---|---|---|---|
| seq-array | 1 | 0 | 1 | 0 | *insufficient sample — n=2* | *insufficient sample — n=1* |
| **OVERALL** | 1 | 0 | 1 | 0 | ***insufficient sample — n=2*** | ***insufficient sample — n=1*** |

## K-runs agreement + abstention (§6.3)

- K-frames scored: **2** · unanimous: **2** · split: **0**
- Total judge runs: **6** · UNPARSEABLE: **0** · abstention rate: **0.0%** · frames with ≥1 abstention: **0**

### Sensitivity row — abstentions DROPPED instead of FAIL-counted

| metric | headline (UNPARSEABLE⇒FAIL) | abstentions dropped |
|---|---|---|
| precision | 50.0% | 50.0% |
| recall | 100.0% | 100.0% |

Headline is STABLE under the abstention-drop sensitivity (moves ≤5pp).

### Advisory council column (SEPARATE — not the scorer's decision rule, §6.3)

bad⇒FAIL when ≥2/3 runs FAIL; good⇒PASS only on K/K PASS. TP=1 FN=0 FP=1 TN=0. ADVISORY column only — NOT the scorer's decision rule (§6.3).

## Provenance lines (R5 / R10 / R7-R18)

- **Independence check (§4.1):** deferred to T15 council (not part of this smoke)
- **Prop-fidelity validated fraction (§3.3 / R10):** deferred (T4 owns the md5 prop-vs-source validation)
- **Adjudication disagreement (§8 / R7-R18):** deferred (T7 label-audit owns the human/second-judge pass)

## Chart reveal-1→2 near-threshold check (§5 step 4)

measured=false · MEANINGFUL_PX=80
> insufficient chart-dist GOOD frames at reveal beats 1 & 2 to measure the marks-only delta (need both clean frameIndex 1 and 2 in the manifest). Not guessed.

## Per-defect verdict table

| frame | label | family | defect | frameIdx | decision | k(P/F/U) | empirical blind | gate verdict |
|---|---|---|---|---|---|---|---|---|
| BAD__seq-array__duplicated-value-token__f0.png | BAD | seq-array | duplicated-value-token | 0 | FAIL | 0/3/0 | true | N/A |
| GOOD__seq-array__good-clean__seq-array__f0__f0.png | GOOD | seq-array | good-clean__seq-array__f0 | 0 | FAIL | 0/3/0 | — | — |

---
_Generated by tutor-web/tools/vision-bench/report-emitter.mjs · scored 2/2 frames · missing verdicts 0 · reconcileMode=fail._
